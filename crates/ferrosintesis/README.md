# ferrosintesis

`ferrosintesis` is reserved for the modeled MIDI synthesizer currently developed
in the `midi-music` repository.

This `0.0.0` release is a name-reservation placeholder. The real crate will
provide the reusable Rust library surface for the synthesizer now living in
`fable5/hollowsynth`.

License: MIT OR Apache-2.0.

