[package]
name = "render-catalog"
version = "0.1.0"
edition = "2021"
description = "Render every album MIDI to a tagged Ogg Opus listening copy"
license = "MIT OR Apache-2.0"
publish = false

[[bin]]
name = "render-catalog"
path = "src/main.rs"

[dependencies]
ferrosintesis = { path = "../ferrosintesis", version = "0.17.0" }
