# 2026.07.09 - DAILY - KILN-Windows - Repo state report

Pass mode: read-only observation/reporting. No tests, app runs, fixes, cleanup,
reviews, ledger transitions, tags, releases, or deploys were performed.

Environment: Windows host `KILN`, worktree
`D:\worktrees\midi-music\20260709-TSK-HUM-daily-report-20260709`.

Baseline: no prior `DAILY` report was found in `wrk_docs/`, so trend comparison
uses the current repo state plus the fallback commit window from 2026-07-08
00:00 through `0de4f63` (`docs(hld): design modal organ pitch controls`).

## Headline

The active maintenance pressure is in the requirements queue, not the bug queue.
There is no bug ledger in this repo yet, while the new `reqs/` ledger moved from
17 captured GM-gap requirements to 9 implemented requirements and 8 accepted
heavy requirements in about a day. None are `Satisfied`, so today's best action
is to convert implemented work into accepted truth, then choose the next heavy
req/fork to build.

## Bug Ledger

- `bugs/` is absent, and no root `BUGS.md` exists.
- Current modeled bug counts: Open 0, Fixed 0, Closed 0, Blocked 0.
- Trend versus prior daily reports: unavailable. There is no prior daily report
  and no current bug ledger to compare.
- Consequence: bug direction cannot be measured from a persistent defect queue
  today. If defect tracking becomes part of this repo's quality loop, initialize
  `bugs/` before relying on bug-count trends.

## Requirements And Backlog

Current `reqs/` counts:

| State | Count |
|---|---:|
| Implemented | 9 |
| Accepted | 8 |
| Integrated | 0 |
| Satisfied | 0 |
| Draft | 0 |

Implemented, awaiting second-eye acceptance or state cleanup:

- `MM-REQ-KILN-00001` - Fiddle routes to Bowed and takes CC1 vibrato.
- `MM-REQ-KILN-00002` - Kalimba has tine decay.
- `MM-REQ-KILN-00003` - Selected synth FX sustain as pads.
- `MM-REQ-KILN-00004` - Marimba/xylophone use wood-bar envelopes.
- `MM-REQ-KILN-00005` - Orchestra Hit is a short layered stab.
- `MM-REQ-KILN-00006` - GM SFX 120-127 are toneless noise fallbacks.
- `MM-REQ-KILN-00007` - Sitar, shamisen, and koto have distinct pluck presets.
- `MM-REQ-KILN-00009` - Strings/choir CC1 vibrato and CC68 legato are opt-in.
- `MM-REQ-KILN-00010` - Choir pad CC70 vowel morph opens formants.

All nine have non-empty `Implemented-by` and `Satisfied-by` fields. They remain
`Implemented`, not `Satisfied`, so the ledger still says they have not passed the
human second-eye gate.

Accepted heavy requirements still unimplemented and unclaimed:

- `MM-REQ-KILN-00008` - Modal/Organ pitch controls. Design doc exists and is
  marked design complete, but not signed off; it awaits Arthur's compatibility
  choice.
- `MM-REQ-KILN-00011` - Fretless bass mwah.
- `MM-REQ-KILN-00012` - Vibraphone motor tremolo.
- `MM-REQ-KILN-00013` - Timpani glide/brightness/ring.
- `MM-REQ-KILN-00014` - Reed organ/accordion/harmonica free-reed character.
- `MM-REQ-KILN-00015` - Electric piano programs do not use the acoustic piano
  voice.
- `MM-REQ-KILN-00016` - Harp soundboard and wound-string law.
- `MM-REQ-KILN-00017` - Bagpipe/shanai reed routing.

No requirement is currently claimed.

## Designs

Complete or integrated:

- The hollowsynth realism build reached v0.8.0, followed by v0.8.1 flatwound
  bass for GM 33/35.
- The synth-lead GM 80-87 HLD was followed by `73297ac`, which added the
  dedicated synth-lead voice.
- The realtime/mddosem HLD is signed off, and `c0cd5cd` landed the hollowsynth
  realtime API: library root, offline wrapper, live raw-MIDI byte parser,
  realtime block rendering, sample prewarm, reset/controller handling, and
  focused realtime tests.
- The Ninth Bell, Heliopause, The Burning Meridian, Tuxedo Noir, Seven Kinds of
  Sunlight, and refreshed gpt5-3-spark MIDI/Opus are now committed album work.

Designed or in flight:

- `wrk_docs/2026.07.08 - HLD - hollowsynth v0.9 brass reeds and orchestral polish.md`
  is design-complete and awaits Arthur forks: strings ST1 default-vs-gated,
  bowed 42/43 bodies ship-or-defer, and The Iron Tide `.opus` refresh choice.
- The v0.9 branch
  `task/20260708-DEV-HUM-hollowsynth-v0-9-brass-reeds-orchestral` is unmerged,
  with 34 commits unique to the branch and 34 trunk commits not yet in it.
- `wrk_docs/2026.07.09 - HLD - hollowsynth modal organ pitch controls.md`
  is design complete but not signed off. It targets `MM-REQ-KILN-00008` and
  calls out a compatibility fork before implementation.
- `task/20260709-TSK-HUM-reqs-heavy-00011-fretless-bass-mwah` exists at current
  trunk with no unique commits yet, so it looks started but not advanced.

## Key Commits Since Fallback Baseline

No prior daily report exists, so this list uses the 2026-07-08 fallback window.
That window contains 71 commits and a diff of 177 files changed, 25,323
insertions, and 534 deletions.

- `610ee18` - added the hollowsynth v0.9 brass/reeds/orchestral-polish HLD plus
  seven appendices.
- `73297ac` - added dedicated synth-lead voice for GM 80-87.
- `c0cd5cd` - added the hollowsynth realtime API.
- `36ccce6`, `f41e98b`, `4696cdb` - bootstrapped and accepted the 17-entry
  requirements ledger, then added oracle targets.
- `073ea2c`, `b81508e`, `dc2bf8b`, `d8628ed`, `a3eecb9`, `6cc4a25`, `d4b87f8`,
  `8b1c366`, `df0ae7d` - implemented the nine light GM-gap requirements now
  recorded as `Implemented`.
- `0de4f63` - added the modal/organ pitch-control HLD for `MM-REQ-KILN-00008`.
- `80bfcee` - finished The Ninth Bell from the demo string chords.
- `8202fd5`, `4326d47`, `f8bcca7`, `e65178d` - added Seven Kinds of Sunlight,
  Heliopause, The Burning Meridian, and Tuxedo Noir.
- `6f9eca5`, `d6a4726` - added GM modulation depth to gpt5-3-spark tracks and
  refreshed the generated MIDI/Opus files.

## Branches And Worktrees

Snapshot source: `git worktree list`, `git branch`, and tracked-only
`git status` checks. "Clean" here means no staged or tracked modifications were
reported; untracked files were not scanned.

Main and active worktrees:

- `D:\language\midi-music` on `main` is equal to `origin/main` at `0de4f63`,
  with no tracked modifications reported.
- The daily-report worktree is equal to `origin/main` before this report commit.
- `task/20260709-TSK-HUM-reqs-heavy-00011-fretless-bass-mwah` is equal to
  `origin/main`, with no tracked modifications reported.
- `task/20260708-DEV-HUM-hollowsynth-v0-9-brass-reeds-orchestral` is unmerged
  and clean-tracked, 34 ahead and 34 behind `origin/main`.

Likely reapable after owner confirmation:

- Several req-loop worktrees/branches are merged into `origin/main` and clean:
  `reqs-loop-implement-first-light`, `reqs-loop-mm-req-00002`, `00003`,
  `00004`, `00005`, `00006`, `00007`, `00009`, plus the no-worktree branches
  `reqs-loop-build-accepted` and `reqs-loop-drain-ready`.
- `task/20260708-TSK-HUM-hollowsynth-realtime-mddosem-design` is merged and
  clean.

Do not touch without owner input:

- Dirty detached red-test worktrees exist for `MM-REQ-KILN-00002`, `00005`,
  `00006`, `00007`, `00009`, and `00010`, each with tracked edits in
  `fable5/hollowsynth/src/voices.rs` or `fable5/hollowsynth/src/engine.rs`.
- `v09-backup-pre-rebase` is an unmerged backup branch with the same tip as the
  v0.9 work branch.

## Tracked Issue List

1. No `bugs/` ledger exists, so bug trends are unavailable.
2. No previous daily report exists, so today is the baseline report.
3. Nine requirements are `Implemented` but not `Satisfied`; each needs
   second-eye verification and a deliberate state transition or rejection.
4. Eight heavy requirements remain `Accepted`, unimplemented, and unclaimed.
5. `MM-REQ-KILN-00008` has a design-complete HLD but awaits the compatibility
   fork before implementation.
6. The v0.9 branch carries substantial unique work and has trunk drift.
7. The v0.9 HLD still needs Arthur decisions on strings ST1, bowed 42/43, and
   The Iron Tide `.opus` refresh.
8. Dirty detached red-test worktrees contain uncommitted tracked edits and need
   owner handling before any cleanup.
9. Merged req-loop and realtime task worktrees/branches appear reapable, but
   cleanup is a separate confirmed action.
10. The realtime API is landed in hollowsynth; downstream `mddosem` integration
    is not evidenced in this repo.

## Recommended Action Plan For Today

1. Run a focused Gate 2 pass on the nine `Implemented` requirements: read the
   source/oracle refs, run the named oracle commands, and move confirmed ones to
   `Satisfied`; reject or reopen anything that does not hold.
2. Decide the compatibility fork for `MM-REQ-KILN-00008`, then either build that
   modal/organ requirement or keep it parked behind the decision.
3. Choose whether to continue `MM-REQ-KILN-00011` from its fresh worktree or pick
   another heavy req by value.
4. Decide the three v0.9 forks before trying to rescue/rebase the v0.9 branch.
5. After active work is safe, run a separate worktree-hygiene cleanup pass for
   merged task worktrees and dirty detached red-test worktrees.

## Verification Notes

This report used deterministic reads of repo files and git metadata. Judgment
claims were checked against the ledger states, design docs, journals, and commit
history. No claim here is based on test execution during this pass.
