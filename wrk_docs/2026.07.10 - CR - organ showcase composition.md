# Code review — organ showcase composition

## Summary

The new one-track album has a clear composition/engine/build/verify/analyze split,
uses no third-party Python dependencies, and keeps every organ-specific claim
machine-checkable. The copied engine matches the repository's self-contained
album convention and remains byte-identical to the existing feature-showcase
engine.

## Required changes

1. **Issue — listening guide contract.** The initial sidecar did not begin with
   `Why this piece` or include the required `Listening guide` and `MIDI
   techniques` headings. `tests/test_render_opus.py` rejected it. Rewrote the
   sidecar to the standard shape and regenerated the Opus so embedded metadata
   matches the source.

## Suggestions

1. **Optional — analyzer sample-rate portability.** The tremulant analyzer
   assumed its RMS envelope was exactly 100 Hz. Derived the real envelope rate
   from the WAV sample rate and block size instead.
2. **Nit — tail inequality readability.** Replaced a chained inequality with two
   explicit decay comparisons.

## Questions

None.

## Apply summary

| # | Item | Type | Action | Notes |
|---:|---|---|---|---|
| 1 | Listening guide contract | Issue | Fixed | Test passes; Opus refreshed |
| 2 | Envelope sample rate | Optional | Fixed | No 44.1 kHz behavior change |
| 3 | Tail inequality | Nit | Fixed | Same intended thresholds |

Changes made: 3. Skipped: 0. Disagreed: 0.

The simplicity pass retained the shared engine copy because album bundles are
intentionally self-contained. It found no dead composition helpers, speculative
abstractions, or extra dependencies to remove.
