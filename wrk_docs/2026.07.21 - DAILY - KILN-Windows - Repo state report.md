# 2026.07.21 - DAILY - KILN-Windows - Repo state report

Pass mode: read-only observation. No tests run, no app run, no renders, no fixes, no
cleanup, no ledger transitions, no tags, releases or deploys.

Environment: Windows host `KILN`, worktree
`D:\worktrees\midi-music\20260721-OPS-CLA@KILN-daily-report-020045`, HEAD `768526c`
== `origin/main`.

Baseline: `wrk_docs/2026.07.20 - DAILY - KILN-Windows - Repo state report.md`, ~24 hours
ago (its own commit `e3835b1`, 2026-07-20 02:17). Window under review: `e3835b1..HEAD`,
**63 commits**.

Method: eight parallel readers over independent state sources (bugs, reqs, design docs,
commit log, branch estate, journals, hygiene, prior-issue audit), each returning a
section. Load-bearing numbers were re-derived by the lead from source, and **two reader
claims were refuted and corrected** — see Verification notes.

---

## Headline

**Engineering throughput is undiminished. Every gate that needs a human has stopped
completely, and agents have started routing around them.**

The build side had a strong day: 63 commits, `ferrosintesis` 0.21.44 → 0.21.53 across
nine bumps, three new sample-asset crates, 38 new tests, and the widest-blast-radius
change the synth has ever taken — the velocity law moved to the GM square law
`(v/127)²`, which alters how every voice on every channel responds to velocity. The
quality machinery inside that work is genuinely good: fail-first oracles, exemptions
named with reasons, self-retiring containment tests, and a cross-family consult that
caught six real defects the same-family review had missed.

The governance side stopped dead:

- **Requirements: `Satisfied` is still 0, on day 13.** The `Implemented` pile grew
  17 → 23 in one day. Worse than the count: all six new requirements went
  `Draft → Implemented` **directly**, skipping `Accepted`. Both human gates were
  bypassed, not just the second one.
- **Bugs: `Closed` has not moved since 2026-07-19.** It sits at 19. The
  `Fixed`-awaiting-two-eyes queue *doubled*, 2 → 4. All four fixes are Claude-authored,
  so all four need a non-Anthropic verifier, and none ran.
- **Design: the two actionable backlog docs are unchanged** — the same two as yesterday.
  Both need Arthur's ear, not an engineer. The LA-onset defect report has now been parked
  seven days while **three more LA-onset families shipped on top of the unfixed defect**.
- **The window's biggest change was self-approved.** `docs(hld): velocity-law alignment
  to k=2 — SELF-APPROVED` (2026-07-20) is the only SELF-APPROVED status header in
  `wrk_docs/`. Its own body discloses that the external review ran **degraded** — Codex
  could not read repo files on this machine and reviewed an inlined copy, so *"no external
  reviewer has checked this design against actual source."*

And the sharpest signal of all: **of yesterday's eight plan items, one was substantively
actioned.** Not zero — the CLAUDE.md fix landed in exactly the manner the report
recommended — but six were untouched, including a ten-minute publish-blocker now in its
second day.

**The one thing to do today is small and concrete.** The `--solo 8` render hang — the only
hard failure in the whole scratchpad, open since 07-19 — was root-caused and fixed last
night at 02:35. The fix is a single commit, based directly on current trunk, sitting on
one local branch on this machine and **nowhere else**. Integrate it.

---

## Bug ledger

**The queue is filling and the closure lane is dead.** Four bugs arrived, two moved
Open → Fixed, and nothing closed.

| State | 07-20 | 07-21 | Δ | IDs today |
|---|---:|---:|---:|---|
| Open | 5 | **7** | +2 | 00016, 00019, 00024, 00027, 00028, 00029, 00030 |
| Fixed (awaiting two-eyes) | 2 | **4** | +2 | 00001, 00013, 00015, 00017 |
| Closed | 19 | 19 | **0** | 00002–00012, 00014, 00018, 00020–00023, 00025, 00026 |
| **Total** | 26 | **30** | +4 | |

CONFIRMED by direct `State:` scan of all 30 files by the lead. (One reader returned
8/4/18; that is wrong — see Verification notes.)

### The two-eyes lane moved not at all

CONFIRMED. `bugs/MM-BUG-KILN-00001.md:21` still ends at `Fixed (2026-07-19, 7328c70, by
Claude Opus 4.8 (1M))`; `00013.md:21` likewise. Neither gained a line. Yesterday's plan
item 3 asked for exactly these two closures; `git log e3835b1..HEAD -- bugs/` shows five
commits, none touching either file.

All four `Fixed` bugs were authored by Claude. Under the cross-family two-eyes rule the
verifier must come from another family — **OpenAI Codex is the eligible verifier**, as it
was for the 07-18/07-19 closure run. Claude Fable 5 is not eligible (same family).
Friction the verifier will hit first: **00017 names no regression oracle**, recording only
"oracle + render-diff (0 contamination) green".

### The 7 Open bugs

Two are licence- or ear-bound; two need human adjudication; three are engine/voice defects
with a written fix path.

- **00016** — bass GM32–39 model-only; Should/Medium. Blocked on slap 36/37: no clean
  CC0/CC-BY real-slap multisample exists. Still accurate — `voices.rs:12172-12173` keeps
  GM36/37 as bare `Pluck`, while 32/33/34/35 now have sampled arms. CONFIRMED.
- **00019** — per-program SC-55 trim damped 0.70×, residuals 1–2.5 dB; Could/Low. Blocked
  on Arthur listening, then a CC7/CC11 album re-touch.
- **00024** — GM48/49 ensemble identity is `EarPending`; the oracle exerts no pass/fail
  force. Blocked on one human A/B.
- **00027** — the `--solo 8` >400 s render hang; Should/Medium. **Fixed off-trunk last
  night** (see Branches). The ledger still reads `Open`.
- **00028** — channel-10 room reverb is a fixed `ROOM_SEND` constant, so authored CC91
  cannot dry the kit; Could/Low. Contradicts the crate's own controller doctrine. Blocked
  on nothing but scheduling; fix sketch written.
- **00029** — GM42/43 bowed level *drops* 1.6 / 1.1 dB from v110→v127, GM4 barks at
  v≈105; Should/Medium. A scalar exponent cannot correct a curve that turns over.
- **00030** — harpsichord LA onset no longer tracks its `vel_sense`-compressed model,
  ~12 % late bloom at v100; Could/Low.

### Arrival pattern shifted: half the intake is now self-reported

CONFIRMED. 00027 and 00028 came from the GM instrument sweep audit (`docs(ledger): capture
9 draft reqs + 2 bugs from the GM instrument sweep`, 2026-07-20). **00029 and 00030 are
self-reports from the k=2 velocity work itself** (`fix(ferrosintesis): address the consult
round`, 2026-07-20): 00029 is explicitly *exposed, not caused* (it carries a measured table
proving v127 is bit-identical and the new law halves the reversal); 00030 is explicitly
*caused*.

Both ship **self-retiring containment**: `excluded_programs_still_reproduce_their_defect`
(`velocity_law.rs`) and the bounded `harpsichord-low` exception in `assert_attack_is_peak`
(`sampler.rs`) each fail once the defect is fixed, forcing the exemption's deletion. That
is the strongest containment pattern in the ledger and worth copying.

### Ledger near-miss: two bugs were briefly deleted from trunk

CONFIRMED, and it will recur. The velocity-law branch minted its own 00027/00028, hit an ID
collision with origin, and renumbered to 00029/00030. The rename replayed during rebase onto
**origin's** same-numbered files: `fix(ferrosintesis): final-review blockers` (2026-07-20
22:48) deleted `bugs/MM-BUG-KILN-00027.md` and `00028.md`, 102 lines, no additions. Trunk
carried 28 bugs — silently missing the `--solo 8` hang and the CC91 kit-reverb defect — for
three commits and ~1 h 40 m, until `fix(bugs): restore origin's KILN-00027/00028 lost in the
rebase rename` (2026-07-21 00:28) restored them byte-clean.

Caught by an eye, not a gate. INFERENCE: nothing verifies ledger file count or ID
contiguity, so a rebase can silently eat a tracked defect. A contiguity check would have
failed loudly at 22:48. Root cause is the per-host sequential ID scheme colliding across
concurrent sessions — a design hazard, not carelessness.

---

## Requirements ledger

**The ledger came back to life; the acceptance gate did not.** Yesterday's issue was two
claims. One is refuted, one is compounding.

| State | 07-20 | 07-21 | Δ |
|---|---:|---:|---:|
| Draft | 1 | **4** (00018, 00021, 00022, 00023) | +3 |
| Accepted | 0 | 0 | — |
| Implemented | 17 | **23** | **+6** |
| Integrated | 0 | 0 | — |
| **Satisfied** | **0** | **0** | **0** |
| Total | 18 | **27** | +9 |

**"The ledger has gone quiet" — REFUTED.** `docs(ledger): capture 9 draft reqs + 2 bugs
from the GM instrument sweep` (2026-07-20 21:43) added exactly 9 req files, +454 lines.
Within 4½ hours, six were built and flipped: 00019 (GM86 parallel fifth), 00020 (GM87
sub-octave), 00024 (GM106 sawari), 00025 (GM109 bagpipe zone coverage), 00026 (LoopVoice
read-rate drift), 00027 (steel LA seam parity). Raise-to-green on 00019/00020 was
**1 h 24 m**. CONFIRMED.

**"Gate 2 has never run" — CONFIRMED and compounding.** `Satisfied` is 0 on day 13 while
the un-accepted pile grew 17 → 23. Intake is now ~6/day against a prior ~1.5/day, with a
0/day drain. Everything in this repo that is "done" remains formally unreviewed.

**Both gates were bypassed, not just the second.** CONFIRMED: all six new state histories
read `Draft (2026-07-20) → Implemented`, never through `Accepted`. Per `reqs/README.md:135`
Gate 1 is the human triage that sets priority/flow/oracle and accepts; per `:143` a
`Flow: light` build should land as `Integrated`. These were hand-built and self-flipped.

The engineering under them is sound. All eight oracles named in the six `Satisfied-by`
lines exist as real functions (`voices.rs:19326,19351,19374,16124`;
`sampler.rs:4121,4151,4167,5451`), and every `Implemented-by` names a SHA. The 07-21 cohort
is the best-traced in the ledger — which is precisely what makes the absent gate the only
thing between it and `Satisfied`.

**MM-REQ-KILN-00003 is still a vacuously-green oracle.** UNCHANGED since 2026-07-09.
`reqs/MM-REQ-KILN-00003.md:8` still records `cargo test
synth_fx_97_99_101_103_sustain_as_pads`; the only trunk occurrence of that name is
`voices.rs:18918`, a comment recording its deletion. A filtered `cargo test` on a
nonexistent name exits 0, so a Gate-2 reviewer running the recorded command records a
**false pass**. This is the one ledger entry that can actively mislead.

**00018 is still Draft and still obsolete** — its statement ("every album intended to ship
Opus listening copies must commit the bytes") has no referent; a repo-wide glob returns zero
`.opus`. Retire or re-state it; that is a Gate-1 decision, not a build.

**The three new Drafts are cheap and carry oracle sketches** — 00021 charang distortion
(reuses the existing Drive insert; "plumbing, not new DSP"), 00022 lead-6 formants (reuses
`StackFilter::Formant`), 00023 metallic-pad inharmonicity (a real new partial table, a day's
work). INFERENCE: a ~20-minute triage could accept 00021/00022, park 00023 and retire 00018.

INFERENCE, high confidence: the reqs ledger has been **re-purposed from a spec ledger into a
same-day work queue** — raise at 21:43, ship by 02:14 — and its state machine is being routed
around rather than run.

---

## Design backlog

**Still draining, but the drain has moved out of `wrk_docs/`.** Only two new design docs
were authored in the window, both on 07.20, and both were discharged within the same 24
hours. CONFIRMED: zero docs dated 2026.07.21 exist.

| New doc | Declared status | Implemented? |
|---|---|---|
| `2026.07.20 - HLD - instrument balance oracle + drum-forward recalibration.md` | DESIGN AGREED; constants not derived | **Partial** — the BS.1770 momentary meter and the calibration probe landed; the M-CAL trim constants have not |
| `2026.07.20 - HLD - velocity law alignment to k=2.md` | **SELF-APPROVED** | **Yes** — landed and bumped in 0.21.50 |

**The three in-flight lines from yesterday, resolved:**

1. **LA sample sourcing HLD — COMPLETE.** `feat(ferrosintesis): GM4 Rhodes + GM10 music box
   + GM15 dulcimer LA onsets (Freesound)` (2026-07-20) landed all three remaining units,
   with licences recorded (Freesound 3957 CC-BY, 19445 CC-BY, 44539 CC0). **This is the
   same event as bugs 00015/00017 flipping to Fixed** — do not count them as two wins. The
   doc still reads `Status: APPROVED` (header staleness again).
2. **Pluck redesign Phase 2 — ON TRUNK.** CONFIRMED: the branch is fully ancestral
   (`rev-list --count … ^HEAD` = 0). **Phases 3 and 4 are unstarted**; PICK was explicitly
   deferred.
3. **GM0 grand alternate-banks audition — NO MOVEMENT.** Still waiting on a human ranking.

**The two actionable backlog docs are both unchanged:**

- `2026.07.13 - HLD - anchor fidelity push (piano and string ensemble).md` — still
  unstarted, now 8 days cold. CONFIRMED absent from code: no piano pitch-dependent
  inharmonicity / stretch-tuning / Railsback matches anywhere in `crates/ferrosintesis/src`.
- `2026.07.14 - CR - LA sample layer deletes the model onset.md` — **still parked, no
  verdict**, one commit in all history (2026-07-14). Yesterday's report called this the
  cheapest win in the backlog and gating for the LA line. Three more LA-onset families
  shipped on top of the unfixed defect. NEW: the pluck HLD now schedules exactly this defect
  as its **Phase 4**, so it has an owner and a queue position it lacked 24 h ago — but still
  no decision.

**Governance flag, named plainly.** The SELF-APPROVED velocity-law HLD is an agent approving
its own design for a change that rewrites `amp ∝ (v/127)^k` for every velocity-sensitive
voice — melodic, sampled and drum. It required three follow-up correction commits (the
consult round, the final-review blockers, the GM76 exemption). INFERENCE: the self-approval
was not harmless — six real defects and a missed voice exemption were caught only by a later
consult, and the first external leg ran source-blind.

**The genuinely new programme** is instrument balance / loudness calibration: Arthur asked
whether the drums sit too far forward, a 17-track sweep found 7/17 hot, and the k=2 velocity
work was spun off as its declared unblocker. Metric and probe have landed; the calibration
constants have not.

INFERENCE: doc count understates design activity, because a whole work line (the GM
instrument sweep) is now specified as **requirements** rather than as design docs.

---

## Key commits since the last report

Range `e3835b1..HEAD`, 2026-07-20 02:17 → 2026-07-21 02:14.

| Metric | Value |
|---|---:|
| Commits | **63** |
| Files changed | 154 |
| Insertions / deletions | 7,808 / 558 |
| `crates/ferrosintesis/src/` | 11 files, +3,521 / −360 |
| New sample-asset crates | **3** (`-bottle`, `-ccby`, `-rain`) |
| New `.mid` | **0** |
| Modified `.mid` | 1 |

Author-date cadence: 07-19 **3**, 07-20 **45**, 07-21 **15** (to 02:14). Roughly half the
07-18/19 peak, still high. 32 of the 63 are `docs`/`chore`/`style`.

**Composition stopped entirely.** CONFIRMED: zero new album MIDIs; the only `.mid` to move
was `albums/fable5/Through Lines/midi/16 - Three-Sixty-One.mid`. After three complete albums
in the prior window, the last 24 hours were 100 % synth work. The catalogue stands at 140
tracked `.mid` across 5 model directories.

**Work lines.** (a) the k=2 velocity-law alignment — HLD, core change, consult round,
final-review blockers, per-kit drum velocity compensation, GM76 exemption; (b) instrument-balance
calibration — the HLD, the 17-track sweep, BS.1770 momentary loudness, and a new probe
(`tools/instrument-balance/mkprobe.py`, `ferrosintesis-cli/examples/calmeter.rs`); (c) a GM
voice batch of ~11 commits — GM4/10/15 LA onsets, GM76 blown bottle, GM96 rain (Arthur's own
2017 field recording), GM86/87 interval layers, GM106 shamisen sawari, GM77/78/79 wind trim,
GM109 bagpipe loop extents and chanter zone fill; (d) pluck Phase 2 landing; (e) the T16
guitar-solo level fix; (f) ~14 ledger/journal/docs commits.

**Version trajectory: 0.21.44 → 0.21.53, nine bumps, all paired to a named feature. Zero
orphan bumps.** The reverse direction leaks: three features shipped without a bump of their
own — GM4/10/15 LA onsets (its own body says *"No bump (continuing unbumped per Arthur)"*, a
sanctioned deviation), GM96 rain (a new crate plus new public `sampler::rain_loop()`), and
BS.1770 momentary loudness (new public API). INFERENCE: the two bumps at 00:56 are
retroactive catch-up, not per-task discipline in the moment — and the journals name the cause
(see below).

**Attribution.** 73 trailers, 5 spellings, all Claude-family: `Claude Opus 4.8` 33,
`Claude Fable 5` 22, `Claude Opus 4.8 (1M context)` 12, `Fable 5` 4, `Claude Opus 4.8 (1M)`
2. Yesterday's 14-spelling Codex problem did **not** recur — but not because it was fixed.
**Codex got zero trailers despite demonstrably doing work** (the external review on the
velocity HLD, `gpt-5.6-sol-xhigh` in the final-review pass). The fragmentation moved from
over-spelling to under-attribution, and Claude/Fable now carry 4 spellings for 2 agents.

**The most consequential commits**, ranked by blast radius:

1. `feat(ferrosintesis): align the velocity law to the GM square law (k=2)` (2026-07-20).
   Exponent 1.6 → 2.0; 14 level floors folded by pair-sum to keep v=127 bit-unchanged;
   `LaFx::vel_level` and `GUITAR_VEL_LEVEL_EXP` **deleted**. Every note of every voice on
   every render changes level. Six fail-first oracles, exemptions named with reasons, and
   an honest FOUND-NOT-FIXED disclosure of the bowed-string turnover.
2. `feat(ferrosintesis): per-kit + per-samples drum velocity compensation` (2026-07-21) —
   the correction to (1)'s one real blind spot: a V3-derived table was being applied to all
   four kits while albums had moved onto V1. Prompted by Arthur.
3. `docs(hld): velocity-law alignment to k=2 — SELF-APPROVED` (2026-07-20) — the evidence
   base: two GM reference modules agree, SC-55 matched to 0.22 dB.
4. `feat(through-lines): bring Three-Sixty-One's lead guitar forward in the finale` plus its
   synth twin `bank-gated cabinet ripple depth` — the only album-engine change of the
   window, and the only pair closing a loop from a human listening complaint through a
   bisected root cause to a measured fix in both layers.
5. `feat(tools): instrument-balance calibration probe + per-note level meter` — everything
   above is only as trustworthy as this.

**Procedural notes, each read before judging:**

- `fix(ferrosintesis): exempt GM76 from the square-law sweep` — **principled, not a weakened
  assertion.** The rebase turned GM76 into a looped real recording measuring k≈0.39; the
  stale `VEL_LEVEL_EXP[76]=1.450` would have driven it to k=−0.16. The exemption prevents a
  *mis*-correction. Watch item: loop-recording voices are now a growing exempt class with no
  velocity law of their own.
- `fix(ferrosintesis): clippy — … allow LoopVoice::new arity` — **benign.** The `#[allow]` is
  `clippy::too_many_arguments`, a style lint; no correctness assertion is suppressed, and the
  same commit *removes* dead code. Its message notes the clippy failure had been tail-masked
  — the exact trap already in `lessons_learnt.md`, caught pre-integration.

---

### Signals from the journals

Ten journals were touched in the window. These are the highest-value planning inputs.

**1. The mandated render-diff — a nuanced picture, better than yesterday except where it
matters most.** `CLAUDE.md` requires a render-diff inventory for any `voices.rs` /
`engine.rs` / `drums.rs` / `sampler.rs` change. Per work line:

| Work line | Render-diff |
|---|---|
| MicroCab bank-gated ripple | **CONFIRMED run** — 11 changed / 0 contamination |
| Trim wind GM77/78/79 | **CONFIRMED run** — 6 changed / 0 contamination |
| GM109 bagpipe loop click | **CONFIRMED run** — 124 albums, 0 changed |
| GM76 blown bottle | **CONFIRMED run** — 0 contamination |
| GM96 rain | **CONFIRMED run** — 0 contamination |
| GM sweep units U1–U3 (GM86/87, GM106, bagpipe zones) | **CONFIRMED run** (U1 twice) |
| U4 steel | N/A, correctly — test-only |
| **GM4/GM10/GM15 LA onsets** | **CONFIRMED DEFERRED** — journal lists it under "Remaining … as the pre-integration check"; it reached trunk anyway |
| Pluck Phase 2 | Run **pre-rebase**; argued rather than re-run after rebasing onto a trunk that had overlappingly LA-wrapped GM15/GM34. Post-rebase **UNVERIFIED** |
| **k=2 velocity law + per-kit drum comp** | **UNVERIFIED — no mention of render-diff in the 249-line journal or in any of its six commit messages** |

The k=2 gap is the one that matters. Its whole point is to re-level every album, so
"expected diffs" is not the question — the check's value there is catching *un*expected
movement in the voices claimed exempt (GM16–20, GM96, GM109, the timbre floors). INFERENCE:
that verification was never performed.

**2. The cross-family consult is earning its keep — and the classes of defect it catches are
generalisable.** Same-family first: a 3-lens adversarial review turned 11 raw findings into
2 survivors, both the same defect. Then Fable 5 + Codex `gpt-5.6-sol-xhigh` found **six real
defects the in-family pass had missed**:

- a v≥32 fit applied below its domain (+32.9 dB at v1) — self-introduced extrapolation;
- a false statement written into code with test authority — the exclusion "pipe organs are
  velocity-independent", when they measure k=2.000 exactly. The journal's own conclusion:
  *"An exclusion with a false justification is worse than a wide tolerance."*
- scope leakage through a shared helper — the three "kept" timbre floors call the changed
  `vel_amp`, so "timbre untouched" was false; forced a `vel_ctrl`/`vel_amp` split;
- differential-test asymmetry — a test pairing a wrapped `make()` against a bare `Pluck`;
- a bound derived from a retired constant;
- **the wrong measurement instrument** — the oracle used unweighted RMS while the evidence
  used BS.1770 momentary. *"Measuring the fix with a different instrument from the one that
  found the defect is how you ship a green suite and a wrong synth."*

Also: a census by literal-pattern grep **missed** a level floor written with computed
constants (`voices.rs:8098`), which would have dropped the contrabass 5.11 dB at full
velocity. And the author recorded a calibration note on themselves: *"I relayed several of
those to Arthur as significant BEFORE refutation ran. They did not survive."*

**3. False premises disproved, all measured.** "Zero in-repo usage of GM86/87" — wrong,
Heliopause authors GM87 (*"trust the MIDI scan, not the audit"*). "Brighter cymbals mask the
lead" — *"intuitive and false"*; the kit hurts via the normalizer and limiter. A ~12 dB steel
high-key seam excess measured 1.03–1.96× today — already fixed in the interim
(*"MEASURE the claimed defect before designing the fix — a 2-day-old measurement in a
multi-agent repo may already be history"*). A Schroeder-dispersion fix was implemented and
made DULCIMER *worse*.

**4. Process failures worth guarding.** A piped `| tail` masked a failing clippy exit and the
commit landed before the lint was seen — **the second instance of this trap**; it belongs in a
hook, not a lesson. `deltic timeout` **detached** the python render-diff job and returned a
false "completed". Two independent lanes hit the identical 24-bit-WAV trap. And
`render_diff.py:116-118` counts ch9/ch10 program changes as melodic programs, producing false
NOT-REACHED rows — a harness bug that erodes trust in the one mandated check.

**5. A practical caveat before any listening session.** The album renders on disk are in
**three different synth states**, so any cross-album A/B is confounded until
`render-catalog` is re-run. Worth doing before Arthur's ear is used as evidence.

**6. Handoff, now superseded.** `wrk_journals/2026.07.20 - JRN - GM sweep build queue.md`
names U5 — the `--solo 8` hang — as the *"LAST UNIT"*, unclaimed, *"no worktree minted"*.
That journal predates the fix: a worktree was minted and the bug root-caused at 02:35, 21
minutes after this report's HEAD. Reconciled below.

---

## Branches and worktrees

**The estate shrank and the risk concentrated.** 31 → 27 worktrees; unique unmerged commits
25 → **17** across **7** branches (was 8). But two genuine work-loss exposures now exist that
did not yesterday.

### The headline: the `--solo 8` fix is one unpushed commit on one machine

`task/20260721-DEV-HUM-solo8-hang-diagnosis`, 1 ahead / 0 behind, commit `075051d`
`fix(ferrosintesis): flush denormal-parked feedback states — the --solo sparse-mix crawl`,
committed 2026-07-21 02:35 — 21 minutes after trunk HEAD. CONFIRMED by direct read:

- **Real fix, real root cause**, and not the stuck-voice theory the ledger suspected. An IIR
  feedback state never reaches zero; it parks below ~1e-20, goes subnormal, and every later
  sample pays the denormal arithmetic penalty. A sparse `--solo` mix leaves the always-running
  buses (hall combs/allpasses, cathedral FDN lines and shelves, echo cross-feedback, glue
  envelope, `Biquad`/`OnePole`) churning parked state for the whole render. That mechanism
  explains every observation on record: 0 live voices at checkpoints, reproduces on two
  binaries, busier solos and the full mix unaffected.
- **Regression guard present**, stated observed-red-first:
  `reverb::tests::tanks_do_not_park_below_the_flush_floor`.
- **Author-reported green**: 456.4 s → 44.1 s (10.3×) with identical render stats, full suite
  596/596 including goldens and byte-stability canaries. NOT independently re-run — this pass
  is read-only. The byte-identity argument is plausible: the flush floor is ~340 dB below the
  16-bit dither floor.
- **Not integration-ready as-is.** Three gaps, all CONFIRMED: no version bump
  (`Cargo.toml` is 0.21.53 on both sides), no ledger flip (`MM-BUG-KILN-00027` still reads
  `Open`, and the branch carries the Open file too), no journal.
- **Loss risk HIGH.** Origin holds only `origin/main` and
  `origin/task/20260718-TSK-HUM-max-richter-leftovers-album`. This commit exists as one local
  ref on KILN and nowhere else.

### The second exposure: three commits on no branch at all

CONFIRMED by the lead. The `cal-baseline` scratch worktree sits at `c9b1dec`, **3 commits
ahead of `origin/main`**, and `git branch -a --contains c9b1dec` returns **nothing** — they are
reachable only through a detached worktree HEAD and become garbage-collectable the moment that
directory is removed. Content is the BS.1770 momentary-loudness work plus two
instrument-balance HLDs. Mitigating: trunk already carries both docs and `loudness.rs` has the
momentary code, so it most likely landed by another route — INFERENCE, not byte-checked.
**Branch it or verify it before touching that directory.**

### Branches with unique commits (7; 17 commits)

| Branch (`task/…`) | Ahead/behind | Work | State |
|---|--:|---|---|
| `20260721-DEV-HUM-solo8-hang-diagnosis` | 1/0 | Denormal flush — the `--solo 8` crawl | **Integrate today** (needs bump + ledger + journal) |
| `20260714-DEV-HUM-sampled-acoustic-drum-kit-…` | 4/426 | Sampled kit Stages D+E | WIP, 7 days old; behind grew 362 → **426**. Rebase cost near-terminal |
| `20260717-FIX-CDX-fix-smf-running-status-…` | 3/341 | SMF running status across meta events | Looks-complete, real parser correctness. Best cherry-pick candidate |
| `20260715-FIX-HUM-investigate-instrument-voice-collisions` | 3/397 | Audition grid-snap + journal | Looks-complete, low value |
| `20260718-TSK-HUM-max-richter-leftovers-album` | 3/293 | "The Remaining" scaffold + T1 | WIP; **the only branch also on `origin`** |
| `20260719-FIX-HUM-fix-brush-slap-…` | 2/117 | Brush-slap velocity scaling | **SUPERSEDED — delete, do not merge** |
| `20260719-TSK-HUM-driven-guitar-29-30-…` | 1/117 | GM29/30 `+6 dB` + oracle + golden | Unique and NOT superseded; golden conflict likely |

Two supersession checks, both CONFIRMED and both answering open questions: **brush-slap is
dead work** — trunk fixed the same defect at `7328c70` by extracting `noise_vel_gain`, and the
ledger flipped *before* the branch's own final commit; the branch holds a parallel,
differently-calibrated implementation (ghost/full 1.525 vs trunk's 1.238). **Driven-guitar
+6 dB did not land** — `engine.rs:641` still reads `24-31 Guitar (untouched)`, so it is
genuinely unique, but at 117 behind its re-baselined golden row has almost certainly moved;
re-measure rather than replay.

### Dirtiness sweep — retried by the lead, still blocked

**UNVERIFIED, second day.** I attempted it myself, not only via a subagent. `git -C <path>
status --porcelain` is refused by this session's sandbox; so are a bash loop over the worktree
list and a PowerShell equivalent. The only observation obtained: **this report's own worktree
is clean.** That is 1 of 27. The prior report found six worktrees dirty, so a non-trivial dirty
population remains the standing prior. **Do not reap anything on the strength of this section.**

### Estate composition and safe clutter

Today: 1 main + 1 stray agent + 16 task + 9 scratch. The −4 is entirely scratch; the `AB-*`
family is gone. INFERENCE: **attrition, not a hygiene pass** — the two branches yesterday
called ACTIVE both landed and are now 0-ahead. The habit persists: two new scratch trees
appeared in 24 h. Of the nine scratch trees, eight are ancestral to `origin/main` and safe;
`cal-baseline` is the exception above.

**Nine merged branches have no worktree and zero unique commits** — one `20260717-DOC-CDX-…`,
four `20260718-FIX-CDX-…`, four `20260719-FIX-CDX-…`. Pure ref clutter, no dirtiness question,
**the one reap available today that the blocked sweep does not gate.** A further 8 task
worktrees are 0-ahead and reapable *pending* the sweep. The stray
`.claude/worktrees/agent-a15ac57b885cb034f` has outlived its session for a second day.

---

## Repo hygiene

**Worse on every re-tested count except CLAUDE.md.** Nothing was pruned; every ledger grew.

### The Cargo.toml MSRV violation is now in its second day — and survived twelve edits

CONFIRMED by the lead's own read. `crates/ferrosintesis/Cargo.toml` declares
`rust-version = "1.87"` (`:5`) and publishes to crates.io. The prohibiting comment sits at
`:43-44`: *"keep each dependency on ONE line — a multi-line inline table is invalid TOML 1.0
and cargo at our MSRV (1.87) refuses to parse it."* Three lines below,
`ferrosintesis-samples-drumkit` (**`:47-51`**) and `ferrosintesis-samples-orchestral`
(**`:62-66`**) still span five lines each.

The line numbers shifted +3 from yesterday because three new sample crates were added above
them. That is the sharpest form of this finding: **`git log e3835b1..HEAD --
crates/ferrosintesis/Cargo.toml` shows twelve commits touching this exact file in 24 hours**,
and the violation survived every one. No guard exists anywhere — grep across `*.rs`, `*.py`
and `*.toml` finds only the prose comment and `lessons_learnt.md`. Whether cargo 1.87 refuses
it *today* remains INFERENCE (no cargo was run); the invariant violation is fact.

### CLAUDE.md drift — genuinely improved, structurally

`docs(ferrosintesis): drift sweep — un-invert GM19, Fx row, 14-row controllers table`
(2026-07-20 22:08) did the right thing: it **replaced the version pins with point-at-source
wording so they cannot re-stale**, and its own message records that *"every correction was
re-verified against current source by a 6-agent fan-out before editing; three claimed drifts
proved FALSE and were not 'fixed'."* That is the fix yesterday's plan asked for.

| Claim | Reality | Verdict |
|---|---|---|
| version — now "trust `crates/ferrosintesis/Cargo.toml`" | 0.21.53 | **FIXED structurally** |
| "the **two** asset crates" | **21** sample crates | UNCHANGED |
| "those **218** files … **17.73 MiB** sample bank" | **1,020** tracked sample `.wav` | **WORSE** (bank grew) |
| `albums/` = fable5, opus4-8, gpt5-5, gpt5-3-spark | `albums/gpt5-6/` exists, still unlisted | UNCHANGED |
| no mention of transcription tooling | `transcribe.py`, `TRANSCRIBE.md`, `test-corpus/` — **0** mentions | UNCHANGED |

A hedge was added ("the per-crate inventory lives in `tools/ferrosintesis-samples/README.md`
— trust it, not counts quoted here"), yet the hard-coded `218 / 17.73 MiB` sentence survives
four sections later, so the file now contradicts itself. Root `README.md`'s `0.13.5` pin is
**gone**, but it still says "the two CC0 asset crates".

### Ledgers all grew; none were triaged

- **`lessons_learnt.md`: 68 entries against a stated cap of 20** (was 61). CONFIRMED by the
  lead. Seven added, none pruned. Index-v1 conformance remains **0** — every entry wraps its
  lead claim mid-sentence, so the SessionStart hook still injects half-sentences. CLAUDE.md
  and the file header agree on the cap of 20, so the discrepancy suspected yesterday does not
  exist.
- **`scratchpad.md`: 36 open items** (was 30) — +6 in 24 h, inflow up, nothing closed
  (3 were closed by the drift sweep, 9 arrived). CONFIRMED. The six newest are all quality or
  coverage nits — GM76 loop voices not following the k=2 law; `DRUM_VEL_LEVEL_EXP` measured on
  V3 only; `LoopVoice` drift; GM109 baking 8 of 26 WAVs; GM32/GM33 indistinguishable to
  Arthur; a stale golden mix fixture passing only on ±20 % tolerance.
- **Cross-ledger accuracy issue:** the `--solo 8` hang is now open in **two** ledgers with no
  pointer between them — `scratchpad.md:485` and `bugs/MM-BUG-KILN-00027.md` — and, since the
  fix landed off-trunk, **both lag reality.**

### Growth, tests, and one new precedent

- Tests: **678 `#[test]` across 36 files** (was 640/32), `voices.rs` 232 — concentration eased
  slightly to 34 % of the suite. CONFIRMED by the lead.
- Tracked `.opus`: **0**. The de-commit held.
- Tracked `.wav`: **1,054** (was 980) = 1,020 sample bank + 32 `freesound-src` + 2 gong.
- **New precedent, correctly sized:** `tools/ferrosintesis-samples/freesound-src/` now tracks
  **32 raw source WAVs**, newly un-ignored. All 32 name-match a baked crate asset — 20 in
  `-ccby`, 11 in `-orchestral2`, 1 in `-bottle` — so the repo now tracks both the input and the
  output of the bake. **At 5.0 MiB this is not a bloat emergency** (the `.opus` problem was
  ~3.1 GiB); it is a precedent worth an explicit decision before the next sourcing round, not
  an alarm. One reader claimed the 11 music-box files "were never baked" — that is wrong; they
  are in `-orchestral2`.
- **`.deltic-integrate.toml` still does not exist.** This repo runs the raw-Git integration
  lane — and the journals name the concrete cost: `deltic integrate --push` gates and pushes
  but **skips the version bump**, which is why U1/U2 landed un-bumped and needed two catch-up
  bumps at 00:56. One file fixes this permanently.

---

## Prior tracked issues — status audit

| # | Issue | Status |
|---|---|---|
| 1 | Cargo.toml MSRV inline tables | **UNCHANGED** — and the file was edited 12× |
| 2 | Render-diff never run for 5 sampled-onset units | **MIXED** — most new lines *did* run it; GM4/10/15 deferred again and shipped; k=2 has no record at all |
| 3 | LA layer deletes the model onset | **UNCHANGED** — no verdict; 3 more LA families shipped on top |
| 4 | CLAUDE.md stale | **PARTIAL — the good kind.** Version/counts de-pinned structurally; `gpt5-6/`, `transcribe.py` and the 218/17.73 MiB line remain |
| 5 | 25 unmerged commits / 8 branches | **PARTIAL** — now 17 / 7, but by branches landing, not triage. Drum-kit branch behind 362 → **426** |
| 6 | Worktree dirtiness UNVERIFIED | **UNCHANGED** — re-probed by the lead, still refused |
| 7 | Reqs Gate 2 never run | **WORSE** — 17 → 23 Implemented, still 0 Satisfied, and Gate 1 is now bypassed too |
| 8 | MM-REQ-KILN-00003 vacuous oracle | **UNCHANGED** |
| 9 | Bugs 00001 + 00013 awaiting two-eyes | **UNCHANGED**, and the queue doubled to 4 |
| 10 | lessons_learnt over cap / non-conformant | **WORSE** — 61 → 68 |
| 11 | scratchpad 30 open + `--solo 8` hang | **MIXED** — 30 → 36 open, but the hang is **root-caused and fixed off-trunk** |
| 12 | Codex trailer spellings | **SUPERSEDED in-window** — zero Codex trailers now, i.e. under-attribution replaced over-spelling |

**Of the eight plan items, one was substantively actioned, one partially, six untouched.**

The one is item 5 (refresh CLAUDE.md). It matters that the drift sweep implemented the
report's *specific* recommendation — de-pin the version rather than hand-edit it again —
20 hours after the report proposed exactly that. No commit or journal cites the report, so
attribution is INFERENCE; but the match is too exact to call coincidence. The partial is
item 3: 00015's history was appended (incidentally, as part of the sampling programme's own
close-out), but neither bug was closed.

The six untouched include the cheapest item on the list — collapsing two dependency tables
onto one line each, ten minutes, on a crates.io publish-blocker.

**One item was anti-actioned.** The report explicitly said "not recommended today: starting
the remaining GM4/10/15 units of the LA sourcing HLD". They shipped the same day — with the
render-diff deferred, thereby reproducing tracked issue 2 in the act.

INFERENCE, and the honest reading: these reports describe the repo accurately and influence
it weakly. They are not written into a void — one specific recommendation was followed
precisely — but the throughput of the build loop vastly exceeds the throughput of anything the
report asks a human to decide.

---

## Tracked issues

1. **The `--solo 8` fix is one unpushed commit on one machine.** Root-caused (denormal
   parking in IIR feedback state), guarded by a fail-first regression test, author-reported
   596/596 green. Needs a bump, a ledger flip and a journal, then integration. Highest value,
   lowest effort, and the only copy is on this disk.
2. **`cal-baseline` holds 3 commits reachable from no branch.** Removing that worktree makes
   them garbage-collectable. Branch or verify before touching it.
3. **The `Cargo.toml` MSRV violation is in its second day and survived twelve edits to that
   exact file.** A prose comment three lines above the defect has now failed twice. It needs
   the fix *and* a guard.
4. **The k=2 velocity law — the widest-blast-radius change in the repo's history — has no
   render-diff record**, and GM4/10/15 deferred theirs and shipped anyway. The check's value
   here is proving the *exempt* voices did not move.
5. **The reqs ledger now bypasses both human gates.** 23 `Implemented`, 0 `Satisfied` on day
   13, intake ~6/day against 0/day drain, and new reqs go `Draft → Implemented` without ever
   being `Accepted`.
6. **The bug closure lane is frozen.** `Closed` static at 19 since 07-19; the
   `Fixed`-awaiting-two-eyes queue doubled to 4, all Claude-authored, none verified. Codex is
   the eligible verifier. 00017 names no oracle, which will block its verifier first.
7. **The LA-onset defect report is 7 days parked while three more LA families shipped on top
   of it.** Measured, code-confirmed, fix already A/B rendered. It needs a decision, not a
   design — and it now has a queue position as pluck Phase 4.
8. **Worktree dirtiness is still UNVERIFIED**, re-probed and re-refused by this session's
   sandbox. It blocks two otherwise-justified reaps. The 9 merged branchless refs are *not*
   gated by it and can go today.
9. **`lessons_learnt.md` is at 68 against a cap of 20 with 0 conformant entries**, so the
   SessionStart hook injects truncated fragments; `scratchpad.md` is at 36 open with no triage
   pass and inflow rising.
10. **Two harness defects erode the checks themselves**: `render_diff.py:116-118` miscounts
    ch9/ch10 program changes as melodic programs (false NOT-REACHED rows), and a piped
    `| tail` masked a failing clippy exit for the *second* recorded time. The second belongs
    in a hook, not a lesson.

Watch, not yet issues: the ledger ID-collision race that briefly deleted two bug files; the
growing class of loop-recording voices exempt from the velocity law with no law of their own;
Codex doing work with zero `Co-Authored-By` attribution; and `freesound-src` establishing a
track-the-source-too precedent (5.0 MiB today).

---

## Plan for today

Ordered by value per unit of effort. Items 1–3 are small, verified and reduce real risk.
Items 5–6 are Arthur's, and they are what everything else is now waiting on.

1. **Integrate the `--solo 8` denormal-flush fix.** Add the version bump, flip
   `MM-BUG-KILN-00027` to `Fixed`, close the duplicate `scratchpad.md:485` entry with a
   pointer to the bug, write the journal, re-run the suite, then ff-push. This closes the only
   hard failure in the scratchpad, removes a single-machine loss risk, and is the cheapest
   real progress available.
2. **Rescue `cal-baseline`'s three orphan commits** — one `git branch` against `c9b1dec`
   before anything touches that directory. A minute's work against silent, unrecoverable loss.
3. **Fix the `Cargo.toml` MSRV violation and graduate the lesson into a guard.** Collapse both
   dependency tables to one line, then add a check that fails if any dependency spans lines.
   The prose comment has now failed twice; prose is not a guard. Confirm with
   `cargo +1.87 check --workspace`.
4. **Run the deferred render-diffs — k=2 first, then GM4/10/15.** Baseline from the commit each
   was rebased *onto*, never a fresh `origin/main` build. For k=2 the question is specifically
   whether the named exempt voices (GM16–20, GM96, GM109, the three timbre floors) moved. Fix
   `render_diff.py:116-118` first, or its false NOT-REACHED rows will muddy the answer.
5. **Arthur — two decisions, and they gate more than anything an agent can do today.**
   (a) The listening verdict on `2026.07.14 - CR - LA sample layer deletes the model onset`;
   it is now seven days old and three more families have shipped on top of it.
   (b) Whether the reqs state machine is enforced or retired. At 6/day intake and 0/day
   acceptance, a batch Gate-2 session grows by roughly an hour a day.
   **Before either listening session, re-run `cargo run --release -p render-catalog`** — the
   renders on disk are in three different synth states, so any cross-album A/B is currently
   confounded.
6. **Book one cross-family (Codex) verification pass** for the four `Fixed` bugs. Three name
   their regression oracle; 00017 needs one chosen first. This is a bounded session that
   unfreezes a lane which has not moved in two days.
7. **Add `.deltic-integrate.toml`.** One file, and it stops `deltic integrate --push` silently
   skipping the version bump — the documented cause of yesterday's two catch-up bumps.
8. **Cheap hygiene, if capacity remains:** delete the 9 merged branchless refs (not gated by
   the dirtiness sweep); delete the superseded brush-slap branch rather than merging it; and
   get one session with a wider Bash allowlist to finally run the worktree dirtiness sweep,
   which has now blocked branch triage for two days.

**Not today.** The anchor fidelity push — still the largest unstarted programme, still priced
at ~84 album re-renders per stage plus a human A/B panel each, which is exactly the scarce
resource already gating three other lines. Also not today: more new GM voice units. The last
24 hours added six and closed none of the governance debt they generate; items 1–4 are
unblocked correctness work on code that has already shipped.

---

## Verification notes

Deterministic facts (ledger counts, commit counts, branch ahead/behind, file contents, test
and asset counts) were read from source by the lead. Reader claims were checked, and two were
refuted:

- **REFUTED** — one reader reported bugs as 8 Open / 4 Fixed / 18 Closed. The lead's direct
  `State:` scan of all 30 files gives **7 / 4 / 19**. The report uses the direct scan.
- **REFUTED** — one reader reported the 11 `musicbox_*` source WAVs as "committed source for a
  voice that was never baked". They are baked, into
  `crates/ferrosintesis-samples-orchestral2/samples/`. The same reader's framing of
  `freesound-src` as "the `.opus` growth mechanism restarted" was downgraded on measurement:
  5.0 MiB, against ~3.1 GiB for the opus problem.
- **CORRECTED** — one reader cited the Cargo.toml violation at the *prior* report's line
  numbers. The lead's direct read gives `:47-51` and `:62-66`; the +3 shift is itself evidence
  the file was edited without the defect being fixed.
- **MODERATED** — one reader concluded these reports are "written into a void". The evidence
  does not support the absolute form: plan item 5 was implemented in the specific manner
  recommended, 20 hours later. The report states the weaker, supportable version.
- **RECONCILED** — a journal names the `--solo 8` unit as unclaimed with "no worktree minted";
  the fix commit is timestamped 02:35, after that journal and after this report's HEAD. The
  branch survey supersedes the journal.

- **CONFIRMED by direct lead read** — `Cargo.toml:47-51` / `:62-66` and the twelve in-window
  commits touching that file; the `--solo 8` fix commit in full, its single-commit branch and
  its absence from any remote; `cal-baseline` at `c9b1dec` contained by no branch, 3 ahead;
  1,054 tracked `.wav` (1,020 bank + 32 source + 2 gong); 21 asset crates; 678 tests / 36
  files; 68 lessons entries; 36 open scratchpad items; 27 worktrees / 27 local branches; 140
  tracked `.mid` with 0 added and 1 modified; no `.deltic-integrate.toml`.
- **UNVERIFIED, held not dropped** — worktree dirtiness (the lead reproduced the sandbox
  refusal across three routes); the `--solo 8` fix's 596/596 green claim and its 10.3× speedup
  (read-only pass, no tests or renders run); whether cargo 1.87 refuses the manifest today;
  `.git` pack size.
- **INFERENCE, labelled inline** — the attribution of the CLAUDE.md fix to yesterday's report;
  the reqs-ledger repurposing read; the render-diff omission on k=2; rebase-cost and
  abandonment-risk judgements; and the claim that `cal-baseline`'s content already landed by
  another route.
