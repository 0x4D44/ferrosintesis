# 2026.07.23 - DAILY - KILN-Windows - Repo state report

Pass mode: read-only observation. No tests run, no app run, no renders, no fixes, no
cleanup, no ledger transitions, no tags, releases or deploys. Integration is owned by the
scheduler, not by this pass.

Environment: Windows host `KILN`, worktree
`D:\worktrees\midi-music\20260723-OPS-CLA@KILN-daily-report-020015`, HEAD `db584d7`
== `origin/main`.

Baseline: `wrk_docs\2026.07.22 - DAILY - KILN-Windows - Repo state report.md` (its own
commit `27acd7f`). Window under review: `27acd7f..db584d7`, **34 commits**, 105 files,
+8,531 / −372.

Method: eight parallel readers over independent state sources (bugs, reqs, design docs,
commit log, branch estate, hygiene, prior-issue audit, headline verification). Load-bearing
numbers were re-derived by the lead from source, and the three headline conclusions were
put through an adversarial check. **One baseline claim was corrected (SELF-APPROVED count),
one ledger figure was found loose (KILN-00042's "6 of 22"), and one load-bearing process
finding was confirmed by hand** — see Verification notes.

---

## Headline

**The engine caught up to its measurements. After a feature-free bookkeeping window, the
M-CAL calibration programme landed on trunk and a High-severity plucked-decay defect got a
real, tested fix — but the same run tripled the Open-bug queue with defects nobody yet owns,
shipped its two timbre changes without the mandated render-diff inventory on record, and
left every human-gated lane exactly where it was.**

The character reversed completely. 07-22 was the first **zero-`feat`** window in the series;
this window lands **five `feat` and five `fix`**, and the M-CAL work that was "in flight, the
estate's centre of gravity" is now integrated: the trim-derivation tool, a certified
128-program derivation, a two-reference panel, and the `DRUM_FORWARD` removal are all on
trunk (`engine.rs:80` now reads `1.0`, was `2.0`). The window's best engineering is
**KILN-00042** — the Karplus-Strong plucked/struck voices decayed 2–12× too fast, steepening
with register, killing the ring on 22 GM programs. The fix (a relative-budget `DamperHold`
generalising the proven `treble_hold_hz` mechanism) is on trunk with **real, non-phantom
regression oracles**, and repairs the bulk of the family (median register tilt 5.00× → 3.42×,
koto 14.1× → 1.0×).

That is genuine, and it is the good news. The rest of the window is the cost side of moving
that fast:

1. **The bug queue nearly tripled — 8 → 19 Open — and the growth is healthy *discovery*, not
   rot.** The M-CAL v3 reference panel is a new measurement instrument that made a whole class
   of decay/level defects machine-visible with numeric dB targets against two reference synths.
   You cannot fix what you cannot measure. But the 15 new bugs are **unowned**, and the queue is
   now a backlog of known-broken timbre/level defects with no plan attached to it.
2. **Two shipped timbre changes carry no render-diff inventory on record.** CLAUDE.md mandates
   the catalog render-diff as a *report* for any `voices.rs`/`engine.rs` change. The KILN-00033
   controller fix in this same window did run one (10 changed / 130 identical). The KILN-00042
   damper law (`voices.rs`) and the M-CAL 5-trim change (`engine.rs`) did not — so their actual
   catalog blast radius is unknown, right as more timbre work is queued to stack on top.
3. **A per-host bug-ID collision nearly deleted the window's headline bug file.** Two worktrees
   minted `KILN-00042` concurrently; the reconciliation commit `e7a852a` `deleted` all 325 lines
   of the plucked-decay bug, and only the *next* commit `9586b2e` re-added them. It was caught by
   a manual clean-tree audit — no structural guard exists.
4. **Everything that needs a human is frozen, again.** Reqs `Satisfied` is 0 on **day 15** with
   `reqs/` untouched (Accepted 0, so no drain can run); the LA-onset listening verdict is **9–10
   days** parked and still gates pluck Phase 4; and the 07-22 report's own top-two plan items —
   the untracked running-status parser defect and the stranded cloud-drain `00030` fix — are
   **exactly** where they were.

Net: agent throughput surged and produced two weeks' worth of real engineering in a day, while
the queue of things that need a person — a listening ear, a triage decision, a render-diff
report, an out-of-band shell — grew faster than it drained.

---

## Bug ledger

**The queue nearly tripled, but every new entry is a *measured* defect and the growth is
discovery outrunning repair, not decay.**

| State | 07-22 | 07-23 | Δ | IDs (07-23) |
|---|---:|---:|---:|---|
| Open | 8 | **19** | **+11** | 00016, 00019, 00024, 00028, 00029, 00030, 00036–00041, 00043–00046, 00048, 00049, 00050 |
| Fixed (awaiting two-eyes) | 0 | **1** | +1 | 00042 |
| Closed | 27 | **30** | +3 | +00033, +00035, +00047 |
| **Total** | 35 | **50** | **+15** | 00036–00050 filed |

CONFIRMED by a direct `- **State:**` scan of all 50 files. Newly closed this window: `00033`
(effect-send persistence), `00035` (GM System-On live≠offline) — both were Open-and-claimed at
07-22 and closed via independent verification — and `00047` (the reminted GM0 piano bug).

### The 15 new bugs — four coherent groups, one measurement instrument

- **00036–00041 (M-CAL audition, `0676071`)** — six timbre defects: GM21 accordion thin/quiet
  (Med), GM31 flageolet is a bare retuned pluck (Low), GM61 brass has no LA layer (Med), GM107
  koto low-register level explosion (Med — later superseded by 00042), GM120 fret-noise pinned
  near-silent (Low), GM126 applause is a monochromatic sizzle (Med).
- **00042–00046 (M-CAL v3 certified run, `f70d1bf`)** — five *measured* level/decay defects with
  numeric SC-55mkII / S-YXG50 targets: **00042** KS plucked decay 2–12× too fast (High, **Fixed**);
  00043 GM7 clavinet decay ~3× too fast (Med); 00044 GM6 harpsichord velocity-response 9.6 dB off
  (Med); **00045** bass family GM32–39 spans 21–25 dB internally vs references' ~9 dB (High); 00046
  GM48/49 string-onset not level-matched to the model (Med).
- **00047 (GM0 piano, `aa3f2ef`)** — upright attacks thump across short phrases (Med, **Closed**).
  This is the *reminted* old-00042 — see the ID-collision note.
- **00048–00050 (couplings KILN-00042 exposed, `2430c79`)** — 00048 KS damper corner scales with
  velocity so decay rate depends on pluck strength (Med, blocks 00042's dark basses); 00049 the
  e-bow sustainer pins its hold in the loop domain against an output measurement (Med); 00050
  above the crossover the damper-hold orders held corners by t60 not brightness (Low, the
  top-register residual).

The whole +15 traces to one new instrument — the M-CAL v3 certified 128-program derivation
against two reference synths. That is discovery working as designed: a class of previously
invisible decay/level defects became machine-visible with dB targets. The queue is dominated by
a single root-cause family (KS decay + level calibration), and 00042 is its shared fix, which
already **supersedes 00039** and absorbs the discarded GM29/30 +6 dB workaround. So the 14
still-Open siblings are closer to *one investigation* than fourteen.

### KILN-00042 — the headline fix (Fixed, awaiting two-eyes)

**What it is.** `voices.rs:3459`, the KS loop step, applies two register-wrong per-round-trip
losses shared by every `Pluck` preset: a fixed-cutoff in-loop damper whose dB/s grows as ~f³,
plus an f^−0.55 t60 key-scale. Together they make plucked notes die 2–12× faster than *both*
references, worsening monotonically up the keyboard, across 22 GM programs (guitars 24–31,
basses, sitar/banjo/shamisen/koto, harpsichord, clavinet, dulcimer, pizzicato, harp).

**The fix (`8e1da29`) is sound, with two honest caveats.**
- The relative-budget `DamperHold {Off, Derived}` opens the loop corner until the damper
  contributes ≤ 30% of the preset's authored t60 loss. It ships **real** regression oracles —
  `damper_hold_preserves_instrument_identity` (`voices.rs:16683`, rendered) and
  `ks_decay_law_holds_across_register` (`voices.rs:14117`, emergent tilt) — verified to exist,
  not phantom. Suite 613/619 green.
- **Caveat 1 — the ledger's "6 of 22 Off" is a loose preset/program conflation.** By the actual
  GM→preset routing, the landed fix reaches **~19 of 22 default programs**; the 6 `Off` entries
  are *presets*, and two of them (OUD, DRIVE_LEAD) are CC0 alt-bank voices whose default programs
  *are* repaired. The High defect does not "mostly remain" — the bulk is addressed. The deferrals
  (GM33/35 → 00048, DRIVE_LEAD → 00049, piano family GM6/7 in flight, top-register residual →
  00050) are honestly ledgered with measured evidence and acceptance tests.
- **Caveat 2 — it is a partial fix.** Median tilt is 5.00× → 3.42×, still above the references'
  1.72–1.82×; the residual is tracked (00050, Low). And the shipped law changed *after* a
  two-expert consult, so any earlier render-diff is stale — see the render-diff finding below.

**SHA note.** The ledger cites `847510e`, which is **not** an ancestor of trunk; the integrated
commit is `8e1da29` (byte-identical patch on a different parent). This is the benign
pre-integration-remint pattern the 07-22 report already documented on 00001/00013, but the cited
SHA is still a dead link from a fresh clone. State is correctly `Fixed`, left open for another
set of eyes per the fixer-must-not-close rule.

### The ID-reminting collision was a real data-loss near-miss

Two worktrees minted `KILN-00042` concurrently: the M-CAL v3 run for the plucked-decay bug, and
the GM0-piano task for the upright thump. The reconciliation (`e7a852a` "remint GM0 piano defect
ID") **`deleted` the 325-line plucked-decay `00042.md`**, moving the GM0 record to `00047`; the
very next commit (`9586b2e` "preserve trunk bug ledger entry") re-added the plucked-decay file.
Had that second step been skipped, the window's headline High-severity bug would have vanished
silently. The end state is clean and self-consistent (00042 = plucked-decay Fixed, 00047 = GM0
Closed), but it was contained by a **manual audit, not any ID-allocation guard**. The per-host
sequential-ID hazard the 07-22 report flagged as theoretical just fired for real. (Cosmetic
fossil: commit `33236b1` "close MM-BUG-KILN-00042" now mis-names the GM0 bug in git history.)

### Actionability of the 19 Open

- **Machine-actionable now** (numeric reference-panel oracle exists): 00043, 00044, 00045 (High),
  00046, 00048, 00049, 00050. 00048 is also the unblocker for 00042's dark basses.
- **Blocked on human ears / timbre judgment:** 00036, 00037, 00038, 00041 (new) + 00019, 00024,
  00029 (pre-existing).
- **Blocked on assets:** 00016 (no clean CC0/CC-BY real-slap multisample). 00040 low value.
- **Blocked on a design call:** 00028 (the prototyped ch-10 `ROOM_SEND` fix re-mixes 86/124 tracks).
- **Fix-exists-off-trunk:** 00030 (cloud drain branch — see Branches).

**Every Open bug is unowned** — no `Owner` field is set on any of the 50 files. The M-CAL
discovery queue has no assignee and no plan.

### Trend — the discharge lane is still manual

Fixed drained to 0 at 07-22; it is now 1 (00042), which finally exercises the age-out mechanism
the 07-22 report noted was untested. But the lane still only turns by hand: 00042's *code* is on
trunk while the *bug* waits in `Fixed` for a booked two-eyes pass, and every Fixed→Closed this
window (00033, 00035, 00047) turned on a human-initiated verify. Nothing gates a `Fixed` bug's
age-out.

---

## Requirements ledger

**Nobody opened it — a third straight day.** CONFIRMED: `git log 27acd7f..db584d7 -- reqs/`
returns zero commits. Byte-frozen since `768526c` (2026-07-21).

| State | 07-21 | 07-22 | 07-23 | Δ |
|---|---:|---:|---:|---:|
| Draft | 4 | 4 | **4** | — |
| Accepted | 0 | 0 | **0** | — |
| Implemented | 23 | 23 | **23** | — |
| **Satisfied** | 0 | 0 | **0** | — |
| Total | 27 | 27 | **27** | — |

`Satisfied` has now been **0 for 15 days** (Gate 1 fired only in the 2026-07-08 batch).
`deltic reqs --ready` needs `Accepted` (`reqs/README.md:137`); Accepted is 0, so the ready set
is provably empty and **no drain can run**. This is a Gate-1 human-triage freeze, not triage cost
— the three easy `Flow: light` Drafts (charang 00021, lead-6 formants 00022, metallic-pad 00023)
are one small commit away, each with an oracle sketch already written.

**All four 07-22 traceability defects persist** (re-verified from source):
- `MM-REQ-KILN-00003` still names a **false-pass oracle** — test `synth_fx_97_99_101_103_sustain_as_pads`
  exists only as a deletion note (`voices.rs:19527`); a filtered `cargo test` on it exits 0.
- **10/10 old implementation SHAs are dangling objects** (spot-checked `073ea2c7…`, `a3eecb96…`,
  `373e7e1` — all `could not get object info`).
- The **six newest GM-sweep reqs carry no `Req-Id:` trailers** (33 repo-wide, zero for
  00019/00020/00024–00027); their integrated SHAs are live but un-enumerable by the documented
  Gate-2 revert path. Old reqs have trailers but dead SHAs; new reqs have live SHAs but no
  trailers — a human at Gate 2 has no mechanical way to enumerate what to revert.
- `MM-REQ-KILN-00018` is still mis-triaged `Draft` — its premise (commit `.opus` bytes) is
  obsolete (`git ls-files '*.opus'` = 0); correct triage is `Retired`.

---

## Design backlog

**The 07-22 stranding is resolved: 12 `wrk_docs/` commits, 7 new docs, +1,596 / −0.** All the
M-CAL design docs that were stranded on a branch landed, and the work reached source
(`DRUM_FORWARD` 2.0 → 1.0; `fc1ef10` applied the 5 panel trims).

### Complete — landed this window
M-CAL v2 envelope-guarded-metric HLD (`DESIGN AGREED, Arthur`), v2 tooling HLD (`SELF-APPROVED`),
the 128-program derivation report, the v3 certified report, the v3 **reference-panel** report
(adds a second reference and ships a trim only where the two agree within 3 dB — the
review-independence problem being engineered against), the GM0 upright-envelope HLD
(`SELF-APPROVED`), and the bug-35 live-SysEx HLD (`SELF-APPROVED`).

### Designed-but-not-started — cold docs (ages as of 2026-07-23)
| Doc | Status | Age | Blocker |
|---|---|---:|---|
| `2026.07.13 - HLD - anchor fidelity push (piano and string ensemble).md` | `DRAFT for review` | **10d** | ~84 re-renders/stage ×3 + a human A/B panel each |
| `2026.07.14 - CR - LA sample layer deletes the model onset.md` | `INVESTIGATION — awaiting Arthur's ear` | **9d** | One A/B verdict; cheapest unstarted item; gates pluck Phase 4 |
| `2026.07.18 - PLN - GM0 grand alternate banks audition.md` | `IN-PROGRESS` | **5d** | Human ranking of rendered auditions |
| `2026.07.19 - HLD - natural pluck excitation redesign.md` Phases 3/4 | `APPROVED` | **4d** | Phase 4 gated on the LA-onset CR above |

**CONFIRMED anchor-fidelity is still 100% absent from source** — no Railsback curve, stretch
tuning, or pitch-dependent piano inharmonicity anywhere in `crates/ferrosintesis/src`.

### SELF-APPROVED HLDs — 17 on trunk, and this corrects the 07-22 baseline
The 07-22 report asserted "the velocity-law HLD remains the only SELF-APPROVED header on trunk."
**That was wrong.** `grep -rl "SELF-APPROVED" wrk_docs/` nets **17** current SELF-APPROVED design
docs (dating to 2026-07-16), +3 this window (M-CAL v2 tooling, GM0 upright, live SysEx). Self-
approval is the **long-standing dominant design norm here, not an emerging one.** It is managed
procedurally — every header documents its review layers, and M-CAL v3's two-reference agreement
gate is real independence tooling — but **none carries a human `SIGNED-OFF` marker**, and the
pattern still shows the 07-22 failure shape: the GM0 self-approve (`99c95b2`) was followed by two
further GM0 fixes and a defect-ID remint, i.e. corrective churn after approval.

---

## Key commits

Range `27acd7f..db584d7`, 34 commits, 0 merges, 2026-07-22 07:21 → 2026-07-23 00:47.

| Metric | This window | Prior (07-22) | Δ |
|---|---:|---:|---:|
| Commits | **34** | 23 | +11 |
| Files / insertions / deletions | 105 / 8,531 / 372 | 25 / 1,110 / 32 | — |
| `feat` commits | **5** | **0** | +5 |
| `fix` | 5 | 6 | −1 |
| Version bumps | **0** (release-only) | 3 | −3 |
| New/modified `.mid` | **0 / 0** | 0 / 0 | — |

**Character shift: bookkeeping window → engineering window.** Type breakdown: 22 docs, 5 feat,
5 fix, 1 test, 1 chore. The five `feat` are the M-CAL landing (`ed9a015` trim tool + 128-program
report, `2f2a274` v2 guarded-metric tooling, `a2fd2fd` certify derivation, `fc1ef10` apply 5
panel trims + reference panel, `4cc1e02` remove `DRUM_FORWARD`). Three are dev-only tooling; two
touch shipped synth code.

**Zero version bumps — correct, not an anomaly.** The `version_bump = "release-only"` contract
held: five `feat` + five `fix` of shipped code and `Cargo.toml` version is unchanged at
**0.21.56**. Under release-only, a bump is now the anomaly.

**Attribution:** Claude Opus 4.8 credited 23×, OpenAI Codex 11×. Both families authored commits.
The KILN-00042 fix body cites a "two-expert consult (Fable + gpt-5.6-sol)" but carries only the
Claude trailer — consultation is acknowledged in prose, not trailer-credited.

**Most consequential by blast radius:** (1) `8e1da29` KS damper generalisation — changes every
plucked/struck preset's register decay; (2) `edf0a5c` + (3) `2f2baa4` GM0 piano — re-normalize 54
committed sample `.wav` and re-shape default-piano articulation for every render; (4) `4cc1e02`
`DRUM_FORWARD` → 1.0 — a global channel-10 level change; (5) `fc1ef10` 5 panel trims — retunes the
level balance of five instrument families.

**Composition at zero for a third day** — no `.mid` added or modified. The repo has been 100%
synth/calibration work for three days.

---

## Branches and worktrees

**M-CAL landed and its worktree is gone; the 9 merged branchless refs were reaped to zero; and
both 07-22 salvage exposures are unchanged.** `git worktree list` = **25** entries: the main
clone, one internal agent worktree, 12 task worktrees, and 11 detached render-diff/audition
baselines.

### Unmerged task work — 16 unique commits across 6 branches (was 25/7)
The −9 is exactly M-CAL landing; no new drift.

| Branch (`task/…`) | ahead/behind | Verdict |
|---|--:|---|
| `20260717-FIX-CDX-fix-smf-running-status-…` | 3 / 400 | **Highest-value salvage** — holds the parser fix + test for a live untracked defect |
| `20260718-…-max-richter-leftovers-album` | 3 / 352 | WIP; only task branch also on `origin` |
| `20260719-…-driven-guitar-29-30-…` | 1 / 176 | **Superseded** — Arthur decided (2026-07-22) to discard the +6 dB lift |
| `20260714-…-sampled-acoustic-drum-kit-…` | 4 / 485 | Rebase cost terminal — redo on trunk or delete |
| `20260715-…-instrument-voice-collisions` | 3 / 456 | Salvage, low value, un-rebasable |
| `20260719-…-fix-brush-slap-…` | 2 / 176 | **Superseded — reapable** (00001 closed citing trunk fix) |

### Two carry-overs, both unchanged and both still matter
- **The running-status parser defect is STILL live, untracked, and the fix is STILL off-trunk.**
  The single-`status` latch persists at `crates/ferrosintesis/src/midi.rs:233-239` (line numbers
  shifted from the 07-22 report's `184-190` as the file grew; mechanism byte-identical). A `0xFF`
  meta or SysEx overwrites `status`, so the next running-status data byte is mis-dispatched —
  silent corruption. `294aa94` on `20260717-FIX-CDX` fixes it with a test
  (`running_status_survives_meta_event`) but is not an ancestor of trunk, and grep for
  `running.?status` across `bugs/` is still **zero**. This was the 07-22 report's #1 plan item.
- **The cloud drain branch still strands a real `00030` fix.**
  `origin/claude/bugs-queue-2q-drain-609csu` (4 ahead / 57 behind, was 4/22) carries
  `427281a fix(sampler)… harpsichord floored onset` and flips 00030 to Fixed; trunk still shows
  `00030` **Open** and carries neither the fix nor the `00028` reclassification measurement.

### Reapable
The **9 merged branchless refs are gone** (reaped to 0 — an improvement). Remaining reapable but
gated: 5 merged 0-ahead task worktree/branch pairs; 11 detached baselines (all ancestors of trunk
or discharged-by-content — `SAMPLES`, `piano-release-baseline`, `AUDITION-base`, `KILN42-uke-*`,
`CAND-C`, `TRIM-baseline`, `verify-cello-seam`, `cal-baseline`, and the two `PROBE-*` probes).

**Dirtiness — UNVERIFIED, 4th consecutive day.** This worktree is clean; every cross-worktree
`git -C <path> status` is refused by the sandbox approval gate. 1 of 25 known clean, 24 unknown.
This has blocked reap decisions for four days and needs an out-of-band fix.

---

## Repo hygiene

Net direction: **worse on the standing issues.** Nothing on the 07-22 hygiene list was fixed;
two ledgers grew; both hard defects aged another day.

- **`lessons_learnt.md`: 70 → 72 entries, 730 lines, cap 20 (3.6× over); +2, 0 pruned.** Both new
  entries wrap their bold nugget across two physical lines, so the SessionStart hook still injects
  mid-sentence fragments. Index-v1 conformance stays ~4 of 72.
- **`scratchpad.md`: 34 → 36 open items** — reversing 07-22's one improving trend. The `--solo 8`
  entry (`:498`) still `- [ ]` while it contradicts **Closed** bug 00027 (day 2).
- **CLAUDE.md ↔ `.deltic-integrate.toml` contradiction — now live, not merely latent.**
  `CLAUDE.md:164-165` ("one version bump per integrated task") vs `.deltic-integrate.toml:3-8`
  ("integration NEVER bumps… release-only"); CLAUDE.md still never names the file
  (`grep -c` = 0). This window shipped source without a bump — correct under the contract, wrong
  under CLAUDE.md's rule. One-line fix outstanding.
- **`Cargo.toml` MSRV violation — day 4, still no guard.** `crates/ferrosintesis/Cargo.toml:47-51`
  (drumkit) and `:62-66` (orchestral) are still 5-line inline tables under a prohibiting comment
  (`:43-44`, "invalid TOML 1.0 … cargo at MSRV 1.87 refuses to parse"). No `.github/`, no tracked
  `.yml`/`.ps1`, no manifest linter. The `.deltic-integrate.toml` gate cannot catch it — fmt/clippy/
  test only need a manifest that *parses* on the current toolchain.
- **`render_diff.py` ch9/ch10 fold — day 6, untouched.** `tools/render-diff/render_diff.py:116-118`
  still folds a channel-10 kit-select PC into the melodic program set. Scratchpad entry still open.
- **CLAUDE.md content drift — unchanged** (file untouched): "the two asset crates" vs **21** actual;
  "218 files / 17.73 MiB" vs 237 sample `.wav` in the two named crates (1,020 across all 21);
  `albums/gpt5-6/` exists but is unlisted.
- **Assets flat:** 140 `.mid`, **0** `.opus`, 1,054 tracked `.wav` (the 54 GM0 sample re-normalizations
  were byte edits, not adds). Tests ≈ 705 (grep estimate, not run; was 683).

---

## Prior tracked issues — status audit

| # | Issue (07-22) | Status |
|---|---|---|
| 1 | Running-status parser defect untracked; fix stranded | **UNCHANGED** — still live at `midi.rs:233-239`, still 0 bugs, fix still off-trunk (400 behind) |
| 2 | Cloud drain branch strands `00030` fix + `00028` measurement | **UNCHANGED** — still 4 ahead/unmerged; 00030 still Open on trunk |
| 3 | CLAUDE.md contradicts `.deltic-integrate.toml`; never names it | **WORSE** — now biting (shipped source, no bump); `grep -c deltic-integrate CLAUDE.md` = 0 |
| 4 | Cargo.toml MSRV violation, no guard | **UNCHANGED — day 4** |
| 5 | Reqs traceability inverted (dead SHAs / missing trailers) | **UNCHANGED** — all re-verified true |
| 6 | Satisfied 0, nobody opened reqs/ | **WORSE — day 15**, zero reqs/ commits |
| 7 | Four work lines on one pair of ears | **WORSE** — LA-onset CR now 9–10d; window *added* a 5-trim listening panel |
| 8 | Worktree dirtiness UNVERIFIED | **UNCHANGED — day 4** |
| 9 | lessons_learnt over cap / scratchpad open | **WORSE (both)** — 70→72, 34→36; reversed the one improving trend |
| 10 | Two harness defects (render_diff, `\|tail`-masks-clippy) | **UNCHANGED** — render_diff.py day 6; no guard for either |

**0 DONE, 0 DISCHARGED, 4 UNCHANGED, 3 WORSE by age, 3 WORSE outright.** Not one prior tracked
issue was resolved. The window's 34 commits went into M-CAL, the GM0/plucked-decay chain, and
closing 00033/00035 — none of the ten items.

### Do these reports move anything? The honest read
CONFIRMED: **zero** commits or journals this window cite the report. The 07-22 plan's cheapest
items (file the parser bug, port the drain evidence, one-line the CLAUDE.md fix, close the `--solo
8` scratchpad entry) are **all still undone**. The engineering the estate *did* do — M-CAL,
KILN-00042 — was on its own trajectory, unrelated to the report's asks. The reports track state
faithfully; they are not, on this evidence, changing what the fleet does.

---

## Tracked issues

1. **The running-status parser defect is live on trunk, untracked, with the fix stranded 400
   commits back.** `midi.rs:233-239` mis-dispatches running status after a meta/SysEx event —
   silent corruption. `294aa94` fixes it with a test but is off-trunk; zero bugs mention it. File it,
   then decide cherry-pick vs re-fix. (07-22 #1, still open.)
2. **Two shipped timbre changes carry no render-diff inventory on record.** CLAUDE.md mandates it as
   a report for `voices.rs`/`engine.rs` changes; the KILN-00042 damper law (`8e1da29`) and the M-CAL
   5-trim change (`fc1ef10`) have none in their bodies or journals, and the KILN-00042 law changed
   *after* the consult (stale any earlier run). By contrast the KILN-00033 controller fix in the same
   window did run one. Their catalog blast radius is currently unknown.
3. **A per-host bug-ID collision nearly deleted the headline bug file.** `e7a852a` deleted the
   325-line `00042.md`; `9586b2e` re-added it. Caught by a manual audit — no ID-allocation guard
   exists. The hazard the 07-22 report called theoretical fired for real.
4. **19 Open bugs, all unowned.** The M-CAL reference panel created a coherent discovery queue with
   no assignee and no plan. Seven are machine-actionable now (00043/44/45/46/48/49/50); four new ones
   (00036/37/38/41) join the human-ears queue.
5. **KILN-00042 waits in `Fixed` for a genuine independent two-eyes**, and the GM0 close (00047) was
   **same-family** — OpenAI Codex both fixed *and* closed it, weaker than the fleet's cross-session
   norm. Re-verify 00047 independently, or record that its two-eyes was same-agent.
6. **The cloud drain branch still strands a real `00030` fix and the `00028` reclassification.** Port
   the `00028` measurement to trunk's bug file at minimum — the naive sketch there re-mixes 86/124
   tracks and the next picker-up won't know. (07-22 #2, still open.)
7. **Reqs is frozen on day 15**; Accepted 0 so no drain can run; traceability inverted so no revert
   path works. Three `light` Drafts are one commit from unblocking the first drain in 15 days.
8. **CLAUDE.md now actively contradicts the integrate contract**, and the `Cargo.toml` MSRV
   publish-blocker is in its 4th day with no guard the new gate can enforce. Both are one-line/one-check
   fixes that have been skipped for days.
9. **The human listening/ears queue grew.** LA-onset CR (9–10d, gates pluck Phase 4), GM0 bank
   ranking, 00024, 00029, plus four new M-CAL timbre bugs — and this window *added* a 5-trim panel
   rather than draining any.
10. **Cross-worktree dirtiness is UNVERIFIED for a 4th day** (sandbox), blocking all reap decisions;
    `lessons_learnt.md` is 72/cap-20 with ~5% index-v1 conformance and `scratchpad.md` reversed to
    36 open, its `--solo 8` entry still contradicting Closed bug 00027.

Watch, not yet issues: SELF-APPROVED is the dominant design norm (17 on trunk) with no human
sign-off; composition at zero for three days; the release-only version policy is holding cleanly.

---

## Plan for today

Ordered by value per unit of effort. Correctness work on already-shipped code first; the human
gates are Arthur's.

1. **File the running-status parser defect as a bug, then port the fix.** A confirmed live
   silent-corruption defect on trunk, untracked, with a written fix + test 400 behind. Cherry-picking
   `294aa94` is likely cheaper than replaying the branch — but it must exist in the ledger first. (This
   has now been the #1 item for two reports.)
2. **Run and record the render-diff inventory for the two shipped timbre changes** — the KILN-00042
   damper law and the M-CAL 5 trims — before any more timbre work stacks on top. This is the mandated
   report we don't have, and it is the only way to know whether either change reached an album it
   shouldn't have. Fix `render_diff.py:116-118` first so its ch9/ch10 misclassification doesn't muddy
   the drum-forward result.
3. **Close KILN-00042 with a genuine independent two-eyes**, and re-verify GM0/00047 across a
   different session/family (its fix and close were both Codex). Cheap, and it discharges the only
   `Fixed` bug in the ledger.
4. **Port the cloud-drain `00028` measurement (and triage the `00030` fix) onto trunk.** At minimum
   annotate `00028`'s file that the naive ROOM_SEND sketch re-mixes 86/124 tracks.
5. **Cheap hygiene, all one-liners:** fix `CLAUDE.md:164-165` to match `.deltic-integrate.toml` and add
   a pointer to it; join the `Cargo.toml` inline tables and add a `cargo +1.87 metadata` check; close
   the `--solo 8` scratchpad entry with a pointer to Closed bug 00027.
6. **Add a structural guard for the per-host bug-ID collision** — the remint near-miss shows the manual
   audit is the only thing standing between concurrent worktrees and a silently deleted bug. Even a
   pre-commit check that a `bugs/` filename isn't being *replaced* by a rename would have caught it.
7. **Arthur — the decisions that gate more than any agent can do today:**
   (a) the LA-onset listening verdict (9–10d, gating pluck Phase 4);
   (b) enforce or retire the reqs ledger — accept 00021/00022/00023 to unblock the first drain in 15
   days, or retire it rather than let `Implemented` accrue against a gate that has fired twice ever;
   also retire the obsolete 00018;
   (c) whether the cross-family two-eyes standard binds — if yes, write it into `bugs/README.md`;
   (d) one session with a wider Bash allowlist to run the cross-worktree dirtiness sweep, blocked 4 days.
   **Before any listening session, re-run `cargo run --release -p render-catalog`** — the shipped synth
   moved materially this window (damper law, DRUM_FORWARD, 5 trims, GM0 piano), so any on-disk render is
   stale and cross-album A/B is confounded.

**Not today.** The anchor-fidelity push (still the largest unstarted programme, still ~84
re-renders/stage plus a human panel — the exact resource already gating four other lines). Also not
today: the machine-actionable M-CAL level bugs (00043/44/45/46/48/49/50) — real work, but they should
wait behind the render-diff on what already shipped, so we're not calibrating on top of an unmeasured
change. And no new GM voices.

---

## Verification notes

Deterministic facts (ledger counts, commit counts, diffstat, branch ahead/behind, file contents,
SHA ancestry, asset counts) were read from source by the lead. The three headline conclusions were
put through an adversarial check.

- **CONFIRMED by direct lead read:** all 50 bug states; window = 34 commits (`git rev-list --count
  27acd7f..db584d7` = 34; `87e8d50^` == `27acd7f`), 105 files, +8,531/−372; `DRUM_FORWARD` = 1.0 at
  `engine.rs:80`; version 0.21.56 with zero window bumps; `midi.rs:233-239` single-status latch and
  zero `running.?status` in `bugs/`; reqs 4 Draft / 23 Implemented / 0 Accepted / 0 Satisfied; the
  KILN-00042 remint delete/re-add (`e7a852a` deletes 325 lines, `9586b2e` restores).
- **CORRECTED — the 07-22 baseline said one SELF-APPROVED HLD on trunk; there are 17.** Self-approval
  is a long-standing norm, not an M-CAL-era emergence.
- **FOUND LOOSE — KILN-00042's ledger figure "6 of 22 programs Off" conflates 6 *presets* with 6
  *programs*.** By actual GM→preset routing the fix reaches ~19 of 22 default programs; OUD and
  DRIVE_LEAD are alt-bank presets whose default programs are repaired. The report states the corrected
  count.
- **CONFIRMED BY HAND (load-bearing) — the "10 changed / 130 identical" render-diff belongs to
  `11dadcc` (KILN-00033, a controller fix), not to the timbre changes.** Neither `8e1da29` (damper) nor
  `fc1ef10` (trims) mentions a render-diff; the KILN-00042 journal shows only "inventory running"
  pre-consult with no completed result, and the law changed after the consult. The finding is a
  *documented absence* — no completed inventory is on record — not proof it was never run.
- **CONFIRMED benign — the KILN-00042 (`847510e`) and GM0/00047 (`11b930c`) cited SHAs are not
  ancestors of trunk;** the integrated commits (`8e1da29`, `edf0a5c`/`2f2baa4`) carry byte-identical
  patches. Dead links from a fresh clone, content preserved — the repo's pre-integration-remint pattern.
- **CONCERN, labelled inline — GM0/00047's "independent" two-eyes is same-family** (Codex fixed *and*
  closed; the closing commit `33236b1` is Codex-authored).
- **UNVERIFIED, held not dropped:** cross-worktree dirtiness (sandbox-denied, 4th day; 1 of 25 known
  clean); whether the render-diff inventory was in fact run for the timbre changes but left unrecorded;
  the ~705 test count (grep estimate, no `cargo` run); whether the heavy `sampler.rs` churn incidentally
  moved the LA-onset defect (its CR still reads `awaiting Arthur's ear`).
- **INFERENCE, labelled inline:** the "healthy discovery, not regression" read on the bug queue; the
  "reports aren't moving the fleet" attribution; rebase-cost and reapability judgements on stale branches.
