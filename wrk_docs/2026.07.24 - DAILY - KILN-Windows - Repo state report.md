# 2026.07.24 - DAILY - KILN-Windows - Repo state report

Pass mode: read-only observation. No tests run, no app run, no renders, no fixes, no
cleanup, no ledger transitions, no tags, releases or deploys. Integration is owned by the
scheduler, not by this pass.

Environment: Windows host `KILN`, worktree
`D:\worktrees\midi-music\20260724-OPS-CLA@KILN-daily-report-020015`, HEAD `2186b73`
== `origin/main`.

Baseline: `wrk_docs\2026.07.23 - DAILY - KILN-Windows - Repo state report.md` (its own
commit `c75f9d2`). Window under review: `c75f9d2..2186b73`, **22 commits**, 81 files,
+10,951 / −1,077, 0 merges, 2026-07-23 02:57 → 2026-07-24 00:52.

Method: six parallel readers over independent state sources (bugs, reqs, design docs,
commit log + features, branch estate, hygiene) plus a prior-issue audit. Load-bearing
numbers were re-derived by the lead from source; the two most contestable conclusions —
the render-diff finding and the design-approval-regime claim — were adversarially checked
by the lead directly against commit bodies and `grep`. **Two prior-report figures were
corrected (the SELF-APPROVED baseline "17" was already ~19, and the design sign-off
crossover is fuzzier than a clean date), and one claim was softened accordingly** — see
Verification notes.

---

## Headline

**A strong engineering window that delivered three things these reports have been asking
for — recorded render-diffs, a pruned `lessons_learnt.md`, and a genuine cross-family
close of the headline bug — plus one real new capability (the score-authored amp control
surface and its live-tuning GUI). But the human-gated queue kept growing faster than it
drained, worktree sprawl accelerated (25 → 33), and the window's single most audible
change shipped without a full render-diff tally.**

The character continued the driven-guitar voicing arc and closed it with tooling. The five
`feat` commits are: a **score-authored amp control surface** (Part A — six overdrive knobs
exposed as NRPN offsets, structurally inert until an album authors them); the **`amp-lab`
live-knob GUI** (Part B — a standalone egui/cpal desktop app to dial GM29/30 defaults by
ear over a looping backing track, every control crossing as the *real* MIDI an album would
author); **4× oversampling of the driven shaper** (to open wider knob ranges and close a
latent alias breach); a **real 5-string banjo (GM105)** built from Arthur's own recording,
replacing the dull "ganjo"; and a **CC0 alt-bank lead amp+cabinet** so GM29/30's alt bank
is a distinct voicing. Three `fix` commits land the deferred second half of the plucked-decay
work (the t60 re-fit) and two calibration repairs (KILN-00046 string seam, KILN-00048
velocity/damper decouple).

The good news, and it is real:

1. **The render-diff gap the 07-23 report flagged is largely closed.** Four separate timbre
   changes now carry explicit, quantified render-diff inventories in their commit bodies —
   banjo (2 changed / 121 identical), GM48/49 seam (104 / 0 contamination), oversampling
   (41 / 83), CC0 alt-bank (11 / 87) — and a four-way bit-identity proof backs the amp
   controller feature (0 / 124). This is a direct, on-record response to the prior report's
   #2 issue, verified by the lead reading each commit body.
2. **`lessons_learnt.md` was pruned 73 → 20 and is now 100% index-v1 conformant** (`fcc5775`).
   The file that sat 3.6× over cap at ~5% conformance for weeks is fixed — the biggest single
   hygiene win in the series.
3. **KILN-00042 got a genuine independent, cross-family two-eyes close** (`308185b`): Claude
   Opus fixed it, gpt-5.6-sol (Codex) verified and closed it, residual split cleanly to a new
   bug (00052) rather than swept under the close.

The cost side — the structural backlog is unchanged or worse:

1. **The window's most audible change shipped without a full render-diff tally.** The plucked
   t60 re-fit (`aadcd57`) re-rings the whole plucked family (nylon 3.8→7.7s, sitar 2.1→12.0s)
   and landed with render-diff only "recommended/running", no final count; KILN-00048
   (`2186b73`) substituted a vel-100 bit-identity *canary* for a full album cmp. So the gap is
   much smaller, but not zero — and it is the two highest-audible-impact changes that lack the
   full inventory.
2. **The bug queue grew 50 → 58 (Open 19 → 25).** Healthy discovery — every new entry is
   Low/Medium (zero High), five are proactive test-quality findings, three are self-inflicted
   fix-couplings the oracle net caught — but only **one** bug left via a completed two-eyes
   close all window. Independent verification throughput, not fix rate, is now the binding
   constraint, and the Fixed-awaiting-eyes pool is creeping (1 → 2).
3. **Every human-gated lane is still frozen or growing.** Reqs `Satisfied` is **0 on day 16**;
   a new req (00028) was *agent-self-accepted*, bypassing the human Gate-1 triage; the
   listening/ears queue **grew again** (the amp-lab GUI adds a fresh GM29/30 decision, Slipstream
   re-voicing needs an ear, new ears-bug 00051), against one drain (the t60 A/B Arthur approved);
   and the running-status parser defect is **live, untracked, and stranded off-trunk for a third
   report**.
4. **Worktree sprawl accelerated: 25 → 33 worktrees, 10 unmerged task branches (34 unique
   commits), 14 detached baselines — and cross-worktree dirtiness is UNVERIFIED for a 5th day**,
   so nothing can be reaped.

Net: the fleet is producing genuinely good work and, for the first time in the series, visibly
acted on two of the reports' standing asks (render-diffs, lessons prune). But the queue of
things that need a *person* — a listening ear, a triage decision, an out-of-band shell for the
dirtiness sweep, the untracked parser bug — is still growing, and the cheap one-line hygiene
fixes keep getting skipped.

---

## Bug ledger

**The queue grew 50 → 58, and it is healthy discovery, not rot: every new entry is Low/Medium
(zero High), five of eight are self-audit test-quality findings, and the other three are the
honestly-ledgered, oracle-caught fallout of this window's own fixes. The watch-item is closure
throughput — independent two-eyes verification, not fix rate, is now the binding constraint.**

| State | 07-23 | 07-24 | Δ | IDs (07-24) |
|---|---:|---:|---:|---|
| Open | 19 | **25** | **+6** | 00016, 00019, 00024, 00028, 00029, 00030, 00036–00041, 00043–00045, 00049, 00050, **00051–00058** |
| Fixed (awaiting two-eyes) | 1 | **2** | +1 | **00046, 00048** |
| Closed | 30 | **31** | +1 | +**00042** |
| **Total** | 50 | **58** | **+8** | 00051–00058 filed |

CONFIRMED by a direct `- **State:**` scan of all 58 files. The Open delta decomposes cleanly:
−00046 and −00048 (both Open→Fixed), +8 new (00051–00058), so 19 − 2 + 8 = 25. 00042 was never
in the Open pool — it moved Fixed→Closed.

### The +8 new bugs — 5 test-quality, 3 measured fix-couplings

**Test-quality / oracle-integrity (5)** — no shipped-audio defect; the remedy is coverage or a
test/doc correction:
- **00052** (Low) — the KILN-00042 register-tilt oracle checks the closed-form corner helper,
  not the rendered `Pluck::new` path; a rewiring that dropped the damper hold would pass green.
  Split out of 00042's two-eyes close.
- **00054** (Medium) — `render_drum` hardcodes legacy `Kit::V1`; the default `Kit::V3` has no
  oracle for the 6 behaviours it re-voices. A coverage gap on the shipping default kit.
- **00055** (Low) — a test-only `percentile` helper documents nearest-rank but computes
  floor-rank, unpinned by any test.
- **00056** (Medium) — 6 album MIDI writers lack the `_resolve_overlaps()` write-time clamp;
  **10,398 same-pitch overlaps are committed** (Riverwake alone 8,426 = 81%). The only new bug on
  the Python composition side. Measured basis, remedy is source-hygiene + promoting an advisory to
  a hard oracle.
- **00057** (Low) — `non_guitar_la_render_is_pinned` freezes a raw-f32 FNV render hash with no
  failure diagnostic; the last un-migrated bit-exact golden.

**Measured timbre/level defects (3)** — all three are *fallout* from this window's own fixes,
caught by existing oracles:
- **00051** (Low) — the plucked t60 re-fit made the model sustain hotter, so the LA guitar
  sample→model crossfade seam blooms ~0.4 dB; `la_level_continuity` caught it (now carries a
  bounded guitar exception).
- **00053** (Low) — GM49 Slow Strings model does not swell at low keys (body/onset 0.76–0.79 where
  both references rise); found while fixing 00046.
- **00058** (Low) — landing 00048 broke GM45 pizzicato's Shaped-vs-Legacy parity key-dependently
  (~7.5 dB spread, unfittable by scalar `exc_trim`); PIZZ dropped from the parity check to keep the
  oracle honest.

**Nuance on 00054–00057 (raised `ceae608`, corrected `f7a258b`):** the four bugs are *not* false
alarms undercut after filing. A 6-agent recon disproved the alarming halves of their seeding
`lessons_learnt.md` nuggets *before* filing; `ceae608` baked the corrections into each bug's State
history at birth, and `f7a258b` then propagated the same corrections back into the source lesson
nuggets. What sits in the ledger is the disproof-surviving residue — each narrower than its seed.
All four remain validly Open, but **00057 in particular must not be read as a latent-red test**:
its "will flip the moment anyone runs `cargo test --release`" premise was empirically falsified
(green in both profiles); only a test-*quality* argument survives (correctly Low/Could).

### KILN-00042 — closed with a genuine cross-family two-eyes

Fixer: Claude Opus 4.8 (1M). Closure verdict (CLOSE-WITH-RESIDUAL, high confidence): gpt-5.6-sol
(Codex, a different model family), per the bug's State history and `308185b`. Opus only *scribed*
the entry (the fixer-cannot-close rule bars self-closure); the judgment is independent and
cross-family. Both focused oracles re-ran green on trunk `aadcd57`, and the residual coverage gap
was split to 00052 rather than buried. The two-eyes rule working as designed.

### Two Fixed awaiting eyes (00046, 00048)

- **00046** (Medium, fix `85005fb`) — level-matches the GM48/49 string-section LA seam; excess cut
  from +1..+6 dB to a [0.75,1.30] parity band; render-diff 104 changed / 0 contamination. It was
  pre-integration reviewed by gpt-5.6-sol (REQUEST-CHANGES → addressed), but that is *not* the
  two-eyes closure — it still needs an independent close.
- **00048** (Medium, fix `2186b73`, HEAD) — anchors the KS damper corner at its vel-100 value so
  decay rate no longer tracks pluck strength; guarded by a new rendered
  `ks_decay_rate_is_velocity_invariant` oracle. Awaits independent close.

Both correctly sit Fixed, not self-closed — the discipline is being followed consistently.

### Ownership & direction of travel

**No Open bug has an Owner set** — every `Owner` field across `bugs/` reads `-`. Expected between
sessions, but the 25-item queue is currently unattended.

Open 8 (07-22) → 19 (07-23) → 25 (07-24). The read is **healthy discovery**: severity of arrivals
is *falling* (two High last window, zero this window); the Open count *overstates* the number of
real problems, since 00042→00043/44/45/48/49/50/51/52/53/58 is essentially one root-cause
investigation (KS decay + level calibration) with tracked sub-residuals. The adversarial caveat:
raw Open more than tripled in two days while only three bugs left the pool all window (one Closed,
two Fixed-not-closed). Net +6 Open/day outruns resolution, and the Fixed pool is silting (1 → 2)
because closure needs a scarce independent-verifier pass — only one completed this window. That
throughput, not the discovery rate, is the metric to watch.

---

## Requirements ledger

**`reqs/` unfroze but is not draining.** The 3-day byte-freeze broke — one new requirement landed —
but nothing moved toward `Satisfied`, both human gates stayed shut, and the one motion was an agent
adding a self-accepted `Implemented` req.

| State | 07-23 | 07-24 | Δ |
|---|---:|---:|---:|
| Draft | 4 | **4** | — |
| Accepted | 0 | **0** | — |
| Implemented | 23 | **24** | **+1** |
| **Satisfied** | 0 | **0** | — |
| Total | 27 | **28** | **+1** |

CONFIRMED (excluding `reqs/README.md`): 4 Draft (`00018`, `00021`, `00022`, `00023`), 24 Implemented,
0 Accepted, 0 Satisfied, 28 files. `reqs/` took 2 commits (`0db723b` add, `618009a` modify), both on
one new file — the "byte-frozen since `768526c`" streak is over.

**MM-REQ-KILN-00028 — score-authored amp params.** Git shows it was born `Draft` at `0db723b`, then
the same-day feat commit `618009a` drove it `Draft → Accepted → Implemented` in one edit. The
`Accepted` transition is stamped *"HLD Part A self-approved: 3-lens adversarial + Codex cross-family
review"* — an **agent self-approval, not the human Gate-1 batch triage**. It names 8 concrete oracles
in `Satisfied-by`, so it is not oracle-blind, and Arthur explicitly requested the feature — a
defensible move. But structurally it cleanly bypasses every human checkpoint and does not drain the
ledger; it adds a 24th `Implemented` req no human has triaged.

**`Satisfied` = 0, day 16.** Last acceptance activity was the 2026-07-08 Gate-1 batch; no requirement
has ever reached `Satisfied`. `deltic reqs --ready` needs `Accepted` (`reqs/README.md`), Accepted is
0, so the ready set is provably empty and **no auto-drain can run**. The three easy `light` Drafts
(00021/00022/00023) are still one small commit each from unblocking.

**07-23 traceability defects — all four still hold** (re-verified): `MM-REQ-KILN-00003` still names a
false-pass oracle (`synth_fx_97_99_101_103_sustain_as_pads` exists only as a deletion note in
`voices.rs`; a filtered `cargo test` on it exits 0); implementation SHAs still dangling; `Req-Id:`
trailers still missing on the 6 newest reqs — **and 00028 also has none**, so 7 reqs are now
un-enumerable by the documented Gate-2 revert path; `MM-REQ-KILN-00018` still mis-triaged `Draft`
(correct triage `Retired`). *Correction to the prior report:* its supporting figure `git ls-files
'*.opus' = 0` is now stale — exactly **1** `.opus` is tracked (`samples/banjo/…`, a sample source,
not a listening render — see Hygiene); the 00018 argument stands on the corrected basis (zero
*listening* renders committed).

---

## Design backlog

**Four new HLDs landed, all amp/pluck engineering, and the design-approval norm is dominated by
agent self-approval — 20 SELF-APPROVED design HLDs on trunk (+1 this window), with no new human
sign-off gate fired.**

Docs added this window (`git log --diff-filter=A c75f9d2..2186b73 -- wrk_docs/`) — **four**:

| Doc | Commit | Status marker |
|---|---|---|
| `2026.07.23 - HLD - score-authored amp parameters (part A, control surface and state).md` | `0db723b` | **SELF-APPROVED** |
| `2026.07.23 - HLD - driven guitar amp+cab voicing.md` | `bb75c77` | **IMPLEMENTED** (awaiting Arthur's A/B) |
| `2026.07.23 - HLD - amp lab (live knob GUI for Part B).md` | `44386be` | **IMPLEMENTED** (awaiting Arthur's ear) |
| `2026.07.23 - HLD - KS damper velocity decouple + bass reconciliation (KILN-00048-00045).md` | `2186b73` | **DRAFT** (stale — fix shipped in the same commit) |

**Self-approval is the entrenched design norm, not an emerging one.** `grep -rl SELF-APPROVED
wrk_docs/` = 23, of which 3 are DAILY reports quoting the term → **20 genuine SELF-APPROVED design
HLDs** on trunk, only **+1 new this window** (the amp Part A HLD). No new human sign-off landed. The
human `SIGNED-OFF` marker still exists repo-wide (it appears on HLDs as recent as 2026-07-16/07-17),
so this is not a clean crossover date — but it has not been exercised in this window, and 20
self-approved HLDs vs a dormant human gate is the durable picture. None of the self-approved docs
carries a human sign-off.

**COLD designed-but-not-started docs** (status lines re-read; ages as of 2026-07-24):

| Doc | Status | Age | Blocker |
|---|---|---:|---|
| `2026.07.13 - HLD - anchor fidelity push (piano and string ensemble).md` | `DRAFT for review` | **11d** | ~84 re-renders/stage ×3 + a human A/B panel |
| `2026.07.14 - CR - LA sample layer deletes the model onset.md` | `INVESTIGATION — awaiting Arthur's ear` | **10d** | One A/B verdict; gates pluck Phase 4 |
| `2026.07.18 - PLN - GM0 grand alternate banks audition.md` | `IN-PROGRESS` | **6d** | Human ranking of rendered auditions |
| `2026.07.19 - HLD - natural pluck excitation redesign.md` Phases 3/4 | `APPROVED` | **5d** | Phase 4 gated on the LA-onset CR above |

All four confirmed cold; every blocker is a pending human listening/ranking call, unchanged from
07-23. **Anchor-fidelity is still 100% absent from `crates/ferrosintesis/src`** — `Railsback` = zero
matches; the `stretch`/`inharmonic` tokens present are all non-piano (bell/tine/reed character,
sample-repitch). No Railsback curve, stretch tuning, or pitch-dependent piano inharmonicity anywhere
in source.

---

## Key commits & new features

Range `c75f9d2..2186b73` — 22 commits, 0 merges. A continuation of the driven-guitar voicing arc,
closing with a new GUI tool and a plucked-family decouple.

| Metric | This window | Prior (07-23) | Δ |
|---|---:|---:|---:|
| Commits | 22 | 34 | −12 |
| Files / insertions / deletions | 81 / 10,951 / 1,077 | 105 / 8,531 / 372 | — |
| `feat` / `fix` | 5 / 3 | 5 / 5 | 0 / −2 |
| Version bumps | 0 (release-only) | 0 | — |
| New/modified album `.mid` | 0 / 0 | 0 / 0 | — |

Type split: 10 docs, 5 feat, 3 fix, 2 chore, 1 wip, 1 revert.

**Where the +10,951 lands — not synth DSP.** Top 3 insertion sources: (1) the `amp-lab` GUI
(`44386be`, +4,597), of which its vendored `crates/amp-lab/Cargo.lock` is **3,187 lines (69%)** — a
standalone egui+cpal tool deliberately kept in its own empty workspace so integration builds never
pull ~200 crates; (2) driven-guitar amp logic in `engine.rs` across `bb75c77`/`618009a`/`c8c8259`
(~2,400 lines to one file); (3) design + journal docs (~1,700), including the lessons prune
(`fcc5775` +482/−753). Banjo samples are binary WAVs (uncounted); oversampling is only ~560 lines.

**The 5 `feat`:**
- `618009a` — **amp params Part A**: six author-controllable overdrive knobs (Drive, Tone, Body,
  Presence, Cab Tone, Tightness) as NRPN 0x30, values as offsets from the shipped voicing (64 =
  unchanged), so structurally inert until an album authors them. Engine-only; no public-API change;
  8 new gated oracles.
- `44386be` — **`amp-lab` live-knob GUI Part B**: a small egui/cpal desktop app to twiddle the six
  knobs over a looping guitar+bass+drums track and pick GM29/30 defaults by ear. Every control
  crosses as the *real* MIDI (CC99/98/6), so a good setting reproduces verbatim in a `.mid`. Audio
  thread owns the synth; UI pushes over a lock-free SPSC ring.
- `c8c8259` — **4× oversample the driven shaper, widen the amp knobs**: raises `DRIVE_OVERSAMPLE`
  2→4 (9.8 dB alias headroom, +0.5% of a core); also fixes a latent alias breach (Part A's oracle
  swept Drive alone, missing the all-knobs-maxed corner at −35 dBc vs the −40 budget). The oracle now
  sweeps every knob including worst-case-all-at-once.
- `4a3a277` — **real 5-string banjo (GM105)** replacing the "ganjo": 24 near-chromatic zones from
  Arthur's own open-back recording, QC-gated on pitch clarity/octave agreement/contamination; a
  +14 dB presence high-shelf restores twang (centroid 773→1344 Hz).
- `bb75c77` — **CC0 alt-bank lead amp+cabinet**: GM29/30's alt bank previously differed by 0.05 dB
  (Arthur: "GM029 main sounds the same as alt"). Now `Drive::new` selects a whole lead voicing on
  `alt` (pre-shaper HPF 90→120 Hz, per-program lead cabinets, darker stage-2 feed); main bank
  verified bit-identical.

**The 3 `fix`:**
- `2186b73` — **KILN-00048**: decouple velocity from the KS loop-damper decay rate; pinned at the
  vel-100 value per preset, velocity→brightness transferred once onto the excitation. Vel-100 renders
  bit-identical.
- `85005fb` — **KILN-00046**: level-match the GM48/49 string-section LA seam via a per-velocity-layer,
  per-key taper; excess cut to a [0.75,1.30] parity band.
- `aadcd57` — **plucked-family t60 re-fit** (deferred second half of KILN-00042): both references ring
  the plucked voices far longer than ferro did; re-fits each preset's t60 (nylon 3.8→7.7, banjo
  0.75→4.9, sitar 2.1→12.0). Nine oracles re-baselined; Arthur approved the amount via a held-note A/B.

**Blast-radius ranking of the shipped synth changes** (none touch `drums.rs`):
1. `85005fb` GM48/49 seam (`sampler.rs`) — **104 of 124 MIDIs** (string sections near-ubiquitous).
2. `c8c8259` oversampling (`engine.rs`) — **41 of 124** (every Drive insert); audibly inert
   (residual −48 dB isolated, −59 dB in-mix).
3. `aadcd57` t60 re-fit (`voices.rs`+`engine.rs`) — **the whole plucked family (~11 presets)**; large,
   *audible*, Arthur-approved — the single most consequential timbre shift this window.
4. `2186b73` KILN-00048 (`voices.rs`) — plucked family, non-vel-100 notes only.
5. `bb75c77` CC0 alt-bank — ~11 tracks (driven guitars on a nonzero-CC0 bank).
6. `4a3a277` banjo (`sampler.rs`+`voices.rs`) — 2 tracks.
7. `618009a` Part A amp params — 0 albums (pure controller feature).

**Render-diff — the 07-23 gap is largely closed** (lead-verified from the commit bodies):
- `85005fb` GM48/49 seam — **"104 changed, 0 contamination, 0 not reached"** ✓
- `4a3a277` banjo — **"124 album MIDIs: 2 banjo tracks change, 121 byte-identical"** ✓
- `c8c8259` oversampling — **41 of 124 change, 83 byte-identical** by construction; inaudible.
- `bb75c77` CC0 alt-bank — **"11 changed, 87 same, CONTAMINATION 0"** (+26 not-reached, a harness
  artifact).
- `618009a` Part A amp params — **0 of 124**, proven four ways (bit-identity oracle across 4
  program×bank; heaviest alt-bank track byte-identical; three more driven tracks byte-identical; all
  16 demos byte-identical); `e23def1` amended the journal to record this. The exhaustive 41-track
  sweep was *skipped* under render contention, so it is a strong-but-not-exhaustive zero.

  **Residual gap:** the **t60 re-fit (`aadcd57`)** landed with render-diff only "recommended/running",
  no final tally (lead-confirmed absent from its body), and **KILN-00048 (`2186b73`)** substituted a
  vel-100 bit-identity *canary* for a full album cmp. These are the two highest-audible-impact changes
  of the window.

**The wip + revert (rejected SC-55/S-YXG50 chase):** `bf19d67` then `febe6af` — net-zero code (+7/−7).
After the first CC0 cut, Arthur asked "maybe something more radical — what do the SC-55 & S-YXG50
distorted guitars sound like?" A reference-informed candidate closed the numeric gap but **failed the
`alt_drive_is_a_distinct_lead_voicing` oracle** and would require revoicing the *main* bank too
(moving every driven-guitar album) — a taste fork only Arthur can settle. Preserved as `wip:` then
reverted; the resolution taken instead was to build the Part A/B control surface so albums author
their own overdrive.

**Attribution:** all 22 commits authored by `0x4D44`; **21 of 22 carry the `Co-Authored-By: Claude
Opus 4.8 (1M context)` trailer** (the exception is the git-generated Revert). No other family is
trailer-credited this window — a shift from 07-23 (Claude 23× / Codex 11×). Codex and Fable appear as
consults in commit *prose* (the amp+cab two-expert consult, the GM48/49 review, the KILN-00048 Fable
HLD), acknowledged in text, not trailers.

**Composition at zero — fourth consecutive day.** No album `.mid` added or modified. The only new
`.mid` are non-album build assets: `demos/banjo_showcase.mid` and `crates/amp-lab/assets/backing.mid`.

---

## Branches & worktrees

**Worktree sprawl accelerated: 33 worktrees (was 25), 10 unmerged task branches holding 34 unique
commits (was 6/16), 14 detached baselines (was 11). Both carry-over defects are still live and still
off-trunk.** `git worktree list` = 33: the main clone, one internal agent worktree, 17 task worktrees
(was 12), 14 detached render-diff/baseline worktrees. Every carry-over branch fell exactly +23 behind
in one day (the count main advanced), so staleness worsens uniformly.

### Unmerged task work — 34 unique commits across 10 branches (was 16 / 6)
The +18 commits / +4 branches is entirely new sample-bank + workspace work; the six carry-overs are
byte-identical tips, just further behind.

| Branch (`task/…`) | ahead / behind | Verdict |
|---|--:|---|
| `20260717-FIX-CDX-fix-smf-running-status-…` | 3 / 423 | **Highest-value salvage** — holds the parser fix `294aa94` + test for a live untracked defect (see A) |
| `20260723-…-mandolin-la-sample-bank-…` | 6 / 11 | **WIP, integrable** — new; owner-recorded mandolin crate at GM25/LSB96 |
| `20260723-…-b1-upright-piano-sample-bank-…` | 6 / 12 | **WIP, integrable** — new; B1 upright as GM0 CC0=5 alt-bank crate |
| `20260723-…-gm25-steel-adopt-eastman-…` | 5 / 19 | **WIP, integrable** — new; Eastman E1D as default GM25 steel |
| `20260718-…-max-richter-leftovers-album` | 3 / 375 | **WIP** — "The Remaining" album, T1 green; only task branch also on `origin` |
| `20260714-…-sampled-acoustic-drum-kit-…` | 4 / 508 | **Redo-on-trunk** — rebase cost terminal |
| `20260715-…-instrument-voice-collisions` | 3 / 479 | **Salvage, low value** — un-rebasable |
| `20260719-…-fix-brush-slap-…` | 2 / 199 | **Superseded — reapable** (00001 closed citing the trunk fix) |
| `20260719-…-driven-guitar-29-30-…` | 1 / 199 | **Superseded** — Arthur discarded the +6 dB lift (07-22) |
| `20260724-…-amp-lab-into-workspace` | 1 / 1 | **WIP, near-trunk** — new (see below) |

### New branches this window
- **`task/20260724-TSK-HUM-amp-lab-into-workspace`** (1/1) — one commit `fc61bce build(amp-lab): make
  it a workspace member so a root build builds it`; a build-config change, 1 behind trunk, trivially
  integrable, currently parked.
- **`task/20260724-OPS-CLA@KILN-daily-report-020015`** (0/0) — this report's worktree, at HEAD.

### Carry-over defect A — running-status parser: STILL live, untracked, fix STILL off-trunk
The single-`status` latch persists on trunk at `crates\ferrosintesis\src\midi.rs:233-239` (read in
this worktree, which is at main HEAD). `status` is one variable; a `0xFF` meta or `0xF0`/`0xF7` SysEx
clobbers the channel running-status byte, so the next running-status data byte is mis-dispatched —
silent corruption. `294aa94` on `20260717-FIX-CDX` fixes it with `running_status_survives_meta_event`
but is not an ancestor of trunk. `grep bugs/` for `running.?status` = **zero** — no bug filed. This
was the 07-22 report's #1 item and the 07-23 report's #1 issue; now a third report live.

### Carry-over defect B — cloud drain still strands the 00030 fix
`origin/claude/bugs-queue-2q-drain-609csu` = 4 ahead / 80 behind (was 4/57). It carries `427281a
fix(sampler)… restores the harpsichord attack (00030)` + a Blocked→Fixed ledger flip, but trunk's
`bugs\MM-BUG-KILN-00030.md` still reads **Open**, and `MM-BUG-KILN-00028`'s file still carries only the
naive ROOM_SEND sketch — the 86/124-track re-mix measurement was not ported.

### Reapable / detached baselines — proliferating
14 detached audition/baseline worktrees (was 11); the +3 are new amp/mix-bus render-diff baselines
(`BASELINE`, `BASELINE-amp`, `BASELINE-MB`). 6 merged 0-ahead task pairs are now reapable (was 5):
`rt-margin-bench`, `sax-la-sample-layer`, `trim-wind-on-blown-bottle`, `t16-guitar-solo`, `bagpipe-…`,
and the new `amp-knob-listening-samples`. None reaped since 07-23.

**Dirtiness — UNVERIFIED, 5th consecutive day.** This worktree is clean; every cross-worktree
`git -C <path> status` is refused by the sandbox approval gate. **1 of 33 known clean, 32 unknown.**
This has blocked all reap decisions for five days and needs an out-of-band session with a wider Bash
allowlist.

---

## Repo hygiene

**Net direction: BETTER — on the strength of one big win, not broad progress.** The lessons prune is a
genuine structural fix; nothing regressed. But all four standing defects are still stuck and the
scratchpad is flat.

- **`lessons_learnt.md`: 72 → 20 entries, now 100% index-v1 conformant — BETTER (the headline).** The
  prune landed in `fcc5775` (73→20, +482/−753; added the `index-v1` marker; moved 3 durable facts to
  `CLAUDE.md`). `grep -c '^- '` = 20 (at cap). Every entry now opens with a single-physical-line bold
  nugget ending in a `file:symbol` pointer, war-story detail demoted to non-injected continuation —
  conformance ~5% → 100%. Two follow-ons kept it clean: `0969f1f` (shared-alias-budget nugget) and
  `f7a258b` (corrected four just-integrated nuggets carrying now-false claims). Still 20, format intact.
- **`scratchpad.md`: 36 open items — UNCHANGED.** The `--solo 8` render-hang entry (`:498`) is still
  `- [ ]` open even though its promoted bug **`MM-BUG-KILN-00027` is Closed** (07-21, two-eyes) —
  the prior report's contradiction is confirmed; it is the un-retired original of a resolved bug.
- **`CLAUDE.md` ↔ `.deltic-integrate.toml` version-bump contradiction — UNCHANGED.** `CLAUDE.md:171`
  ("one version bump per integrated task") vs `.deltic-integrate.toml:8` (`version_bump =
  "release-only"`); `grep -c deltic-integrate CLAUDE.md` = 0 — CLAUDE.md still never names the file.
  This window shipped source (KILN-00048, the amp GUI) with no bump — correct under the contract, wrong
  under CLAUDE.md's rule. One-line fix outstanding.
- **`Cargo.toml` MSRV multi-line inline tables — UNCHANGED, day 5, no guard.** `crates/ferrosintesis/
  Cargo.toml:47-51` (drumkit) and `:62-66` (orchestral) are still 5-line inline tables under the
  prohibiting comment (`:43-44`). No `.github/`, no tracked `.yml`/`.ps1` — the fmt/clippy/test gate
  only needs a manifest that parses on the current toolchain.
- **`render_diff.py` ch9/ch10 fold — UNCHANGED, day 7.** `tools/render-diff/render_diff.py:116-118`
  still folds a channel-10 kit-select PC into the melodic program set. Scratchpad entry still open.
- **`CLAUDE.md` content drift — UNCHANGED.** Still says "the **two** asset crates" (`:106`) and "218
  files / 17.73 MiB" (`:228`); reality is **21** sample crates, **237** tracked `.wav` in the two named
  crates (1,036 across all 21). `albums/gpt5-6/` exists and is unlisted.
- **Assets — essentially flat, one sanctioned new category.** Tracked album `.mid` = 124 (whole-tree
  142, +2 demos/GUI asset). Tracked `.opus` = **1** (was 0) — `samples/banjo/banjo-5string-openG-…opus`,
  a deliberately committed **source recording** allowlisted by a new `.gitignore` rule (`!samples/**/
  *.opus`), *not* a listening render, so the "zero committed listening `.opus`" invariant holds. Tracked
  `.wav` = 1,070 (was 1,054).

---

## Prior tracked issues — status audit

| # | Issue (07-23) | Status | Evidence |
|---|---|---|---|
| 1 | Running-status parser defect live+untracked+stranded | **UNCHANGED** | `midi.rs:233-239` still latched; 0 bugs mention it; `294aa94` not an ancestor of trunk (branch 423 behind). |
| 2 | Shipped timbre changes carried no render-diff inventory | **BETTER** | Four timbre changes now carry on-record inventories (banjo 2/121, GM48/49 104/0, oversampling 41/83, CC0 11/87) + a four-way proof for the amp feature. Residual: t60 re-fit `aadcd57` has no full tally; KILN-00048 used a vel-100 canary. |
| 3 | Per-host bug-ID collision — no structural guard | **UNCHANGED** | No `.githooks`/tracked hook files; no guard commit in the window. |
| 4 | Open bugs unowned (19→25) | **WORSE** | 25 Open confirmed; `Owner` = `-` on every file; backlog grew +6, ownership still zero. |
| 5 | KILN-00042 awaiting independent two-eyes; 00047 same-family | **BETTER** | 00042 now Closed (`308185b`), verified cross-family by gpt-5.6-sol. But 00047 unchanged (Codex fixed+closed, still same-family), and `bugs/README.md` codifies only a generic "second pair of eyes", not the cross-family standard. |
| 6 | Cloud-drain strands 00030 fix + 00028 measurement | **UNCHANGED** | Branch still 4 ahead/unmerged; 00030 still Open; 00028 measurement not ported. |
| 7 | Reqs frozen; Accepted 0; 3 light Drafts | **UNCHANGED** | Accepted still 0, Satisfied 0 (day 16); Drafts 00021/22/23 (and 00018) still un-triaged; Implemented 23→24 (00028). |
| 8 | CLAUDE.md contradicts integrate contract; MSRV blocker | **UNCHANGED** | Version-bump line and `.deltic-integrate.toml` still contradict; Cargo.toml still multi-line inline tables (day 5). Both one-line fixes skipped. |
| 9 | Human listening/ears queue grew | **WORSE** | Added the amp-lab GM29/30 decision + Slipstream re-voicing + new ears-bug 00051; one drain (t60 A/B approved). Net grew — same "added a panel, drained little" pattern. |
| 10 | Dirtiness UNVERIFIED (day 4); lessons over cap; scratchpad 36 | **MIXED (one BETTER)** | lessons now 20/index-v1 (resolved); scratchpad still 36; dirtiness still sandbox-blocked (day 5). |

**Tally: 0 DONE / 3 BETTER / 5 UNCHANGED / 2 WORSE** (with #10 mixed). Two of the reports' standing
asks — render-diffs (#2) and the lessons cap (#10) — were finally acted on. The five cheapest items
(file the parser bug, the two one-line CLAUDE.md/MSRV fixes, port the drain evidence, close the
`--solo 8` scratchpad entry) are all still undone.

---

## Tracked issues

1. **The running-status parser defect is live on trunk, untracked, fix stranded 423 commits back.**
   `midi.rs:233-239` mis-dispatches running status after a meta/SysEx event — silent corruption.
   `294aa94` fixes it with a test but is off-trunk; zero bugs mention it. File it, then cherry-pick vs
   re-fix. (#1 for the third report running.)
2. **The two most-audible changes of the window shipped without a full render-diff tally.** The
   render-diff practice materially improved (four inventories on record), but the t60 re-fit
   (`aadcd57`) has no final count and KILN-00048 (`2186b73`) used a vel-100 canary. These re-ring the
   whole plucked family — run and record the album inventory before more timbre work stacks on top.
3. **Independent two-eyes throughput is now the binding constraint.** 25 Open, all unowned; only one
   completed cross-family close all window (00042); the Fixed pool is silting (00046, 00048 waiting).
   Fix rate is fine; verification is the bottleneck.
4. **Per-host bug-ID collision still has no structural guard** — the 00042 delete/re-add near-miss was
   caught by a manual audit; nothing prevents the next one.
5. **Reqs is frozen on day 16.** Accepted 0 so no drain can run; a new req (00028) was
   agent-self-accepted, bypassing Gate-1; the traceability defects (false-pass oracle, dangling SHAs,
   missing `Req-Id:` trailers now on 7 reqs) still block the Gate-2 revert path. Three `light` Drafts
   are one commit from unblocking the first drain in 16 days.
6. **Design approval is dominated by agent self-approval** (20 SELF-APPROVED HLDs, +1 this window; no
   human sign-off fired this window), and the pattern has now reached the reqs ledger (00028
   self-Accepted). None carries a human sign-off marker.
7. **The cloud-drain branch still strands a real 00030 fix and the 00028 measurement** (4 ahead / 80
   behind). Port the 00028 measurement to trunk's bug file at minimum — the naive ROOM_SEND sketch
   re-mixes 86/124 tracks and the next picker-up won't know. (07-23 #6.)
8. **The cheap one-liners keep being skipped:** CLAUDE.md ↔ `.deltic-integrate.toml` (never names the
   file, day-3+ contradiction now biting); Cargo.toml MSRV inline tables (day 5, no guard);
   `render_diff.py` ch9/ch10 fold (day 7); CLAUDE.md content drift (2→21 crates, unlisted `gpt5-6/`).
9. **Worktree sprawl is accelerating** (25→33; 10 unmerged branches / 34 commits; 14 detached
   baselines) and **cross-worktree dirtiness is UNVERIFIED for a 5th day**, blocking all reaping. 6
   merged pairs + candidate detached baselines are waiting on an out-of-band shell.
10. **The human listening/ears queue grew again** — amp-lab GM29/30 default, Slipstream re-voicing,
    GM0 bank ranking, the LA-onset CR (10d, gating pluck Phase 4), new ears-bug 00051 — against one
    drain (the t60 A/B). This is the scarce resource gating four separate work lines.

Watch, not yet issues: composition at zero for a fourth day (100% synth/tooling); the release-only
version policy holding cleanly (0.21.56, zero bumps); the `amp-lab` GUI is a genuinely new capability
(author-controlled overdrive) but adds a standalone tool + a pending Arthur decision.

---

## Plan for today

Ordered by value per unit of effort. Correctness on already-shipped code first; the human gates are
Arthur's.

1. **File the running-status parser defect as a bug, then port the fix.** A confirmed live
   silent-corruption defect on trunk, untracked, with a written fix + test 423 behind. Cherry-picking
   `294aa94` is likely cheaper than replaying the branch — but it must exist in the ledger first.
   (The #1 item for three reports.)
2. **Run and record the album render-diff for the t60 re-fit (`aadcd57`) and confirm KILN-00048's
   blast radius beyond the vel-100 canary.** These are the two highest-audible changes and the only
   ones lacking a full tally. Fix `render_diff.py:116-118` (ch9/ch10 fold) first so the drum channel
   doesn't muddy the plucked-family result.
3. **Discharge the Fixed pool with independent cross-family closes of 00046 and 00048**, and re-verify
   or annotate GM0/00047 (still a same-family close). This is the throughput bottleneck; both fixes
   already have oracles and reviews on record.
4. **Cheap hygiene, all one-liners:** align `CLAUDE.md:171` with `.deltic-integrate.toml` and name the
   file; join the `Cargo.toml` MSRV inline tables and add a `cargo +1.87 metadata` parse check; close
   the `--solo 8` scratchpad entry (pointer to Closed 00027); correct CLAUDE.md content drift (2→21
   crates, list `gpt5-6/`).
5. **Port the cloud-drain 00028 measurement (and triage the 00030 fix) onto trunk** — at minimum
   annotate 00028's file that the naive ROOM_SEND sketch re-mixes 86/124 tracks.
6. **Add a structural guard for the per-host bug-ID collision** — even a pre-commit check that a
   `bugs/` filename isn't being *replaced* by a rename would have caught the 00042 near-miss.
7. **Arthur — the decisions that gate more than any agent can do today:**
   (a) the `amp-lab` GM29/30 default listen (the whole Part A/B arc was built to hand this to your ear);
   (b) the LA-onset listening verdict (10d, gating pluck Phase 4);
   (c) enforce or retire the reqs ledger — accept 00021/00022/00023 to unblock the first drain in 16
   days, or retire it rather than let `Implemented` accrue against a gate that has fired twice ever;
   also retire the obsolete 00018;
   (d) whether the cross-family two-eyes standard binds — if yes, write it into `bugs/README.md`;
   (e) one session with a wider Bash allowlist to run the cross-worktree dirtiness sweep, blocked 5 days.
   **Before any listening session, re-run `cargo run --release -p render-catalog`** — the shipped synth
   moved materially (t60 re-fit, GM48/49 seam, oversampling, banjo, CC0 alt-bank), so any on-disk render
   is stale and cross-album A/B is confounded.

**Not today.** The anchor-fidelity push (still the largest unstarted programme, still ~84
re-renders/stage plus a human panel — the exact resource already gating four other lines). Also not
today: the machine-actionable M-CAL level bugs (00043/44/45/49/50) — real work, but they should wait
behind the render-diff on the plucked changes so we're not calibrating on top of an unmeasured shift.
And no new GM voices.

---

## Verification notes

Deterministic facts (ledger counts, commit counts, diffstat, branch ahead/behind, file contents, SHA
ancestry, asset counts) were read from source by the lead. The two most contestable conclusions were
adversarially checked directly.

- **CONFIRMED by direct lead read:** all 58 bug states (Open 25 / Fixed 2 = 00046+00048 / Closed 31);
  window = 22 commits (`c75f9d2..2186b73`), 81 files, +10,951/−1,077, 0 merges; version 0.21.56 with
  zero window bumps; reqs 4 Draft / 24 Implemented / 0 Accepted / 0 Satisfied (28 files); `midi.rs:233-
  239` single-status latch and zero `running.?status` in `bugs/`; `lessons_learnt.md` = 20 entries via
  `fcc5775`; `scratchpad.md` = 36 open items.
- **CONFIRMED BY HAND (load-bearing) — the render-diff finding.** Read the commit bodies: `85005fb`
  records "104 changed, 0 contamination, 0 not reached"; `4a3a277` records "2 banjo tracks change, 121
  byte-identical"; `aadcd57` (t60 re-fit) contains **no** render-diff tally; `2186b73` (KILN-00048)
  records only "vel-100 render bit-identical" (a canary, not a full cmp). So #2 is genuinely BETTER
  with a real residual gap on the two most-audible changes.
- **CORRECTED — the 07-23 baseline's "17 SELF-APPROVED" undercounts;** the empirical grep is 20 genuine
  design HLDs today (was already ~19 at `db584d7`). The window delta is **+1, not +3**. Self-approval is
  a long-entrenched norm, not an M-CAL-era emergence.
- **SOFTENED — the design sign-off "crossover".** A reader proposed a clean "last human SIGNED-OFF
  2026-07-14, first SELF-APPROVED 2026-07-16" date; the lead's own grep finds `SIGNED-OFF` text on HLDs
  as recent as 2026-07-16/07-17, so the crossover is fuzzy. The durable, defensible claim — 20
  self-approved HLDs, no new human sign-off this window — is what the report asserts; the precise
  dormancy-days figure is not.
- **CORRECTED — tracked `.opus` is now 1, not 0** (`samples/banjo/…opus`, a sample source allowlisted
  this window, not a listening render). The "zero committed *listening* `.opus`" invariant still holds;
  the `MM-REQ-KILN-00018` mis-triage argument stands on the corrected basis.
- **CLARIFIED — MM-REQ-KILN-00028 was born `Draft`** (`0db723b`), then driven `Draft→Accepted→
  Implemented` the same day by `618009a`. Net effect matches the "human gates skipped" read; the literal
  birth state is Draft, and the `Accepted` step is an agent self-approval.
- **UNVERIFIED, held not dropped:** cross-worktree dirtiness (sandbox-denied, 5th day; 1 of 33 known
  clean); whether the Part A "0 of 124" would survive the skipped exhaustive 41-track sweep; the exact
  audible impact of the t60 re-fit (no full album render-diff on record).
- **INFERENCE, labelled inline:** the "healthy discovery, not rot" read on the bug queue; the
  "verification throughput is the binding constraint" conclusion; rebase-cost and reapability judgements
  on stale branches.
