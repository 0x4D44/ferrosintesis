# 2026.07.25 - DAILY - KILN-Windows - Repo state report

Pass mode: read-only observation. No tests run, no app run, no renders, no fixes, no
cleanup, no ledger transitions, no tags, releases or deploys. Integration is owned by the
scheduler, not by this pass.

Environment: Windows host `KILN`, worktree
`D:\worktrees\midi-music\20260725-OPS-CLA@KILN-daily-report-020014`, HEAD `715f8a3`.

Baseline: `wrk_docs\2026.07.24 - DAILY - KILN-Windows - Repo state report.md` (its own
commit `a1dec8c`). Window under review: `a1dec8c..715f8a3`, **76 commits**, 298 files,
+18,482 / −3,996, 0 merges, 2026-07-24 02:59 → 2026-07-25 01:32 (committer dates).

**Trunk moved during the pass.** `origin/main` advanced `715f8a3` → `2723a3b` at
2026-07-25 01:52, one commit past this worktree. That commit is material — it closes the
issue that has led three consecutive reports — so it is reported here, outside the window
proper, and flagged as such throughout.

Method: seven parallel readers over independent state sources (bugs, reqs, design docs,
commit log + features, branch estate, hygiene, prior-issue audit). Five returned within
the pass; the **hygiene** and **prior-issue** sections were then completed by the lead
directly from source rather than reported as unverified. Every load-bearing number below
was read from source at least once by the lead. The two most contestable conclusions —
that the 28-bug closure surge is disciplined rather than a rubber stamp, and that
render-diff discipline improved — were adversarially checked by reading the closure
journal's per-bug evidence and by counting tallies across every synth-touching commit,
not by taking commit messages on trust. **Two prior-report framings are corrected** (the
"unowned bugs" reading and the tracked-`.opus` count) — see Verification notes.

---

## Headline

**The biggest window of the series (76 commits vs 22) inverted the trend these reports
have been warning about. The Fixed queue drained to zero through 28 independent,
cross-family closes carrying per-bug fails-before controls, and the running-status parser
defect — the #1 item for three consecutive reports — was fixed on trunk at 01:52 this
morning. Two owner-recorded instruments shipped end to end. The cost side is sharper than
before: an audible 9.2 dB bass regression sat on trunk for four days and was caught by
Arthur's ear, not by the oracle net.**

The window's real character is a **quality-assurance surge, not a feature surge**. Seven
autonomous code-review passes raised 24 of 37 new bugs; a cross-family verifier then
closed 28 in one day. Feature output was two instruments — a mandolin at the XG cell
(GM 25 + bank LSB 96) and Arthur's Eastman E1D as the default GM 25 steel plus a sampled
GM 120 fret noise — both built from recordings Arthur owns outright.

The good news, and it is substantial:

1. **The running-status parser is fixed on trunk** (`2723a3b`, tracked as
   `MM-BUG-KILN-00096`). Not by the stranded 2026-07-17 branch — by a fresh fix that is
   strictly better: it also covers the SysEx half the old branch never touched, ships
   three regressions red-before-green-after, and confirms confinement by scanning all 141
   committed `.mid` for the pattern (zero hits, so no render changes). The three-report
   #1 issue is closed.
2. **The two-eyes rule held 28 of 28, all cross-family, with zero self-closes.** Every
   close names Codex GPT-5.6-Sol as verifier against fixes authored by Claude Opus 4.8,
   and `wrk_journals\2026.07.24 - JRN - Fixed queue two-eyes closure pass.md` records a
   *separate negative control per bug* — each mechanically restoring the pre-fix
   behaviour on a temp worktree and confirming the oracle goes red. This is the
   discipline working at scale, not a batch rubber stamp.
3. **The adversarial-review method proved itself and got written down.** Three derived
   oracles authored 2026-07-24 were each holed the *same day* by a reviewer briefed to
   refute rather than confirm. `715f8a3` amends CLAUDE.md with the lesson: a derived
   oracle is only as good as its enumeration predicate, and `pub fn *_bank` was "a
   hand-maintained assumption wearing a source-scan's clothing".

The cost side:

1. **An audible regression survived four days and only a human ear found it.** `dbef98a`
   (2026-07-20) made the LA sampled onset the default bank for GM 32–35. `LaVoice::render`
   is a *sum-to-one* crossfade, so the modeled bass was muted outright for the first 50 ms
   and sole owner only at 350 ms — while the sampled onset sat 15–18 dB under the model it
   displaced, with nothing filling the deficit. On real material (median bass note 146 ms,
   90% shorter than the handover) the model never speaks. Cost: **9.2 dB RMS / 11.7 dB
   peak** on the isolated stem, across **42 of 124 album MIDIs**. It was found because
   Arthur A/B'd a Tubular Bells render and said the bass was "better — and louder" on the
   old one. Fixed `5b0c777` / `e143c48`.
2. **Render-diff coverage is 8 of 26 synth-touching commits (31%), or 8 of 18
   audio-capable ones (44%).** The *biggest* changes do carry proper inventories, which is
   real progress. But the prior window's two named gaps — the plucked t60 re-fit
   (`aadcd57`) and KILN-00048 (`2186b73`) — are **still unrecorded**, and `20483d5` shows
   why that matters: KILN-00048 silently moved the steel model's level inside the crossfade
   window and broke a level-parity fit, discovered only on a rebase collision.
3. **Every human-gated lane moved backwards.** Reqs accreted 28 → 31 with `Satisfied` at
   **0 on day 17** and `Accepted` at 0, so the auto-drain is provably idle. Two bugs are
   newly **Blocked on Arthur** (00045, 00068). Zero new design docs were authored, and the
   last human `SIGNED-OFF` marker is 11 days old.
4. **The stranded cloud-drain branch is now causing duplicated work.** A worktree
   `20260725-FIX-HUM-kiln-00030-harpsichord-la-onset-velocity` was opened *today* against
   a bug whose fix has existed on `origin/claude/bugs-queue-2q-drain-609csu` since 07-20.
   Trunk's ledger has no knowledge of it. This carry-over has stopped being a hygiene item
   and started costing engineering time.

Net: the fleet's verification machinery is now demonstrably strong — the closure sweep and
the adversarial oracle audit are the best process evidence in the series. The weakness has
moved: it is no longer fix rate or verification throughput, it is that **the audio-side
oracle net still cannot see a class of defect a human ear catches immediately**, and that
everything requiring Arthur is queueing faster than he drains it.

---

## Bug ledger

**The ledger nearly doubled and then drained itself: 37 raised, 28 closed, and the `Fixed`
pool emptied to zero because one cross-family verifier cleared the whole queue in a single
pass. Two-eyes held 28 of 28 with zero self-closes. The only things now waiting are two
`Blocked` items, both parked on Arthur.**

| State | 07-24 | 07-25 | Δ |
|---|---:|---:|---:|
| Open | 25 | **34** | **+9** |
| Blocked | 0 | **2** | +2 |
| Fixed (awaiting two-eyes) | 2 | **0** | −2 |
| Closed | 31 | **59** | **+28** |
| **Total** | 58 | **95** | **+37** |

CONFIRMED by a direct `- **State:**` scan of all 95 files at HEAD, cross-checked against
the same scan at `a1dec8c`. New IDs are `MM-BUG-KILN-00059`…`00095`, contiguous — of
which 24 are already Closed, 12 Open, 1 Blocked. (`00096` was raised and fixed at 01:52,
after this worktree's HEAD.)

**Severity of the 37 arrivals:** Critical 0, High 5 (`00059`, `00064`, `00074`, `00075`,
`00076`), Medium 23, Low 9. Priority `Must` on 5.

### The +28 closes — the discipline held

Three commits did all of it: `0b774db` (00046, 00048 — the two baseline `Fixed`),
`b98a6a9` (00059, 00060, 00067, splitting residuals to 00071–00073), and `01b89ac`
(23 in one sweep).

- **28 of 28 name a verifier**: Codex GPT-5.6-Sol in every case.
- **28 of 28 are cross-family** — the fixer is Claude Opus 4.8 throughout.
- **0 self-closes.** Several go further and state in-file that the recorder "did NOT
  perform the verification and did not author the fix".
- **The evidence is real, not asserted.** The closure journal records a distinct
  fails-before negative control for each of the 23, e.g. *00074*: restoring fixed-size
  delay-line constructors makes `every_program_renders_at_every_common_rate` fail at a
  requested tap of 2062 frames against capacity 2048. *00086*: restoring one source frame
  per output frame drops loop-lag NCC to −0.003 from 0.998. Gates green with stdin closed
  — 666 lib tests, 25 amp-lab, 31 Python, clippy clean including `--no-default-features`.
- **Attribution strength splits 23/5.** `01b89ac` is co-authored by the verifier itself
  (`OpenAI Codex GPT-5.6-Sol`), so its independence is evidenced by the commit trailer.
  The other 5 are recorded by the fixer's family, disclaimed in-file — INFERENCE: those
  five rest on self-report, though the per-bug evidence is equally specific.

So `Fixed` is zero because the backlog was drained, not because closure was skipped. This
is the single healthiest signal in the series.

### The 2 Blocked — both on Arthur, neither a difficulty block

- **`MM-BUG-KILN-00045`** (High) — ferrosintesis spreads GM 32–39 over 25.02 dB where the
  SC-55mkII and S-YXG50 span ~9 dB. The target levels are not derivable unattended; it
  needs a **listening call**.
- **`MM-BUG-KILN-00068`** (Medium) — `ferrosintesis` declares `MIT OR Apache-2.0` while a
  default build embeds ten CC-BY/MIT sample banks, and `-samples-sax` declares only
  `CC-BY-4.0`. The edits are one line each, but changing a **published** crate's licence
  expression is a distribution-semantics change that can break dependents' licence gates.
  Needs Arthur's **decision**, not agent judgement.

### The oracle-hole trio — 2 of 3 fixed, 1 still Open

`KILN-00071` (licensing oracles assert `contains(crate_name)`, so they pass on a NOTICE
gutted to a bare list of names) and `KILN-00073` (the prewarm scan keys off `pub fn
*_bank`, missing private fns, differently-named public fns, and `GONG_LAYERS` which the
counter cannot see at all) are both **Closed**. **`KILN-00072` remains Open** (Could/Low):
`manifest.rs` comment-stripping still models only TOML basic strings, so a literal string
containing `#` is falsely flagged and a `"` inside one can hide a malformed table. It is
the last unfixed hole in a guard the repo now leans on.

### Where the bugs came from, and ownership

The 7 `review: autonomous code-review pass` commits raised **24 of 37** — coverage-driven
sweeps over `-samples-ccby`, `-bass`, `-bottle`, `amp-lab`, `-rain`, `-mandolin`,
`-fretnoise` (`ca92d60` alone raised 9, all amp-lab). The other 13 are fix-work fallout,
including the three adversarial splits above.

**No bug carries an `Owner`; all 95 read `-`.** *Correcting the prior report:* this is not
a defect. `bugs/README.md:38-45` defines `-` as the unowned marker and owner fields as
**leased automation claims** carrying run/host/branch/fingerprint metadata. Between leases
the resting state is `-`. Calling it "the queue is unattended" over-read the schema.

---

## Requirements ledger

**The ledger accreted and drained nothing. Three new Drafts landed, all from Codex review
passes; `Accepted` is 0, so `deltic reqs --ready` is provably empty and no auto-drain can
fire; `Satisfied` remains 0 on day 17.**

| State | 07-24 | 07-25 | Δ |
|---|---:|---:|---:|
| Draft | 4 | **7** | **+3** |
| Accepted (= the `--ready` pool) | 0 | **0** | — |
| Implemented | 24 | **24** | — |
| **Satisfied** | 0 | **0** | — |
| Total | 28 | **31** | +3 |

CONFIRMED across all 31 files. Draft IDs: `00018`, `00021`, `00022`, `00023`, `00029`,
`00030`, `00031`.

**The three new reqs** (`756a3e1`, `ca92d60`, `b1e2a6b`) all report the repo's own named
recurring defect — hand-maintained lists that drift:
- **00029** (light) — retain offline-auditable licence evidence for login-gated CC-BY
  Freesound sources.
- **00030** (heavy) — the amp-control NRPN protocol is defined three times
  (`crates\amp-lab\src\amp.rs:7-50`, `crates\ferrosintesis\src\engine.rs:417-438`,
  `crates\ferrosintesis\README.md:139-150`); wants one authority or a derived parity oracle.
- **00031** (light) — fret-noise inventory duplicated between `fretnoise_bake.py` and the
  generated crate lib.

**Day 17 with zero acceptances.** `reqs/README.md:137` defines `--ready` as `Accepted` +
routable `Flow` + unclaimed; with Accepted pinned at 0 the ready set is empty **by
construction**. The last genuine human Gate-1 batch was **2026-07-08** (17 days). Gate 2
(→ `Satisfied`) has **never fired** in the ledger's life. Nothing has ever left: 0 of 31
Satisfied, 0 of 31 Retired.

**All four traceability defects still hold, and (b) is worse than a spot-check showed:**
- **(a)** `MM-REQ-KILN-00003`'s declared `Satisfied-by` oracle
  `synth_fx_97_99_101_103_sustain_as_pads` exists nowhere as a test — only as a *deletion
  note* in a doc comment at `crates\ferrosintesis\src\voices.rs:20457` (the live test is
  `synth_fx_96_103_route_to_fx_voice`, line 20461). A filtered `cargo test` on the named
  oracle matches zero tests and exits 0 — a silent false pass.
- **(b)** **All 10** SHAs cited by the 07-08/07-09-era reqs are **dangling**; the 8
  post-restructure ones resolve. INFERENCE: the `.opus` history purge rewrote every
  pre-restructure SHA, silently voiding that evidence.
- **(c)** `Req-Id:` trailers stopped at `e4de770`, **2026-07-09** — 16 days. 10 of the 24
  `Implemented` reqs have no commit carrying their ID, so the README's documented
  `git log --grep='Req-Id: <ID>'` revert path cannot audit them.
- **(d)** `MM-REQ-KILN-00018` is not merely un-triaged, it is **stale**: it demands
  committing the bytes produced from committed MIDI, a premise retired when renders became
  git-ignored build output. It should be `Retired`, not left Draft.

**Direction: accreting.** Inflow 3, outflow 0, across 76 commits. Agents keep filing
Drafts into a queue nothing consumes.

---

## Design backlog

**The design backlog did not move. Zero new HLD, PLN or design docs were authored; every
`wrk_docs/` file added was a code-review record. All four cold designed-but-not-started
items are exactly where 07-24 left them, and no new human sign-off landed.**

Docs **added** this window (`--diff-filter=A`): **14 files, 7 pairs**, each a
`2026.07.24 - CR - 20260724-REV-CLA@KILN-code-review-*.md` plus its `review-coverage/`
fragment — transactional read-only review passes over one crate each, declaring their
baseline SHA. None carries an approval marker; **they design nothing**.

Docs **modified**: one — `wrk_docs\2026.07.18 - HLD - XG bank-LSB variation voices (CC32)
+ CC84 portamento.md` (`3cb7cb6`), adding the `(25, 96)` mandolin row and a dated
amendment. Status line unchanged (SELF-APPROVED, 2026-07-18).

### The four cold items — none moved

| Doc | Status | Age | Blocker |
|---|---|---:|---|
| `wrk_docs\2026.07.13 - HLD - anchor fidelity push (piano and string ensemble).md` | `DRAFT for review` | **12d** | **HUMAN** — Arthur's read + a per-stage listening panel |
| `wrk_docs\2026.07.14 - CR - LA sample layer deletes the model onset.md` | `INVESTIGATION — awaiting Arthur's ear` | **11d** | **HUMAN** — one A/B verdict; gates pluck Phase 4 |
| `wrk_docs\2026.07.18 - PLN - GM0 grand alternate banks audition.md` | `IN-PROGRESS` | **7d** | **HUMAN** — the ranking *is* the deliverable |
| `wrk_docs\2026.07.19 - HLD - natural pluck excitation redesign.md` Phases 3/4 | `APPROVED` (Arthur, 2026-07-19) | **6d** | **NONE — agent-actionable** |

**Pluck Phases 3/4 is the one cold item with no human gate.** Arthur approved the phased
plan and self-paced execution on 2026-07-19. No window commit references Phase 3
(picked-family migration) or Phase 4 (restrike/hammer unification). It is idle for lack of
a claim, not lack of a decision — the obvious pickup.

**Anchor fidelity remains 100% absent from source.** `Railsback` — **0 matches** across
`crates/`. No stretch-tuning table and no pitch-dependent piano inharmonicity coefficient
exists. *Refining the prior characterisation:* the `stretch`/`inharmonic` tokens that do
exist are non-piano implementations (cymbal/metal stacks, Rhodes tine, bell, banjo head,
reed, sample repitch); two hits naming a piano are **prose in test comments**
(`crates\ferrosintesis\src\testutil.rs:1663`, `crates\ferrosintesis\src\engine.rs:7129`),
not code.

**Approval regime unchanged: 20 genuine SELF-APPROVED design HLDs, 0 new this window.** No
new human `SIGNED-OFF` landed; the most recent genuine one is
`wrk_docs\2026.07.14 - HLD - ferrosintesis voice-quality overhaul.md`, **11 days old**.
(Note two files matching `SIGNED-OFF` are *negations* — "NOT human SIGNED-OFF" — so naive
greps overcount.)

---

## Key commits & new features

**Two owner-recorded instruments shipped end to end, alongside an audio-thread hardening
pass and an adversarial audit of the previous day's oracles. Composition stayed dormant:
3 album `.mid` moved, but only to add bank-select bytes — 1,626 notes change instrument,
zero notes are added.**

| Metric | This window | Prior (07-24) | Δ |
|---|---:|---:|---:|
| Commits / merges | **76** / 0 | 22 / 0 | **+54** |
| Files / insertions / deletions | 298 / 18,482 / 3,996 | 81 / 10,951 / 1,077 | — |
| `feat` / `fix` | **12 / 26** | 5 / 3 | +7 / +23 |
| Version bumps | 0 (release-only) | 0 | — |
| Album `.mid` added / modified | 0 / **3** | 0 / 0 | — |

Type split: 26 fix, 22 docs, 12 feat, 7 review, 4 test, 3 build, 2 style, 1 chore.
Version confirmed still `0.21.56`; `.deltic-integrate.toml:8` sets
`version_bump = "release-only"`, so zero bumps is correct.

### The five arcs

1. **Mandolin sample bank** (13 commits, `8b6a2d3` → `d444c9b`). Owner-recorded source
   cuts → bake (`7dcef3e`) → crate wiring (`71b7898`) → zone banks with a
   fretboard-structure oracle (`ee4bcbc`) → the XG cell GM 25 + bank LSB 96 (`8f4ffc1`) →
   4-round-robin bank (`32aabe6`) → real-sample tremolo (`0f80b7a`), then two follow-on
   defects (`31468cd` KILN-00087 tremolo retrigger identity, `d444c9b` KILN-00088
   bank-scoped strike phase). Album adoption in `0d8ae49`.
2. **Eastman E1D / GM 25 steel** (4 commits). New repo-root `samples/` store plus masters
   (`810318c`), 16 verified zone cuts (`e72bf7b`), Eastman as default GM 25 behind a
   four-way CC0 split (`d893cf9`), and a gain re-fit after a rebase collision (`20483d5`).
3. **GM 120 fret noise** (3 commits). A CC0 crate of 12 owner-recorded finger-slides
   (`1780465`), voice dispatch and calibration (`30e6877`, closes KILN-00040), docs
   (`48343d2`). `FRETNOISE_LEVEL = 0.23` sits the slide −11.2 dB under a steel chord
   against SC-55mkII (−11.6) and S-YXG50 (−10.9); character now lands between both
   references on every axis where the modeled burst was a distant hiss.
4. **amp-lab hardening** (8 fixes, KILN-00076…00083). Realtime-safety work on the GUI:
   allocation measurement, `Send`-not-`Sync` ring endpoints, large-callback and non-f32
   device survival, sample-accurate event scheduling, and a "Copy Settings" export that
   emitted code calling a function that did not exist. `b501c47` folded the crate into
   the workspace, deleting its 3,187-line nested `Cargo.lock`.
5. **Derived-oracle / licensing audit** (7 commits). `096f09a` (KILN-00060, all ten
   attribution-bearing banks credited), `9850aa1` (KILN-00059, prewarm 80 banks not 24),
   `11a180f` (KILN-00067, MSRV 1.87 restored), then the adversarial follow-ups `743944e`
   (KILN-00071), `83082d2` (KILN-00073), `96e2a47` (KILN-00069), `b403010` (KILN-00070),
   and `715f8a3` tempering the CLAUDE.md convention with the lesson.

### Render-diff discipline — better where it counts, thin overall

26 commits touched `voices.rs` / `engine.rs` / `drums.rs` / `sampler.rs`. **8 record a
numeric tally (31%); excluding the 8 test-only, rustfmt and cfg-gating commits, 8 of 18
audio-capable (44%).**

| Commit | Tally | The numbers |
|---|---|---|
| `2a8f39a` mallet echo cut | YES (in `9b1822b`) | 141 catalog: **52 DIFF / 89 SAME / 0 unexpected**; first-cut expected 61, all 9 of the gap explained |
| `5b0c777` GM 32-35 bass restore | YES (in `261ac9d`) | 124 albums: **42 changed / 82 same / 0 contamination / 0 not-reached** |
| `0f80b7a` mandolin tremolo | YES (in `648f590`) | **0 changed / 124 same / 0 contamination / 0 not-reached** |
| `d893cf9` Eastman default GM 25 | YES (in `05bdecc`) | 16 expected changes, 0 contamination, 17 not-reached (all ch-9 drum PC 25 — see hygiene) |
| `d444c9b` KILN-00088 | YES | 141-MIDI census predicted 2 files; renders matched, controls included |
| `31468cd` KILN-00087 | YES | 5 of 5 byte-identical |
| `83082d2` KILN-00073 | YES | 8 of 8 byte-identical |
| `db666a3` KILN-00086 | YES | 2 of 141 (both demos), byte-identical, positive control at 48 kHz |
| `30e6877` GM 120 sampled | **PARTIAL** | demos 2 changed / 0 contamination; album numbers never landed |
| `af0bfcf` KILN-00061 | **VAGUE** | "the album render-diff is clean" — no counts |
| `e143c48`, `20483d5`, `32aabe6`, `8f4ffc1`, `ee4bcbc`, `71b7898`, `1780465`, `0019574`, `03502b3`, `9850aa1` | NO | several argue no-change by construction |

The quality of the tallies that *do* exist is high — `648f590` even explains why it
baselined against the branch fork point (`85005fb`) rather than trunk head, so other
agents' integrated work would not be misattributed. That is the practice to keep.

**The prior window's two gaps are still unrecorded (CONFIRMED).** No commit body in
`a1dec8c..715f8a3` mentions `aadcd57` or `2186b73`. KILN-00048's independent close
transplanted the regression test onto parent `44386be` — a correct fails-before check, but
**not** a render-diff inventory. The vel-100 bit-identity canary remains its only render
evidence. And `20483d5` records the concrete cost: KILN-00048 moved the STEEL model's
level inside the 0.05–0.28 s crossfade so a previously-fitted gain fell through
`la_steel_high_key_level_parity`'s 0.8 floor (key 79 read 0.79), found only on a rebase.

### Where the 18,482 insertions land

Docs and ledgers dominate; this is not a DSP-heavy window by line count.

| Area | Insertions |
|---|---:|
| `wrk_docs` + `wrk_journals` + `bugs` + `lessons_learnt.md` + `CLAUDE.md` | **7,724 (42%)** |
| `crates/ferrosintesis/src` | 4,265 (23%) — `sampler.rs` +1,833, `voices.rs` +787, `engine.rs` +477, `licensing.rs` +410 |
| root `Cargo.lock` | 3,086 — absorbing amp-lab's tree on workspace merge |
| `crates/amp-lab` | 1,413 (against −3,319, mostly the deleted nested lockfile) |
| generated sample-crate `lib.rs` + provenance | 818 |

### Album composition — a re-voicing, not composition

`0d8ae49` **does** modify committed album MIDI, ending a four-day drought in the literal
sense: `Hollow Hill/midi/01` (137,016 → 137,026 bytes), `02` (83,680 → 83,685),
`The Signal Fire/midi/01` (182,712 → 182,725). But the deltas are 5–13 bytes of
bank-select controllers. Three channels always *written as* mandolin — faking it with a
bare GM 25 steel guitar played in 32nd-note repeats — now select the real instrument;
1,626 notes change instrument, **zero notes are added**, durations and manifests unchanged.
Bank select is written before each Program Change and reset to LSB 0 before the channel
becomes something else, because the committed `.mid` is the portable source of truth even
though ferrosintesis reads the bank at note-on. **No new music was composed this window.**

### Blast-radius ranking

1. `2a8f39a` mallet bus-echo cut — **52 of 141** catalog MIDIs (largest sonic move).
2. `5b0c777` GM 32-35 modeled-bass restore — **42 of 124** album MIDIs, 11 albums,
   35,831 notes.
3. `d893cf9` Eastman as default GM 25 — 16 files, 0 contamination.
4. `d444c9b` / `0d8ae49` mandolin — 2–3 album tracks.
5. `db666a3` GM 96, `30e6877` GM 120 — demos only (the GM 120 album leg was never recorded).

### Attribution

69 trailers across 76 commits: **Claude Opus 4.8 (1M) — 65 (94%)**, Claude Opus 5 (1M) — 3
(the tail of the window, plus `2723a3b`), OpenAI Codex GPT-5.6-Sol — 1 (the 23-bug closure
sweep). The 7 `review` commits are scheduler-owned and carry no trailer; their Codex
attribution rests on the bug files' own text — UNVERIFIED, though the *verification*
attribution that matters for two-eyes is corroborated by `01b89ac`'s trailer.

---

## Branches & worktrees

**Sprawl stopped growing for the first time in the series — 33 worktrees flat, unmerged
task branches 10 → 9, stranded commits 34 → 31 — but the shape is calcifying, not
improving. One carry-over defect closed today; the other has started costing real work.**

| Class | Count |
|---|---:|
| Main clone (`D:\language\midi-music`) | 1 — at `715f8a3`, now 1 behind `origin/main` |
| Agent-internal | 1 (merged, reapable) |
| Task worktrees | 16 |
| Detached baseline / audition | **15** (was 14) — now the largest single class |
| **Total** | **33** |

### Unmerged task branches — 9 branches / 31 commits

| Branch (`task/…` unless noted) | ahead/behind | Verdict |
|---|--:|---|
| `20260723-…-b1-upright-piano-sample-bank-from-arthur` | 9 / 1 | **Integrable WIP** — freshest, actively rebased |
| `20260725-…-ferrosintesis-review-remediation` | 2 / 5 | **Integrable WIP** — live loop build (crates.io packaging) |
| `20260718-…-max-richter-leftovers-album` (also on origin) | 3 / 453 | **Integrable WIP** — additive `albums/fable5/` files, low conflict risk |
| `20260717-FIX-CDX-fix-smf-running-status-…` | 3 / 501 | **Superseded — reap** (see Defect A) |
| `20260719-…-fix-brush-slap-…` | 2 / 277 | **Superseded** — trunk's `MM-BUG-KILN-00001` is already Closed |
| `20260719-…-driven-guitar-29-30-mix-presence-trim` | 1 / 277 | **Redo-on-trunk** — a bare mix constant, 277 commits of mix work since |
| `20260714-…-sampled-acoustic-drum-kit-…` | 4 / 586 | **Redo-on-trunk** — carries a 586-commit-stale version bump |
| `20260715-…-instrument-voice-collisions` | 3 / 557 | **Salvage, low value** (journal only) |
| `origin/claude/bugs-queue-2q-drain-609csu` | 4 / 158 | **Salvage — urgent** (see Defect B) |

"31 unique commits" is a sum of per-branch ahead counts — INFERENCE, not de-duplicated
(`git rev-list --not` is sandbox-denied).

**Reapable (0 ahead): 8** — `rt-margin-bench`, `sax-la-sample-layer`,
`trim-wind-on-blown-bottle`, `bagpipe-held-note-loop-click`, `t16-guitar-solo`,
`amp-knob-listening-samples`, the agent-internal worktree, and the new empty
`20260725-FIX-HUM-kiln-00030-harpsichord-la-onset-velocity`.

### Carry-over defect A — running-status parser: **FIXED on trunk today**

The defect was real in the `715f8a3` tree this report holds:
`crates\ferrosintesis\src\midi.rs:233-237` used one `status` variable for both the running
status and the current event's status byte, so the `0xFF` arm (line 240) and the
`0xF0 | 0xF7` arm (line 260) both latched a system byte the channel path then reused.

`2723a3b` (2026-07-25 01:52, tracked as `MM-BUG-KILN-00096`) splits the two roles:
`running_status` latches only `0x80..=0xEF`; a system byte applies to its own event and is
never latched. A track opening with a data byte now errors cleanly as
`BadStatusByte { status: 0 }` rather than misparsing. It ships **three** regressions —
`running_status_survives_meta_event`, `running_status_survives_sysex_event`,
`system_status_never_becomes_the_running_status` — all red on pre-fix trunk with the
predicted `UnexpectedEof`, 670 lib tests green. Confinement: all 141 committed `.mid`
scanned with an independent SMF walker, **zero** contain running status after a meta/SysEx
event, so no album parse and no render changes.

Note the fix is **strictly better than the stranded branch**: `294aa94` never covered the
SysEx half. `294aa94` is confirmed NOT an ancestor of trunk, and its branch is now dead
weight — reap it.

### Carry-over defect B — cloud drain: worse, and now duplicating work

`origin/claude/bugs-queue-2q-drain-609csu` is still unmerged at **4 ahead / 158 behind**.
It carries `427281a` (the floored-onset-law fix for KILN-00030) plus a Blocked → Fixed
ledger flip.

- Trunk's `bugs\MM-BUG-KILN-00030.md` still reads **Open**, `Attempts: fix=0`, state
  history ending at the 2026-07-20 raise. Trunk has no knowledge a fix exists.
- **New this report:** a worktree
  `D:\worktrees\midi-music\20260725-FIX-HUM-kiln-00030-harpsichord-la-onset-velocity` was
  opened **today** against the same bug. Work is being redone because the existing fix is
  invisible from trunk.
- **The KILN-00028 measurement was never ported.** Trunk's `bugs\MM-BUG-KILN-00028.md` is
  bare `Open` with the original sketch intact — *including its false premise*
  ("exact-render-preserving for every existing file"). The branch version is `Blocked` and
  carries the refutation: **86 of 124 album tracks author CC91 on channel 10**, values
  20–110, which would rescale the drum room send 0.5×–2.9× and re-mix 86 established
  tracks. Anyone picking up 00028 from trunk will re-prototype a fix already known to fail
  the controller-feature render-diff rule.

### Dirtiness — 1 of 33 checkable, 32 UNVERIFIED (6th consecutive report)

`git -C "<path>" status --porcelain` is **denied by the sandbox in both Bash and
PowerShell**; only the cwd form works. This worktree is clean (CONFIRMED). The other 32 are
**UNVERIFIED — do not assume clean**. Two are near-certainly dirty by INFERENCE (the new
KILN-00030 worktree holds 0 commits, so any work there is uncommitted; the
review-remediation loop build is live). This blocks every reap decision and needs a `git -C`
allowance in the runner's permission set.

---

## Repo hygiene

**Net: mixed, with one genuine resolution. The MSRV defect is properly fixed and now
guarded; the lessons file is being actively curated at cap. Against that, the scratchpad
grew, three carry-over one-liners are untouched, and a new workspace-MSRV gap surfaced.**

- **`lessons_learnt.md` — cap MAINTAINED (20), but the format rule is NOT met.** `34cebd6`
  merged two pairs into one failure class each, trimmed the two worst nuggets
  (234 → 175 and 174 → 152 chars), and landed the GM 8/9/10 echo-send lesson. No entry was
  dropped; every symbol referenced across all 21 pre-merge entries was grepped and still
  exists. `715f8a3` then added the derived-oracle caveat. Curation is genuinely active.
  **But *correcting the prior report's "100% index-v1 conformant"*:** the spec at
  `lessons_learnt.md:5-8` demands a first line of ≤~120 chars carrying a `file:symbol`
  pointer, and **19 of 20 first lines exceed 120 chars** (longest 175), while strict
  `file:symbol` conformance is **9 of 20** — three give a file but no symbol, one a symbol
  but no file, and seven carry no pointer at all. The prior claim measured *structure*
  (a self-contained bolded first line, which is genuinely 20/20), not the stated rule.
  This is not cosmetic: the SessionStart hook injects **only** these first lines, so every
  session pays for the overrun.
- **`scratchpad.md` — WORSE: 36 → 41 open items (+5), none retired.** The new five are
  substantive, not noise: (i) `gen_crate_lib.py` emits a `src/lib.rs` that is not
  rustfmt-clean once a bank's WAV names are long — it failed the integration gate on the
  mandolin crate after everything else was green; (ii) `tools/ferrosintesis-samples/
  test_prepare.py` (27 tests) **is run by no gate** — `.deltic-integrate.toml`'s
  `workspace` component lists `tools/` in its paths but every gate command is cargo, so
  Python changes trigger a Rust-only gate; (iii) the workspace MSRV gap below; (iv) offline
  renders may not parallelise; (v) 8 clean body-knock samples captured but unused.
  The `--solo 8` render-hang entry is **still open at `scratchpad.md:512`** even though its
  promoted bug `MM-BUG-KILN-00027` is Closed — the same contradiction, third report.
- **`CLAUDE.md` ↔ `.deltic-integrate.toml` — UNCHANGED.** `CLAUDE.md:176` still says "a
  shipped-code change needs one version bump per integrated task";
  `.deltic-integrate.toml:8` sets `version_bump = "release-only"`. `grep` for
  `deltic-integrate` and `release-only` in `CLAUDE.md` returns **nothing** — the repo file
  still never names the contract. This window shipped substantial source with zero bumps:
  correct under the contract, wrong under `CLAUDE.md:176`.
- **Cargo.toml MSRV inline tables — RESOLVED, and guarded.** All 23 sample-crate
  dependencies in `crates\ferrosintesis\Cargo.toml:47-69` are now one line each, under the
  warning comment. `crates\ferrosintesis\src\manifest.rs` exists and is deliberately a
  **text** check, not a build check, so it fails on any machine in the ordinary test run
  without a second toolchain — the right design, since the fleet's current cargo parses the
  broken form happily. (Residual: `KILN-00072`, its literal-string hole, is still Open.)
- **NEW — workspace MSRV is broken by a transitive dep** (`scratchpad.md:10-15`).
  `cargo +1.87 check --workspace` **fails**: `image@0.25.10` requires rustc 1.88, pulled via
  amp-lab's eframe/egui stack. `ferrosintesis` and every sample crate pass `+1.87`, so the
  *shipped* MSRV is real — but the fleet's documented proof command cannot pass as written
  until `image` is pinned back or amp-lab's MSRV is bumped. Confirmed identical on the
  `dfbf1f7` baseline, so it is pre-existing, not introduced.
- **`render_diff.py` ch9/ch10 fold — UNCHANGED, day 8, and actively producing noise.**
  `tools\render-diff\render_diff.py:116-118` still folds every sounded channel's programs
  into one melodic set, so a channel-10 kit-select PC is treated as a melodic program. This
  is exactly what produced the Eastman inventory's "17 not-reached (all ch-9 drum PC 25)".
- **`CLAUDE.md` content drift — partly fixed.** The stale "218 files / 17.73 MiB" figure is
  **gone**. Still wrong: `CLAUDE.md:110` says the `embedded-samples` feature "compiles the
  **two** asset crates into the final binary" — the feature list at
  `crates\ferrosintesis\Cargo.toml:16-39` names far more, and **23** sample crates exist.
  `albums/gpt5-6/` exists on disk and is still **unlisted** in the layout section.
- **Assets — the new-crate trap was avoided.** Tracked `.wav` 1,070 → **1,206** (+136);
  whole-tree tracked `.mid` **142** (album `.mid` still 124). All **23**
  `crates/ferrosintesis-samples-*` directories have their `!…/samples/*.wav` allowlist line
  in `.gitignore`, and for all 23 the tracked count equals the on-disk count; the two new
  crates carry their samples (mandolin 40, fretnoise 12), so neither is committed as a
  crate that fails to build from a clean checkout.
- **NEW — that `.gitignore` allowlist is itself the unguarded hand-maintained list.** The
  23 `!crates/ferrosintesis-samples-*/samples/*.wav` lines (`.gitignore:45-66`, plus `bass`
  orphaned at `:72`) grow one entry per new bank and nothing derives them. This is exactly
  the defect class CLAUDE.md's own "Hand-maintained lists are the recurring defect here —
  derive them" section warns about, and it is the **one remaining instance with no oracle**
  — the licensing, manifest and prewarm lists all now have one. The failure mode is the
  documented build-breaking trap: a crate committed without its samples. It has not fired
  yet only because the last two authors remembered.
- **Tracked `.opus` = 5, not 1.** *Correcting the prior report's figure:*
  `samples/acoustic-guitar-eastman-e1d/{picked,plucked}.opus`,
  `samples/banjo/banjo-5string-openG-2026-07-23.opus`, and
  `samples/fret-noise-eastman-e1d/DR0000_020{3,4}.opus`. All five are first-party
  performance masters under `samples/`, deliberately tracked as source. **Zero album
  listening copies are committed** — the invariant holds.

---

## Prior tracked issues — status audit

| # | Issue (07-24) | Status | Evidence |
|---|---|---|---|
| 1 | Running-status parser live + untracked + stranded | **DONE** | `2723a3b` (01:52 today) splits the latch, ships 3 regressions red-before, scans all 141 `.mid` for confinement; tracked as `MM-BUG-KILN-00096`. |
| 2 | Two most-audible prior changes lacked a render-diff tally | **UNCHANGED (for the named two) / BETTER overall** | No commit in the window mentions `aadcd57` or `2186b73`. But 8 tallies landed, including 52/141 and 42/124 on the two biggest changes. Coverage 8 of 18 audio-capable commits. |
| 3 | Per-host bug-ID collision — no structural guard | **UNCHANGED** | No `.githooks/`, no tracked hook, no guard commit in the window. |
| 4 | Open bugs unowned (25) | **MISFRAMED — see correction** | 34 Open, all `Owner: -`. But `bugs/README.md:38-45` defines `-` as the resting state between leased automation claims. Not a defect. |
| 5 | Two-eyes: 00046/00048 awaiting eyes; cross-family not codified | **BETTER in practice / UNCHANGED in doctrine** | 00046 + 00048 Closed (`0b774db`) with transplanted-oracle fails-before proof; 28 of 28 closes cross-family. But `bugs/README.md:30-31` still says only "a second pair of eyes". |
| 6 | Cloud drain strands 00030 fix + 00028 measurement | **WORSE** | Still 4 ahead / 158 behind; 00030 still Open on trunk; 00028's false premise still on trunk; **a new worktree opened today to redo the same bug**. |
| 7 | Reqs frozen; Accepted 0; light Drafts unblocked | **WORSE** | Draft 4 → 7; Accepted 0; Satisfied 0 on day 17; 00021/22/23 still Draft; 00018 still mis-triaged. |
| 8 | CLAUDE.md ↔ integrate contract; MSRV inline tables | **MIXED** | MSRV **RESOLVED** + `manifest.rs` guard. Version-bump contradiction UNCHANGED (`CLAUDE.md:176`). New: workspace `+1.87` still fails via amp-lab. |
| 9 | Human listening/ears queue grew | **MIXED** | Arthur drained one A/B — and it **caught a 9.2 dB regression on 42 albums** the oracles missed. But 00045 and 00068 are newly Blocked on him, and the four cold design items are unmoved. Net grew. |
| 10 | Dirtiness UNVERIFIED (d5); lessons at cap; scratchpad 36 | **MIXED** | Dirtiness UNVERIFIED **day 6** (1 of 33). Lessons maintained at 20. Scratchpad **WORSE** (41). |

**Tally: 1 DONE / 1 BETTER / 2 UNCHANGED / 2 WORSE / 3 MIXED / 1 misframed-and-corrected.**
The first DONE in the series, and it is the item that led three reports.

---

## Tracked issues

1. **The audio-side oracle net cannot see a whole class of defect.** A 9.2 dB bass loss on
   42 of 124 albums lived on trunk 4 days (`dbef98a` 07-20 → `5b0c777` 07-24) and was found
   by Arthur's ear. Root cause: `LaVoice::render` is a **sum-to-one** crossfade, so making
   a sampled onset the default bank *mutes* the model it displaces and nothing fills the
   deficit if the wrap gain is under-fitted. `e143c48` shows the measurement trap too — the
   obvious `[0.05, 0.35]` window reads the defect as a mild −2..−5 dB because the model
   dominates across the fade; only `[0, 50 ms]` sees the true −8.2 dB. **Any future
   default-bank switch needs a level-continuity oracle measured in the pre-fade window.**
2. **The cloud-drain branch is now costing engineering time, not just hygiene.** A worktree
   opened today to redo KILN-00030 whose fix has existed since 07-20 on
   `origin/claude/bugs-queue-2q-drain-609csu`, and trunk's `MM-BUG-KILN-00028.md` still
   carries a premise the branch disproved (86 of 124 tracks author CC91 on ch 10).
3. **The prior window's two render-diff gaps are still unrecorded**, and `20483d5` proves
   the cost is real: KILN-00048 moved the STEEL model's level inside the crossfade and
   broke a fitted gain, caught only by a rebase collision.
4. **Render-diff coverage is 44% of audio-capable commits.** The big changes are covered
   well; the medium ones are not. `30e6877` (GM 120) has demo numbers but no album leg;
   `af0bfcf` claims "clean" with no counts.
5. **Reqs is accreting on day 17.** Accepted 0 → the auto-drain is structurally idle;
   Draft 4 → 7; Satisfied has never fired. Traceability is worse than reported: **all 10**
   pre-restructure SHAs are dangling, `Req-Id:` trailers stopped 16 days ago, and 10 of 24
   Implemented reqs are un-auditable by the documented revert path.
6. **`KILN-00072` is the last unfixed hole in the derived-oracle guards** the repo now
   leans on — `manifest.rs` still mis-parses TOML literal strings.
7. **The cross-family standard is practised but not codified.** 28 of 28 closes were
   cross-family; `bugs/README.md:30-31` still asks only for "a second pair of eyes".
8. **Design authorship stopped.** Zero new design docs; 20 SELF-APPROVED HLDs unchanged;
   the last human `SIGNED-OFF` is 11 days old. All work this window was ledger-driven.
9. **The cheap one-liners keep being skipped:** `CLAUDE.md:176` version-bump contradiction
   (and the file still never names `.deltic-integrate.toml`); `CLAUDE.md:110` "two asset
   crates" (23 exist); `albums/gpt5-6/` unlisted; `render_diff.py:116-118` ch9/ch10 fold
   (day 8, actively adding noise); `scratchpad.md:512` open against a Closed bug.
10. **Cross-worktree dirtiness UNVERIFIED for a 6th report** (1 of 33), blocking all
    reaping — 8 branches are reapable and 15 detached baselines are now the largest
    worktree class. Needs a `git -C` sandbox allowance.

Watch, not yet issues: **no music was composed for a fifth day** (the album `.mid` edits
are re-voicing, not notes); the Python prep suite (27 tests) is run by no gate; offline
renders may not parallelise, which would make a full catalog render ~2 h rather than
~10 min and would make `render-catalog --jobs` useless.

---

## Plan for today

Ordered by value per unit of effort. Correctness on already-shipped work first; the human
gates are Arthur's.

1. **Port the cloud-drain evidence to trunk before the redo branch lands.** Cherry-pick at
   minimum the ledger commits carrying KILN-00030's Blocked → Fixed flip and KILN-00028's
   CC91 measurement. This is the only item on the list that is *actively wasting work right
   now*, and it is ledger-only.
2. **Fix `render_diff.py:116-118` (ch9/ch10 fold), then run the two missing inventories.**
   Fix the harness first so drum-channel program changes stop polluting results (it already
   produced 17 spurious not-reached), then record the album render-diff for the t60 re-fit
   (`aadcd57`) and KILN-00048 (`2186b73`). Budget for this being slow — see the
   render-parallelism doubt.
3. **Add the pre-fade level-continuity oracle that would have caught the bass regression.**
   Issue #1 is the most valuable engineering lesson of the window. A default-bank switch
   must be gated on a `[0, fade_start]` window measurement, not the fade span — the fade
   span demonstrably averages the defect away. This generalises beyond bass.
4. **Claim pluck redesign Phases 3/4.** The only cold design item with no human gate,
   APPROVED 6 days ago, idle purely for lack of a claim.
5. **Close `KILN-00072`** — small, Low, and the last hole in a guard the repo now depends on.
6. **Cheap hygiene, all one-liners:** reconcile `CLAUDE.md:176` with
   `.deltic-integrate.toml:8` and name the file; correct `CLAUDE.md:110` (two → 23 asset
   crates) and list `albums/gpt5-6/`; retire `scratchpad.md:512` with a pointer to Closed
   KILN-00027; add the Python suite to the integrate gate (check the runner has `python`
   on PATH first).
7. **Codify the cross-family two-eyes standard in `bugs/README.md`** — the practice is
   28 of 28; the doctrine should say so, or the next close will regress to same-family.
8. **Reap the superseded running-status branch and worktree** — `294aa94` is now dead
   weight, definitively superseded by `2723a3b`.
9. **Arthur — the decisions that gate more than any agent can do today:**
   (a) **`MM-BUG-KILN-00045`** — the GM 32–39 level spread (25 dB vs the references' 9 dB);
   the targets are not derivable without your ear, and it is a High blocking a whole family;
   (b) **`MM-BUG-KILN-00068`** — the licence expression on a *published* crate; one line, but
   it is a distribution-semantics call, not an agent call;
   (c) the LA-onset listening verdict (11 d) — it gates pluck Phase 4;
   (d) the GM 0 grand bank ranking (7 d) and the amp-lab GM 29/30 default;
   (e) **enforce or retire the reqs ledger** — accept 00021/00022/00023 to fire the first
   drain in 17 days, or retire it rather than let Drafts accrue against a gate that has
   fired twice ever; also Retire the now-stale 00018;
   (f) one session with a `git -C` sandbox allowance for the dirtiness sweep, blocked 6 days.

   **Before any listening session, re-render.** The synth moved materially — the mallet
   bus-echo cut alone changes 52 of 141 catalog MIDIs, plus the bass restore (42/124),
   mandolin, Eastman GM 25 and GM 120 fret noise. Any on-disk render is stale and
   cross-album A/B is confounded. Allow more wall-clock than usual pending the
   parallelism question.

**Not today.** The anchor-fidelity push (still the largest unstarted programme, still
gated on a human panel — the exact scarce resource already blocking four other lines). No
new GM voices; the window already added three instruments and the fix queue from them is
still 12 Open. And no new sample crates until the `gen_crate_lib.py` rustfmt trap is
closed — it broke an integration gate once already.

---

## Verification notes

Deterministic facts (ledger counts, commit counts, diffstat, branch ahead/behind, file
contents and line numbers, SHA ancestry, asset counts) were read from source by the lead.
Judgment claims were checked against the evidence rather than the commit messages.

- **CONFIRMED by direct lead read:** all 95 bug states (Open 34 / Blocked 2 / Fixed 0 /
  Closed 59); window `a1dec8c..715f8a3` = 76 commits, 298 files, +18,482 / −3,996, 0 merges;
  reqs 7 Draft / 24 Implemented / 0 Accepted / 0 Satisfied (31 files); version `0.21.56`
  with zero bumps; `lessons_learnt.md` = 20 at a declared cap of 20; `scratchpad.md` = 41
  open items with `--solo 8` at line 512; `CLAUDE.md:176` version-bump line and zero
  mentions of `.deltic-integrate.toml`; `CLAUDE.md:110` "two asset crates" against 23 real
  crates; all 23 sample-crate deps one-line in `Cargo.toml:47-69`; `manifest.rs` and
  `licensing.rs` both present; `render_diff.py:116-118` fold intact; `.gitignore` carries an
  allowlist line for all 23 sample crates; mandolin 40 / fretnoise 12 samples tracked;
  tracked `.wav` 1,206, `.mid` 142, `.opus` 5.
- **ADVERSARIALLY CHECKED — the closure surge is disciplined.** The claim tested was
  "28 closes in one day is a rubber stamp". Refuted by reading
  `wrk_journals\2026.07.24 - JRN - Fixed queue two-eyes closure pass.md`: it records a
  *distinct* negative control per bug, each restoring the specific pre-fix behaviour and
  naming the resulting failure with numbers. `0b774db` goes further — the verifier
  transplanted only the new tests onto each fix's exact parent rather than trusting the
  regression suite. Independence is corroborated by `01b89ac`'s own `Co-Authored-By: OpenAI
  Codex GPT-5.6-Sol` trailer.
- **ADVERSARIALLY CHECKED — render-diff "improved" is only half true.** The claim was
  tested by enumerating all 26 commits touching the four shipped synth files and reading
  each body, rather than sampling the good ones. Result: 8 numeric tallies (31%; 44% of
  audio-capable), 1 partial, 1 vague. The headline therefore reports both the strong
  inventories *and* the coverage fraction, and the prior window's two named gaps are
  confirmed still absent by grepping every commit body in the range for `aadcd57` and
  `2186b73` (zero hits).
- **CONFIRMED OUT-OF-WINDOW:** `origin/main` = `2723a3b` while this worktree's HEAD is
  `715f8a3`; read from the local ref without fetching. Its body, regression names and
  141-file confinement scan were read directly.
- **CORRECTED — tracked `.opus` is 5, not 1.** The 07-24 report's figure counted only the
  banjo master; the Eastman guitar and fret-noise masters are also tracked. All five are
  `samples/` performance sources, so the "zero committed *listening* `.opus`" invariant is
  unaffected.
- **CORRECTED — "Open bugs unowned" is a misreading of the schema.** `bugs/README.md:38-45`
  defines `-` as the unowned marker and owner fields as leased automation claims with
  run/host/branch/fingerprint metadata. All-`-` is the expected resting state between
  leases, not evidence of an unattended queue. Dropped from the tracked-issue list.
- **REFINED — the anchor-fidelity absence claim.** `Railsback` is genuinely 0 matches. The
  prior report's characterisation of the `stretch`/`inharmonic` hits as "non-piano" is right
  in substance; two hits do name a piano but are prose in test comments, not code.
- **UNVERIFIED, held not dropped:** cross-worktree dirtiness (sandbox-denied, 6th day; 1 of
  33 known clean); the "31 unique unmerged commits" figure (a per-branch sum, not
  de-duplicated — `git rev-list --not` is denied); the Codex attribution of the 7 autonomous
  *review* passes (their commits carry no trailer; only the *verification* attribution is
  trailer-corroborated); whether the GM 120 album render-diff leg would be clean.
- **INFERENCE, labelled inline:** that the dangling pre-restructure req SHAs are fallout of
  the `.opus` history purge; rebase-cost and reapability judgements on stale branches; the
  reading that the window is a QA surge rather than a feature surge.
