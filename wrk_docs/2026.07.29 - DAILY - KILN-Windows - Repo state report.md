# 2026.07.29 — DAILY — KILN-Windows — Repo state report

**Pass mode.** Read-only observation. This pass reads the repo and writes one report. It does not
clean, test, fix, review, render, or action its own findings. Actioning is the separate "B" prompt's
job.

**Integration is owned by the scheduler, not by this pass.** This is a scheduled ops run: deltic
commits and integrates the deliverables. This pass ran no `git fetch`, staged nothing, and pushed
nothing.

**Environment.** Windows host `KILN`, worktree
`D:\worktrees\midi-music\20260729-OPS-CLA@KILN-daily-report-020005`, HEAD `f74273f`
(= `main` = `origin/main`; `git status --porcelain` empty).

**Baseline and window.** The last integrated daily report is `7bd678b` (2026-07-28 03:25:52), which
is exactly `855df28` (the 07.28 report's HEAD) plus the report-landing commit. The window is
**`7bd678b..f74273f` = 83 commits** over **21 h 25 m** of commit time. Churn: **195 files,
+9,772 / −772**.

One nuance the commit dates hide: **authored** work spans 2026-07-26 23:52 → 2026-07-29 00:51
(~49 h), because the B1 piano branch landed carrying its original author dates.

**Method.** Six parallel readers over independent state sources (bug ledger, reqs + design backlog,
commit log, branches/worktrees, prior-plan audit, repo hygiene), plus **two adversarial skeptics on
the two claims the report turns on**. Both returned **NARROWER TRUE CLAIM**, and both corrections are
carried below — §1 and §3 differ materially from my first draft because of them. I verified the
headline facts myself rather than taking a reader's word: the 00038 closure, the reopen recurrence,
the `Verify retry after` census, the gate flip, the worktree count, the dangling `006fda6`, the
`gpt5-5` question, and the `REPO_SOURCE_DIRS` defect.

---

## Headline

**The ledger's last engineering blocker cleared — by Arthur's ear, not by automation — and the repo's
ref sprawl collapsed from 31 worktrees to 3. Meanwhile the report's own agent lane scored zero for
the first time, and the highest-value fix it asked for recurred verbatim 14 hours later.**

The good is real and specific. Arthur auditioned GM61 brass three times, rejected two candidates with
timestamped defects ("the high chord around 0:15 still synthetic", the 0:21 aftertouch "farty"), and
passed revision 03 with "Sounds good" — closing `MM-BUG-KILN-00038`, open since 2026-07-21. Zero Open,
zero Blocked bugs remain. A B1 piano workstream landed properly through a rebase, not a sweep. And
`gate=deltic` appeared in the ledger **for the first time ever**: the repo's own contract gate ran
during five fix sessions, where previously no gate token but `manual`/`cargo`/`focused` had existed.

Five things cut against it.

1. **The reopen tax is unfixed and recurred verbatim.** `MM-BUG-KILN-00157` was booked
   `Fixed → Open` because the verifier could not run `python3` — the fix was sound and 5 of 6 gate
   steps were green. Across all 164 bug files, `Verify retry after` is `-` **164/164** and
   `indeterminate=0` **164/164**. The schema's own park path has never once been used (§2).
2. **A fix landed that reinstates the defect class this repo documents as its recurring one.**
   `MM-BUG-KILN-00168`'s fix hardcodes a list of *one* source directory where four exist, and deletes
   the module's own warning against doing exactly that (§4).
3. **The mass reap left one commit four days from unrecoverable**, against Arthur's written
   instruction to keep it. `006fda6` is now on no ref (§3).
4. **The plan scored 1 of 14 (7%) — agent lane 0 of 6**, the series' worst, while the window did 83
   commits of work the plan never named (§9).
5. **Third consecutive window with no music.** GM0 — the repo's most-used program — now renders
   differently on **83 of 141** catalogue files, and nobody has listened at album scale (§5).

The through-line, and the most useful thing in this report: **the bug ledger is the only channel in
this repo with throughput.** Eleven bugs were raised this window by autonomous review and all eleven
were fixed inside it. Ten Draft reqs moved nowhere. Six agent-lane report asks moved nowhere. The
report has been writing its asks into the two channels that do not execute (§9, §11).

---

## 1. Bug ledger

**164 bug files** plus `README.md`.

| State | Count |
|---|---:|
| Closed | **157** |
| **Fixed** | **7** |
| Open | **0** |
| Blocked | **0** |
| **Total** | **164** |

`grep -L "^- \*\*State:\*\* Closed"` returns exactly seven files, so the list below is complete.
**No Open, no Blocked — a first for the series.**

### The 7 pending fixes

All seven are the same shape: `Priority: Could`, `Severity: Low`, `Attempts: fix=0, doubt=0,
indeterminate=0`, `Held branch: -`, every owner field `-`. All were raised by `gpt-5.6-sol@high` off
autonomous code-review passes, and all landed `gate=manual` between 22:39 and 00:51 — the last
2 h 12 m of the window.

| ID | Defect | Fix commit |
|---|---|---|
| 00161 | Kawai provenance maps two zones to wrong upstream labels | `b95a70f` |
| 00162 | Kawai aliases duplicate ⅓ of packaged + decoded PCM | `364ca8f` |
| 00163 | Kawai rebakes retain stale WAVs behind unrelated validation | `4c68345` |
| 00165 | Steinway aliases duplicate ½ of packaged + decoded PCM | `1b3a172` |
| 00166 | Steinway rebakes retain obsolete WAVs | `84c6446` |
| 00168 | B1 source recordings evade committed-input provenance oracle | `9df73cb` |
| 00169 | Advertised B1 regeneration deletes natural-tail validation | `429720f` |

**The pool is a mid-cycle cut, not a stall.** The verify wave ran 17:13–19:50 and closed five; the fix
burst ran 22:39 → 00:51 and landed these seven; the window ends on the last fix. Nothing is claimed,
parked, or burning a counter, and the pool is ~3 h old against a historical Fixed→Closed latency of
2–18 h in the autonomous lane.

**Two corrections to my own first draft, both from the skeptic.** I had written that the seven carry
no fixer `model=` token and therefore the two-eyes rule could not be checked. The first half is FACT —
a closed control (`00158`) carries three `model=` tokens, these carry only the raiser's. The
conclusion was wrong: **the fixer is identifiable from each commit's `Co-Authored-By: OpenAI Codex`
trailer**, and a missing `model=` is normal for every manual-land bug in the repo's history. The
narrower true finding is that the ledger field is incomplete while the information survives in git.
Separately, `gate=manual` is **not** a bypass signal and "manual land" is **not** a new verb — 28 such
commits since 2026-07-26.

**What does survive as a gap: none of the seven records validation evidence.** Five still read
`<unfixed — raised only>` under `## Fix`. Compare `bugs/MM-BUG-KILN-00160.md:76-79`, which records
`cargo test -p ferrosintesis inventory::tests — 19 passed`. A verifier picking these up gets no fix
prose and no test record to check against.

### Trend

| Metric | 07.22 | 07.23 | 07.24 | 07.25 | 07.26 | 07.27 | 07.28 | **today** |
|---|---:|---:|---:|---:|---:|---:|---:|---:|
| Total bugs | 35 | 50 | 58 | 95 | 121 | 146 | 153 | **164** |
| **Open** | 8 | 19 | 25 | 34 | 7 | 17 | 1 | **0** |
| Blocked | n/t | n/t | 0 | 2 | 14 | 0 | 1 | **0** |
| **Fixed** | 0 | 1 | 2 | 0 | 13 | 0 | 0 | **7** |
| Closed | 27 | 30 | 31 | 59 | 87 | 129 | 151 | **157** |
| Commits (window) | 23 | 34 | 22 | 76 | 191 | 194 | 234 | **83** |
| **Worktrees** | 29 | 25 | 33 | 33 | 45 | 33 | 31 | **3** |
| `scratchpad.md` open | 34 | 36 | 36 | 41 | 43 | 15 | 16 | **16** |
| `lessons_learnt.md` (cap 20) | 70 | 72 | 20 | 20 | 20 | 22 | 22 | **22** |
| reqs total | n/t | n/t | n/t | n/t | 32 | 38 | 39 | **42** |
| `#[test]` in `crates/` | n/t | n/t | n/t | n/t | 880 | 928 | 979 | **1006** |
| **`gate=auto` ever** | – | – | – | – | – | 0 | 0 | **0** |
| ferrosintesis version | .56 | .56 | .56 | .56 | .56 | .56 | .56 | **0.21.56** |

The version is unchanged and that is **correct**: `.deltic-integrate.toml:8` sets
`version_bump = "release-only"`.

**Raised: 11** (00157–00163, 00165, 00166, 00168, 00169) — all by autonomous review, all one coherent
class: **sample-crate provenance and bake-inventory integrity**. **Closed: 6**, corroborated two ways
(157 − 151, and six closure events).

### `MM-BUG-KILN-00038` — closed by ears, and the route was reversed

The 07.28 report recorded Arthur's approved route as *"model the section, don't sample it"*, with a
6-point contract and five cited papers. **That decision was reversed inside this window.** Arthur
rejected the modelled candidate as still synthy and approved a MuseScore MS Basic sampled-section
route instead. `bugs/MM-BUG-KILN-00038.md:21` now ends:

> → Closed (2026-07-28, independently verified by Arthur from the revision-03 WAV: "Sounds good")

Three audition rounds, two rejections with named timestamps, and a fix that reproduced both residual
defects numerically before fixing them (residual at 0:15 went 0.170 → 0.988; the 30 Hz section-wide
aftertouch flutter went 0.183 → 0.005). This is the human-oracle loop working exactly as intended, and
it is the only closure in the window that automation could not have produced.

### Ledger hygiene

- **Leases: spotless.** Zero populated owner fields across eleven owner-family fields × 164 files.
- **Two-eyes: holding.** All six closures are cross-family — `codex@xhigh` fixes, `claude` verifies,
  `gpt-5.6-sol` raises. Best is `00157`, which records a real falsification test: 6/7 tests fail on
  pre-fix `0b03fcd`, 7/7 pass on trunk.
- **`State history` corruption: unchanged, and provably so** — only 00038 and 00156 were modified this
  window, and neither is in the damaged set. `00045` (out of order), `00068` (unparseable — a closure
  spliced mid-sentence), and `00154` (closure appended to a `### Fix summary` markdown heading) are all
  still damaged. I now measure **4** separator forms and **33** wrapped records, against yesterday's 3
  and 30 — a predicate difference on an unmodified file set, not drift.
- **ID allocator: the `00155` collision is still live**, and the apparent "gaps" are not gaps.
  00137/00138/00144/00164/00167 are all **reqs**; bugs plus reqs form a contiguous 1–169 shared
  sequence. So the two ledgers draw from one number line from ~00137 onward, which is what makes
  `MM-BUG-KILN-00155` / `MM-REQ-KILN-00155` possible. The documented per-type `max+1` rule does not
  describe the observed behaviour.
- **Five Closed bugs still carry a populated `Held branch`** (00145, 00147, 00149, 00151, 00154) — and
  the named branches **no longer exist**. The fields now point at nothing. The content is on trunk, so
  nothing is lost, but the field is lying.

---

## 2. The reopen tax — asked for, not done, recurred 14 hours later

Yesterday's tracked issue #1 and plan item 1 asked for two things: route a non-executable verify to
`Verify retry after` instead of `Fixed → Open`, and fix the counter that zeroes `indeterminate`.

**Neither happened, and the pattern repeated.** `bugs/MM-BUG-KILN-00157.md:21`:

> → Open (2026-07-28, deltic:auto role=verify … verdict=doubt
> **reason=fix-is-statically-sound-and-5-6-gate-steps-are-green-but-python3-is-denied-in-th**
> model=claude)

The fix was sound. Five of six gate steps were green. The sixth could not run because the sandbox
denied `python3`. Deltic booked that as a reopen, incremented `doubt`, and left both park fields
untouched. The downstream cost followed immediately: `62f5e90 drain(…): record failed fix attempt`,
then `c56fc45 hold(…): fix owner branch`, then a fresh Codex fix run and a second Claude verification —
which explicitly notes it "ran the Python regression the earlier `verdict=doubt` verifier could not".
**One environment denial burned a full fix-and-verify cycle.**

The census across all 164 files is complete, not a sample:

| Field | Value | Files |
|---|---|---:|
| `Verify retry after` | `-` | **164 / 164** |
| `indeterminate` | `0` | **164 / 164** |
| `doubt` | `1` | 6 (00038, 00150–00153, 00157) |

**Caveat that matters for who can fix this.** The routing decision is made by deltic, whose source is
outside this repo. I can state as fact that the ledger output is unchanged and the pattern recurred; I
cannot see whether deltic-side code was touched. This is not an item an agent working in `midi-music`
can close.

---

## 3. The reap — safe for committed work, but one commit is four days from gone

Between the two reports, worktrees went **31 → 3** and local branches **44 → 3**. I verified today's
count myself (`git worktree list`). Worktree and branch deletions leave no commits, so the reap itself
is not reconstructible.

**A skeptic briefed to refute "nothing was destroyed" returned NARROWER TRUE CLAIM.** What survives is
genuinely reassuring:

- **All 52 unique `code=` shas across the bug ledger still exist and are all ancestors of `main`.**
  Missing-or-off-trunk count: **zero**. That is the strongest available evidence that no tracked fix
  was lost.
- Every surviving local branch is an ancestor of `main`.
- The two non-report remote refs really are superseded (the Richter album is complete on trunk;
  `MM-BUG-KILN-00030` is Closed on trunk via a different commit).
- It was **not** a `gc`, a fresh clone, or a relocation — `main`'s reflog still holds 465 entries back
  to 2026-07-13, and 10-day-old unreachable objects survive. Only a deliberate deletion sweep fits.

**What the skeptic killed:**

1. **`006fda6` is genuinely unintegrated and now unreferenced.** I confirmed both myself:
   `git cat-file -t 006fda6` returns `commit`, and `git branch -a --contains 006fda6` returns **empty**.
   Authored 2026-07-19, so git's default two-week prune makes it unrecoverable around **2026-08-02** —
   about four days out. `git gc --prune=now` would destroy it today.
2. **This violates an explicit written instruction.** `a6d043e` on trunk records Arthur verbatim:
   *"discard the lift, do not integrate 006fda6; the branch stays as the record of the original
   observation."* The branch and both pinning probe worktrees were deleted together.
3. **A second preserve-intent violation:**
   `wrk_journals/2026.07.11 - JRN - drum v3 realism build.md:227` says a baseline worktree "is left in
   place for the eventual refresh". It is gone.
4. **~10 dated task branches were deleted too** — a different class from the drain refs, unaccounted
   for in any record.
5. **The reap is entirely unlogged.** No commit, no journal, no scratchpad entry.
6. **Whether uncommitted work died is unknowable.** Cross-worktree dirtiness had gone unverified for
   eight consecutive reports; 28 worktrees were then removed without it ever being checked.

**One command closes the only irreversible item:** `git branch recover-006fda6 006fda6`.

---

## 4. The 00168 fix reinstates this repo's signature defect

This is the sharpest finding in the report, and I verified every part of it myself.

`crates/ferrosintesis/src/provenance.rs:170`, added by `9df73cb` this window:

```rust
const REPO_SOURCE_DIRS: &[&[&str]] = &[&["samples", "b1-upright"]];
```

A hand-written list of one. `ls samples/` returns **four** committed first-party source roots:
`acoustic-guitar-eastman-e1d`, `b1-upright`, `banjo`, `fret-noise-eastman-e1d`. Three of them are live
bake inputs for **published** crates — the Eastman guitar, the banjo feeding
`ferrosintesis-samples-orchestral2`, and the fret-noise bank that CLAUDE.md records as GM120's
default-on source since 2026.07.24.

Three things make this worse than an ordinary partial fix:

- **The same function derives its other half.** `tools/ferrosintesis-samples` is enumerated by
  `read_dir` immediately above. Only the repo-root sources got a hardcoded list.
- **The diff deletes the module's own warning** against exactly this — text noting that an earlier
  draft "did carry a hand-written list… That guard would have inherited the exact defect it exists to
  catch."
- **It contradicts CLAUDE.md's most emphatic instruction**, written after three separate lists drifted
  the same way: *"when a bug reports 'X is missing from list L', enumerate all of L before fixing. The
  reported item is evidence the list is unmaintained, not a spec of the work."*

The new test asserts only the reported instance, not the class. This bug is still `Fixed`, not
`Closed`, so **the verify pass can catch it** — which is the argument for not sitting on this report.

**The counter-evidence, in fairness:** the other six pending fixes are sound. The skeptic checked git
blob identity for every aliased WAV and found all 16 Kawai and all 27 Steinway aliases resolve to
byte-identical blobs with identical measured root frequencies, so the dedup cannot change playback.
Those fixes add `ptr::eq` sharing tests, source-derived alias maps with negative controls, and
correctly extend the crate `include` lists.

---

## 5. What shipped — and still no music

`crates/` took 146 files / +2,466 / −689. But the window's biggest single change is not in the synth:
`tools/ferrosintesis-samples/prepare.py` took **+2,140** and its test file **+1,141** — together 34% of
all insertions.

**`drums.rs` and `dsp.rs` are byte-identical.** So are `lib.rs`, `error.rs` and `offline.rs`. The
public API delta is zero; the semver seal was never approached. `Cargo.lock` still has **zero
`source =` lines**, so the offline-build invariant holds. 28 workspace members, 25 sample crates,
both unchanged.

| Change | GM | What | Render-diff reported |
|---|---|---|---|
| `6064e95` | 0 | B1 sustain handoff replaced with the static piano wrapper | 141 MIDIs; 80/44 albums + 3/14 demos; zero contamination |
| `a3ca00a` | 0 | **Natural recorded tail** — non-looping, byte-companded into a custom `b1t ` RIFF chunk | 141 files; **all 83 GM0 files changed, all 58 non-GM0 byte-identical**; zero contamination |
| `a0d6723` + `bb2bc01` | **61** | Sampled 10-zone MuseScore brass-section onset + transient handover; then re-baked to fix truncated body and 30 Hz flutter | 135 MIDIs; 28 GM61 changed, 107 identical; zero contamination |
| `1b3a172` / `364ca8f` | 0 alt-banks | Steinway (−36 WAVs) and Kawai (−26 WAVs) deduped to logical aliases | No render change intended; bank-identity oracles added |

**Every mandated render-diff inventory was actually run and recorded.** This is the strongest
discipline in the window. The brass journal even explains its non-diffs — *"the stock classifier's 93
`NOT REACHED` rows are expected: it knows program numbers but not CC68 state"* — which is precisely what
CLAUDE.md asks for.

**The practical consequence: anyone who re-renders now hears a different acoustic grand on 83 of 141
catalogue files.** GM0 is the repo's most-used program and it moved three times in 21 hours.

**And no music, for the third consecutive window.** Verified with git magic-prefix pathspecs, not bare
globs: zero `.mid` changed, zero `album_manifest.json`, zero files under `albums/`, zero under the
repo-root `samples/` performance-master store. Across roughly 400 commits and three windows, the
repository whose stated purpose is generative albums has not touched a single album engine.

**Tests (source scan, not a run count): 979 → 1,006 `#[test]` under `crates/`; Python 148 → 190.**
The Python jump is the notable one — the sample bakery is now the most heavily tested thing changed.

**CHANGELOG: untouched.** It documents none of the GM61 sampled onset, the `b1t ` tail format, the GM0
handoff rework, the alias dedup, or the corrected `prewarm_samples()` memory contract (13.4 MB, not
"twice the payload") — the last of which is user-visible.

---

## 6. The gate story — a genuine first, then a reversion

Yesterday escalated to Arthur: *"whether `deltic integrate` honours `.deltic-integrate.toml` is
unverified."* The window gives a partial answer from the ledger rather than from timing.

I verified the census at both ends myself. At `7bd678b` there is **no `gate=deltic` token at all**; at
HEAD there are **5**. So the repo's own contract gate ran, and recorded itself, **for the first time in
the ledger's history** — on `MM-BUG-KILN-00156`–`00160`, landing 16:23–16:58. The other seven fixes
record only `gate=manual`.

`.deltic-integrate.toml` in full is 72 lines with **no AUTO block**. Its six gate steps are `fmt`, two
`clippy --workspace --locked -- -D warnings`, two `cargo test --locked`, and
`python3 -m unittest discover -s tools/ferrosintesis-samples`.

**`gate=auto` has still never appeared** — third consecutive confirmation, verified with positive
controls so the search is not vacuous.

**A correction to yesterday's reasoning, which I think was wrong.** The 07.28 report inferred a skipped
gate from a six-commit batch sharing one committer timestamp 37 s after authoring. But committer
timestamps are set by `git rebase` at *replay* time, and the gate runs afterwards on the final tree — so
a short span is not evidence of a skip. This window has two such batches (14 commits in 39 s; 5 commits
in 10 s) and they are ordinary rebase-and-land events.

What the batches *do* prove is different and still worth stating: **the 17 intermediate commits in
those batches were never individually gated.** Only a batch tip could have been. Any of them may be red
in isolation, which breaks `git bisect` and breaks the "always buildable trunk" invariant CLAUDE.md
asserts.

---

## 7. Requirements and design backlog

### reqs/ — 42, and the gate is still shut

**28 Satisfied · 10 Draft · 3 Implemented · 1 Retired · 0 Accepted · 0 Integrated.**

The only change in 83 commits is the **addition** of three Drafts. **Zero existing req files were
edited** — fourth consecutive report with no Gate-1 triage and no Gate-2 acceptance. Because
`deltic reqs --ready` requires `Accepted`, the drain remains *permitted to take nothing*. The
bottleneck has never been requirement supply.

The three new Drafts — `MM-REQ-KILN-00164`, `00167`, `00170` (Kawai / Steinway / B1 "sample assets must
be independently verifiable against pinned inputs") — are one template applied three times, all raised
by `gpt-5.6-sol@high` off autonomous review, all `Could`/`heavy`. `00170` is explicit that it "records
prevention debt, not evidence of current audio corruption".

Three identical-shaped drafts in one day is a signal in itself: the sample-provenance subsystem has a
**structural** hole that the seven point fixes do not close. §4 is the same hole showing up as a bad
fix.

The three `Implemented` reqs (00029, 00031, 00032) all carry real named oracles and have waited since
2026-07-25 — **fourth consecutive report**.

### wrk_docs/ — 162 top-level + 25 fragments, and design came back

**10 files added, and 5 are genuine new design** — a real reversal from yesterday's 0 of 6. All five
are the B1-piano and sampled-brass HLDs, all five shipped inside the same window, and all five were
**updated in flight** as the build progressed. That is the healthy pattern, and it is worth naming
because the report has criticised its absence for several days.

The other five are autonomous review artefacts, plus five matching coverage fragments.

### Designed-but-not-started is 3, not 6 — a correction

Re-verified against code at HEAD, and the standing list is wrong:

| Item | Verdict |
|---|---|
| Anchor fidelity — piano inharmonicity | **genuinely open** — `voices.rs:1433` `let inharm = 0.00045;`, no key term |
| Anchor fidelity — CC64 sympathetic send | **genuinely open** — `engine.rs:3865-3867`, program test only, no pedal term |
| MetalPlate V4 | **genuinely open** — `drums.rs:27-40` `enum Kit { V1, V2, V3, Brush, Synth }` |
| LA sample layer deletes the model onset | **engineered past** — `sampler.rs:3335-3346` now documents a sum-to-one crossfade contract guarded by `la_level_continuity` |
| Bass sub-octave | **deliberately killed** — the doc itself says SHELVED, its premise "is FALSE" |
| Choir Klatt cascade fork | **deliberately killed** — doc says SUPERSEDED / NOT BUILT, Arthur chose a preset-darkening fix |

GM47 timpani remains outstanding on the model side (`voices.rs:1870` still tops at 2.245·f0; `:1908`
still lowpasses the strike at 300 Hz), though a Stage-4 sample layer exists that may mask it.

### Doc-header drift is 3, not 4 — and one claim was never true

The choir stage-2 HLD does **not** carry two mutually exclusive status lines. Its banner and its status
field both say SUPERSEDED and agree, and the file is unchanged this window — so **this claim was already
wrong when first made, and has been re-reported four times.** Struck.

The other three (drum-v3 "PROPOSED", bowed-string "DRAFT", b1-upright "bake in progress") are real. But
**no written rule anywhere requires a post-ship HLD update** — I searched CLAUDE.md, the global
instructions, and the repo. Either write the convention down in one line or stop reporting it; right now
it costs a paragraph a day and asks for work nobody agreed to.

### scratchpad and lessons — both frozen

`scratchpad.md`: **16 open / 34 done**, untouched. Oldest open items are 16 days old, and one of them is
worth surfacing on its own: **the repo root has no LICENSE file**, for a crate the docs say is published.

`lessons_learnt.md`: **22 against its own cap of 20**, byte-identical across this window — extending the
untouched streak to **317 commits**. Neither gotcha yesterday's plan named was added.

---

## 8. Branches, worktrees, and repo hygiene

**3 worktrees, 3 local branches, 4 real remote refs.** `origin/main` == local `main` == HEAD ==
`f74273f`. Forensic residue (BASELINE-*, VERIFY-*, PROBE-*, bisect-*) is **zero**, down from 17 — that
growth trend is retired.

**Genuinely unintegrated content is now two items, not three:**

1. **The 2026.07.25 daily report** (`f79b6dc`, 1 commit, 745 lines, pure docs) — **4th consecutive ask**.
   `wrk_docs/` runs 07.09, 07.20–07.24, **[07.25 missing]**, 07.26–07.28. The reap deleted the local
   branch, so `origin` now holds the only copy.
2. **`006fda6`** — dangling, four-day window (§3).

`42f53af` landed as `cd0ebbd` and `MM-BUG-KILN-00156` is Closed. The B1 branch yesterday warned against
auto-integrating was **rebased and landed properly** — the correct outcome.

### The publish contradiction got worse — 7th report

It is no longer 2-vs-3. Two more files were found asserting *published*:

| Says **published** | Says **not published** |
|---|---|
| `CLAUDE.md:198` | `crates/ferrosintesis/CHANGELOG.md:8` — "only a name-reservation stub `0.0.0`" |
| `.deltic-integrate.toml:6` | `RELEASING.md:10` — "nothing real has been published" |
| `bugs/MM-BUG-KILN-00025.md:28` | `bugs/MM-BUG-KILN-00101.md:73` |
| `bugs/MM-BUG-KILN-00068.md:26` | |

**4 vs 3.** Ground truth is *not published*. `CLAUDE.md:198` was authored 2026-07-13; **15 subsequent
CLAUDE.md commits have passed over it**, two of them editing the immediately adjacent lines. The
load-bearing one is `.deltic-integrate.toml:6`, because it is machine-read and is the stated rationale
for `version_bump = "release-only"`.

Related and new: `RELEASING.md:3` says "25 crates: `ferrosintesis` plus 24 sample-asset crates" while
disk and `CLAUDE.md:203` both say 25 sample crates. The release runbook's count is off by one — the exact
hand-maintained-list defect CLAUDE.md warns about, sitting in the release procedure.

### CLAUDE.md defects — none fixed, and one prior report was wrong

CLAUDE.md took **zero commits** this window.

- **The album-dir list is wrong once, not twice.** `ls albums/` returns `fable5`, `gpt5-3-spark`,
  **`gpt5-5`**, `gpt5-6`, `opus4-8`. **`gpt5-5/` does exist** — the 07.28 report's claim that it does not
  is refuted, and I confirmed this myself. The only defect is the omission of `gpt5-6/`. This matters
  operationally: **yesterday's plan item 6, executed literally, would have deleted a correct entry** and
  left the file still wrong.
- **New, previously unreported:** `CLAUDE.md:52-53` names the main clone as `D:\language\ferrosintesis` /
  `~/language/ferrosintesis`. The actual registered trunk-holder is **`D:/language/midi-music`** — almost
  certainly a leftover from a repo rename. This is the "never work in the main clone" warning pointing at
  the wrong path, so an agent trusting it could work in the real trunk-holder believing it safe.
- `CLAUDE.md:197` still contradicts `version_bump = "release-only"`.
- `crates/amp-lab/README.md` still carries four commands that fail post-excision.

### Items that should be retired from this report

- **MSRV "declared but never proven"** — now stale. `wrk_journals/2026.07.26 - JRN - smooth B1 piano
  sustain handoff.md:746` records `cargo +1.87 check --workspace` passing, and two earlier journals do
  the same. Still prose rather than a gate step, but no longer unproven.
- **b1-upright cannot be re-baked (74 vs 52)** — fixed. `prepare.py:2297` pins `_B1_LAYER_COUNTS`
  (25+27=52) and `validate_b1_generated_inventory` raises on any mismatch before writing.
- **wrk_journals only carries review artefacts** — improved. 8 of 13 added journals are substantive
  engineering logs, including a full one for the window's largest change.
- **The driven-guitar trim as an open decision** — see §9.

### Smaller items

- **`crates/ferrosintesis-samples-drumkit2` has no review-coverage record anywhere** — not in the
  20-row ledger, not in any of the 25 fragments. It is also the one crate with an inter-crate
  dependency, so it is the last link in the publish chain.
- `review-coverage.md` is unchanged: **20 rows against 28 members, all dated 2026-07-18, no oracle**,
  while the fragment directory grew 20 → 25. Producer with no consumer, 4th report.
- **No cloud CI at all** (`.github/` does not exist) — correctly matching fleet policy.
- `raw_loudness.csv` (105 lines) is tracked at the repo root — render-derived measurement output with no
  `wrk_docs/` home. Low risk, but it is the class of file the repo purged `.opus` for.
- **Latent release hazard:** `FILE_COUNT` changed 48→32 and 54→27 in crates pinned `=0.1.0` with versions
  unchanged. Consistent with `release-only`, but any published 0.1.0 now differs in content from the repo.

---

## 9. Prior-plan audit — 1 of 14, and the agent lane scored zero

Every window commit post-dates the plan, so causation was possible for all 14 items.

| Lane | Items | Executed | Rate |
|---|---:|---:|---:|
| Agent | 6 | **0** (1 partial) | **0%** (17% charitable) |
| Arthur | 8 | **1** | 13% |
| **Total** | **14** | **1–2** | **7–14%** |

**Agent lane:** item 1 (reopen routing) NOT DONE and recurred; item 2 PARTIAL (00156 landed, but via the
drain's recovery pass, not the named rebase — and all five stale `Held branch` fields remain); items 3–6
(integrate `f79b6dc`, write the review-coverage oracle, prune lessons, fix four doc lines) all NOT DONE,
with `CLAUDE.md`, `lessons_learnt.md`, `scratchpad.md` and `review-coverage.md` all byte-identical
across the window. Item 3's own fallback — "if it fails a fourth time, file a bug against the
integration lane" — was also not done.

**Arthur lane:** only **(a)** executed, and it executed magnificently (§1). (b) publish gate question,
(c) auto-land authority, (d) crates.io status, (e) reqs triage, (f) guitar trim, (g) `git -C` allowance,
(h) HLD header convention — all unanswered in-repo. Some may have been answered in conversation; that is
invisible from here.

| Report | Items | Executed | Rate |
|---|---:|---:|---:|
| 07.21 | 8 | 2 | 25% |
| 07.22 | 8 | 0 | 0% |
| 07.23 | 7 | 1 | 14% |
| 07.24 | 7 | 2 | 29% |
| 07.25 | 9 | 2 | 22% |
| 07.26 | 12 | 6 | 50% |
| 07.27 | 13 | 1–2 | 8–15% |
| **07.28** | **14** | **1–2** | **7–14%** |
| **Cumulative** | **78** | **15–17** | **19–22%** |

### The prediction failed, and the reason is structural

Yesterday predicted that naming an explicit first action would make asks land. All six agent items named
one. **All six failed.** The seven-window mean is ~22%; today is 0–17%.

The window was not idle — 83 commits, 11 bugs raised and fixed, a sampled brass bank, a B1 tail format,
27 new tests, 13 journals. **The machine worked hard on things the plan did not name.**

Here is what I think the evidence actually shows, and it is not about phrasing:

| Channel | Items in | Items executed |
|---|---:|---:|
| **Bug ledger** | 11 raised | **11 fixed** |
| Reqs ledger | 10 Draft | 0 |
| Daily-report agent lane | 6 | 0 |

**The bug ledger is the only channel with throughput.** Autonomous review raises bugs, the drain
enumerates them, fixes land the same night. Reqs stall at a human gate that has not opened in four
reports. Report prose is enumerated by nothing at all.

So the report has been writing its asks into the two channels that do not execute. The falsifiable test
for tomorrow is in §11: the agent-lane asks are stated as **bug-ledger candidates**, not as prose. If
items filed as bugs land and items left as prose do not, that is a point for this reading.

### Errors in the 07.28 report

**13 of 14 headline figures reproduce exactly** from `7bd678b`'s tree — 153 bug files, 151/1/1/0, 39
reqs, 979 `#[test]`, 16 scratchpad, 22 lessons, 151 wrk_docs, 20 fragments, 234 commits, the `00155`
duplicate, zero `source =` lines, `drums.rs`/`dsp.rs` byte-identical, and the 37-second batch timing.
The series' hand-counting defect did not recur.

Three genuine errors:

1. **`albums/gpt5-5/` does exist** (§8). The claim it does not was itself the correction of earlier
   reports that had it right — and it made plan item 6 destructive as written.
2. **The batch-timing inference was unsound** (§6): committer timestamps are set at rebase replay, so
   they cannot evidence a skipped gate.
3. **The driven-guitar trim was reported as an open decision. It was decided on 2026-07-22.**
   `a6d043e` on trunk records: *"Decision (Arthur): discard the lift, do not integrate 006fda6."* The
   branch's diagnosis was measured and found wrong in kind; the real cause was fixed under KILN-00042.
   **The report series has re-raised a settled question for six days**, and asked Arthur to decide it
   again yesterday. Struck permanently — the only live residue is the dangling object (§3).

---

## 10. Tracked issues

| # | Issue | Age | Owner |
|---|---|---|---|
| 1 | **`006fda6` is unreferenced and prunes ~2026-08-02**, against `a6d043e`'s explicit "the branch stays as the record". One command fixes it. The only irreversible item in this report | **new** | agent |
| 2 | **`MM-BUG-KILN-00168`'s fix hardcodes 1 of 4 source dirs and deletes the warning against doing so** — the exact anti-pattern CLAUDE.md names as recurring. Still `Fixed`, so the verify pass can still catch it | **new** | agent |
| 3 | **Blocked verifies are still booked as reopens.** Recurred on 00157 14 h after the ask. `Verify retry after` 164/164 unused, `indeterminate` 164/164 zero. Fix lives in **deltic**, not this repo | **2nd** | **Arthur** (harness) |
| 4 | **2026.07.25 daily report still not integrated** (`f79b6dc`, pure docs). The reap deleted the local branch; `origin` holds the only copy | **4th** | deltic |
| 5 | **Reqs Gate 1 has not opened in four reports** — 0 Accepted, so the drain may take nothing. Draft 7 → 10; 3 `Implemented` reqs with real oracles waiting since 07-25 | **4th** | **Arthur** |
| 6 | **Publish contradiction is now 4 files vs 3** and `.deltic-integrate.toml:6` is machine-read. 15 CLAUDE.md edits have passed over the false line | **7th** | **Arthur** |
| 7 | **`review-coverage.md`: 20 of 28 members, 11 days stale, no oracle**; fragments grew to 25 with nothing folding them in. `drumkit2` is covered nowhere | **4th** | agent |
| 8 | **`lessons_learnt.md` 22/20, byte-identical for 317 commits** | **7th** | agent |
| 9 | **Cross-worktree dirtiness unverifiable** — `git -C` refused again. 28 worktrees were reaped without it ever being checked | **9th** | **Arthur / harness** |
| 10 | **`State history` is corrupting** — 00045 out of order, 00068 unparseable, 00154's closure on a markdown heading; 4 separator forms, 33 wrapped records. Five Closed bugs point at deleted branches | **3rd** | agent |
| 11 | **Third consecutive zero-music window.** GM0 moved on 83 of 141 catalogue files with no album-scale listen | **watch** | **Arthur** |

**Explicitly retired — stop re-raising:** the driven-guitar trim as an open decision (settled 07-22);
b1-upright re-bake (guarded); MSRV "never proven" (evidenced); wrk_journals as review-only (8 of 13
substantive); worktree/branch sprawl (31→3, residue zero); held-branch stranding (branches gone, all 52
`code=` shas on trunk); the choir doc's "contradictory headers" (never true); "designed-but-not-started
is 6" (it is 3); `gpt5-5/` does not exist (it does).

---

## 11. Plan for today

§9's finding is that the report's agent lane has no execution channel, while the bug ledger has 11-for-11
throughput. So the agent items below are written as **bug-ledger candidates with a stated defect and
acceptance test**, not as prose asks. That is the falsifiable change this report is making.

### Agent lane

1. **Recover `006fda6` before it prunes.** *First action:* `git branch recover-006fda6 006fda6` in the
   main clone. This is the only irreversible item in the report and it has ~4 days. Do it before
   anything else, then note in `bugs/MM-BUG-KILN-00042.md` that the record is re-pinned.
2. **Raise a bug against the 00168 fix and widen the oracle to all four source roots.** *Defect:*
   `crates/ferrosintesis/src/provenance.rs:170` hardcodes `&[&["samples", "b1-upright"]]` while
   `samples/` holds four roots, three feeding published crates. *Acceptance:* enumerate `samples/*` by
   `read_dir` as the sibling `tools/` branch already does, restore the deleted warning, and add a
   negative control that fails if a new root is added and left unpinned. Do this **before** 00168 is
   verified — a Closed bug is much harder to reopen than a Fixed one.
3. **Write the `review-coverage.md` oracle.** *First action:* copy `crates/ferrosintesis/src/licensing.rs`'s
   pattern — read the root `Cargo.toml` `members` array **as text**, require one row per member, and let
   it fail red today on the 8 uncovered. Do not hand-add 8 rows. Note `drumkit2` is absent from the
   fragments too, so the fragment fold-in is a second, separate step.
4. **Integrate `f79b6dc`.** *First action:* it is 1 commit of pure docs on `origin` only. If it fails a
   fourth time, **stop asking and raise it as a bug against the integration lane** — yesterday's fallback,
   which was also not done. Four identical asks with no diagnosis is a stuck loop.
5. **Fix the CLAUDE.md defects, correctly this time.** *Exact edits:* `:33-34` and `:310` — **add**
   `gpt5-6/` (do not remove `gpt5-5/`, which exists); `:52-53` — change the main-clone path to
   `D:\language\midi-music` / `~/language/midi-music`; `:197` — reconcile with
   `version_bump = "release-only"`; `crates/amp-lab/README.md` — four post-excision commands. Also
   `RELEASING.md:3` — 24 → 25 sample crates.
6. **Prune `lessons_learnt.md` to 20** (`/prune-lessons-learnt`) and add the two this window earned:
   *a derived oracle that hardcodes half its enumeration is a hand-maintained list wearing a source-scan's
   clothing* (`provenance.rs:REPO_SOURCE_DIRS`), and *committer timestamps are set at rebase replay, so a
   short batch span is not evidence of a skipped gate*.

### Arthur lane — only you can do these

- **(a) The reopen tax lives in deltic, not here.** Route a non-executable verify to `Verify retry after`
  rather than `Fixed → Open`, or give the verify runner `python3`. It recurred 14 h after being asked,
  and no agent working in this repo can close it. Highest-value item you own.
- **(b) Open reqs Gate 1.** Nothing is `Accepted`, so the drain is permitted to take nothing — 4th
  report. And Gate-2 the three `Implemented` reqs (00029, 00031, 00032), which carry real oracles and
  have waited since 07-25. The Draft queue is now growing on machine-minted prevention debt; the gate is
  what culls it.
- **(c) Is ferrosintesis published to crates.io?** 7th ask, now 4 files vs 3, one machine-read. One
  sentence retires it permanently. Related: the repo root has **no LICENSE file**, 16 days open.
- **(d) May autonomous fixes auto-land?** `gate=deltic` appearing for the first time is real progress —
  the contract gate ran and recorded itself. But `gate=auto` is still zero and the last seven fixes were
  hand-landed. Either authorise a land path with the gate as its condition, or state that manual landing
  is the intended end state.
- **(e) A `git -C` allowance for the runner.** 9th ask. It is now more than a reporting gap: 28
  worktrees were deleted without their dirtiness ever being checkable.
- **(f) Was the mass reap intended, and by what?** It is unlogged, it took ~10 dated task branches and
  the harness branch as well as the drain refs, and it deleted two things whose records said to keep them.
  If it was a tool, it needs a preserve-list. If it was manual, it needs a journal entry.
- **(g) Should HLD headers track shipped state?** 5th report, and there is **no written rule** either way.
  One line in CLAUDE.md settles it permanently in whichever direction you prefer.

**Not today:** anchor-fidelity push, MetalPlate V4, GM47 timpani — genuine design work, not maintenance.

**The question worth more than any item above:** the synth has taken roughly 400 commits across three
windows with zero notes of music written, and GM0 — the most-used program in the catalogue — now renders
differently on 83 of 141 files. **Is it time to render the catalogue and listen, or to compose?** The
tooling is in better shape than it has ever been; the albums are the reason it exists.

---

## Verification notes

**Verified by me directly, reading the source once and quoting it:** the 00038 closure text and its route
reversal; the 00157 reopen reason string; the `Verify retry after` / `indeterminate` / `doubt` census
across all 164 files; the `gate=` census at both window ends (including the absence of `gate=deltic` at
baseline); `git worktree list` = 3; `006fda6` exists and `git branch -a --contains` is empty; its
2026-07-19 date; `ls albums/` showing `gpt5-5` present; `provenance.rs:170` and `ls samples/`;
`CLAUDE.md:33-34` and `:52-53`; the 83-commit count and window boundaries; `7bd678b` = `855df28` + 1;
`manual land` present 28 times since 07-26; the model-token control on 00158 vs the pending seven.

**ADVERSARIALLY CHECKED — NARROWER TRUE CLAIM, twice, both corrections carried.**

1. **"The 7-deep `Fixed` pool is benign."** The queue-state half survives — nothing is wedged, the pool
   is ~3 h old, and `gate=manual` is not a bypass. But the skeptic killed three things: 00168's fix is
   superficial (§4); none of the seven records validation evidence; and my own claim that the two-eyes
   rule was at risk is **refuted** — the fixer is identifiable from the `Co-Authored-By` trailer. It also
   proved the alias dedups sound by checking git blob identity on all 43 aliased WAVs.
2. **"The reap destroyed nothing."** Survives for committed, ledger-tracked work — all 52 `code=` shas
   exist and are on trunk. Killed on four counts: `006fda6` is genuinely unintegrated and now
   unreferenced; two preserve-intent records were violated; ~10 dated task branches were deleted, a class
   the claim ignored; and uncommitted work is unknowable because dirtiness was never checked.

**Corrections to prior reports, each verified from source:** `albums/gpt5-5/` exists; the driven-guitar
trim was decided on 2026-07-22 and has been re-raised for six days; the batch-timing gate inference is
unsound; the choir doc never had contradictory headers; designed-but-not-started is 3, not 6; doc-header
drift is 3, not 4, and no rule requires the update; MSRV is now evidenced; b1-upright re-bake is guarded;
wrk_journals is no longer review-only.

**Not verified / explicitly unknown:**

- **Nothing was built, tested, rendered or listened to.** Test counts are `#[test]` **source scans**, not
  run counts. Every render-diff figure is quoted from the commit that recorded it, not re-run.
- **Whether deltic's verify-routing or integration code changed** — its source is outside this repo. I
  observed only ledger output.
- **Whether `gate=deltic` means the contract gate ran on the landed tree or only inside the fix session** —
  the ledger records the token, not the tree it ran against.
- **Cross-worktree dirtiness** — `git -C` refused again, 9th report. The 2 other worktrees' working-tree
  state is unknown.
- **The full set of unreachable objects.** `git fsck` was denied, so the reap skeptic could only test shas
  named in the ledger, journals and reports. An unreachable commit nobody wrote down would be invisible.
  `git fsck --unreachable` from an allowed session would close this.
- **The names and contents of the ~10 deleted dated task branches**, and any uncommitted work in the 28
  removed worktrees — unrecoverable and unverified.
- **Whether Arthur-lane questions were answered out of band** — only in-repo evidence is visible here.
- **Whether the 00168 fix's three uncovered source roots are currently mis-pinned** — I proved the oracle
  cannot see them; I did not check whether their pins are in fact wrong.
