# 2026.07.31 — DAILY — CRUCIBLE-Windows — Repo state report

**Pass mode.** Read-only observation. This pass reads the repo and writes one report. It does not
clean, test, fix, review, build, render, or action its own findings. Actioning is the separate "B"
prompt's job.

**Integration is owned by the scheduler.** This is a scheduled ops run: deltic commits and integrates
the deliverables. This pass ran no `git fetch`, staged nothing, and pushed nothing.

**Environment.** Windows host **`CRUCIBLE`** — **the first report in this series from a box other than
KILN.** All 11 prior dailies are `KILN-Windows`. Worktree
`D:\worktrees\ferrosintesis\20260731-OPS-CLA@CRUCIBLE-daily-report-021412`, HEAD `a395a66`
(= `origin/main`; `git status --porcelain` empty). The main clone here is
`D:/language/ferrosintesis` — **not** `D:/language/midi-music`, and §7 explains why that difference
matters more than it looks.

**Baseline and window.** The last integrated daily report is `73ec2f2` (2026-07-30 02:37:59), the
commit that landed the 07.30 report. The window is **`73ec2f2..HEAD` = 10 commits** over **19 h 07 m**
(2026-07-30 07:06:10 → 2026-07-31 02:12:54). Churn: **22 files, +1,231 / −34**. I verified no commit
falls in the gap between the two reports' windows: `ab5f811..73ec2f2` contains exactly `73ec2f2`
itself, so the 58-commit and 10-commit figures abut cleanly with nothing unaccounted for.

**Method.** Five parallel readers over independent state sources (bug ledger, commit log, design
backlog, refs/hygiene, prior-plan audit), and I verified every load-bearing claim myself rather than
taking a reader's word. The headline defect (§1) I traced end-to-end through six source sites of my
own reading.

**The readers disagreed on one load-bearing point and I settled it against the majority-sounding
answer.** Two readers and I converged on the host-path finding (§7); a third argued from the `main`
reflog that there is only one clone, renamed — which would mean `006fda6` is gone for good. Branch and
tag counts refute it (§7), and the correct status is that the artifact is intact on the other box. Two
reader inferences are corrected in this report rather than passed through; both would have changed a
recommendation.

---

## Headline

**The repo declared its first real crates.io release on the same day a reachable panic sat unfixed in
the public render path — and that panic is a recurrence of a bug closed twelve days ago, whose fix was
scoped to its own bullet list.**

`88e3014` rewrote `CHANGELOG.md`, `CLAUDE.md` and `RELEASING.md` to assert that **`0.21.56` is "the
first real crates.io release"**, dated 2026-07-30. Hours later, an autonomous review found
`MM-BUG-CRUCIBLE-00003`: a seven-byte MIDI track panics `ferrosintesis::offline::render`. I confirmed
it from source, every link:

| # | Site | What I read |
|---|---|---|
| 1 | `crates/ferrosintesis/src/midi.rs:333` | `let prog = c.u8()?;` — **no seven-bit mask** |
| 2 | `crates/ferrosintesis/src/midi.rs:310`, `:315`, `:348` | note keys **are** masked: `let key = c.u8()? & 0x7F;` |
| 3 | `crates/ferrosintesis/src/engine.rs:3117` | `s.program = prog;` — stored unmasked |
| 4 | `crates/ferrosintesis/src/engine.rs:3726` | `for (ci, strip) in self.strips.iter_mut().enumerate()` — every strip, every block |
| 5 | `crates/ferrosintesis/src/engine.rs:3818-3819` | `if ci != 9 { program_trim_lin(strip.program) }` — no early `continue` guards it |
| 6 | `crates/ferrosintesis/src/engine.rs:1200`, `:1222-1223` | `PROGRAM_TRIM_DB: [f32; 128]`, indexed `[program as usize]` |

Index 255 into a 128-element array. **No NoteOn is required** — I checked lines 3726–3818 for a
`continue`, `is_empty()` or `voices.len()` guard and there is none, so the six-second default tail
reaches the trim on a channel that never sounds. A second sink takes the same byte:
`crates/ferrosintesis/src/voices.rs:14261` indexes `VEL_LEVEL_EXP[program as usize]`, also `[f32; 128]`
(`:14116`). No test covers any of it — `grep` for the repro bytes and for `prog: 255` across
`crates/ferrosintesis/src/` returns nothing.

**The recurrence is the real finding.** `MM-BUG-KILN-00025` was *"Renderer panics (index out of bounds)
on a MIDI note key ≥ 128"*, Closed 2026-07-19. Its own fix record
(`bugs/MM-BUG-KILN-00025.md:21`) reads:

> SMF NoteOn, NoteOff, and PolyAftertouch **key bytes** are normalized to seven bits at the parser
> trust boundary. … `note_key_data_bytes_are_limited_to_seven_bits` covers **all three key-bearing
> event kinds**.

The bug listed three sites as bullets; the fix masked exactly those three; the guard was **named after
the instance**. Twelve days later the identical defect surfaced in the adjacent match arm. My census of
every channel-voice data byte in `midi.rs`:

| Status byte | Data byte | Masked? | Line |
|---|---|---|---|
| `0x80` NoteOff | key | **yes** | `:310` |
| `0x90` NoteOn | key | **yes** | `:315` |
| `0xA0` PolyAT | key | **yes** | `:348` |
| `0x90` NoteOn | velocity | **no** | `:316` |
| `0xB0` CC | number | **no** | `:328` |
| `0xB0` CC | value | **no** | `:329` |
| **`0xC0` Program** | **program** | **no** | **`:333`** |
| `0xD0` Aftertouch | value | **no** | `:337` |

Three masked, five not. **This is CLAUDE.md's own doctrine failing on the repo that wrote it** —
*"when a bug reports 'X is missing from list L', enumerate all of L before fixing. The reported item is
evidence the list is unmaintained, not a spec of the work."* KILN-00025 treated its bullet list as the
spec.

**One correction, and it sizes the fix.** A reader inferred velocity was a second panic vector. It is
not: `VEL_LEVEL_EXP` is indexed by **program**, not velocity (`voices.rs:14261`) — a second sink for the
same byte. I grepped `crates/ferrosintesis/src/*.rs` for `vel as usize`, `num as usize` and
`val as usize` and found **none**. So today exactly one unmasked byte panics; the other four are a
latent inconsistency at the trust boundary, not live crashes. Fix the program byte now; normalise the
rest as policy, which is what the bug itself proposes.

**Four things frame it.**

1. **The queue inverted.** Yesterday: 0 Open, 3 Fixed awaiting closure. Today: **0 Fixed, 6 Open**, all
   raised today, none claimed. The drain finished cleanly and the refill is entirely machine-generated
   (§1).
2. **Whether `0.21.56` is actually published is UNVERIFIED and the repo's own evidence points at
   "no."** The commit subject says *prepare*; the CHANGELOG says *is*; there is **no journal dated
   2026-07-30 at all** (261 journals, zero for that date) though the repo's doctrine routes "what
   happened" to `wrk_journals/`. §2.
3. **Yesterday's plan scored 0–1 of 15 — but the raw rate over-states it.** Commit volume fell 6.3×,
   and per commit the window held or improved. What survives the confound is sharper: `lessons_learnt.md`
   and `CLAUDE.md` were *both* edited in-window by other actors, and neither edit was the ask. §9 also
   restates the 07.30 "enumeration" thesis, which the reqs ledger refutes as written.
4. **Twelfth day with no new music**, and this pass makes it the fifth consecutive zero-music report.

---

## 1. Bug ledger — the queue inverted

**181 bug files** plus `README.md`. (`bugs/README.md:57` carries an example `State: Open` line; excluding
it is what makes the arithmetic close. Anchor on `^- \*\*State:\*\*` — a bare `State:` grep also picks up
prose mentions.)

| State | Count |
|---|---:|
| Closed | **175** |
| **Open** | **6** |
| Fixed | **0** |
| Blocked | **0** |
| **Total** | **181** |

**The arithmetic reconciles three ways.** 174 at baseline + 7 raised = 181. 171 Closed + 4 closed = 175.
3 Fixed − 3 closed = 0. `git diff --name-status 73ec2f2..HEAD -- bugs/` returns exactly ten lines: six
`A` for `MM-BUG-CRUCIBLE-00001..00006`, one `A` for `MM-BUG-KILN-00180`, three `M` for
`00177`/`00178`/`00179`.

**Correction to my own brief:** `MM-BUG-KILN-00180` was not merely closed in-window — it was **created**
in-window, by the very closure commit `118e696` that closed 00178, as that closure's oracle-design
residual. Full lifecycle `118e696` (07:06:10) → `19585c5` (13:03:55) = **5 h 58 m**.

### The six open CRUCIBLE bugs — today's entire actionable queue

| ID | Pri / Sev | Area | Defect | Raiser |
|---|---|---|---|---|
| **00003** | Should / Med | SMF parser + renderer | **Malformed Program Change panics rendering** — confirmed, see Headline | `gpt-5.6-sol@xhigh` |
| 00004 | Should / Med | SMF tempo map | Equal-tick tempo changes lose authored order | `gpt-5.6-sol@xhigh` |
| 00005 | Should / Med | SMF parser | Event reads cross declared `MTrk` boundaries | `gpt-5.6-sol@xhigh` |
| 00006 | Should / Med | realtime renderer | Voice buckets allocate above 64 same-channel voices | `gpt-5.6-sol@xhigh` |
| 00001 | Should / Med | packaging / licensing | Published YDP notices omit performer Dr. Mikhail Krishtal | *(no model recorded)* |
| 00002 | Could / Low | YDP provenance | Provenance calls tritone-spaced roots "minor thirds" | *(no model recorded)* |

All six: every owner field `-`, `Held branch: -`, `Attempts: fix=0, doubt=0, indeterminate=0`. **Nothing
is claimed.** They arrived in two batches 62 minutes apart — `1b627a9` (00001–00002, from the
`ydp-grand` review) and `a395a66` (00003–00006 **plus** `reqs/MM-REQ-CRUCIBLE-00007.md`, from the
`crates/ferrosintesis/` review).

**Two verified independently by me beyond 00003.** `00004` holds exactly as written: `midi.rs:279`
pushes `tempos.push((tick, us))` — a bare 2-tuple with **no sequence field** — while the same loop
maintains a `seq` counter for events at `:300`, `:334`, `:354`, and `:362` is `tempos.sort_unstable();`.
Equal-tick tempos therefore order by *tempo value*. The `seq` discipline that exists for events was
simply not extended to tempos. `00005`'s rewind (`midi.rs:358`, `c.pos = end;`) is present as described.

**None duplicates another** — the three parser bugs are disjoint (data-range, ordering, bounds), and
each of 00003/00005/00006 explicitly dedups against a named Closed bug. **00006 is the weakest of the
six**: it is the only one whose "Actual" is a *predicted* runtime behaviour that no measurement
demonstrates, and its own Fix section concedes the existing `live_polyphony_is_capped` test "measures
only the final count."

**00003 is under-prioritised at Should/Medium.** It is an unauthenticated-input process abort in the
default offline render path of a crate whose CHANGELOG now claims a published release, reachable from
seven bytes with no note. Its already-Closed sibling KILN-00025 carried the same shape. That is the one
item in the queue with a mechanical, code-confirmed reproduction.

**A new hygiene defect: two incompatible raiser conventions landed the same day.** `00001` and `00002`
record `raised via deltic bugs new` with **no `model=` token** and leave `## Fix` as
`<unfixed — raised only>` (putting remediation under an Observation sub-heading instead); `00003`–`00006`
record `model=gpt-5.6-sol@xhigh` and write a populated `## Fix` while still Open. Two of six open bugs
have no raiser attribution at all.

### The `<unfixed — raised only>` regression from 07.30 — **fully closed**

Yesterday's finding was 3-of-3 bugs marked `Fixed` with an empty `## Fix`. All three now carry
substantive, commit-attributed bodies **with fails-before evidence**:

- `00177` (`:76-85`, `8853255`) — the `key >= 68` hand-written boundary is gone; regression extends 4 → 12
  key/velocity cases. Fails-before: reverting the gate fails at *"GM67 key 64 velocity 72: spectral-envelope
  correlation excess 0.631."*
- `00178` (`:80-89`, `190b8c6`) — two rough G#3 zones dropped from the runtime tables. Fails-before:
  restoring the two zone lines fails the test.
- `00179` (`:78-90`, `fcae2c2`) — positive control added; absolute bar replaced by the differential
  `exposed < 0.65 * control_exposed`. Its counterfactual table shows `HARMONICS` 8→1 **passes** the
  pre-fix oracle and **fails** the fixed one — proving the reported vacuity was live, not theoretical.

The marker survives in 13 files, but not on any `Fixed` bug: two are the correctly-Open CRUCIBLE bugs,
and eleven are Closed KILN files where the fix narrative lives in the `State history` block instead —
a stale template artifact. Four of the eleven were spot-checked; seven are UNVERIFIED.

### `MM-BUG-KILN-00180` — the window's one source change, and its best record

Raised by `claude-opus-5@high`: *"The MM-BUG-KILN-00178 roughness bar (0.996) is fitted to the removed
zone, not derived: six shipping baritone zones sit below it."* The old test passed only because it
checked one key that happened to land above a fitted constant; a 20-row census showed **five healthy
shipping zones already below the bar** while the real defect measured 0.77865.

The fix (`f1ad832`, OpenAI Codex GPT-5) censuses every zone in both runtime banks, derives the bar from
the shipping population as `10 × median(1 − correlation)`, and loads both packaged-but-unshipped G#3
takes as positive **and** negative controls. The closure carries four independent classes of evidence:
replication digit-for-digit before judgement; a bar-lands-in-a-real-gap table (worst healthy 0.00704 /
bar 0.03105 / defect 0.22135); discrimination proven in **both** directions; and a vacuity mutation
sweep where *"every path I could construct fails closed."* It also **declines** to raise an ID for an
8.3-semitone zone gap on the grounds that *"raising a bug with no observed symptom would be
speculative."* This is the highest-quality closure record in the window and the model to copy.

### Ledger hygiene

| Check | 07.30 | Today | Δ |
|---|---|---|---|
| Owner-field leases populated | 0 | **0** | clean |
| `Verify retry after` populated | 0 | **0** | clean |
| **`Held branch` populated on Closed bugs** | 5 | **5** | **not cleared** |
| Damaged `State history` files | 3 | **3** | **untouched** |
| `gate=manual` | 38 | **39** | +1 |
| `gate=auto` **ever** | 0 | **0** | **5th confirmation** |

**The five stale `Held branch` fields got worse, not merely older.** All five (`00145`, `00147`,
`00149`, `00151`, `00154`, each at `:18`) are self-labelled `host-local:KILN:` — and the ledger is now
being worked from CRUCIBLE. A KILN-local branch name is not resolvable from this host, so any tooling
that honours a held branch here fails or silently no-ops. Four days stale and now **cross-host** stale.

**The three damaged `State history` files persist verbatim**, none touched this window:
`00045:21` jumps `Open → Closed` with **no `Fixed` step**, so no fix commit is recorded; `00068:21` has
an **unbalanced parenthesis** that swallows the arrow and the whole Closed clause; `00154:21`
**terminates at `Fixed`** while its `State:` field says `Closed` — and it is also one of the five stale
`Held branch` files, double-flagged.

**ID allocator: still exactly 3 collisions** (`00155`, `00170`, `00176` exist in both ledgers) — no
change. The KILN bug line runs 00001–00180 with exactly five gaps (`00137`, `00138`, `00144`, `00164`,
`00167`), which are precisely the five req-only IDs: **the allocator worked for those five and failed
for three.** The CRUCIBLE line is clean — bugs took 00001–00006, the req took 00007, both minted by the
same commit `a395a66` with zero overlap. That is a useful positive control, and it suggests the three
legacy collisions were concurrency races rather than a grammar flaw (INFERENCE — I did not examine
those three ID pairs' commit history).

**Two-eyes: 4 of 4, all cross-family.** `00177`/`00178`/`00179` fixed by GPT-5.6, verified by
`claude-opus-5@high` (`118e696`); `00180` fixed by OpenAI Codex GPT-5, verified by `claude-opus-5@high`
(`19585c5`). The reporter/fixer distinction is handled explicitly rather than assumed —
`bugs/MM-BUG-KILN-00180.md:116-117`: *"I raised this bug but did not fix it — the fix is GPT-5's, so the
two-eyes rule holds (it binds the fixer, not the reporter)."* Both closures name the exact trunk verified
against.

### Trend

| Metric | 07.26 | 07.27 | 07.28 | 07.29 | 07.30 | **today** |
|---|---:|---:|---:|---:|---:|---:|
| Total bugs | 121 | 146 | 153 | 164 | 174 | **181** |
| **Open** | 7 | 17 | 1 | 0 | 0 | **6** |
| Blocked | 14 | 0 | 1 | 0 | 0 | **0** |
| **Fixed** | 13 | 0 | 0 | 7 | 3 | **0** |
| Closed | 87 | 129 | 151 | 157 | 171 | **175** |
| Commits (window) | 191 | 194 | 234 | 83 | 58 | **10** |
| Worktrees | 45 | 33 | 31 | 3 | 2 | **2** |
| `scratchpad.md` open | 43 | 15 | 16 | 16 | 0 | **0** |
| `lessons_learnt.md` (cap 20) | 20 | 22 | 22 | 22 | 11 | **12** |
| reqs total | 32 | 38 | 39 | 42 | 43 | **44** |
| `#[test]` in `crates/` | 880 | 928 | 979 | 1006 | 1019 | **1019** |
| Python tests in `tools/` | n/t | n/t | n/t | 190 | 203 | **203** |
| `wrk_docs/` top-level | — | — | — | — | 164 | **167** |
| `gate=auto` ever | — | 0 | 0 | 0 | 0 | **0** |
| ferrosintesis version | .56 | .56 | .56 | .56 | .56 | **0.21.56** |

**Commit volume fell 234 → 83 → 58 → 10 across four windows.** The version is unchanged and that is
correct: `.deltic-integrate.toml:9` sets `version_bump = "release-only"`.

**Direction of travel: the ledger flipped from drain to refill, and the refill is machine-generated.**
Yesterday's queue emptied cleanly — a genuinely good outcome, and the fix/verify pipeline deserves the
credit. But the six that replaced it were raised by two autonomous passes inside 62 minutes and **no
human or agent has triaged them.** The generator has changed: yesterday's dominant source was the
verification step itself (4 of 10 raises were closure residuals); today's is the scheduled review
rotation (6 of 7).

---

## 2. The release assertion — and why I cannot confirm it

`88e3014` *"chore(release): prepare crates.io release metadata"* rewrote three documents:

| File | Now says |
|---|---|
| `crates/ferrosintesis/CHANGELOG.md:8-10` | *"The `0.21.56` release **is the first real crates.io release**. It supersedes the `0.0.0` name-reservation stub published on 2026-07-09"* |
| `crates/ferrosintesis/CHANGELOG.md:14` | `## [0.21.56] - 2026-07-30` |
| `CLAUDE.md:200-207` | *"the `0.0.0` crates.io package is a code-free name-reservation stub; real library releases start at `0.21.56`"* |
| `RELEASING.md:8-13` | *"Real library releases start at `0.21.56`; **always inspect crates.io for the current state**"* |

**Three pieces of evidence say this is preparation, not a record of a publication that happened.**

1. The commit subject is *"**prepare** crates.io release metadata."*
2. **There is no journal dated 2026-07-30.** The repo holds 261 journals; the series runs
   `2026.07.29 …` then jumps to `2026.07.31 …`. CLAUDE.md routes *what happened* to `wrk_journals/`, and
   a first-ever crates.io publication is the most journal-worthy event available. Nothing recorded it.
3. `RELEASING.md` **hedges** (*"always inspect crates.io"*) where `CHANGELOG.md` **asserts**. The authors
   did not treat it as settled either.

**Why this matters.** Yesterday's report closed a seven-report contradiction by getting every document
to agree the crate was *not* published. Within 24 hours the documents flipped to say it *is*. If the
publish did not run, the repo has re-opened the identical contradiction pointing the other way — and
this time the false direction is the dangerous one, because `README.md:151,154` tell a reader to
`cargo add ferrosintesis`. **UNVERIFIED: no network access, and none is authorised on this pass.** One
lookup settles it; until then, treat every "published" claim in the tree as unconfirmed.

A minor self-inflicted wound in the same commit: `CLAUDE.md:200-201` states the rule *"`crates/ferrosintesis/Cargo.toml`
holds the current number — **trust it, not versions quoted in docs**"*, and the commit then quotes
`0.21.56` in the docs three lines later, and again in `RELEASING.md:12`. It matches the manifest today,
so it is inert — but it is exactly the drift pattern that rule exists to forbid.

### The drumkit2 falsehood is still there, now inside a release-declaring file

Yesterday flagged `crates/ferrosintesis/CHANGELOG.md:68-69` for describing drumkit2 as carrying *"the
four accent-cymbal banks (crash, sizzle crash, splash, china)."* It is now at **`:79-80`**, shifted
twelve lines by `88e3014`'s own insertions and **textually unchanged**. Ground truth:
`crates/ferrosintesis-samples-drumkit2/samples/` holds 36 WAVs with exactly **three** prefixes —
`china`, `crash`, `splash`. There is no `sizzle` sample, and `src/lib.rs` contains no `sizzle`.

A dedicated release-metadata pass edited this file and walked past a factually wrong line twelve rows
below its own hunk. The sizzle bank was deleted on 2026-07-29 by `bbf122d`.

---

## 3. What shipped — one source file, and it cannot move a sample

The window's 22 files contain exactly **one** `.rs` file: `crates/ferrosintesis/src/sampler.rs`
(`f1ad832`, +84/−17). It is **100% test-only**, and the proof is mechanical: `mod tests` opens at
`sampler.rs:5771`, gated one line above at `:5770` by `#[cfg(all(test, feature = "embedded-samples"))]`;
both diff hunks sit at old-file lines 7896 and 7923, ~2,100 lines *inside* that module; and the diff
adds and removes **zero** `pub` items. The runtime bank functions `sax_bar_p()` (`:2387`) and
`sax_bar_f()` (`:2405`) are read by the new test but not modified.

**No render-diff inventory was recorded, and that is correct here.** CLAUDE.md:178-184 names
`sampler.rs` unconditionally, so this commit is nominally in scope — but the mandate's stated purpose is
catching audio contamination, and a `#[cfg(test)]`-only edit has an empty expected-diff set by
construction. Worth flagging for the record: **no artefact in the window states that justification.**
`bugs/MM-BUG-KILN-00180.md:27` gets close (*"Not a shipped audio defect"*) and `:154-158` records the
`--no-default-features` check (*"the test is compiled out, 0 run, 745 filtered"*), but nobody wrote
"no render-diff needed because the change is behind `cfg(test)`." A future reader auditing inventory
compliance will find a gap where there is none.

**Semver: no event.** No public API changed. `Options` (`engine.rs:1757`), `RealtimeOptions`
(`live.rs:36`) and the error enums (`error.rs:23`) sit in files absent from the change set.
`crates/ferrosintesis/Cargo.toml:3` reads `0.21.56`, unchanged, correct under `release-only`.

**Offline-build invariant holds.** `Cargo.lock` has **zero** `source =` lines. 28 workspace members, 25
sample crates — both matching CLAUDE.md exactly. `crates/amp-lab` remains the deliberate non-member.

**Tests flat: 1,019 Rust `#[test]`, 203 Python `def test_` — both unchanged.** That is exactly what
`f1ad832` predicts: it *replaced* one test with another rather than adding one. A count-only dashboard
would read this window as inert on testing, which would mislead — the *quality* of one oracle moved
substantially, from a fitted constant to a population-relative classifier with controls in both
directions.

**`lessons_learnt.md`: 11 → 12**, and the pair of commits that did it is good practice worth naming.
`bf885c2` added *"A one-case bar hides fitting; census every value it would flag"* at diagnosis;
`155126c` rewrote the same entry six hours later because the original cited
`baritone_sax_key58_avoids_a_rough_source_zone` — a test `f1ad832` had already deleted. The correction
repoints it at the fixed exemplar and adds the remediation shape. Trap plus remedy, not trap alone.

---

## 4. Still no music — twelfth day

`git diff --stat 73ec2f2..HEAD -- albums/ demos/ '*.mid' '*album_manifest.json'` returns **completely
empty**. Zero music files of any kind.

| Pathspec | Last commit touching it |
|---|---|
| `albums/` | `23f648b` **2026-07-27** — a POSIX-portability *docs* fix |
| any `*.mid` | `ff3928c` **2026-07-25** — a *bug fix* (KILN-00056 overlap clamp) |
| `demos/` | `2e4add1` **2026-07-25** — deleting a dead check |

**Last genuinely new music: 2026-07-19 — 12 days.** Even the most recent touches are non-musical. The
catalogue is unchanged at **124 album MIDIs + 17 demo MIDIs = 141**, across five model directories
(`fable5`, `gpt5-3-spark`, `gpt5-5`, `gpt5-6`, `opus4-8`).

This is the **fifth consecutive report** with no music movement. The synth has meanwhile changed the
default sound repeatedly — GM48 on 81 of 124 album tracks and the bowed family on 89 in the previous
window alone — with no album-scale listen recorded since.

---

## 5. Requirements and design backlog

### reqs/ — 44, and Gate 1 has now never opened for six reports

**28 Satisfied · 12 Draft · 3 Implemented · 1 Retired · 0 Accepted · 0 Integrated.**
(`reqs/README.md:105` carries an example `Satisfied` line; excluding it closes the arithmetic.)

`git diff --name-status 73ec2f2..HEAD -- reqs/` returns exactly one line: `A
reqs/MM-REQ-CRUCIBLE-00007.md`. **Zero `M`.** No req changed state in 10 commits — **sixth consecutive
report** with no Gate-1 triage and no Gate-2 acceptance. Because `deltic reqs --ready` requires
`Accepted`, the drain remains **permitted to take nothing.**

The three `Implemented` reqs (`00029`, `00031`, `00032`) have now waited **six days** since 2026-07-25.
`MM-REQ-KILN-00029.md:20` is explicit that nothing technical blocks it: *"the oracle is complete, so this
is awaiting a second pair of eyes rather than missing coverage."*

**The new req reproduces the anti-pattern the 07.30 report named, within 24 hours.**
`MM-REQ-CRUCIBLE-00007` (Draft/Could/heavy) asks that sampled-drum routing, level, repitch, jitter and
hybrid modeled-tom parameters derive from **one typed descriptor set**. It names five restatements of
the same facts — `drums.rs:1383-1395` vs `:1874-1902` (where the code *itself* says keep the two
mappings synchronized), `sampler.rs:4831-4853` vs `:5099-5128`, and two tests — and flags a latent
`unwrap` at `sampler.rs:5128`.

It is the *same failure mode* as the seven-Draft seam (N places restating one fact set with no
derivation link) at a **different location**: entirely inside `crates/ferrosintesis/src/` runtime
tables, not the Python-bake ↔ asset-crate boundary. Classified by location the seam count is unchanged
at **7 of 12 Drafts (58%)**, down from 7 of 11 by denominator alone. Classified by failure mode it is
**8**. Either way, yesterday's recommendation — *"triage the seam, not the seven"* — is now
*"not the seven **or eight**"*: per-req intake minted another instance of the identical anti-pattern
one day after the report that named it.

### Designed-but-not-started: still 4, all byte-identical at HEAD

| Item | Verdict | Line at HEAD |
|---|---|---|
| Piano inharmonicity anchor | **open** | `voices.rs:1433` and `:1492` both `let inharm = 0.00045;`; `kf` is the *partial index*, not the key — no key-dependent term |
| CC64 sympathetic send | **open** | `engine.rs:3830` `let is_piano = ci != 9 && voices::is_acoustic_piano(strip.program);` — program-only, **no damper term**; send unconditional at `:3881-3883` |
| MetalPlate V4 | **open** | `drums.rs:27-40` — variants are exactly `V1, V2, V3, Brush, Synth`. No `V4`. HLD dated 2026-07-14 — **17 days** |
| GM47 timpani | **open** | `voices.rs:1870` `(2.245, 0.20, 1.1)`; `:1908` `lowpass(300.0, 0.8, sr)` |

All four line numbers are **unchanged from the 07.30 report** — nothing was started, and nothing moved
under them.

### wrk_docs/ — genuine new design is zero again

**167 top-level + 28 fragments** (was 164 + 26; the +3 reconciles as 164 excluding the 07.30 report
itself, +1 for it, +2 this window). All four additions are process output — two CR docs and their two
coverage fragments. **No `HLD`, `PLN`, `VAL` or `ADR` file was added.** The last HLD by filename date is
2026-07-18: **13 days with no new design document.**

**`review-coverage.md`: 20 rows against 28 members, every row dated `2026-07-18` — 13 days stale, on the
day, across the entire ledger. Sixth consecutive report.** The file was not touched this window while
two more fragments landed on top of it, and there is still no oracle; `review-coverage.md:12-13` states
*"Reconcile additively: every current area has a row"* as prose enforced by nobody. The eight uncovered
members are unchanged: `drumkit2`, `b1-upright`, `ccby`, `rain`, `bass`, `bottle`, `mandolin`,
`fretnoise`.

Sharper than a sixth re-listing: **both new fragments target `crates/ferrosintesis/`, already the
most-reviewed row.** The window's review effort went entirely to the covered 71% and moved the uncovered
eight not at all. And `fretnoise` sits uncovered while `MM-REQ-KILN-00031` — an `Implemented` req about
that exact crate — waits six days for sign-off.

### scratchpad and lessons

**`scratchpad.md`: 0 open, 3 done, unchanged** and absent from the window diff. All three `[x]` items
carry closure evidence dated 2026-07-29. This is the one clean ledger in the repo.

**`lessons_learnt.md`: 12 of 20, 8 slots free.** And the lesson yesterday's plan explicitly asked for
— *committer timestamps are set at rebase replay, so a short batch span is not evidence of a skipped
gate* — **still exists nowhere.** A case-insensitive grep across the whole worktree for
`rebase replay|committer timestamp|committer date|batch span` hits **only four files, all
`wrk_docs/*DAILY*` reports**. Zero in `lessons_learnt.md`, `CLAUDE.md`, `wrk_journals/`, `reqs/`. This
correction has now been re-derived in **four successive reports** and durably written down in none of
them — while two lessons commits landed in this very window on a different topic. The file was open and
being edited. This was an omission, not a blockage.

---

## 6. Journals — a gap on the day that declared a release

**261 journals, and zero dated 2026-07-30.** The series runs `2026.07.29 - JRN - …` (seven files) then
jumps straight to `2026.07.31 - JRN - …` (two, both autonomous-review artefacts).

That day carried a bug raised-fixed-closed, three further closures, and the release-metadata
preparation. None of it left a journal. Yesterday's report quoted a self-diagnosis from the 07.29 batch
— *"None of the six fixes left a journal of its own"* — and the trend continued to its limit. The
verification prose inside the bug files is unusually good and carries the substance, so the knowledge is
not lost; but the layer CLAUDE.md designates for *what happened* recorded nothing on the most
consequential day of the window.

---

## 7. The host-path finding — yesterday's plan item 4 is wrong and must not be actioned

**This is the correction with the most potential to do damage, so I settled it twice, independently.**

The 07.30 report (line 902) instructs a future agent to edit `CLAUDE.md:55-56` to read
`D:\language\midi-music`. **On this box that edit would break the one paragraph whose entire purpose is
stopping an agent from dirtying the trunk-holder**, and would revert a deliberate, commit-messaged fix.

| # | Evidence | Verdict |
|---|---|---|
| 1 | `git worktree list` here → trunk-holder `D:/language/ferrosintesis` | FACT |
| 2 | 07.30 report:717 → KILN's trunk-holder was `D:/language/midi-music` | FACT |
| 3 | `CLAUDE.md:55-57` last written by **`23f648b`, 2026-07-27** — three days *before* the KILN pass read it | FACT |
| 4 | 07.30 report:716 quotes the line as already saying `ferrosintesis` | FACT |
| 5 | `git log 73ec2f2..HEAD -- CLAUDE.md` → only `88e3014`, whose diff touches lines 200-207 only | FACT |
| 6 | `git remote -v` → `https://github.com/0x4D44/ferrosintesis`, same on both passes | FACT |

**Items 3 and 4 are decisive.** The text KILN read on 07.30 is byte-identical to the text I read today.
The repo did not change between the passes; **the observing host did.** So this is (B) — two clones with
differently-named local directories — not (A), a rename between passes.

**A reader proposed the competing hypothesis and I refuted it, which is worth recording because it
would have changed §8's conclusion.** One reader argued from this clone's `main` reflog — continuous,
254 entries, oldest `eb49ccd main@{2026-07-18 15:38:22}: clone: from https://github.com/0x4D44/ferrosintesis`
— that there is **one** clone whose directory was renamed between the passes, and therefore that
`006fda6` was destroyed in the move. Two measurements kill it:

| Measurement | 07.30 on KILN | Today on CRUCIBLE |
|---|---|---|
| Local branches | **2** | **5** (`main`, task, 3× `park/*`) |
| Tags | **1** (`archive/KILN42-uke-post`) | **0** |

**A directory rename preserves branches and tags.** The three `park/*` branches have existed since
2026-07-18/19 — long before the 07.30 pass — yet that pass reported only two local branches; and its
tag is absent here. Both facts are impossible under a single renamed clone and expected under two.
The continuous reflog means only that *this* clone was made on 2026-07-18 and used since; it carries
no information about KILN's. **`006fda6` is not lost — it is on the other box.**

That clone URL also dates the GitHub rename: the repo was **already** named `ferrosintesis` on
2026-07-18, so `23f648b` on 07-27 was the documentation catching up, and KILN's older directory —
created before the rename — simply kept the old name.

And `23f648b`'s own commit body says exactly this was intended:

> CLAUDE.md's one live `D:\language\midi-music` pointer was both machine-specific and stale on the repo
> name; it now names the main clone **without pinning a host**.

The author de-pinned the host deliberately, foreseeing this divergence. The 07.30 report treated a
host-local mismatch as a document defect; its conclusion *"pointing at a directory that does not exist"*
is true **on KILN only**.

**Correct remediation is not a doc edit** — rename KILN's clone to `D:\language\ferrosintesis`, or accept
the divergence and leave CLAUDE.md alone.

**A real, previously unflagged defect sits three lines away:** `CLAUDE.md:7` still opens
`` `midi-music` is a collection of **original, generative instrumental albums** ``. That *is* a stale
repo name — host-independent, and not the line anyone has been flagging.

---

## 8. Branches, worktrees, and hygiene

**2 worktrees, both clean at `a395a66`. 10 refs total. Zero tags. Zero forensic residue** (no
`BASELINE-*`, `VERIFY-*`, `PROBE-*`, `bisect-*`, `recover-*`). Local `main` fast-forwarded to `a395a66`
mid-session, so `main` == `origin/main` == HEAD.

| Ref | Ahead / behind | Verdict |
|---|---|---|
| `origin/task/20260725-OPS-CLA@KILN-daily-report-020014` | 1 / 701 | **INTEGRATE** — sole copy of the missing 07.25 report |
| `origin/claude/bugs-queue-2q-drain-609csu` | 4 / 858 | REAP (carried from 07.30) |
| `origin/task/20260718-TSK-HUM-max-richter-leftovers-album` | 3 / 1153 | REAP (carried from 07.30) |
| `park/20260718-commuted-excitation-prototype` | 6 / 1114 | **REAP** — local-only |
| `park/20260719-commuted-excitation-r2` | 4 / 1017 | **REAP** — local-only |
| `park/20260719-recmatch-excitation-r3` | 2 / 977 | **REAP** — local-only |

A good cross-check on both passes: every remote branch's *behind* count grew by exactly **+11** against
the 07.30 figures, and the window is 10 commits — so the 07.30 baseline was `73ec2f2~1` and both sets of
arithmetic are internally consistent.

**The three `park/*` branches are new to this report only because they are local to CRUCIBLE.** Reflogs
show this clone has held them since 2026-07-18/19 — ~12 days, not new. They appear in `refs/heads` only.
Their tips are self-labelled failed experiments (*"PARKED, reverted next"*, *"PARKED — fails expanded
frozen gates"*, *"PARKED"*), and the durable knowledge is already on trunk: all three HLDs and journals
are tracked, and `MicroCab` appears 21 times in `engine.rs`. **REAP, low priority** — unlike `006fda6`,
no preserve instruction attaches to them. Their existence is a second independent confirmation of §7.

**`archive/KILN42-uke-post` is absent here** — this clone has zero tags. Absent ≠ deleted; it is a
KILN-local tag.

### `006fda6` — the routing problem is the finding

**The object does not exist in this clone.** `git cat-file -t 006fda6` → `fatal: Not a valid object
name`. **This carries no information about whether it survives on KILN** — `git fetch` only transfers
objects reachable from refs, and `006fda6` sits on no ref anywhere, so its absence here is the
*expected* state.

`bugs/MM-BUG-KILN-00042.md` was untouched this window; last touched `308185b`, 2026-07-23.

**`git branch recover-006fda6 006fda6` must run on KILN, in `D:\language\midi-music`. It is
unrunnable from CRUCIBLE.** This is the third consecutive ask against Arthur's explicit preserve
instruction in `a6d043e`, and the deadline is now **~2 days** (eligibility ~2026-08-02 07:47:30 from the
07.30 report's cited author date, which I could not re-verify because the object is absent). Git prunes
on object *mtime* and only when a `gc` runs, so the real deadline could be later — or could fire on any
auto-`gc` triggered by ordinary work on KILN. **Routing this to a CRUCIBLE agent will silently fail**,
which may be exactly what happened for the last two asks.

### The 2026.07.25 report — 6th ask

Series gap confirmed: `07.09`, `07.20`–`07.24`, **[07.25 absent]**, `07.26`–`07.30`. `f79b6dc` still
exists, still reachable only from `origin/task/20260725-OPS-CLA@KILN-daily-report-020014`, still one
commit / one file / 745 insertions / pure docs. A zero-conflict-surface cherry-pick that has survived six
reports.

### CLAUDE.md and README defects — all still open

- **`gpt5-6/` missing from two lists.** `albums/` holds five model dirs; `CLAUDE.md:33-34` lists four and
  `:320` omits it again. `:320` is a genuine miscategorisation, not a nit: all three gpt5-6 albums *are*
  `tracks/`-shaped, and `:323` scopes the `movements/` shape to fable5 — so gpt5-6 falls into neither
  documented shape. **`albums/gpt5-5/` does exist** (two albums, 13 tracked MIDIs) — any fix must **add**
  `gpt5-6/`, never remove `gpt5-5/`.
- **`crates/amp-lab/README.md` — the highest-value doc fix in the set**, because it is the only one that
  hands an agent a command that fails. Four wrong post-excision commands (`:7`, `:12`, `:14`, `:14-15`),
  and `:10-15` still claims *"It is a **workspace member**"* against `Cargo.toml:3`'s *"Deliberately NOT a
  member."* **Correction to the 07.30 report:** it cited `Cargo.toml:32` vs `:10`; at HEAD `Cargo.toml:10`
  reads *"while this was a member"* (past tense, correct) and `:32` is a `[profile.release]` comment. The
  false claim is in **`README.md:10`** — the report crossed its file references. The defect is real; the
  citation was wrong.
- **Root `README.md:164-165`** still says the CLI *"is not published as a separate crate"* against
  `RELEASING.md:3-4`, `CLAUDE.md:213-214` and `crates/ferrosintesis-cli/README.md:12`. **3-to-1, and
  `88e3014` did not touch `README.md` at all.**

**Correction to the 07.30 report:** it described the max-richter branch as *"a strict prefix of main"*.
It is **3 ahead**, so it is not a strict prefix. The REAP verdict may still be right; the descriptor is
not.

**No cloud CI** — `.github/` absent, 6th clean report. **`raw_loudness.csv`** still tracked at the root
at 105 lines, 6th mention.

---

## 9. Prior-plan audit — 0–1 of 15, and why the raw rate over-states it

**The 22-file change list settles most of this deterministically.** Five of the eight agent items require
touching files that are simply not in it — `tools/render-diff/render_diff.py`, `README.md`,
`wrk_docs/review-coverage.md`, `crates/ferrosintesis/src/provenance.rs`, `crates/amp-lab/README.md`. They
are NOT DONE by construction, not by judgement.

| # | Item | Verdict |
|---|---|---|
| 1 | Recover `006fda6` | **NOT DONE** — no `recover-*` ref; `00042` untouched. Now known to be **unrunnable from this host** (§8) |
| 2 | Two render-diff inventories + fill three `## Fix` sections | **PARTIAL** — all three Fix sections landed with real fails-before evidence (§1); **no** render-diff inventory anywhere |
| 3 | Fix `render_diff.py`'s albums-only default | **NOT DONE** — `tools/` absent from the change set |
| 4 | Fix CLAUDE.md defects + root README | **NOT DONE** — `88e3014` touched only `CLAUDE.md:200-207`; `README.md` untouched. **And its `:55-56` prescription was itself wrong** (§7) |
| 5 | Write the `review-coverage.md` oracle | **NOT DONE** — file untouched; no `.rs` references it. 6th skip |
| 6 | Test the arrow-glyph hypothesis | **NOT DONE** — all three damaged files untouched |
| 7 | Rebase-timestamp lesson + file the cfg hazard | **NOT DONE** — lesson exists nowhere (§5); no bug mentions `ferrosintesis_repository_tests` |
| 8 | Three `provenance.rs` warts | **NOT DONE** — file absent from the change set |

**Item 8's tautology is independently re-confirmed at HEAD**, by a third reader reading both bodies
fresh. `repo_recording_dirs(samples)` (`provenance.rs:195-204`) is
`read_dir(samples).filter(is_dir)` with no further filter; `source_dirs()` (`:170-193`) is the
`tools/*-src` roots ∪ that same set; and
`source_registry_covers_every_repo_root_recording` (`:274-297`) builds its candidates from an identical
`read_dir(&samples).filter(is_dir)`. **Candidates ⊆ registered by construction, so `missing` is always
empty and the test cannot fail.** The only way it ever trips is a TOCTOU race between `:276` and `:278`
— an artifact, not a guard. The stale floor at `:185-191` still reads `out.len() >= 4` against a real 7.
*Line correction to the 07.30 report:* the "79 committed sources" doc comment is at `:60` and `:63`, not
`:61`.

**Arthur lane (a)–(g): no in-repo evidence for any.** Gate 1 did not open; no music; `git -C` and
outside-worktree reads were refused again on this pass too; `gate=auto` still 0; the 3 ID collisions
stand; MetalPlate V4 has no keep-or-retire decision; no HLD-header rule was written. **Standing caveat:
absence of in-repo evidence is not proof a question went unanswered.**

One 07.30 prediction came **true**: it warned that leaving Gate 1 shut means *"per-req triage keeps
minting an eighth."* `MM-REQ-CRUCIBLE-00007` is that eighth, raised the next day (§5).

| Report | Items | Executed | Rate |
|---|---:|---:|---:|
| 07.26 | 12 | 6 | 50% |
| 07.27 | 13 | 1–2 | 8–15% |
| 07.28 | 14 | 1–2 | 7–14% |
| 07.29 | 13 | 2–4 | 15–31% |
| **07.30** | **15** | **0–1** | **0–7%** |
| **Cumulative** | **106** | **17–22** | **16–21%** |

### The confound, quantified — most of the drop is volume, and I will not over-read it

| | 07.29 window | 07.30 window (this one) |
|---|---:|---:|
| Commits | 58 | **10** |
| Commit span | 17 h 31 m | **19 h 06 m** |
| **Commits / hour** | **3.31** | **0.52** |
| Plan size | 13 | **15** |
| Executed | 2 | **0 (1 partial)** |
| Items per commit | 0.034 | **0.000–0.050** |

**A 6.3× collapse in commit rate over an essentially identical calendar day.** Normalised per commit,
this window is **equal to or slightly better than** the previous one. So most of the headline 15% → 0–7%
drop is a **volume artifact, not a routing signal**, and any narrative built on the raw rate over-reads.
Two alternative explanations also fail: there is no weekday/weekend structure in the series (the best
rate, 50%, is mid-series at 07.26, and 07.21→07.31 is unbroken daily), and the pass did not run early —
the two review passes are the window's *last* two commits, landing 62 and 0 minutes before the boundary.

**The structural fact underneath the volume.** The 10 commits decompose as a bug drain (07:06–13:05,
whose fix runs started **2026-07-29 22:35–23:56, before the report existed**), one release chore at
18:15, and two scheduled reviews at 01:10 and 02:12 — with two idle gaps of 5 h and 7 h. The window was
low-volume because activity was **sparse**, not because time was short. INFERENCE: **no session was ever
tasked with executing the 07.30 plan.**

### Two observations that survive the confound entirely

These concern *composition*, not count, so volume cannot explain them:

1. **`lessons_learnt.md` was edited twice this window** — the exact file item 7(i) named — and **neither
   edit was the ask.** What landed was distilled by the bug-closure pass.
2. **`CLAUDE.md` was edited this window** — the exact file item 4 named — and it changed a **different
   paragraph**, driven by a release chore.

Access was never the constraint. Both target files were open and being written to by other actors; the
report's asks still did not get in. A third instance: `wrk_docs/review-coverage/` gained two fragments
from the enumerated review passes while the aggregate `review-coverage.md`, which nothing walks, sat at
its 2026-07-18 state.

### The thesis needs restating — the reqs ledger is a counterexample

The 07.30 thesis was *an ask lands when something enumerates it*. **As stated it is false**, and the
repo's own reqs ledger refutes it: `deltic reqs drain` **is** a scheduled enumerator, it exists, and its
throughput this window was **zero** — because `Accepted` has been zero for six reports. Enumeration is
present; output is nil. The binding constraint there is a **closed human gate**.

The thesis collapses two different failure modes. **The restatement I would defend: an ask lands when a
scheduled actor owns it *and* no closed human gate stands in front of it.** That explains the whole
window:

| Channel | Scheduled actor? | Human gate? | Out |
|---|---|---|---|
| Bug drain | yes | no | **1 fixed, 4 closed** |
| Review rotation | yes | no | **2 passes → 6 bugs, 1 req** |
| Reqs ledger | **yes** | **yes (closed)** | **0** |
| Daily-report agent lane | **no** | — | **0–1** |

And that restatement partly deflates the insight: "work happens when a scheduled job does it" is closer
to a scheduling observation than a lesson about phrasing.

**Two honesty notes on the scoring.** First, item 2's completed half is **not** the plan's win — the
three fix runs predate the report, and filling `## Fix` is what a closure pass does anyway. Attributing
it to the report would repeat exactly the confound the 07.30 report was careful about. Second, **item 1
did not fail for lack of an enumerator at all** — it was blocked by a permission (`git -C`, 11th
refusal, re-confirmed by my own denied call this pass).

**And the 07.30 report misrouted that escalation.** Its item 1 says *"this needs Arthur — which makes it
item (e) below"*, but `(e)` is the **ID allocator**; the `git -C` allowance is **`(c)`**. The most urgent,
time-limited item in the report pointed its reader at the wrong ask. Small, but it ends the series'
two-report zero-error streak, and it plausibly contributed to two missed asks.

**The experiment the 07.30 report proposed has now failed to run twice, for the same structural reason:**
a docs-lane report cannot mutate a ledger, so nothing in `bugs/` or `reqs/` was ever created by it. That
is a stronger finding than either arm's result — and it applies to this report too. The two items below
that genuinely need doing cannot be filed by this pass.

---

## 10. Tracked issues

| # | Issue | Age | Owner |
|---|---|---|---|
| 1 | **`MM-BUG-CRUCIBLE-00003`: confirmed reachable panic in the public render path** from a 7-byte file, no NoteOn needed. A recurrence of `KILN-00025`, whose fix was scoped to its own three bullets while five sibling data bytes stayed unmasked | **new** | agent |
| 2 | **The `0.21.56` "first real crates.io release" claim is unverified** — commit says *prepare*, CHANGELOG says *is*, RELEASING.md hedges, and **no journal exists for 2026-07-30**. One network lookup settles it | **new** | **Arthur** |
| 3 | **`006fda6`: ~2 days to prune eligibility, and it is unrunnable from CRUCIBLE.** Must be run on KILN in `D:\language\midi-music`. Silent-failure routing may explain two prior misses | **3rd** | **Arthur / KILN** |
| 4 | **Reqs Gate 1 has never opened in six reports** — 0 Accepted so the drain may take nothing; 3 `Implemented` reqs with complete oracles waiting 6 days; the seam minted an 8th instance one day after being named | **6th** | **Arthur** |
| 5 | **2026.07.25 daily report still unintegrated** (`f79b6dc`, 1 commit, 745 lines, pure docs, sole copy on `origin`) | **6th** | deltic |
| 6 | **`review-coverage.md`: 20 of 28 members, every row 13 days stale, no oracle, 28 fragments unfolded.** Both new fragments went to the *most*-reviewed row | **6th** | agent |
| 7 | **CLAUDE.md / README defects**, three of them re-verified and one newly found: `gpt5-6/` missing from two lists; **`CLAUDE.md:7` still names the repo `midi-music`**; `amp-lab/README.md`'s four dead commands; `README.md:164` CLI-publish 3-to-1. **Do NOT action 07.30's `:55-56` prescription — see §7** | **3rd** | agent |
| 8 | **`CHANGELOG.md:79-80` still claims drumkit2 carries "four accent-cymbal banks (crash, sizzle crash, splash, china)"** — ground truth is three, and the file now declares a real release | **2nd** | agent |
| 9 | **Five stale `Held branch` fields are now cross-host stale** — all self-labelled `host-local:KILN:` while the ledger is worked from CRUCIBLE | **2nd** | agent |
| 10 | **Three damaged `State history` files untouched**; the arrow-glyph hypothesis that would explain them is still untested | **5th** | agent |
| 11 | **The rebase-timestamp lesson has been re-derived in four reports and written down in none** — and `lessons_learnt.md` was edited twice this window on another topic | **2nd** | agent |
| 12 | **Twelfth day with no new music**; fifth consecutive zero-music report. `gate=auto` still 0 after five confirmations | **watch** | **Arthur** |

**Retired this report — stop re-raising:** the `<unfixed — raised only>` regression (fully closed, all
three with fails-before evidence); worktree/branch sprawl (2 worktrees, zero residue, three local
`park/*` now assessed); the `006fda6` *knowledge* risk (durable on trunk — only the artifact is at
risk); and the 07.30 host-path "defect", which §7 shows was never a repo defect at all.

---

## 11. Plan for today

§9's finding is that only enumerated channels produce output. So the first two items say explicitly
where they belong, and the rest are ordered by whether anything will actually walk them.

### Agent lane

1. **Fix `MM-BUG-CRUCIBLE-00003` — and enumerate the class, not the instance.** This is the one item with
   a confirmed, mechanical reproduction and it sits in a crate the repo now says is published. The fix
   belongs at the **parser trust boundary**, not at the array index — masking only where it panics leaves
   an invalid program in channel state. *Acceptance:* a parser-wide seven-bit policy covering all five
   unmasked data bytes (`midi.rs:316`, `:328`, `:329`, `:333`, `:337`), plus two regressions — parser
   coverage for `C0 FF`, and a **public `offline::render`** test proving the 7-byte file cannot panic.
   **And rename the guard for the class**: `KILN-00025`'s regression is called
   `note_key_data_bytes_are_limited_to_seven_bits`, which is exactly why nobody noticed programs were
   uncovered. Only the program byte panics today; the other four are latent — say so in the fix note
   rather than implying five live crashes.
2. **Settle the release question before anything else ships** (needs one network lookup, so it may be
   Arthur's — item (a)). If `0.21.56` is live, item 1 is urgent for consumers and `README.md:151,154`
   are finally correct. If it is not, three documents are now false in the opposite direction from the
   contradiction they closed 24 hours ago, and the CHANGELOG's `## [0.21.56] - 2026-07-30` heading needs
   to move back under `## [Unreleased]`.
3. **Fix the drumkit2 CHANGELOG line while the release file is open** —
   `crates/ferrosintesis/CHANGELOG.md:79-80`, "four accent-cymbal banks (crash, sizzle crash, splash,
   china)" → three (crash, splash, china). One line, in the document that describes the release, wrong
   for two reports.
4. **Fix the doc defects, with the §7 correction carried.** `CLAUDE.md:33-34` and `:320` — **add**
   `gpt5-6/`, do **not** remove `gpt5-5/`, and note gpt5-6 is `tracks/`-shaped; `CLAUDE.md:7` — the repo
   is `ferrosintesis`, not `midi-music`; `crates/amp-lab/README.md:7,10,12,14` — the four post-excision
   commands and the "workspace member" claim; `README.md:164-165` — the CLI *is* in the publish set.
   **Leave `CLAUDE.md:55-56` alone.**
5. **Write the `review-coverage.md` oracle** — 6th ask, and the only item with an unbroken record of
   being skipped. *First action:* copy `crates/ferrosintesis/src/licensing.rs`'s pattern — read the root
   `Cargo.toml` `members` array **as text**, require one row per member, let it fail red on the eight
   uncovered. Do not hand-add eight rows; that is the defect, not the fix.
6. **Two one-line notes that keep being skipped:** the rebase-replay committer-timestamp lesson (8 slots
   free, 4th re-derivation), and the `ferrosintesis_repository_tests` cfg hazard filed as a bug with the
   **corrected** remedy — a repo-only sentinel, never a bare `assert!(cfg!(…))`, which ships in the
   crates.io archive and fails there.

**Not today:** the arrow-glyph diagnosis (worth doing, but it needs deltic's parser source, which is
outside this repo); the four designed-but-not-started items; and the reqs seam oracle, which needs
Arthur's Gate-1 call first.

### Arthur lane — only you can do these

- **(a) Is `0.21.56` actually on crates.io?** The single highest-value answer available today. Everything
  in §2 and the priority of item 1 hangs on it, and nothing in the repo can settle it.
- **(b) `006fda6` must be recovered on KILN, not here** — `git branch recover-006fda6 006fda6` in
  `D:\language\midi-music`. ~2 days. Two prior asks may have failed silently because they were routed to
  the wrong box; this one names the box.
- **(c) Open reqs Gate 1 — and triage the seam, not the eight.** Sixth report with 0 Accepted. Accepting
  one *"derive descriptors from a single typed source"* req would close seven Drafts and stop the ninth
  being minted. Also Gate-2 the three `Implemented` reqs, one of which states outright that it is waiting
  only on a second pair of eyes.
- **(d) Is it time to compose, or to listen?** Twelve days, five reports. The synth has moved the default
  sound on most of the catalogue since the last album-scale listen. This is the question I would most
  like answered.
- **(e) May autonomous fixes auto-land?** `gate=auto` is zero after five confirmations and every recent
  landing is `manual`. Either authorise a land path with the contract gate as its condition, or state
  that manual landing is the intended end state. Either answer retires the question permanently.
- **(f) Two boxes now share this ledger.** CRUCIBLE minted its own clean ID line, which is good — but five
  `Held branch` fields point at KILN-local branches, `006fda6` is only reachable from KILN, and a tag
  exists on one box and not the other. Worth one decision on whether host-local state should ever be
  written into shared ledger files.
- **(g) Is MetalPlate V4 still wanted?** Listed "designed, not started" for seven reports; the HLD is 17
  days old. This window's precedent (the ride-bell was retired *by decision*) is the right shape.

---

## Verification notes

**Verified by me directly, reading the source and quoting it:** the full six-site panic chain in §1,
including the absence of any `continue`/`is_empty()`/`voices.len()` guard between `engine.rs:3726` and
`:3818`, and the absence of any existing test for it; the eight-row mask census in `midi.rs`;
`PROGRAM_TRIM_DB`'s and `VEL_LEVEL_EXP`'s `[_; 128]` declarations and both index expressions; the
absence of `vel as usize` / `num as usize` / `val as usize` anywhere in `crates/ferrosintesis/src/`;
`KILN-00025`'s fix record verbatim; the state census and all three arithmetic reconciliations; the
window boundary and the `ab5f811..73ec2f2` gap check; `23f648b`'s diff and commit body; the 261-journal
count and the zero for 2026-07-30; the CHANGELOG release assertion and the drumkit2 line at `:79-80`;
and the absence of a prior 07.31 report.

**Reached independently by me and by a reader, agreeing exactly:** the panic chain (§1) and the
host-path verdict (§7). The latter matters most, because acting on the 07.30 prescription would have
broken this box.

**Corrections applied to reader output rather than passed through — both material:**

1. **`VEL_LEVEL_EXP` is indexed by *program*, not velocity** (`voices.rs:14261`) — a second sink for one
   byte, not a second vector. This changes the fix's shape, so it is not cosmetic.
2. **A reader concluded `006fda6` was destroyed by a directory rename and reported the item as
   "foreclosed."** It is not. The single-clone premise is refuted by the branch and tag counts in §7, and
   the correct status is "on the other box, still recoverable, ~2 days." Had I passed that through, this
   report would have written off an artifact Arthur explicitly asked to keep.

**Corrections to the 07.30 report, each verified from source:** its `CLAUDE.md:55-56` prescription is
wrong and harmful on this host (§7); the amp-lab false claim is in `README.md:10`, not `Cargo.toml:10`;
the max-richter branch is 3 ahead and therefore not "a strict prefix of main"; the `provenance.rs` doc
comment is at `:60`/`:63`, not `:61`; `MM-BUG-KILN-00180` was created as well as closed in-window, giving
a 5 h 58 m lifecycle rather than ~3.5 h; and **its plan item 1 misroutes the `git -C` escalation to
Arthur item `(e)` (the ID allocator) when it is `(c)`** — the first error in the series after two clean
reports, on the report's most time-critical item.

**Not verified / explicitly unknown:**

- **Nothing was built, tested, rendered or listened to.** Test counts are source scans, not run counts.
  Every render-diff figure, gate result and correlation measurement is **quoted** from the commit or bug
  file that recorded it, not reproduced. The "842 passed / clippy clean / fmt clean" figures come from
  `bugs/MM-BUG-KILN-00180.md:171-174`.
- **Whether `ferrosintesis 0.21.56` is live on crates.io.** No network access, none authorised. This is
  the report's single most consequential unknown — three documents were rewritten on the strength of it.
- **Whether `006fda6` survives on KILN.** The object is absent from this clone, which is the *expected*
  state for an unreferenced object and carries **zero** information either way. Its author date and
  prune deadline are quoted from the 07.30 report, not re-derived.
- **`MM-BUG-CRUCIBLE-00001`, `00002` and `00006` at the code level.** 00001's licence claim needs the
  FreePats page and the CC BY 3.0 text (no network); 00002's cited `PROVENANCE.md` lines were not opened;
  00006's four `engine.rs` ranges were not read. 00003 and 00004 I verified fully; 00005 partially
  (`midi.rs:358` confirmed, the cursor's bounds not traced).
- **Which model ran the `005314` review.** Neither the CR doc, its journal, nor `MM-BUG-CRUCIBLE-00001`
  and `00002` record a `model=` token — and the `REV-CLA@` run prefix is demonstrably unreliable here,
  since the `014814` run carries the same prefix while its five ledger items all record
  `model=gpt-5.6-sol@xhigh`.
- **Main-clone working-tree cleanliness, and unregistered directories under `D:/worktrees/ferrosintesis/`.**
  `git -C`, `ls` and `Glob` outside this worktree were all refused again — **11th consecutive report**.
  `git worktree list` proves only 2 *registered* worktrees, which is exactly the blind spot this check
  exists to cover.
- **Whether the seven unchecked Closed files carrying `<unfixed — raised only>` are stale template
  artifacts.** Extrapolated from 4 of 11 spot-checks.
- **Whether Arthur answered any of the 07.30 (a)–(g) in conversation** — invisible from the repo.
- **Whether deltic's parser, ID allocator or review-rotation logic changed** — its source is outside this
  repo; I observed only ledger and document output.
