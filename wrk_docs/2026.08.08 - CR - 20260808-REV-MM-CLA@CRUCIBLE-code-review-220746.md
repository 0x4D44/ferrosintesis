# Code review — `crates/ferrosintesis-samples-dark-salamander/`

- **Date:** 2026-08-08
- **Baseline:** `40cae49881a0ca9f8388f6db09368f9719fd2629` (fresh Deltic-supplied `origin/main` worktree; no fetch run)
- **Mode:** read-only static review; no app, render, build, test, package, generator, or exploratory harness ran
- **Outcome:** one confirmed Low documentation defect; current payloads and routing are clean
- **Ledger status:** no new bug or requirement was minted after deduplication

## Scope and method

The pass reviewed every file in
`crates/ferrosintesis-samples-dark-salamander/`. Consumer, generator, history,
and existing ledger files were read only where needed to verify or deduplicate a
claim.

Independent subagents covered correctness, security, performance/scale,
reliability, maintainability, and test coverage. A separate devil's-advocate pass
attacked every surviving candidate. The lead re-read each cited path and retained
only findings with a complete static evidence chain.

The crate contains 54 WAVs: nine pitch zones, three dynamics, and two round
robins. Every current file is canonical mono PCM16 at 44.1 kHz, has consistent
RIFF and data extents, and is 133,048 bytes. The aggregate is the pinned
7,184,592 bytes, and no two payloads share a SHA-256 digest. A read-only,
independent evaluation of the current shelf/filter and normalization formula
matched every committed PCM frame against its paired raw-Salamander source. The
inventory table, lookup behavior, package include list, licence, README, and
provenance agree with the current files.

## Confirmed finding

### Low — `darkgrand_bank` still documents the superseded CC0 slot

- **Confidence:** High
- **Where:** `D:\worktrees\ferrosintesis\20260808-REV-MM-CLA@CRUCIBLE-code-review-220746\crates\ferrosintesis\src\sampler.rs:1685`; authoritative routing at `D:\worktrees\ferrosintesis\20260808-REV-MM-CLA@CRUCIBLE-code-review-220746\crates\ferrosintesis\src\voices.rs:1399`; source-derived documentation guard at `D:\worktrees\ferrosintesis\20260808-REV-MM-CLA@CRUCIBLE-code-review-220746\crates\ferrosintesis\src\altbank.rs:1434`
- **Problem:** the function rustdoc says dark-Salamander is GM0 CC0 bank 4. The authoritative `GM0_SOURCES` table routes it at CC0=5. The adjacent sampler identity comment, generator, crate module docs, manifest, README, and provenance all correctly say 5.
- **Root cause:** commit `c3285e87` moved dark-Salamander from slot 4 to 5. The earlier line introduced by the first fix for `MM-BUG-KILN-00122` remained unchanged. The source-derived guard checks the sampler's leading identity block, not this later function comment.
- **Impact:** runtime routing is correct, so this cannot select the wrong instrument. The stale internal contract can mislead maintenance and is a small recurrence of the selector-documentation drift that the guard was meant to reduce.
- **Concrete fix:** change `bank 4` to `bank 5`. Extend the source-derived guard to check `darkgrand_bank`'s identity rustdoc, or remove the numeric claim there and point to `GM0_SOURCES` so the slot has one source of truth.
- **Effort:** Small
- **Tracking:** report-only in this transaction. Closed `MM-BUG-KILN-00122` is the exact deduplication match. The bug-tracking rules require a regression to reopen that record rather than mint a duplicate, while this run is expressly forbidden from modifying tracked files.

## Existing tracked requirement

Draft `MM-REQ-KILN-00033` already requires dark-Salamander to remain a complete,
deterministic, non-mutatingly verified projection of the raw grand bank. It covers
note identity, duration, PCM format, and measurable treble attenuation. The
current 54-file projection is clean, but the durable oracle remains Draft. No
duplicate requirement was created.

## Not reported

- **Interrupted rebake as a new defect — downgraded.** The writer publishes each WAV through a sibling `.part` file and atomic replacement. A process kill can interleave old and new complete files, but rerunning the deterministic command repairs the bank. The repo has previously accepted per-file rather than whole-directory atomicity on Windows, and Draft `MM-REQ-KILN-00033` is the right durable validation path.
- **Malformed embedded WAV as a runtime or security defect — refuted.** The shared parser is assertive, but it receives only compile-time first-party assets. Every current reviewed asset has exact bounds and the required PCM format. A fallible parser is defense-in-depth, not a current dark-bank defect.
- **Unreachable sample tails — refuted.** Authored pitch bend can raise the source read rate by up to four times, so the lower-zone tail frames are reachable.
- **Linear lookup as a scale problem — refuted.** The fixed 54-entry scan runs during bounded bank initialization, not per rendered frame.
- **Missing or duplicate payload, wrong route, wrong root, stale output, package omission, or licence mismatch — refuted for the current tree.** Static inventory, RIFF, hash, projection, source, and routing checks agree.

## Unverified, not reported

- A dark-specific edge-key and pitch-bend handoff matrix would strengthen coverage, but no audible discontinuity or runtime failure was established. The highest zone can exhaust near the model handoff under some pitches; its terminal samples are zero and model weight is already dominant. This remains test debt, not a confirmed defect.
- Audible timbre, dynamics, pitch quality, and the upstream archive were not checked. This pass did not run audio or access the network.

## Next coverage

After Deltic applies this pass's coverage artifact, the oldest moved sample areas
include `crates/ferrosintesis-samples-drumkit/`,
`crates/ferrosintesis-samples-gong/`,
`crates/ferrosintesis-samples-grand/`,
`crates/ferrosintesis-samples-headroom/`, and
`crates/ferrosintesis-samples-musescore-grand/`, all last reviewed on 2026-07-26
and changed afterward. The next pass should take
`crates/ferrosintesis-samples-drumkit/` first as the higher-risk generated area,
subject to the merged ledger's final ordering.
