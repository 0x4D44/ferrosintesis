# Code review — honky-tonk sample crate

- **Pass:** `20260813-REV-MM-CDX@KILN-code-review-211154`
- **Area:** `crates/ferrosintesis-samples-honkytonk/`
- **Baseline:** Deltic-supplied fresh `origin/main`; no fetch performed
- **Mode:** read-only source review; no app, test, build, render, generator, or exploratory harness ran

## Outcome

The pass found no new current defect. It confirmed one durable verification gap
and captured Draft requirement `MM-REQ-KILN-00186`. No new bug was filed: the
only concrete regeneration defect rediscovered here is already Open as
`MM-BUG-KILN-00181`.

The current nine WAVs are complete, unique, and structurally valid. Their names
match the embedded table, generator note list, and consumer zone table.

## Confirmed finding

### CR-1 — Honky-tonk assets and zone mappings lack an independent oracle

- **Severity:** Low prevention debt; a realized payload swap would have high audio impact
- **Confidence:** High that the gap exists; current asset correctness is not inferred from it
- **Where:**
  `D:\worktrees\ferrosintesis\20260813-REV-MM-CDX@KILN-code-review-211154\crates\ferrosintesis-samples-honkytonk\src\lib.rs:66`;
  `D:\worktrees\ferrosintesis\20260813-REV-MM-CDX@KILN-code-review-211154\tools\ferrosintesis-samples\gen_crate_lib.py:84`;
  `D:\worktrees\ferrosintesis\20260813-REV-MM-CDX@KILN-code-review-211154\crates\ferrosintesis\src\sampler.rs:1730`
- **Tracked as:** Draft `MM-REQ-KILN-00186`
- **Effort:** Medium; proposed Heavy flow

The generated crate tests prove the directory-derived filename set, aggregate
byte count, RIFF/WAVE magic, and lookup against the same generated table. They
do not independently bind a filename to its source payload or measured root.
All nine WAVs are exactly 133,048 bytes, so swapping C2 and C6 preserves every
crate-local assertion while the unchanged consumer root table plays both zones
from the wrong recordings. The same checks also do not prove RIFF and data
extents, the complete PCM contract, or the absence of duplicate payloads.

**Concrete requirement:** provide a non-mutating verifier tied to the pinned
FreePats archive and bake recipe. Compare every output for per-name payload
identity, strict RIFF/PCM structure and extents, duration, measured root, and
the nine source-to-zone mappings. Negative controls must cover an equal-sized
note swap, duplicate payload, malformed chunk extent, changed PCM format, and
changed root mapping. Share verifier architecture with the Kawai, Steinway, B1,
Salamander, and Headroom Drafts while keeping this bank's source substitutions
and roots explicit.

## Adversarial verdicts — not reported as new findings

- **Refuted — malformed WAV handling is a present High defect.**
  `D:\worktrees\ferrosintesis\20260813-REV-MM-CDX@KILN-code-review-211154\crates\ferrosintesis\src\sampler.rs:55`
  can panic on malformed compile-time assets or accept a truncated declared
  chunk. Runtime callers cannot supply these private embedded bytes, and every
  current honky-tonk file has matching RIFF/data extents and PCM16 mono 44.1 kHz
  metadata. The latent hardening belongs in `MM-REQ-KILN-00186`; it is not a
  current High bug.
- **Confirmed but deduplicated — wrong-shaped archive-cache manifests abort
  regeneration.**
  `D:\worktrees\ferrosintesis\20260813-REV-MM-CDX@KILN-code-review-211154\tools\ferrosintesis-samples\prepare.py:1393`
  can raise on valid JSON with the wrong shape, including through the
  honky-tonk archive path. Open `MM-BUG-KILN-00181` already records the exact
  failure and fix, so no duplicate was filed.
- **Already tracked — the published crate omits the exact source digest.** Draft
  `MM-REQ-KILN-00144` covers putting generator-only source hashes into packaged
  provenance. It does not cover output identity or zone mappings, so it does
  not duplicate CR-1.
- **Refuted — current inventory, package, or version drift.** The crate is
  `0.1.1`; the parent dependency pins `=0.1.1`; the lockfile agrees. The manifest
  packages source, all samples, README, NOTICE, provenance, and CC0 text. Nine
  tracked WAVs match nine embedded entries and nine consumer zones. The stale
  output guard runs before extraction or writes; closed `MM-BUG-KILN-00143`
  remains satisfied.
- **Refuted — lookup or memory scale defect.** `get()` scans nine immutable
  entries during one-time bank initialization. All payload hashes differ, so
  there is no undeclared packaged or decoded alias.
- **Refuted — runtime injection, unsafe code, or network exposure.** The crate
  forbids unsafe code, has no dependencies or build script, performs exact
  string lookup, and embeds trusted compile-time assets.
- **Unverified — external and audible truth.** The live archive bytes, exact
  upstream SHA-256, external licence page, audible character, and all-zone
  pitch remain unverified. No network, audio, test, package, or build command
  ran; this report does not claim a green build.

## Lens coverage

Correctness checked inventory, lookup, packaging, versions, and consumer roots.
Security checked unsafe code, injection surfaces, package/legal contents, and
source authentication. Performance checked package size, decoded memory,
lookup paths, and prewarm behavior. Reliability checked chunk parsing, cache
failure modes, stale outputs, and initialization. Maintainability checked
generated sources of truth and provenance drift. Test coverage challenged
equal-sized substitutions, malformed containers, and mapping errors. The
devil's advocate independently refuted the proposed High bug, deduplicated the
cache defect, and confirmed the new Draft requirement.

## Next coverage

After Deltic applies this pass's coverage artifact,
`crates/ferrosintesis-samples-musescore-grand/` is next due: its latest review
was 2026-07-26 and its code moved on 2026-07-29. The following moved areas are
`crates/ferrosintesis-samples-musescore/` and
`crates/ferrosintesis-samples-orchestral2/`, both last reviewed 2026-07-27.

