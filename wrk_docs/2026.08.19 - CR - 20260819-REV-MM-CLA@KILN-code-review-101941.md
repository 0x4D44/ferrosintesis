# Code review — crates/ferrosintesis-samples-drumkit2/ (2026-08-19)

Pass 20260819-REV-MM-CLA@KILN-code-review-101941. Static review only: no build,
test, render, or app run. Method: lead read the whole crate
(`src/lib.rs`, `Cargo.toml`, `README.md`, `PROVENANCE.md`), the shared `Bank`
contract, and the consumer seams; four parallel reviewer lenses (correctness,
adversarial test-oracle, docs/provenance, devil's advocate) fanned out on worker
seats; every reported finding was independently re-verified by the lead against
the source before filing. This area was previously reviewed 2026-07-29, 2026-08-11
and 2026-08-16, so dedup against the open ledger was a first-class step.

## Confirmed findings (all filed as bugs)

| # | Sev | Conf | Finding | Where | Bug | Effort |
|---|-----|------|---------|-------|-----|--------|
| 1 | Medium | High | `regen_samples_table.py` filters `.wav` only; both drum-kit `samples/` dirs are all-FLAC, so the documented table-refresh recipe exits "no .wav files" and nothing else refreshes `SAMPLES`/`FILE_COUNT`/`EXPECTED_BYTES` | `tools/ferrosintesis-samples/regen_samples_table.py:155` (recipe at `crates/ferrosintesis-samples-drumkit2/PROVENANCE.md:111-115`) | MM-BUG-KIL-00303 | Small |
| 2 | Low | High | Both drum-kit PROVENANCE files still describe pre-FLAC payloads: "36 WAVs (7,647,408 raw bytes)" vs 36 FLACs at 4,301,372 (`EXPECTED_BYTES`), `.wav` naming examples, "Output: 16-bit mono WAV"; outside MM-BUG-CRUCIBLE-00044's enumerated fix list | `crates/ferrosintesis-samples-drumkit2/PROVENANCE.md:11-13,80-82,103`; sibling `:18-19,107` | MM-BUG-KIL-00304 | Small |
| 3 | Low | High | `decode_riff_wav` clamps an over-declared `data` chunk to EOF and returns a silently short buffer, contradicting the crate's panic-on-undecodable contract; the FLAC branch rejects the same failure. Latent (all current assets are FLAC) | `crates/ferrosintesis-samples-drumkit2/src/lib.rs:289` (twin at drumkit `:815`) | MM-BUG-KIL-00305 | Small |
| 4 | Low | High | Velocity-split oracle pins too few boundaries and lacks the sibling's structural `vel_hi` invariant — a moved interior split or a non-ascending table (making `china_vl3_*` unreachable) stays green workspace-wide | `crates/ferrosintesis-samples-drumkit2/src/lib.rs:406-413` vs drumkit `:899-900` | MM-BUG-KIL-00306 | Small |
| 5 | Low | High | Re-exec cold-cache probe judges its child by exit status only; libtest exits 0 on a zero-match `--exact` filter, so a drifted `NAME` silently disables the MM-BUG-CRUCIBLE-00023 oracle (both crates) | `crates/ferrosintesis-samples-drumkit2/src/lib.rs:437,457`; drumkit `:1032,1037` | MM-BUG-KIL-00307 | Small |
| 6 | Low | High | No oracle binds an embedded name to the bytes of the file it names — swapping two `include_bytes!` paths within an articulation (timbre inversion across velocity layers) passes every test; aggregate size pin is permutation-invariant and `get(name)==Some(bytes)` is self-referential | `crates/ferrosintesis-samples-drumkit2/src/lib.rs:418-432` | MM-BUG-KIL-00308 | Small |
| 7 | Low | High | The KILN-00125 realtime prewarm guard is half-vacuous (post-render assert on a saturating 0/1 `OnceLock` counter can never fail once the pre-render assert passed) and half schedule-dependent (sampler tests warm the process-global cache first) — the only end-to-end guard against FLAC-decoding in the audio callback | `crates/ferrosintesis/src/live.rs:1372,1390`; counter at drumkit2 `src/lib.rs:251-253` | MM-BUG-KIL-00309 | Small-Med |
| 8 | Low | High | `routed_banks()` hand-copies the 13-bank list; neither crate's `BANKS` export is referenced anywhere in the consumer, so a future bank ships with no boundary-click coverage — the repo's "hand-maintained lists" defect class on the crate seam | `crates/ferrosintesis/src/sampler.rs:7129-7145` | MM-BUG-KIL-00310 | Small |

Findings 7 and 8 sit in `crates/ferrosintesis/`, not the target area, but are the
target crate's consumer-seam guards and were found by reviewing that seam.

## Not reported (and why)

- Stale `.wav` rustdoc on `get()` (`src/lib.rs:215-217`) — already Open as
  MM-BUG-CRUCIBLE-00044 (this crate is in its enumeration); rediscovered by two
  lenses, suppressed.
- SPLASH missing from `decoded_banks_are_valid_audio` + weak `peak > 16000`
  predicate — already Open as MM-BUG-KILN-00203; rediscovered, suppressed.
- Re-exec probe child-mode mis-selection on an inherited env marker — already Open
  as MM-BUG-KILN-00202 (distinct from finding 5, which is the parent trusting a
  child that ran nothing).
- Checked and sound (devil's-advocate lens, lead-verified): index arithmetic
  (china 0-19, crash 20-31, splash 32-35) pinned by
  `every_bank_take_resolves_through_this_crates_source`; `SAMPLE_RATE_HZ`/`Bank`
  divergence impossible (re-exported, not redeclared); cross-crate basename
  shadowing covered by `payload.rs`'s `no_two_asset_crates_ship_the_same_wav_basename`;
  the packaged `include` list ships everything the crate's own tests need;
  GM routing 49/57/52/55 and the `=0.2.0`/`=0.1.0` publish pins correct.
- Nits not worth tracking: "these banks banks" doc typo (`src/lib.rs:403-404`);
  `inventory_matches_packaged_samples` ignores non-wav/flac stray files in
  `samples/` (they would still be packaged by `include`).

## Process notes

- Bash and Workflow tools were denied to the lead session; `deltic bugs new` was
  run through a worker agent instead (allocation only — content written by the
  lead). Nothing was hand-numbered.
- deltic minted these eight IDs with host token `KIL`, while every earlier record
  on this box says `KILN` (sequence continued 00303+ across the token change, and
  concurrent passes minted KILN-00301/00302 the same morning). Filed as allocated;
  worth a human glance at the host-token derivation.

## Coverage

Reviewed `crates/ferrosintesis-samples-drumkit2/` in full (source, tests,
manifest, docs, licence) plus its consumer seams in `sampler.rs`/`live.rs`.
Coverage artifact: `wrk_docs/review-coverage/20260819-REV-MM-CLA@KILN-code-review-101941.md`.

Next passes: the main ledger (`wrk_docs/review-coverage.md`) has every row at
2026-07-18 (now stale at 32 days) and lacks rows for
`crates/ferrosintesis-flac/`, `-samples-b1-upright/`, `-samples-bass/`,
`-samples-bottle/`, `-samples-ccby/`, `-samples-fretnoise/`, `-samples-mandolin/`
and `-samples-rain/` — those never-reviewed areas rank first, with
`crates/ferrosintesis-flac/` the highest-risk (a hand-written decoder every asset
crate now routes through).
