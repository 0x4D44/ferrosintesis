# hollowsynth realtime / mddosem design journal

## 2026-07-08

- Created dedicated worktree `D:\worktrees\midi-music\20260708-TSK-HUM-hollowsynth-realtime-mddosem-design` from `origin/main`.
- Read repo instructions and `lessons_learnt.md`.
- Inspected current `hollowsynth` structure: CLI-only binary, SMF parser, offline engine loop, voice render trait, embedded lazy samples.
- Inspected the pending v0.9 worktree at `D:\worktrees\midi-music\20260708-DEV-HUM-hollowsynth-v0-9-brass-reeds-orchestral` enough to understand likely engine/instrument collision risk.
- Inspected `mddosem`'s synth contract: raw MIDI bytes via `MidiWriter`, additive stereo blocks via `MidiSynth::generate_samples`, and threaded synth support through `ThreadedSynth`.
- Confirmed design intent: make `hollowsynth` a standalone live MIDI/block-render library that `mddosem` can wrap, without making `hollowsynth` depend on `mddosem`.
- Drafted the initial HLD with scope, non-goals, API sketch, engine refactor boundary, parser requirements, downstream mddosem fit, tests, and risks.
- Ran three targeted adversarial design reviews and a refutation pass using the same three reviewers.
- Folded survived findings into the HLD: private live-command boundary, concrete `render_add` error contract, parser reset behavior, `Send` requirement, mddosem threaded latency caveat, reset matrix, stronger parser edge cases, controller-heavy chunk tests, and stronger offline regression gates.
- Ran the required external Claude Opus review in read-only plan mode. `--add-dir` failed in this environment with "No messages returned from query", so the successful pass reviewed the HLD from the current worktree and relied on the HLD's embedded mddosem evidence.
- Folded Claude's useful findings into the HLD: fixed-64 output-ring mechanism for chunk invariance, CC121 program-default re-derivation, high-nibble channel length parsing, and sparse/dense realtime gain calibration.
- Implemented the signed-off V1: library root, offline wrapper, realtime raw-MIDI API, live parser, `EngineCore` extraction, sample prewarm, `Voice: Send`, reset/controller handling, realtime output limiter, and scoped realtime tests.
- Full `cargo test` passed after the first implementation pass.
- Independent code review found CC121 over-reset, CC121 active-voice stale state, extended-SysEx false reset, version bump, and two optional API hardening items. Applied all of them.
- Final local gate passed in `fable5/hollowsynth`: `cargo fmt --check`, `cargo clippy --all-targets -- -D warnings`, `$null | cargo test`, and `cargo build --release`.
- Arthur approved the HLD. Marked it `SIGNED-OFF`; implementation can start from this scope.
