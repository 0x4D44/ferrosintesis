# 2026.07.08 — JRN — hollowsynth synth-lead voice (GM 80–87)

## Task
Turn the gap-audit finding "synth leads 80–87 are missing (render as a warm
pad)" into a shipped change. Lean end-to-end process (Arthur): understand →
design → adversarial review → external (Codex) review → sign-off (auto-approved)
→ implement (tests-first) → code review + simplify → validate → commit. Then
capture the remaining ~15 gaps as reqs.

## Scope locked
Arthur picked **synth leads 80–87** from a 3-way scope question. Ambition
question resolved: **not** a 10-way parallel build (all changes share
`voices::make` + engine tables as a bottleneck and collide with the live
brass/reed agent); instead build leads now and capture the rest as reqs.

Safety posture: program 81's two albums (*Sub Rosa*, *Winter Guests*)
intentionally re-render; everything else byte-identical. Accepted under the
"dev sandbox / no back-compat / no feature flags" steer.

## Worktree
`D:/worktrees/midi-music/20260708-DEV-HUM-hollowsynth-synth-lead-voice`
branch `task/20260708-DEV-HUM-hollowsynth-synth-lead-voice` off origin/main.

## UNDERSTAND (done)
Read the trait (`voices.rs:26`), `SawStack`/`pad`/`strings`/`choir`
(`:1395–1660`), dispatch (`:2044`), engine families (`:61`), CC1 vibrato
per-block (`:1011`), CC68 legato gate (`:694`), portamento (`:748`),
`fx_profile` (`:253`), and all of `dsp.rs`. Key levers confirmed:
- CC1 vibrato = add `80..=87` to `vibrato_family`; engine pushes it through the
  working `SawStack::set_pitch`.
- CC68 legato = implement `SawStack::legato_to`; engine already calls it.
- Square = new `BlepPulse` (two polyBLEP edges) + a `LayerOsc` enum.
- Byte-identical for pads is structural: `pad()` + `88..=95` dispatch untouched;
  `new()` delegates to `new_wave(Wave::Saw)` keeping RNG draw order.

Test infra for oracles: `testutil::centroid`/`peak_locate`, engine
`render(&song, &test_opts(sr))` + `cycle_freq_spread` (mirror
`cc1_mod_wheel_adds_vibrato`, `engine.rs:1712`).

## DESIGN (done)
`wrk_docs/2026.07.08 - HLD - hollowsynth synth-lead voice GM 80-87.md`.
Size M (~120 LOC across dsp/voices/engine). 8 oracles.

## REVIEWS (done)
- **Adversarial (2 lenses, refuted vs code):** survivors R1 release-oracle
  unsatisfiable (release 0.16→0.10), R2 attack-oracle differential, R3/R4 beat-fooled
  measurements (≥1 s window, calibrate), R5 add Q, R6 add committed byte-identity
  guards + RNG-draw-order pin. Cleared: BlepPulse signs, CC68-gate-never-implicit,
  vibrato_family inert-when-unauthored, fx_profile/dispatch clean.
- **External (Codex gpt-5.5 xhigh, read-only, 1 pass):** no critical. X1 the golden is
  tolerance-based not bit-exact → use exact `to_bits()` hashes pinned from baseline;
  X2 gate `legato_to` to lead instances (`lead_legato` flag) → byte-identity is now
  *literal* not corpus-only; X3 CC68 oracle → engine-level; X4 square oracle → split
  raw-BlepPulse + routing; X5 RMS-window envelope; X6 BlepPulse signs OK; X7 keep 86/87
  reqs explicit. All folded into the HLD (§9).

Design **SIGNED-OFF** (auto-approved). Baseline binary captured from clean HEAD
(== origin/main) before touching src/.

## Next
Implement tests-first to the 8 oracles → code review + simplify → validate
(baseline cmp + pin golden hashes + fmt/clippy/test) → commit + version bump.
Then reqs-capture the remaining ~15 gaps.
