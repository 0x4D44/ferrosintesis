# 2026.07.08 - JRN - reqs loop MM-REQ-KILN-00007

Manual reqs-loop pass for `MM-REQ-KILN-00007`, a light hollowsynth requirement to split GM104/106/107 out of the shared BANJO preset while keeping GM105 unchanged.

Claimed the req as `codex-gpt5@KILN` on branch `task/20260708-TSK-HUM-reqs-loop-mm-req-00007` in worktree `D:\worktrees\midi-music\20260708-TSK-HUM-reqs-loop-mm-req-00007`.

Implemented three new pluck presets in `fable5/hollowsynth/src/voices.rs`: `SITAR`, `SHAMISEN`, and `KOTO`. Routing is now 104 -> SITAR, 105 -> BANJO, 106 -> SHAMISEN, 107 -> KOTO. Updated the GM routing guard and README table, and bumped hollowsynth from 0.8.2 to 0.8.3 because shipped synth code changed.

Oracle work:

```powershell
$null | deltic timeout 120 cargo test sitar_shamisen_koto_have_distinct_pluck_presets --manifest-path fable5/hollowsynth/Cargo.toml
```

Red proof was run in `D:\worktrees\midi-music\20260708-TMP-HUM-mm-req-00007-red` at claim commit `e0866db` with the final oracle inserted only. It failed because program 104 still returned `BANJO` instead of `SITAR`.

The first green oracle compared each program with a different pluck seed. The oracle-fit skeptic flagged that as a real weakness because the seed affects pick position, brightness, decay, and excitation noise. I tightened the oracle to render all compared programs under three shared seeds at the same pitch, then reran red and green. The final exact oracle output recorded in the req passed with `1 passed; 0 failed; 94 filtered out`.

Validation run after the final oracle change:

```text
deltic timeout 120 cargo fmt --check --manifest-path fable5/hollowsynth/Cargo.toml
PASS

$null | deltic timeout 300 cargo test --manifest-path fable5/hollowsynth/Cargo.toml
PASS: 93 passed; 0 failed; 2 ignored

$null | deltic timeout 300 cargo clippy --manifest-path fable5/hollowsynth/Cargo.toml --all-targets -- -D warnings
PASS

deltic timeout 300 cargo build --release --manifest-path fable5/hollowsynth/Cargo.toml
PASS
```

Existing-MIDI scan found no tracked use of GM104/106/107. GM105 appears in two tracked MIDIs and both are banjo tracks, so preserving 105 -> BANJO is the compatibility-critical case.

Prior-render proof used a baseline worktree and the current worktree to render `fable5\Hollow Hill\midi\02 - Hollow Hill, Part Two.mid`, one of the GM105 users. Both renders had SHA-256 `8B842ED489EBFD719B3189B62F2EA69B9CA7C1DBC7F02DEB5DA8403FD024C479`, and `cmd /c fc /b` reported no byte differences.

Adversarial review:

- Oracle-fit reviewer confirmed the oracle measures rendered timbre at matched pitch, and its seed-fairness finding was fixed.
- Regression reviewers confirmed 104/106/107 are unused in tracked MIDI, 105 stays BANJO, and the routing guard pins the mapping.
- Landing-hygiene reviewer correctly found the branch was not landable before the final req transition, oracle-output recording, gates, and artifact cleanup.

The req landing commit uses a temporary `PENDING_SHA` marker in the state history because a commit cannot contain its own hash. A req-only follow-up replaces it with the real implementation commit SHA.
