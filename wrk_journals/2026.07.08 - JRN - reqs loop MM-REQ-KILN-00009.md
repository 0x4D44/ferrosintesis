# 2026.07.08 - JRN - reqs loop MM-REQ-KILN-00009

Implemented `MM-REQ-KILN-00009`, extending SawStack strings and choir to answer authored CC1 vibrato and CC68 legato.

What changed:
- Widened `vibrato_family` to include GM 48-54.
- Replaced the lead-only SawStack legato flag with a `legato_enabled` flag set by strings, choir, and leads; pads stay outside CC68 slurs.
- Added the named Gate 2 oracle `strings_choir_cc1_vibrato_and_cc68_legato_are_opt_in`, covering every GM 48-54 program for CC1 vibrato, no-change on CC1=0, CC68 slur voice count, and retuned slur pitch.
- Updated the hollowsynth README and bumped hollowsynth from 0.8.2 to 0.8.3.

Validation notes:
- The strengthened oracle fails on the claimed baseline because program 48 has no CC1 response.
- The final oracle and full hollowsynth gate pass are green.
- A MIDI scan found 13 committed files already author CC1/CC68 while on GM 48. That means those renders are expected to change, but they are inside the authored-controller exception.
- A no-authored-controller prior render (`fable5/Heliopause/midi/01 - Heliopause, Part One.mid`) is byte-identical between the baseline and current release binaries.

Review notes:
- The first adversarial oracle review found the oracle was too narrow. I strengthened it before landing.
- The compatibility review independently confirmed the existing opt-in exception set and found no inherited-controller leak.
- The landing-hygiene review confirmed this branch will conflict with the parallel v0.9 worktree in the shared hollowsynth files; integration should sequence and rebase deliberately.
