# 2026.07.08 - JRN - reqs loop MM-REQ-KILN-00010

Manual reqs-loop pass for `MM-REQ-KILN-00010`, a light hollowsynth requirement to extend CC70 vowel morphing to the choir-pad GM program 91 while preserving the authored-channel invariant.

Claimed the req as `codex-gpt5@KILN` on branch `task/20260708-TSK-HUM-reqs-loop-mm-req-00010` in worktree `D:\worktrees\midi-music\20260708-TSK-HUM-reqs-loop-mm-req-00010`.

Implemented:

- Added `engine::vowel_family()` so CC70 retargeting includes programs 52-54 and 91.
- Left `voices::make(91, ...)` on the existing lowpass `pad()` path unless CC70 is authored.
- Added `SawStack::vowel_morph_start`; for program 91, the first `set_vowel()` converts the existing lowpass stack to a formant stack. This avoids future-controller lookahead and keeps pre-CC70 audio unchanged.
- Added the named oracle `choir_pad_91_cc70_vowel_morph_opens_formants`, including a late-CC70 case that bit-compares the pre-controller window against a no-CC70 render.
- Updated README and bumped hollowsynth for the shipped synth change.
- Post-integration update: rebased the branch onto `origin/main` at `2c60131`
  after `MM-REQ-KILN-00007` landed. The implementation commit is now
  `df0ae7daa90a8f3b4f9fa13bce4aa13346e8ada3`, and the package version is now
  `0.8.12` because trunk already carried `0.8.11`. The rebase kept the
  accumulated GM voice/routing additions and merged the README SawStack row.

Red proof:

```powershell
$null | deltic timeout 120 cargo test choir_pad_91_cc70_vowel_morph_opens_formants --manifest-path fable5/hollowsynth/Cargo.toml
```

Run in `D:\worktrees\midi-music\20260708-TMP-HUM-mm-req-00010-red` at claim commit `2851206` with only the final oracle inserted. It failed because baseline program 91 ignored CC70: `choir-pad vowel didn't open: mm 0.006643727572020437 vs ah 0.006643727572020437`.

Green oracle:

```powershell
$null | deltic timeout 180 cargo test choir_pad_91_cc70_vowel_morph_opens_formants --manifest-path fable5/hollowsynth/Cargo.toml
```

Passed after rebase with `1 passed; 0 failed; 113 filtered out`.

Validation:

```text
deltic timeout 120 cargo fmt --check --manifest-path fable5/hollowsynth/Cargo.toml
passed (no output)

$null | deltic timeout 300 cargo test --manifest-path fable5/hollowsynth/Cargo.toml
test result: ok. 112 passed; 0 failed; 2 ignored; 0 measured; 0 filtered out; finished in 8.58s

$null | deltic timeout 300 cargo clippy --manifest-path fable5/hollowsynth/Cargo.toml --all-targets -- -D warnings
Finished `dev` profile [unoptimized + debuginfo] target(s) in 1.00s

deltic timeout 300 cargo build --release --manifest-path fable5/hollowsynth/Cargo.toml
Finished `release` profile [optimized] target(s) in 3.61s
```

Existing-MIDI scan parsed 53 tracked `.mid` files with no parse errors, no program-91 files, and no CC70 while program 91 was selected. Eleven files author CC70, so `fable5\Heliopause\midi\01 - Heliopause, Part One.mid` was used as the prior-render proof. After rebasing, `git diff --name-only 4696cdb c0cd5cd -- "*.mid"` showed no tracked MIDI changes, and before this integration push `git diff --name-only 4696cdb origin/main -- "*.mid"` was also empty, so the scan still applies. I reran the prior-render proof against baseline worktree `D:\worktrees\midi-music\20260708-TMP-HUM-mm-req-00010-baseline-rebase` at `c0cd5cd`; baseline and current renders had SHA-256 `E6595EEB10020827C7B422463DD0B93F59C6C806B57F1213B1157AC310C69F6D`, and `cmd /c fc /b` reported no differences.

I attempted a fresh inline Python MIDI scan via `deltic timeout 120 python -`, but `deltic timeout` did not pass the here-string as Python stdin, so Python entered its interactive reader and timed out. I did not use that failed run as evidence.

Adversarial review:

- Oracle-fit skeptic found a real flaw in the first implementation: whole-song lookahead made program 91 use the new formant voice before CC70 arrived. Fixed by converting inside `SawStack::set_vowel()` only when CC70 is actually authored; reviewer confirmed the temporal issue fixed.
- Compatibility skeptic verified the prior MIDI scan remains valid because no tracked MIDI changed between the scan base and `c0cd5cd`, then reran the Heliopause baseline/current render; `fc /b` reported no differences.
- Rebase skeptic checked there are no conflict markers, that `EngineCore::note_on` and `EngineCore::render_block_add` both use `vowel_family(program)`, and that the CC121 reset path still does not convert program 91 without CC70 authoring.
- Landing-hygiene skeptic confirmed the branch was rebased, the implementation SHA was recorded, generated WAVs were not staged, and the future integration conflict with the v0.9 branch is real.

The req state history now records the rebased implementation commit `df0ae7daa90a8f3b4f9fa13bce4aa13346e8ada3`.
