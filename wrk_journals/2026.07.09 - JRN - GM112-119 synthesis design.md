# 2026.07.09 - JRN - GM112-119 synthesis design

Goal from Arthur: design and then, after explicit sign-off only, implement
high-quality default-on synthesis for GM 112-119. These are the remaining
melodic percussion GM programs currently falling through to the steel-guitar
fallback.

Scope confirmed in one line: replace the GM112-119 steel-pluck fallback with
program-specific modeled voices for Tinkle Bell, Agogo, Steel Drums, Woodblock,
Taiko Drum, Melodic Tom, Synth Drum, and Reverse Cymbal.

Read the repo instructions and lessons. Relevant constraints:

- Work in this task worktree, not the main clone.
- Design doc and journal live under `wrk_docs/` and `wrk_journals/`.
- Stop at the sign-off gate before implementation.
- Use real numeric audio oracles; this repo has no ears.

Code context:

- `voices::make` routes 120-127 to `SfxNoise`, 104-111 to modeled/pluck/reed
  voices, and then falls through to `Pluck::new(&STEEL, ...)`, so 112-119 are
  currently wrong.
- `Modal` already supports deterministic modal partials, strike noise, note-off
  release, pitch bend, and a strike pitch-glide hook.
- `bell()` / `wood_bar()` cover struck pitched idiophones.
- `drums.rs` contains useful acoustic patterns for membranes/cymbals, but its
  builders are private and channel-10-specific. Reusing the ideas in `voices.rs`
  is cleaner than making drum internals public.
- `Reverse Cymbal` is the only program that needs a new non-modal one-shot
  voice because it needs a crescendo envelope.

Draft design direction:

- Keep all new melodic-program work in `voices.rs`.
- Add small modal helpers and tables for 112-118.
- Add one `ReverseCymbal` voice for 119.
- Extend routing/readme/tests. No feature flag, no opt-in seam.

Awaiting design review and external review before presenting for sign-off.

Adversarial design review:

- Two reviewers inspected the HLD. I did a local third pass.
- Surviving findings:
  - require per-program audio signatures, not only family-level signatures;
  - specify fixed oracle keys/windows/search bounds/tolerances;
  - avoid centroid-only brightness checks;
  - add existing-album blast-radius validation for this default-on `voices.rs`
    change;
  - add short-note lifecycle tests so melodic-channel note-off does not choke
    struck/effect voices.
- Refuted findings:
  - Reverse Cymbal may be intentionally pitch-agnostic; the main semantic oracle
    should be the reverse swell, not key sensitivity;
  - the HLD already guards against premature helper abstraction.

Fable consult:

- Read the Fable collaboration skill and used consult mode.
- Smoke test succeeded: Claude Code resolved `--model fable` and returned
  `FABLE_OK`.
- Full consult timed out without output. A narrower self-contained consult also
  timed out without output. No Fable design advice was folded in.
- A recent Fable/Claude child process did not respond cleanly to normal process
  cleanup commands; I stopped retrying after targeted bounded attempts to avoid
  widening the blast radius.

External review:

- Ran one Claude Opus read-only review after folding adversarial findings.
- It reported no critical findings and one major issue: comparison oracles must
  render every compared program at the same key, or they risk measuring register
  rather than timbre. Folded this into the HLD.
- Folded minor useful points: more explicit membrane-trio clone checks,
  Woodblock pitch-window relaxation, Reverse Cymbal peak/ceiling contract,
  frozen calibrated thresholds, and anti-glide checks for new glide voices.

Preliminary blast-radius scan:

- Ran a read-only parser over all 58 `.mid` files in the worktree.
- Found 0 program-change events for GM112-119.
- The HLD still requires the formal implementation-time scan and
  baseline-vs-current album byte-identity check before commit.

Implementation:

- Marked the HLD signed off after Arthur approved it.
- Added modal voices for GM112-118 and a dedicated `ReverseCymbal` voice for
  GM119.
- Routed programs 112-119 away from the steel fallback.
- Added `gm112_119_melodic_percussion_are_modeled`, covering routing, audible
  output, per-program identity, pitch/glide behavior, short-note lifecycle,
  GM119 key invariance, and GM119 eventual death.
- Extended `gm_routing_pins_voice_kinds` and kept the GM120-127 SFX fallback
  guard green.
- Documented the new GM112-119 coverage in the README and bumped shipped
  versions.

Code review:

- Independent review found two required issues.
- Issue 1 was valid: the release helper rendered the whole tail in one call
  instead of modelling the engine's block-by-block removal path. Fixed by
  rendering released tails in 128-sample blocks and adding explicit GM119
  alive-through-swell / eventually-dead assertions.
- Issue 2 was valid: formal blast-radius scan and album byte-identity validation
  needed to be run and recorded before final commit readiness.
- Optional suggestion to split the large GM112-119 oracle is skipped for now:
  the single test is intentionally a focused acceptance oracle for one GM range,
  and the failure messages are program-specific.

Rebase:

- Rebasing onto current `origin/main` brought in the default drum-kit work and
  version `0.10.8`.
- Updated this task's shipped-code bump to `0.10.9`, keeping both crate manifests
  and `Cargo.lock` in lockstep.

Formal blast-radius validation:

- Re-ran the MIDI program-change scan after implementation: 58 `.mid` files
  scanned, 0 GM112-119 program changes found.
- Added a detached baseline worktree at
  `D:\worktrees\midi-music\20260709-BASE-CDX-gm112-119-bytecheck`, checked out at
  `origin/main` (`68c7893`).
- Built release binaries in both baseline and task worktrees.
- The first selected byte-check track,
  `albums/gpt5-5/The Long Turning/midi/01 - The Long Turning.mid`, did not finish
  within a 300 s timeout in either binary, so it was not used as evidence.
- Rendered the single-track album `albums/fable5/midi/The Iron Tide.mid` with
  both binaries. `fc /b` reported no differences between the WAVs.
- Matching Iron Tide WAV SHA-256:
  `04FC94400702502370C90F4449332294A271F6B72C671E2E564981B690CFC4A8`.
- Also rendered `albums/opus4-8/midi/07 - Snow.mid` with both binaries. `fc /b`
  reported no differences between the WAVs.
- Matching Snow WAV SHA-256:
  `CF6517BEBFB423A4894D5D8F6EE23EA0F6E66825BD8230E8CD607CE9E4C95F46`.
