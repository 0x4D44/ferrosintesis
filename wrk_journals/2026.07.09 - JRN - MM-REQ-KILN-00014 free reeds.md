# 2026.07.09 - JRN - MM-REQ-KILN-00014 Free Reeds

## 2026-07-09

- Started task worktree `D:\worktrees\midi-music\20260709-TSK-HUM-reqs-heavy-00014-reed-organ-accordion-ha` from updated `origin/main` after integrating `MM-REQ-KILN-00013`.
- Read `CLAUDE.md`, `lessons_learnt.md`, `reqs/MM-REQ-KILN-00014.md`, the current `Organ` implementation, `make()` routing, engine CC1 routing, existing organ/Leslie tests, and the Winter Guests GM20 harmonium use.
- Confirmed intent: GM 20-23 should sound like free reeds rather than pipe/Hammond organs, and GM22 harmonica should take CC1 as pitch vibrato rather than a Leslie tremulant ramp.
- Wrote draft HLD and ran three independent design reviewers. Their material findings survived cross-check: the original oracle language was too qualitative, GM21/GM23 CC1 routing was undecided, GM16-19 unchanged behavior needed direct proof, GM22 needed program-change/reset/held-note/alt-bank routing coverage, and Winter Guests needed full-render health checks beyond stem containment. Folded those into the HLD. Bounded the suggestion to create a separate `FreeReed` type: allowed if `Organ` extension gets awkward, but not required up front.
- Ran the required one-pass external review with Claude Opus in read-only plan mode. It found one significant validation miss: RIVERWAKE uses GM21 accordion, so Winter Guests is not the only committed free-reed user. Folded in targeted RIVERWAKE GM21 changed-stem validation and non-GM21 identity validation. It also flagged musette/scoop oracle false-positive risks already addressed by numeric windows and composition tests. Current objective says completed designs are auto-approved, so marked the HLD signed off for implementation.
- Added focused red tests before implementation. `reed_organ_accordion_harmonica_have_free_reed_character` fails because GM20's onset click ratio is effectively the same as GM19 (`0.0518` vs `0.0524`). `gm22_cc1_is_harmonica_vibrato_not_leslie` fails because GM22 CC1 has no pitch spread (`0.02 Hz`, plain `0.01 Hz`). This confirms the tests distinguish the current generic-organ routing.
- Implemented the free-reed split inside the existing `Organ` voice: GM20 gets slower harmonium-style reed ranks with bellows/rattle energy; GM21/GM23 get detuned musette ranks; GM22 gets stronger breath/rattle plus a one-way pitch scoop that composes with `set_pitch`. Split engine CC1 routing so GM16-19 still use Leslie, GM22 uses default-bank pitch vibrato, and GM20/21/23 CC1 is inert. Focused voice and engine tests are green after tuning the oracles away from single-bin/noisy AM false positives.
- Code review found a real held-note bug: CC1 routing used the channel's current program, so a program change could move an already-held GM19/GM22 voice into the wrong controller family. Fixed by storing each active voice's spawn-time program and routing CC1/alt-bank exclusions per voice. Review also found the first GM16-19 unchanged oracle was not independent; replaced the duplicated-constructor fixture with frozen render hashes. Targeted re-review reported no required changes.
- Validation: `cargo fmt --check`, `cargo clippy -- -D warnings`, release build, and `cargo test -- --skip altbank::tests::sawstack_v1_canary_frozen` all pass. The skipped altbank canary is the known pre-existing clean-trunk failure from the prior task.
- Render validation: Winter Guests `python build.py --verify` passes. Part One channel 4 harmonium changes (`D78734...` baseline vs `193655...` current) while channel 0 is byte-identical (`2B3A18...` both). Current Part One full render passes `analyze.py --track 1` with zero clicks and zero interior silence. RIVERWAKE channel 8 accordion changes (`40399D...` baseline vs `B46E14...` current) while channel 0 is byte-identical (`6D8D37...` both).
