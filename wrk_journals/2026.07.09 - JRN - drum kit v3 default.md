# Journal - drum kit v3 default

Arthur asked to improve ferrosintesis drums end to end with a lean gated design
flow, making the best drum kit the default rather than preserving old album drum
byte identity.

## 2026-07-09

- Started `task/20260709-TSK-HUM-drum-kit-v3-default` in
  `D:\worktrees\midi-music\20260709-TSK-HUM-drum-kit-v3-default` using
  `deltic start-task`. The main clone at `D:\language\midi-music` had unrelated
  untracked demo files; I left them alone.
- Read repo instructions and lessons. The authored-channel invariant is a known
  default for synth growth, but this task explicitly waives it for drums because
  the product goal is better defaults.
- Read the relevant render path:
  - `engine.rs` initializes `Strip.kit` as `Kit::V1`;
  - channel-10 Program Change only switches to `Kit::V2` for non-zero programs;
  - drums are spawned by `drums::make(key, vel, sr, seed, kit)`;
  - samples are currently only passed to melodic voices.
- Read `drums.rs`: V2 improves crash 49/57, toms, open hat 46, and snare 38/40,
  while many cymbal keys remain close to the old `CymSpec` model.
- Read `sampler.rs` and `samples/prepare.py`: the LA layer is default-on,
  disabled by `--no-samples`, and currently has no drum samples.
- Checked the VSCO 2 CE GitHub tree enough to confirm CC0 percussion assets
  exist for cymbal, bass drum, and snare sample overlays.
- Drafted the HLD at
  `wrk_docs/2026.07.09 - HLD - ferrosintesis drum kit v3 default.md`.
- Ran the lean internal adversarial design review with three helper agents:
  DSP realism, Rust integration/testability, and repo/artifact scope. Sent their
  combined findings back through the same three reviewers for refutation.
- Folded the surviving findings into the HLD: exact cymbal-oracle shape,
  sample source pinning, engine-level sample tests, non-zero Program Change
  acceptance, artifact target inventory, Synth Feature Showcase freshness,
  performance guard, and reproducible before/after stem commands.
- Ran the single external fresh-eyes review with Claude Opus in read-only plan
  mode. It found no critical issues and one major gap: the default samples-on
  cymbal path needed timbre guards, not just engagement tests. Folded that into
  the HLD, along with mode-count wording, repeatable drum-check MIDI generation,
  and a note that dense ride patterns are the CPU hot path.
- Arthur approved the design. Marked the HLD `SIGNED-OFF` and moved to
  implementation.
- While adding drum samples, the first pinned VSCO generation run timed out and
  left a partial cached `piano_C2_f_rr2.wav`. A second run reused it and failed
  in `read_wav`. Arthur asked to bring in Fable 5. I used a narrow consult for
  this hard cache/validation call. Fable diagnosed partial-cache poisoning plus
  a strict-length gap that could silently accept truncated 24-bit inputs, and
  returned an atomic-download/self-heal design with a stdlib regression oracle.
  Captured that as `wrk_docs/2026.07.09 - HLD - VSCO sample cache healing.md`.
- Implemented the pinned sample-cache fix and generated the eight drum WAV
  overlays from VSCO 2 CE. `python -m unittest test_prepare.py` passes in
  `crates/ferrosintesis/samples`.
- Added V3 drum code: default `Kit::V3`, dense MetalPlate cymbals for crash,
  ride, china, and splash, V3 kick body/click cleanup, and a small sample overlay
  for crash/kick/snare. Focused Rust tests for `drums::tests`, sampler tests,
  and the engine channel-10 sample/default-kit seams are green.
- Rebuilt and verified `demos/synth_feature_showcase`; the showcase no longer
  requires a non-zero channel-10 Program Change for drums.
- Created the deterministic before/after MIDI with
  `target/drum-kit-v3/make_drum_check.py` and rendered:
  - `target/drum-kit-v3/before-origin-main.wav` — peak 0.8910, RMS 0.0441;
  - `target/drum-kit-v3/after-v3-samples.wav` — peak 0.8910, RMS 0.0432;
  - `target/drum-kit-v3/after-v3-no-samples.wav` — peak 0.8910, RMS 0.0434.
	  Baseline was built from detached worktree
	  `D:\worktrees\midi-music\20260709-TMP-CDX-drum-kit-v3-before-origin-main`
	  at `origin/main` (`5b752e6662a165986388f228cb4b38e636002c3f`).
- Scanned `render_opus.py::ALBUMS` for committed listening copies whose source MIDI
  uses channel-10 drums. Refreshed exactly 31 tracked `listening/*.opus` files and
  confirmed `git ls-files --others --exclude-standard listening` is empty.
- The first targeted Opus helper used `%TEMP%`, which is on `C:`, and Windows
  refused atomic replacement into the `D:` worktree. Re-ran with temp files under
  `target/drum-kit-v3/tmp-opus` on the same drive.
- Full-mix spot-check WAV metrics against the origin/main baseline:

  | Track | Before peak | After peak | Before RMS | After RMS | Duration before/after |
  |---|---:|---:|---:|---:|---:|
  | Sub Rosa | 0.8910 | 0.8910 | 0.1005 | 0.0940 | 459.1s / 459.1s |
  | Burning Meridian | 0.8910 | 0.8910 | 0.0737 | 0.0705 | 189.3s / 189.3s |
  | Ninth Bell | 0.8910 | 0.8910 | 0.0540 | 0.0578 | 350.6s / 350.6s |
  | Tuxedo Noir | 0.8910 | 0.8910 | 0.1092 | 0.1049 | 190.6s / 190.6s |
  | Showcase Ignition | 0.8910 | 0.8910 | 0.0731 | 0.0682 | 153.6s / 153.6s |
- Code review found one small real issue: `sampler::SampleOverlay` did not forward
  every `Voice` controller method even though its doc promised transparent routing.
  Fixed by forwarding vowel, breath, and vibrato controls to the wrapped model.
- Simplicity pass found no dead abstraction to delete. V2 references that remain are
  deliberate legacy tests/HLD context, not shipped default-kit behavior.
- Validation found two expected oracle updates after making V3 the default: the
  golden channel-10 fixture is brighter/louder, and the coarse stereo anti-collapse
  correlation threshold needed a small relaxation for the broadband V3 wash. Refreshed
  those oracles while leaving the explicit hats-left, kick/snare-centred, and mono-sum
  checks in place.
- Final gates run:
  - `cargo fmt --check`
  - `python -m unittest test_prepare.py`
  - `python prepare.py` with SHA-256 before/after check over `samples/*.wav`
  - `cargo test --workspace`
  - `cargo clippy --workspace --all-targets -- -D warnings`
  - `cargo build --release -p ferrosintesis-cli`
  - `python build.py --verify` in `demos/synth_feature_showcase`
  - `git diff --check`
