# Journal - drum kit v3 default

Arthur asked to improve ferrosintesis drums end to end with a lean gated design
flow, making the best drum kit the default rather than preserving old album drum
byte identity.

## 2026-07-09

- Started `task/20260709-TSK-HUM-drum-kit-v3-default` in
  `D:\worktrees\midi-music\20260709-TSK-HUM-drum-kit-v3-default` using
  `deltic start-task`. The main clone at `D:\language\midi-music` had unrelated
  untracked demo files; I left them alone.
- Read repo instructions and lessons. The authored-channel invariant is a known
  default for synth growth, but this task explicitly waives it for drums because
  the product goal is better defaults.
- Read the relevant render path:
  - `engine.rs` initializes `Strip.kit` as `Kit::V1`;
  - channel-10 Program Change only switches to `Kit::V2` for non-zero programs;
  - drums are spawned by `drums::make(key, vel, sr, seed, kit)`;
  - samples are currently only passed to melodic voices.
- Read `drums.rs`: V2 improves crash 49/57, toms, open hat 46, and snare 38/40,
  while many cymbal keys remain close to the old `CymSpec` model.
- Read `sampler.rs` and `samples/prepare.py`: the LA layer is default-on,
  disabled by `--no-samples`, and currently has no drum samples.
- Checked the VSCO 2 CE GitHub tree enough to confirm CC0 percussion assets
  exist for cymbal, bass drum, and snare sample overlays.
- Drafted the HLD at
  `wrk_docs/2026.07.09 - HLD - ferrosintesis drum kit v3 default.md`.
- Ran the lean internal adversarial design review with three helper agents:
  DSP realism, Rust integration/testability, and repo/artifact scope. Sent their
  combined findings back through the same three reviewers for refutation.
- Folded the surviving findings into the HLD: exact cymbal-oracle shape,
  sample source pinning, engine-level sample tests, non-zero Program Change
  acceptance, artifact target inventory, Synth Feature Showcase freshness,
  performance guard, and reproducible before/after stem commands.
- Ran the single external fresh-eyes review with Claude Opus in read-only plan
  mode. It found no critical issues and one major gap: the default samples-on
  cymbal path needed timbre guards, not just engagement tests. Folded that into
  the HLD, along with mode-count wording, repeatable drum-check MIDI generation,
  and a note that dense ride patterns are the CPU hot path.
- Arthur approved the design. Marked the HLD `SIGNED-OFF` and moved to
  implementation.
