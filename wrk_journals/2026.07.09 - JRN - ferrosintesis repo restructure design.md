# Journal: ferrosintesis repo restructure design

## 2026-07-09

- Goal: design the repo restructure before implementation, with a hard sign-off
  gate before code changes.
- Created worktree:
  `D:\worktrees\midi-music\20260709-DOC-CDX-ferrosintesis-repo-restructure-design`.
- Read `CLAUDE.md`, `lessons_learnt.md`, root `README.md`, `render_opus.py`,
  `fable5/hollowsynth` manifests/API files, and the published placeholder under
  `crates/ferrosintesis`.
- Confirmed current facts:
  - real synth is still `fable5/hollowsynth` at version `0.9.2`;
  - `crates/ferrosintesis` is a `0.0.0` placeholder;
  - `render_opus.py` hardcodes the old nested synth binary path;
  - current synth package is 9.7 MiB uncompressed / 6.9 MiB compressed with the
    embedded sample bank, so crates.io package size is currently viable;
  - `ropus v0.12.18` is published and available.
- Wrote initial HLD at
  `wrk_docs/2026.07.09 - HLD - ferrosintesis repo restructure.md`.
- Ran the lean internal adversarial review with two helper reviewers and one
  refutation pass.
- Survivors folded into the HLD:
  - final crate version must be explicit (`0.9.3`);
  - `cargo package --no-verify` is too weak for publishable package validation;
  - do not add `Options::Default` because CLI delay defaults are song-derived;
  - validate every album key, not only a one-album render;
  - native Opus output needs `ogg` container writing as well as `ropus`;
  - prove audio preservation with an old/new WAV byte comparison;
  - root workspace owns `Cargo.lock`;
  - update `reqs/*.md` path references.
- Verified current album count from `render_opus.py`: 15 entries, 53 MIDI files.
- Checked local `ropus 0.12.18` source: examples use `ogg` for Ogg-Opus output
  and accept only 8/12/16/24/48 kHz without resampling.
- Attempted the required one-pass external review with Claude Opus in plan mode.
  It did not run because the CLI reported the weekly limit is exhausted until
  2026-07-13 02:00 Europe/London. Recorded this in the HLD; no code changes
  should start unless Arthur explicitly waives the external review or resumes
  after the limit resets.
- Arthur reported Claude is back; reran the one-pass external review with Claude
  Opus in plan mode.
- Folded Claude's useful findings into the HLD:
  - native CLI Ogg-Opus is deferred to a follow-up because it is orthogonal to
    the restructure and would not preserve the committed corpus reproduction path;
  - `render_opus.py` stays on external `ropusenc`, only the synth binary path
    changes;
  - `ferrosintesis-cli` is workspace-local / `publish = false`, so verifying
    package publication for it is not a gate;
  - the library manifest must keep the placeholder's license/metadata;
  - old/new WAV byte comparison uses a representative set, not one tiny smoke file;
  - album path validation becomes a bijection over `albums/**/*.mid`;
  - sample-bank tracking is explicitly verified with `git ls-files`;
  - reqs ledger edits should update live traceability/commands, not historical
    transcripts.
- Arthur approved the HLD. Marked it `SIGNED-OFF`; implementation may start in
  this worktree.
- Rebased the signed-off design branch onto current `origin/main`. The integrated
  synth version is now `hollowsynth 0.10.0`, so the HLD's one-patch-bump target
  changed from `ferrosintesis 0.9.3` to `ferrosintesis 0.10.1`.
- During implementation review, `origin/main` moved again to `hollowsynth 0.10.1`
  with the timpani strike-glide work. Rebased over it, confirmed the timpani code
  was carried into `crates/ferrosintesis/src/voices.rs`, and bumped the restructure
  target to `ferrosintesis 0.10.2`.
- Built the old `fable5/hollowsynth` release binary and rendered two baseline WAVs
  for byte comparison: one fable5 album track and one general-MIDI album track.
- Moved the synth into a root Cargo workspace:
  `crates/ferrosintesis` for the library and `crates/ferrosintesis-cli` for the
  offline WAV renderer named `ferrosintesis`.
- Moved the model album trees under `albums/` and updated `render_opus.py`,
  user-facing docs, crate docs, `.gitignore`, lessons, and live requirement
  ledger fields to the new paths.
- First workspace test run compiled after the move but exposed a pre-existing
  stale canary: `altbank::tests::sawstack_v1_canary_frozen` failed with the same
  deterministic pad(89) fingerprint on the clean main clone (`0x63cb52bc25cbdd4f`
  actual vs `0x66cf721ab5a542fb` expected). Re-pinned that expected test value in
  the moved crate; no render code changed.
- Code review survivors:
  - the first implementation pass was behind `origin/main` and would have dropped
    the newly integrated timpani strike-glide code;
  - the first implementation pass also had untracked workspace files before
    commit.
  Rebased over `origin/main`, confirmed
  `crates/ferrosintesis/src/voices.rs` still contains the timpani implementation,
  and committed the root workspace files explicitly.
- Simplicity pass kept the CLI as a thin wrapper around `ferrosintesis::offline`,
  kept `render_opus.py` on the existing external `ropusenc` flow, exposed only
  `live` and `offline` publicly from the library, and restored
  `#![forbid(unsafe_code)]` on both crates after confirming there is no unsafe
  code.
- Final validation after the rebase and cleanup:
  - `cargo fmt --all --check` passed.
  - `git diff --check` passed; Git only warned that touched Rust files will be
    normalized to CRLF when Git next writes them.
  - `python -m py_compile render_opus.py` and representative album build scripts
    passed.
  - `cargo test --workspace` passed: 176 passed, 4 ignored.
  - `cargo clippy --workspace --all-targets -- -D warnings` passed.
  - `cargo package -p ferrosintesis --allow-dirty` passed, packaging 91 files:
    9.7 MiB uncompressed / 6.9 MiB compressed.
  - `cargo build --release -p ferrosintesis-cli` passed.
  - `render_opus.all_midis()` still finds 53 MIDI files.
  - `git ls-files crates/ferrosintesis/samples/*.wav` finds 71 tracked sample
    WAVs, and `git ls-files fable5/hollowsynth` is empty.
  - Live-file stale-name scan over the workspace manifests, crate files,
    `render_opus.py`, README/CLAUDE, and fable5 album docs found no
    `hollowsynth` references.
  - Minimal `render_opus.py` smoke rendered one temporary `Tuxedo Noir` track to
    Opus through `target/release/ferrosintesis.exe` and `c:\apps\ropusenc.exe`;
    output size was 2,460,535 bytes.
  - Current-trunk old/new WAV byte comparison passed against `origin/main`
    commit `947638347e7bb2f24210508f8e6c7aac82efaa76`:
    `The Burning Meridian / 02 - Lanterns on the Water` matched SHA-256
    `D47C15886A7BD91C3C8D32C2CB4B1412BD56610E7FD5640632242C4D2AE7F496`, and
    `The Spark / 01 - The Quiet Before Dawn` matched SHA-256
    `623831A5FE29F824F0619C297EEE3EB32F1C576501F1C4DD990F678710694FBD`.
