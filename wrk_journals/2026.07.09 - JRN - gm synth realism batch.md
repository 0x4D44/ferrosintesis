# JRN — GM synth realism batch (task/20260709-DEV-HUM-gm-synth-realism-batch)

Worktree: D:/worktrees/midi-music/20260709-DEV-HUM-gm-synth-realism-batch (off origin/main @ 7edc8b0).
Driven by /loop (loop-build). Repo is the only memory — keep pointers current.

## Plan (in order)
1. **Drum stragglers** — model GM channel-10 keys 58 vibraslap, 67/68 hi/lo agogo,
   71/72 whistles, 73/74 guiro, 78/79 cuica in `crates/ferrosintesis/src/drums.rs`
   (currently fall to generic 1 kHz tick fallback). Agogo modal voice exists for
   GM113 melodic (`voices.rs::agogo`) — reuse the design, not necessarily the code.
   Add numeric oracles (pitch/timbre/decay) in the drums test section. NOTE: V3 kit
   is now the default (commit 68c7893); check both kit paths for the fallback.
2. **Poly aftertouch + CC2 breath** — parser currently DROPS 0xA0 (midi.rs ~L212);
   add EvKind::PolyAftertouch, engine per-note pressure target; CC2 breath as an
   expression-like scaler. Strictly opt-in (authored-channel invariant): channel
   that never authors them renders byte-identical. PROVE: build baseline binary in
   scratch `git worktree add <tmp> HEAD`, render all `render_opus.py::ALBUMS` MIDIs
   with both binaries, `cmp` byte-identity.
3. **LA attack layers for guitars/brass/reeds** — HLD first in
   `wrk_docs/2026.07.09 - HLD - LA attack layers guitars brass reeds.md`, following
   the pinned-VSCO pipeline precedent (`crates/ferrosintesis/samples/prepare.py`,
   samples/README.md). Then implement: sample bank per family in sampler.rs,
   crossfade into modelled sustain like piano/violin/flute. Opt-in is automatic
   (sampler layer changes voice output) — this DOES change sound of existing albums
   using those programs, so byte-identity waivers must be explicit; refresh
   listening assets for affected albums at the end.
4. **Strings/choir saw-stack realism** — ARCHITECTURAL FORK: write HLD only
   (`wrk_docs/`), then STOP the loop and ask Arthur. Do not implement.

## Gates (terminal b)
`cargo fmt --check`, `cargo clippy --workspace --all-targets -- -D warnings`,
`$null | cargo test --workspace` (stdin closed), byte-identity proof for untouched
albums (+ explicit waiver list for item 3), ONE version bump (ferrosintesis
Cargo.toml + Cargo.lock, currently 0.11.x after drum v3 — check), refreshed
listening assets where waived. Never push.

## State
- DONE: worktree minted; journal created.
- DONE: Item 1 @ 783e9e7 — all nine straggler keys modelled in drums.rs (shared
  kit-agnostic match; V1/V2/V3 identical by oracle), 6 new oracles, gate green
  (fmt/clippy/198 tests). Mechanism note: Tone glide now accepts negative
  (upward) rates for cuica; unconstructible before, so inert for existing voices
  (frozen-V1 fingerprint green).
- DONE: Item 2 @ 539e330 — poly aftertouch (EvKind::PolyAftertouch, per-note
  smoothed pressure lane mirroring channel AT; the two compose: dB lifts add,
  vibrato factors multiply) + CC2 breath (second squared expression scaler at
  the strip, also feeds the BR9 brass timbre). Strictly opt-in; CC121 resets
  both lanes (strip + per-note). Byte-identity proof: all 53 committed album
  MIDIs rendered with baseline (783e9e7) and patched release binaries —
  ALL 53 WAV pairs SHA256-identical, zero diffs (and a pre-scan found no
  album authoring 0xA0 or CC2). 3 new oracles (per-note selectivity on a
  violin double stop, CC2 backoff audibility on flute, never-authored
  neutrality incl. CC2=127 and poly-AT on a silent key, both bit-exact).
  Gate green: fmt --check, clippy -D warnings, 201 workspace tests. No
  version bump (batch bumps once at integration). Scratch baseline worktree
  and temp WAVs removed. Note: docs commit de84c47 (policy rewording) landed
  on the branch mid-task; controller features stay opt-in, so item 2 is
  unaffected.
- DONE: Item 3 HLD — `wrk_docs/2026.07.10 - HLD - LA attack layers guitars
  brass reeds.md`. Key calls: brass+reeds source from the SAME pinned VSCO rev
  (verified: Trumpet incl. straight mute, F Horn, Tenor Trombone, Tuba, Oboe,
  Clarinet, Bassoon all present; NO sax, NO guitar); guitars need a second CC0
  source (FreePats / Iowa MIS — licence is an OPEN QUESTION for Arthur, guitars
  phase last so brass/reeds don't wait); saxes 64-67, synth brass 62-63,
  electrics/distorted 26-31, basses 32-39 stay pure-model in v1; budget ≤ +8 MB
  on the current 10.5 MB payload; per-family (gain, fade): LA_GUITAR
  (0.05,0.20), LA_BRASS (0.10,0.32), LA_REED (0.06,0.24), all via existing
  LaVoice::wrap with the 0.5-2.05 repitch fallback.
- NEXT: Item 3 implementation, phase 1 BRASS (trumpet/mute/trombone/tuba/horn
  banks per HLD §2.1/§7): prepare.py entries, sampler.rs banks, LA_BRASS wrap
  for 56-61, oracles, render-diff inventory.
- WAIVER CHECK DONE (item 1): scanned every committed .mid in the repo for
  ch-10 note-ons on keys 58/67/68/71-74/78/79 — ZERO hits. Item 1 is
  asset-free: no album or demo renders differently, no listening refresh needed.
- Decisions: none yet.
- Surprises: main clone working tree is dirty with drum-v3-shaped uncommitted
  changes (another agent worked in the clone?) — reported to Arthur, not touched.
