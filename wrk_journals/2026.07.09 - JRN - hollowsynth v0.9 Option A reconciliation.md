# 2026.07.09 — JRN — hollowsynth v0.9 Option A reconciliation

Continuation of the v0.9 build (see `2026.07.08 - JRN - hollowsynth v0.9
implementation.md` for the feature-phase history + decisions). **Read this file
first each iteration**, advance ONE green unit, commit (atomic, explicit paths),
update Pointers.

## Why this branch exists (the pivot)

The v0.9 feature branch (`task/20260708-…-orchestral`, HEAD da72e32, safe at ref
`v09-backup-pre-rebase`) finished all phases gated-green. At finalize, `origin/main`
had moved **66 commits** ahead of the branch point (80bfcee) — a whole reqs-loop
pipeline that **independently built most of the v0.9 orchestral roadmap** through a
different channel. Concretely, trunk now ALREADY has:

- **Orchestra hit (GM 55)** — `orch_hit` (MM-REQ-KILN-00005). My OrchHit = duplicate.
- **SawStack reworked** — `enum Wave`/`LayerOsc(Saw/Pulse)`, `StackFilter::Formant`
  vowel-morph choir, per-layer `vib_rate`, `sweep`, CC68 `legato_enabled`,
  `set_trem`/`set_vowel` on the Voice trait. = trunk's strings/choir/bowed polish.
- **Fiddle (GM 40) → Bowed** (073ea2c), **choir CC70** (df0ae7d), kalimba, wood-bars,
  sfx fallbacks, synth-lead 80-87, realtime API, `dsp::BlepPulse` (±1 square, duty 0.5).

Trunk still does NOT voice **56-71** (falls to `_ => Pluck(&STEEL)`) and never touched
`drums.rs`. So Arthur chose **Path (a)**: land the clean, unique, non-colliding delta —
**brass 56-63 + reeds 64-71 + drums kit-v2** — on top of trunk; DROP my now-superseded
orch-hit/strings/choir/bowed polish (preserved at `v09-backup-pre-rebase`). The polish
returns later as a Bank-Select alt bank (see FOLLOW-UP).

## Method

New branch `task/20260709-DEV-HUM-hollowsynth-v0-9-brass-reeds-drums` off `origin/main`
(NOT a git merge — a clean semantic port, so trunk's work is never at risk). Lift only
the self-contained families from the backup branch's `voices.rs`:
`git show <OLD>:…/voices.rs` sections — Brass 2527-3234, Reed 3235-3620 (DROP OrchHit
3621-3762). Use trunk's `dsp::polyblep`; my variable-duty zero-DC pulse lands as a
distinct **`ReedPulse`** (trunk's `BlepPulse` is a square-only, duty-0.5 primitive — my
reed needs zero-DC-for-any-width, so keeping both is correct, not dup). `drums.rs` is
wholesale-mine (trunk untouched). `OLD` = `task/20260708-DEV-HUM-hollowsynth-v0-9-brass-reeds-orchestral`.

## Regression policy (relaxed, Arthur 2026.07.08)

Byte-identity of untouched committed renders is the DEFAULT + contamination tripwire,
not absolute. An UNEXPLAINED diff = investigate. An EXPLAINED, quality-improving diff =
acceptable + documented + flagged for Arthur's listen. Here the plan is byte-identity
for everything trunk already voiced (brass/reeds fill EMPTY slots; drums kit-v2 is an
opt-in seam no album authors) — so the contamination check should show ZERO diffs on all
committed albums. Any diff = bug to investigate. NEVER weaken the fmt/clippy/test gate.

## Pointers (update every iteration)

- **NEXT: port the Reed family (64-71)** into trunk's `voices.rs`. Add `ReedPulse` +
  `guard_width` + the calibration test to `dsp.rs` (rename from my `BlepPulse`; uses
  trunk's `polyblep`). Lift Reed 3235-3620 from OLD (struct/presets/impl/`reed()`),
  update its `BlepPulse`→`ReedPulse` refs + import, add `make()` arm `64..=71 => reed`,
  bring the reed oracles + testutil helpers. Gate scoped, commit.
- **THEN: port the Brass family (56-63)** — lift 2527-3234 (the `set_breath` trait
  default at OLD:56 is additive — trunk's Voice trait lacks it), `make()` arm
  `56..=63 => brass`, brass oracles + helpers. Gate, commit.
- **THEN: engine brass/reed wiring** — extend trunk's `vibrato_family` (+56..=71),
  `fx_profile` (61..=63 chorus, 64..=67 reed sends per HLD §4.2), the BR9 CC11 breath
  writer (program-gated 56..=63 → `set_breath`), `set_breath` calls. Trunk already has
  `at_authored`/aftertouch + `vowel`/`vibrato_family`/`fx_profile` machinery — EXTEND,
  don't duplicate. Gate, commit.
- **THEN: finalize** — full gate (fmt/clippy -D/test --release); contamination check
  (build baseline binary from `origin/main`, render ALL committed albums with both,
  `cmp` — expect ZERO diffs since brass/reeds fill empty slots + drums seam is dormant);
  version bump 0.8.12→0.9.0 (Cargo.toml+lock); README (brass/reed families + kit-v2
  seam); then integrate (fetch/rebase origin/main/gate/`push origin HEAD:main`), remove
  worktree, delete branches. Report done.
- **DONE:** branch off origin/main; drums.rs wholesale-ported; engine kit-v2 wiring
  (Strip.kit field + spawn + ch9-PC→V2); testutil 5-arg drums::make fix; **drum tests
  GREEN (27 passed) incl. v1_drum_render_is_frozen canary + kit_v2_seam.** Gate: from
  `fable5/hollowsynth`, `deltic timeout 600 cargo fmt --all --check && … clippy
  --all-targets -- -D warnings && … cargo test --release </dev/null`.

## FOLLOW-UP (deferred, Arthur approved — the A/B alt bank)

Resurrect old SawStack + my strings/choir/bowed/orch-hit as a **GM Bank Select**
(CC0 MSB/CC32 LSB) alt bank, for A/B testing + tonal variety. Old SawStack comes back
as a DISTINCT struct (isolated by construction → zero risk to trunk's default bank,
renders byte-identical to my branch so my oracles carry over). It IS audible, not just
plumbing: strings ST1 = envelope-tracked brightness (trunk has none), choir-v2 =
consonant onset + SATB scatter vs trunk's vowel-morph, bowed = per-instrument bodies vs
trunk's uniform Bowed. Source of truth: `v09-backup-pre-rebase` (da72e32). Scope as its
own task/reqs entry when picked up.
