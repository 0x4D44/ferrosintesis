# Journal: Opus listening tree

## 2026-07-09

- Goal: move committed final `.opus` listening copies out of album source trees
  into a top-level tree that can be dragged into an audio player.
- Created worktree:
  `D:\worktrees\midi-music\20260709-TSK-HUM-opus-listening-tree`.
- Read `CLAUDE.md` and `lessons_learnt.md`.
- Chose `listening/<artist>/<album>/` over another `audio/` folder so the purpose
  is unambiguous and the album source tree stays focused on composition source,
  MIDI, manifests, and docs.
- Moved 39 committed `.opus` files with `git mv`, preserving track filenames.
- Updated `render_opus.py` so future renders write to `listening/` while keeping
  the existing temporary WAV plus external `ropusenc` flow.
- Updated root docs, repo instructions, and live Fable 5 README references.
- Validation:
  - `python -m py_compile render_opus.py` passed.
  - `git diff --check` passed; Git only warned that touched text files will be
    normalized to CRLF when Git next writes them.
  - A Python import check confirmed `render_opus.all_midis()` still finds 53
    MIDIs, `render_opus.opus_path_for()` maps them under `listening/`, 39
    committed `.opus` files exist there, and no `.opus` files remain under
    `albums/`.
  - `git ls-files '*.opus'` still reports 39 files.
  - A live-doc stale-reference scan found no remaining per-album
    `audio/*.opus` references in `README.md`, `CLAUDE.md`, `render_opus.py`, or
    `albums/fable5`.
