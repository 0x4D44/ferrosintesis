# Journal - Synth Showcase Opus Render

## 2026.07.09

- Arthur asked to add the new Synth Feature Showcase MIDI demos to
  `render_opus.py` and render all of them as an album.
- Created isolated worktree
  `D:\worktrees\midi-music\20260709-TSK-HUM-render-synth-showcase-opus`.
- Added `demos/synth_feature_showcase` to the `render_opus.py` album metadata so
  the existing ferrosintesis-to-ropusenc pipeline writes tagged listening copies
  under `listening/OpenAI Codex/Synth Feature Showcase/`.
- Rendered all five tracks with
  `python render_opus.py --album "Synth Feature Showcase" --jobs 5`.
- Validation passed:
  `python -m compileall -q render_opus.py demos/synth_feature_showcase`;
  `python build.py --verify`;
  `cargo build --release -p ferrosintesis-cli`;
  checked that five `.opus` files exist and are larger than 1 MB;
  `git diff --check`.
