# 2026.07.09 — JRN — Through Lines double album

Task: Arthur's 15-piece brief — Fable 5 self-portrait, Fine Line trilogy, state of
the world, evolution, two Beatles-idiom pieces, Claude lineage, Bond-style action
cue, plus five of my choosing. Wide MIDI feature range; synth extensions allowed.

## Log

- Minted worktree `task/20260709-DEV-HUM-advanced-midi-double-album`; entered it.
- Researched "The Fine Line" (Harmony of the Seas): AquaTheater show, 360°
  immersive, high divers / slackliners / aerialists, night show, pulsing
  lights+music. No public composer credit found — trilogy is designed from the
  show's three physical acts (dive, wire, panorama), not from its actual score.
- Tuxedo Noir already owns swung spy-LOUNGE; the Bond-inspired piece is therefore
  a fast symphonic ACTION cue (different tempo world, no m(maj9) ending).
- Album concept locked: **"Through Lines"** — every track is a line (lineage,
  wire, fault line, bass line, timeline). Two recurring devices bind the album:
  the FABLE pitch cell (F–A–B♭–E, L silent) and Morse rhythm lanes. Leitmotif
  recurrences are machine-verified across tracks ("through-line oracles").
- Launched two Explore agents: fable5 movements-shape conventions + engine.py API;
  ferrosintesis GM/CC coverage map and gaps (to size the synth-extension work).
- Convention survey: multi-track = Winter Guests `Part` pattern; engine.py copied
  verbatim from The Ninth Bell (most-evolved 53-def engine). Ninth Bell's
  "GM 55-71 unmodeled" whitelist rule is STALE (pre-v0.9) — not copied.
- Synth survey: mallets 8-14, harp 46, timpani 47, muted trumpet 59, pizz (CC0
  alt-bank) all EXIST. Real gaps: GM 112-119 (fall through to steel-guitar
  pluck), no gong, no brush kit, GM15 folded into tubular bells. Crate at 0.10.7.
- Blast-radius scan (all albums/** + demos/** MIDIs, = full render_opus ALBUMS
  set): only hit is ch-10 program 8 in the showcase demo ⇒ brush kit keys on
  program 40 EXACTLY; waiver set for 112-119/15/gong is EMPTY.
- Launched the ferrosintesis v0.11 workflow (design ×4 → cross-section critic →
  implement → gate loop → byte-identity + adversarial review), run wf_407439b3.
- Wrote album spine: conductor.py (federated Part registry, 15 tracks),
  material.py (FABLE cell F-A-Bb-[rest]-E with the silent L; 8-chord aeolian
  bridge chorale — consonance oracle green first try; walker/dive/orbit motifs;
  ledger theme; Morse lanes CLAUDE / HOLD THE LINE / GERMAN BIGHT / GOODNIGHT).
- Committed docs (3444a95) + spine (058c222). Scaffold agent building build.py /
  verify.py / analyze.py / 15 honest-red stubs against the composer contract.
- Scaffold landed self-verified (ea6c7c3). Patched movements/__init__.py to LAZY
  per-track loading first — with eager loading, one composer's half-saved syntax
  error would break all 14 other agents' `--track N` verify loops.
- Launched the 15-composer workflow (compose → independent review → fix → album
  critic), run wf_bc567fa5.
- 22:15-ish: SESSION LIMIT hit (resets 02:10 London). Synth workflow wf_407439b3
  completed Design + Reconcile (consolidated implementation plan cached in the
  run journal — includes the Kit::Brush crash_spec `== Kit::V2` silent-failure
  trap, the fold_key sharing decision, family-gate/fx_profile final table);
  implement/gate agents all died on the limit → GATE_RED sentinel. Stopped the
  composition workflow before its chains burned through as nulls. One-shot cron
  at 02:17 resumes BOTH via resumeFromRunId (cached prefixes replay free).
  NB cron is session-only: if this session dies overnight, relaunch both
  workflows manually with the resume incantations in the cron prompt above.
- ~02:30: resumed both; SECOND limit hit (resets 03:20). Stopped composition
  again; one-shot cron 03:27 (091c46fe) resumes both. Composer drafts on disk
  before the wall: T05/T06/T07/T12/T13/T15 (~4.4k lines, uncommitted) — the
  pipeline's salvage stage verifies-and-completes drafts rather than rewriting.
- ~03:30: THIRD limit hit (resets 03:50). Root cause of the repeat: the two
  workflows RACE for the same window — composition's ~10 concurrent composers
  drain it before the synth's single implementer gets a token. Strategy change:
  SERIALIZE — cron 03:57 (faa6f59b) resumes synth ONLY; composition resumes only
  after the synth completes (and its change is committed if green). Drafts now:
  + T03 joined; 7 tracks, ~5.4k lines uncommitted in movements/.
- 2026.07.10 ~05:30: ferrosintesis v0.11 DONE, committed 216da4a (8 files,
  +2170/−44, version 0.11.0, 48 new tests, 231 green, fmt/clippy clean). The
  implementer salvaged the prior window's uncommitted draft rather than
  rewriting. Byte-identity PROVEN over all 73 repo MIDIs: 68 identical; the 5
  different are the in-flight Through Lines tracks that author v0.11 features
  (agogo/taiko/tinkle/reverse-cymbal/brush/gong) — the opt-in invariant
  demonstrated both ways. Adversarial review: 0 critical / 0 major / 5 minor →
  parked in scratchpad.md (open-hat kit gate pattern, PIZZ course_couple dup,
  fold_key bounds untested, brush-slap ghost-note burst, RevCym note-off
  semantics). Task #2 closed. Resuming composition (serialized).
- ~06:30 (4th window): 11/15 TRACKS COMPOSED, reviewed (0 criticals each), and
  re-verified green by hand at commit time → f7f09bf. T11 651-line draft parked
  as wip 3a4505e. Window died (resets 05:30) before T01/T02/T14 composed and
  before the album critic. Cron 05:37 (7cc1f2d9) resumes the workflow — cached
  chains replay, T11 salvages its draft, three compose fresh, critic runs last.
- ~08:20 (5th window): T14 ESTUARY SUITE composed AND reviewed ok (1,819 lines,
  ~13 min). T02 (999 lines) + T11 (1,128 lines) composed, oracle-green and
  hash-deterministic by my own local runs, but their independent reviews died →
  committed all three (8bf8074); 14/15 banked. New failure modes this window:
  transient "Not logged in" auth errors (killed T01's compose+salvage) and a
  64k output-token overflow on T02's composer FINAL MESSAGE (module was fine;
  salvage completed it) — endgame prompts now cap report length. Wrote a
  surgical endgame workflow (through-lines-endgame.js: compose T01 → reviews
  T02/T11 → album critic) instead of resuming the giant one; cron 10:47
  (3b766735) launches it after the 10:40 reset.
- ~12:00: ENDGAME green, zero errors. T01 composed+reviewed (18 oracles); T02
  and T11 reviews ok; ALBUM CRITIC verdict PASS — 281 structural oracles green,
  85.88 min, every track within ~15 s of target, program palette fully inside
  v0.11, no shared-file drift. Committed T01 (b2649c3); tasks 4/5/6 closed
  (assembly re-verified by my own run).
- Rendering: all 15 WAVs (render_all.sh, 4-wide; note deltic timeout mangles
  nested bash -c quoting — script file avoided it). analyze.py found 12 audio
  failures across 8 tracks — the render-vs-score gap doing its job (3× pan-Haas
  mono collapse, 5× dynamics contours flat in dB, T14 epilogue at −54 dB
  inaudible). Fan-out of 8 audio fixers (one hit a NEW "Fable 5 limit" cap;
  resume completed all 8). Standout diagnoses: T03 traced 4.8 dB mono loss to
  one pan-52 harp channel (0.9 ms Haas comb at ~530 Hz); T06 measured that
  hard-panned CONTINUOUS streams pin near 3 dB mono loss at ANY pan value;
  T02's non-falling coda was two aftertouch blooms peaking inside the fade
  window. Two fixers independently hit the `--verify doesn't rewrite the MIDI`
  stale-render trap → lessons entry.
- FINAL STATE: full rebuild + full --verify PASS, fresh 15/15 renders +
  analyze.py PASS (0 failures). Album bundle committed (15ee542): MIDIs,
  manifest, ALBUM.md, README.md, render_all.sh, render_opus.py entry, lessons.
  Opus listening copies rendering. Synth v0.11 (216da4a) integration-ready:
  gate green, byte-identity proven over all 73 repo MIDIs, review 0 crit/0
  major/5 minor (scratchpad). Awaiting Arthur's integration go — never pushed.
- INTEGRATION COLLISION (Arthur's go, ~13:30): origin/main had moved 22 commits
  — another agent landed a realism batch including ITS OWN GM 112-119
  (94dfa73), kit V3 default (any ch-10 prog change → V3), sampled attack
  layers, poly-AT/CC2, the SAME 0.11.0 version, a new Atlas of Becoming album,
  and a policy reframe (923d231): timbre improvements are now DEFAULT-ON with
  asset refresh; controller features stay opt-in; byte-identity is a report,
  not a gate. Resolution per doctrine (trunk wins) + Arthur's call (keep our
  voices as an ALT-BANK): aborted the conflicted rebase, two-step
  `rebase --onto` transplanted the album chain onto trunk minus the superseded
  synth commit (216da4a preserved via backup/pre-integration). render_opus
  ALBUMS auto-merged (Through Lines + Atlas). Album --verify green on the
  rebased chain. Launched the bank-B graft workflow: alt-bank 112-119 set B +
  gong on 14 (CC0-gated), dulcimer 15 default, brush kit grafted onto the V3
  selector (prog 40 → Brush, else V3), bump 0.12.0, diff-free proof + review.
  Next after graft: album channels authoring 112-119 opt into CC0 bank B (the
  audio oracles were calibrated against those voices), re-render, re-analyze,
  re-opus, full gate, push.
