# Default solo bowed strings — journal

## 2026-07-10 — understand and first design

- Created task worktree `D:\worktrees\midi-music\20260710-TSK-HUM-deepen-solo-bowed-strings` from `origin/main` at `d5b52a5`; the main clone remains untouched.
- Read repository instructions, lessons, the default `Bowed` implementation and factory, sampler routing/tests, engine controller/bus routing, and the frozen alternate-bank bowed implementation and oracles.
- Confirmed the default collapse: GM40–45 and GM110 all use one violin-bodied `Bowed` and the same violin attack bank.
- Confirmed the natural-vibrato timing defect: a full-rate oscillator advances once per `CTRL=16`, producing about one-sixteenth of the named rate.
- Found that excluding GM44/45 would preserve known incorrect behavior, not a clean separate implementation. The draft therefore includes the alternate bank's proven tremolo and pizzicato shapes at the same routing seam.
- Scanned all 87 MIDI files registered by `render_opus.py`, tracking program and CC0 bank state per channel. Sixty-one files contain affected default-bank notes. Counts: GM40 5,107 notes/47 files; GM41 2,789/37; GM42 4,823/47; GM43 7,064/43; GM44 232/2; GM45 121/2; GM110 2,412/6. No affected note uses the alternate bank.
- Chose a no-new-WAV design. The active strings/choir/Iowa work and sample-crate split already own the sample payload/layout seam, and the active branch is close to the 19 MiB target before Iowa steel.
- Drafted `wrk_docs/2026.07.10 - HLD - default solo bowed strings.md`. Next: three independent adversarial reviews, cross-refutation, one Claude fresh-eyes pass, fold survivors, commit the design artifacts, and stop for Arthur's explicit sign-off.

## 2026-07-10 — adversarial review

- Ran two independent completed critiques: DSP/acoustics and simplicity/product risk. A third integration reviewer was stopped after it did not return within the bounded pass, so it supplied no evidence and no finding.
- Both completed reviewers' cross-refutation turns failed before running because the helper harness could not refresh its access token. Treated all findings as unverified at that point, then reran the refutation checks sequentially as allowed by the lean-process fallback.
- Confirmed seven survivor groups: corrected vibrato would cause ±40% bow-noise AM; GM41–43 onsets were underspecified; absolute HF level and strict darkness ordering are weak identity oracles; natural/CC1/aftertouch composition lacked an upper bound; GM110's handover criterion was too weak; GM44/45 are solo articulation proxies; and routing tests belong with `voices::make`.
- Ran the existing frozen `altbank::tests::bowed_bodies_differ` oracle. It passed and printed absolute HP2.5k margins of 5.3%, 13.4%, and 15.3%, confirming the inherited table works but also that the violin/viola margin is too small to mix with a ±3 dB level allowance without normalization.
- Ran `sampler::tests::la_level_continuity`; it passed, but source inspection confirmed its 2.4× adjacent-window cap is too weak for GM110's new shorter handover.
- Folded every successfully checked survivor into the HLD. No finding was rejected after a successful refutation check. Next: one fresh Claude review, then triage, commit and hard sign-off handoff.

## 2026-07-10 — external fresh-eyes review

- Used the `invoke-claude-code` skill for one read-only Claude Opus pass, with plan permission mode, the Agent tool denied, stdin closed and a 1,200-second shell timeout. The first process output was lost when the tool cell was interrupted; verified no matching review process remained, then restarted the same single pass. It returned after 590 seconds with no repo writes.
- Verdict: approve with changes. Accepted explicit full affected-Opus refresh, an absolute/pre-change arco-AM guard, and a measured time-to-80%-RMS attack oracle rather than tying observed rise to ADSR constants.
- Corrected one stale premise before accepting its useful part: the reviewer cited the lesson about old generated GM43 notes at MIDI 12–23, but the fresh current corpus scan found GM43 34–78. Added a +3 dB 30–80 Hz regression cap at actual corpus-low key 34 rather than memorialising stale notes.
- Claude independently confirmed the important architecture: no GM48–54 duplication, correct `sr/CTRL` fix, necessary bow-noise decoupling, bounded GM44/45 mechanisms, frozen alt-bank boundary, unchanged generic sampler, and preserved controller seams.
- External review is folded into the HLD. Next: verify the docs diff, commit the design-only artifacts, and stop at Arthur's hard sign-off gate.

## 2026-07-10 — sign-off and implementation start

- Arthur explicitly approved the reviewed HLD. Marked it `SIGNED-OFF` with the approved scope and non-goals.
- Implementation remains sequenced after the active GM48–54/Iowa and sample-crate-split prerequisites. Next: fetch `origin`, verify those branches landed, rebase this isolated task branch, then write failing focused tests before production code.

## 2026-07-10 — prerequisite gate audit

- Fetched `origin`; trunk is `2b939c1` (cathedral-organ listening refresh). Neither prerequisite is on trunk.
- The GM48–54/Iowa worktree is detached mid-rebase. Its earlier source conflict has progressed, but it currently has 40 unresolved committed-Opus conflicts plus its journal modification. It is not safe or authorised for this task to resolve or integrate.
- The sample-crate-split worktree still has the full uncommitted package migration: manifests/docs, `sampler.rs`, `voices.rs`, engine/drum seams, 178 WAV moves, two new sample crates and tooling. It directly owns paths this implementation must target.
- The signed HLD says implementation starts only after both prerequisites land. No production/test code has been written. Next continuation: `git fetch origin`, verify both changes are ancestors of `origin/main`, then rebase this task branch before the tests-first unit.

## 2026-07-10 — sequencing gate amended

- Rechecked after Arthur reported cleanup. GM48–54/Iowa is integrated on `origin/main`; its old worktree is removed. The sample split is clean and gated at `68a52ab` but not integrated.
- Inspected the exact split diff. In `voices.rs` it adds `samples && embedded_samples_available()` at the top of `make` plus feature-gated tests; it does not touch `Bowed` or the GM40–45/110 arm. `sampler.rs` changes embedded-WAV lookup plumbing without changing `LaVoice` behavior.
- Arthur approved going ahead now and integrating the sample split later. Amended the HLD sequencing decision: build against current trunk; later split rebase preserves this voice/factory work. The second task to integrate takes the next patch version.
- Next: commit this approved amendment, rebase onto `origin/main`, run the current scoped baseline tests, then add failing acceptance tests before production code.

## 2026-07-10 — core implementation

- Rebased the four design/sign-off commits cleanly onto current `origin/main`. Baseline bowed, GM110 and LA continuity tests passed before code changes.
- Wrote the default-family tests first. The initial compile failed because `Bowed::new` did not accept a program, proving the old one-preset seam. Added behavioral oracles for body/onset identity, named vibrato rate, GM44 tremolo, GM45 pizzicato, sample routing, body-band prominence, fiddle attack/handover, natural-range pitch, legato, level/safety and natural+CC1+aftertouch composition.
- Implemented one compact private `BowedPreset` map. GM40/41/42/43/110 now own body, pressure ceiling, vibrato rate/depth, attack, bite and a minimal level trim. The voice remains one struct; no articulation framework or new dependency was added.
- Ported the frozen alternate bank's proven GM44 raised-cosine stroke/re-bite mechanism and GM45 `PluckPreset` into independent default-bank data. `altbank.rs` remains untouched.
- Corrected the natural LFO to `sr / CTRL` and removed the `1 + 0.4*vib_val` bow-noise multiplier. GM41–45 now skip the violin sample; GM40 retains its existing handover; GM110 uses a separately tested 0.08–0.28 s handover.
- Test-driven tuning settled at modeled attack bases 52/70/90/105/125 ms for fiddle/violin/viola/cello/bass. Viola's 220 Hz peak needed +7.5 dB for the signed 10% body-band separation; its 0.98 level trim keeps the family within ±3 dB. Cello/bass pressure ceilings were lowered until adjacent normalised HF fractions separated by at least 5% without violating level or pitch guards. Fiddle needed a 0.23 onset bite and a wider pressure ceiling for the signed early/post-handover identity margins.
- Corrected one reviewed oracle after observing it fail for the wrong reason: normalised envelope autocorrelation measured a 0.456 periodicity peak from tiny body-filter AM. Replaced it with actual 5.5 Hz envelope depth (`bandpassed RMS / mean`), bounded below 0.03 for arco with GM44 >= 0.08 as the positive control.
- Focused result: 7/7 new default bowed tests green; 14/14 combined default/frozen/engine bowed tests green; LA continuity green; scoped clippy with warnings denied green. Next: full crate tests, then commit the green core unit before independent review.

## 2026-07-10 — code review and simplicity pass

- Full ferrosintesis suite after the core unit: 296 passed, five ignored calibration/advisory tests, no failures. Core committed as `51a008f`.
- Ran the `code-reviewer` workflow once against the committed diff. Accepted and fixed four Issues: encode body length in the type; prove GM44 velocity/re-bite; add the signed true-DC guard; and make sample-routing tests behave correctly in the later split's modeled-only build without adding a feature flag here.
- Simplicity pass removed two redundant velocity autocorrelations and compiled the now-test-only `vib_val` field out of shipped voices. Kept local oracle helpers explicit; rejected a general helper abstraction as wrong-altitude indirection.
- Review record: `wrk_docs/2026.07.10 - CR - default solo bowed strings.md`. No critical finding and therefore no repeated review round. Next: rerun focused/full checks, commit review fixes, then establish baseline/current release binaries for the all-corpus diff inventory.
- Review fixes are green: `cargo fmt --all --check`, scoped `clippy -D warnings`, and the complete ferrosintesis suite (296 passed, five ignored, no failures). A misplaced test-only attribute briefly exposed a compile error in the unrelated wind voice; moved it to `Bowed::vib_val` and reran the whole gate before committing.
