# Scratchpad review — 2026.07.10

Reviewed every open `scratchpad.md` item against current `origin/main`, including
the showcase-audio item that arrived upstream during the pass. The file had no
previous-pass `[x]` records, so there was nothing to sweep.

## Actioned

- Guarded the v2 snare head-envelope reads behind `wire.is_some()`, removing the
  per-sample sine calculation from every non-wire drum voice.
- Kept a cymbal's secondary sizzle band instant when the primary wash swells and
  added an exact regression oracle for that seam.
- Reused the shared pluck coupling constant in alt-bank pizzicato and added a
  complete pitch-class/bounds oracle for all four `fold_key` call-site ranges.
- Used the already-supported standard CC120 All Sound Off message for Through
  Lines track 11's authored brake-crash choke. The refreshed channel-10 stem is
  33.76 dB quieter between the choke and the next ride hit than the old MIDI.
- Bumped ferrosintesis once, from v0.13.0 to v0.13.1.

The synth changes are output-neutral: baseline v0.13.0 and candidate v0.13.1
rendered all 87 MIDI files in `render_opus.py::ALBUMS` byte-identically.

## Promoted

- `MM-BUG-KILN-00001`: brush-slap ghost notes use an absolute second-contact
  burst. At velocity 30 the second 13–22 ms window measured 2.336× the first.
- `MM-BUG-KILN-00002`: the synth showcase still has four audio-oracle failures
  across tracks 1, 4 and 5 on v0.13.1; tracks 2 and 3 pass.

## Retired

- The four listed GM43 lanes were already raised to MIDI 36 or above by
  `5b752e6`.
- The SawStack contamination canary passes on current trunk.
- `CymSpec: Default` and a `RD_DEFAULTS` reed preset base were rejected as
  low-value abstractions that would hide required, deliberately explicit
  acoustic parameters.
- The v0.11 open-hat kit predicate and reverse-cymbal documentation were already
  corrected by the v0.12 integration.

## Decision left open

Wind and Bowed still construct their roughly 5 Hz autonomous vibrato LFOs at the
full sample rate but tick them only once per `CTRL` samples. Fixing that real
quality bug changes committed flute, whistle and fiddle renders, so it remains
open for Arthur's explicit listening-scope decision.
