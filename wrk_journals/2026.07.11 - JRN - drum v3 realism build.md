# JRN — ferrosintesis V3 drum realism build

**This file is the loop's single source of truth. Read it first every iteration.**
Fresh context each pass: the repo + this journal are the only memory.

## Task

Build the V3 drum realism roadmap. Full spec: `wrk_docs/2026.07.11 - HLD -
ferrosintesis drum v3 realism roadmap.md` (in this worktree; read the cited section per
unit). Scope = ALL roadmap proposals EXCEPT the deferred snare rimshot and the Tier-3
skips. Design was verified against code (incl. a Fable consult for the cymbal cascade).

Worktree: `D:\worktrees\midi-music\20260711-DEV-HUM-drum-v3-realism-build` (branch
`task/20260711-DEV-HUM-drum-v3-realism-build`). Base: origin/main `fa08886` (includes the
HLD). Baseline gate GREEN at start: `cargo test -p ferrosintesis` = 288 passed.
ferrosintesis version at start: **0.13.1**.

## Terminal state (stop the loop ONLY here)

- **(b) Done:** full gate green + version bumped once + render-diff inventory triaged +
  affected `listening/*.opus` refreshed + full-mix spot-checks recorded → STOP, report,
  ask Arthur for the integration go. **Never push.**
- **(a) Fork:** a genuine judgment call Arthur must make (an oracle threshold that only a
  human listen can settle, an unexpected render-diff that implies a real bug, a spec
  ambiguity) → STOP and ask. Do NOT power through by guessing.
- A "mechanical/next-session remainder" is neither — keep going.

## Iron rules (every unit)

1. **Oracle-first.** Write the fail-first / differential oracle, run it, OBSERVE it fail
   on the current V3 render (proving it detects the defect), THEN implement, THEN observe
   it pass. Calibrate any new detector on a known-answer synthetic FIRST (SYN-A/B/C for
   the cymbal; a gaussian-vs-click-train for the snare crest/kurtosis helper).
2. **Determinism.** Render-time randomness reuses the ONE shared `white` draw
   (`drums.rs` MetalPlate:464, Drum:267) — never insert a draw into `self.rng`, never
   reorder. Build-time draws for new state go AFTER all existing draws on that path. V1
   and V2 stay BYTE-IDENTICAL — only the V3/Brush arms change. The determinism guard
   (`testutil::guards::determinism_bit_identical`) and the V1 fingerprint freezes must
   stay green untouched.
3. **Guards.** Keep the golden fixture, choke oracles, and anti-sine-tail guards green.
   When a unit moves the ch-9 solo render beyond the golden tolerance (±2.5 dB RMS / ±20%
   centroid), that is EXPECTED (drum sound is intentionally changing): re-capture GOLDEN
   (`$null | cargo test -p ferrosintesis print_golden_fixture -- --ignored --nocapture`),
   paste the new ch-9 row + `GOLDEN_MASTER_PEAK` into `testutil.rs`, in the SAME commit,
   with the reason in the commit message. Do NOT weaken any assertion to pass — if green
   needs weakening, that's a fork (a): stop and report.
4. **Commit each green unit** atomically, explicit paths (never `git add -A`), on the
   task branch. End messages with a `Co-Authored-By: Claude Opus 4.8 (1M context)
   <noreply@anthropic.com>` trailer.
5. **Bound long commands** with `deltic timeout <secs>` and run tests with stdin closed
   (`$null | cargo test ...`). Scope checks to `-p ferrosintesis` per unit; the full
   workspace gate is for the finalize phase.
6. **No version bump until the finalize phase** (one bump for the whole task).

## Scoped check per unit

`$null | deltic timeout 480 cargo test -p ferrosintesis <name_filter> --manifest-path
D:\worktrees\midi-music\20260711-DEV-HUM-drum-v3-realism-build\Cargo.toml`
plus `cargo fmt` before commit. Clippy/full-workspace/release in the finalize phase.

## Unit plan (landing order) — advance ONE per iteration

Mark each `[x]` when its commit lands green; move NEXT.

### Phase A — Snare (keys 38/40). HLD §SNARE.
- [x] **A1 — crest/kurtosis testutil helper** (infra; HLD P-S1 oracle). Add a
  `crest_factor` (or kurtosis) helper to `testutil.rs`; oracle-0 calibrate it on a
  synthetic gaussian-noise vs click-train of known answer. No synth change → golden
  untouched. Commit.
- [ ] **A2 — P-S1 PhISEM collision-grain gate.** Replace the periodic 186 Hz slap-AM
  (`drums.rs:298`, the `1.0 + w.am_depth*head*head_phase_sin.max(0.0)` term) with the
  seeded stochastic grain gate (HLD §P-S1 detail: λ=head^0.4, fire when white*0.5+0.5 <
  λ·rate_max, e←1 on fire else e*=dmul(~0.8ms), gate g=floor+(1-floor)e floor≈0.15, mild
  one-sided bias). Derive `u` from the shared `white` (zero new draws). Recompute
  `WIRE_MAKEUP` for the mean-gate loss. **Re-aim DR-O8(b)** oracle: it currently MAXIMISES
  the 186 Hz line — flip it to assert the line is now ABSENT
  (`env_autocorr_peak(HP-2800 sustain[20,120ms], 1/300, 1/120) < 0.2`) + crest >1.3× +
  DR-O10 parity ±2 dB + |mean|<1e-4. Re-capture GOLDEN. Commit.
- [ ] **A3 — P-S4 Bessel head modes + beating pairs.** Re-seat `SNARE_TONES`
  (`drums.rs:561`) on air-loaded ratios; split 2–3 modes into detuned pairs (fixed Δf).
  Oracle: `env_autocorr_peak` around fundamental shows a 2–8 Hz beat; hold settled pitch;
  DR-O10 parity. Re-capture GOLDEN. Commit.
- [ ] **A4 — P-S3 resonant head + shell air mode.** Add 1–2 resonant-head sines + one
  shell-air `Biquad::bandpass` (~200–450 Hz, Q 2–4) on shared white; re-point P-S1 λ to
  the resonant-head env. Oracle: `spectral_band_rms(200,450)` over [0,50ms] > control vs
  no-body build. Re-capture GOLDEN. Commit.
- [ ] **A5 — P-S2 model crack transient.** Short HP ~4–6 kHz noise burst at t=0, velocity
  scaled, build draws appended after existing snare draws. Oracle:
  `hp_rms(seg[0,3ms],5000)/rms(seg[10,40ms])` rises + monotone across vel 40/80/120;
  bounded, zero-mean. Re-capture GOLDEN if moved. Commit.

### Phase B — Toms (keys 41/43/45/47/48/50). HLD §TOMS.
- [ ] **B1 — P-T1 air-loaded ladder + shell/cavity body.** Replace 3-mode `tom_tones`
  (`drums.rs:543`) with a 5–6 mode ladder on ratios ≈1/1.5/2/2.4/2.9(/3.5), amps
  1.0→0.12, per-mode t60 taper; add one wideband HP attack-cloud `NoiseBand`; add one
  shell/cavity `Biquad::bandpass` per key (rack ~150–300 Hz, floor toms 41/43 ~80–150 Hz)
  on shared white. If >8 modes, switch toms to `dsp::Sine`. Oracle (glide disabled): ≥5
  resolved `peak_locate`/`mag_at` peaks in 100–1500 Hz each >5% of fundamental; shell
  band-rms non-trivial; DR-O5 settle-pitch still passes; per-strike variance preserved.
  Re-capture GOLDEN. Commit.
- [ ] **B2 — P-T2 HF attack.** Add a short HP-passed click `NoiseBand` above the mid
  burst. Oracle: `hp_rms(seg[0,10ms],3000)/rms(seg[50,150ms])` rises; no click on silent
  channels. Re-capture GOLDEN if moved. Commit.
- [ ] **B3 — P-T3 two-head beating pair.** Detuned resonant twin (Δf 2–5 Hz on the
  fundamental, ~0.6× amp, ~1.4–1.6× t60) on the low 2–3 modes; keep Δf·t60 ≳ 1. Oracle:
  `env_autocorr_peak` 2–8 Hz beat; `t60_of([150,400ms]) > t60_of([0,80ms])`. Re-capture
  GOLDEN. Commit.

### Phase C — Cymbals (MetalPlate: 49/57 crash, 51/59 ride, 52 china, 55 splash). HLD §CYMBALS + the verified cascade detail. Build C1+C3 as one coupled system.
- [ ] **C0 — SYN synthetics + trajectory machinery** (infra). Build SYN-A/B/C and the
  test-local trajectory helper (`E(t;lo,hi)` = `spectral_band_rms` over 25 ms / 5 ms hop,
  3-tap smooth; first-crossing peak-time = center of first window ≥0.90·max). Calibrate:
  SYN-A reports the bloom (fixes the ±10 ms window bias), SYN-B fails all bloom checks
  (and the CURRENT V3 crash must fail them too — the real fail-first), SYN-C 6-sine chord
  fails DENS-1/DENS-2-upper and 16-pair field passes. No synth change → golden untouched.
  Commit.
- [ ] **C1 — P-C1 energy-cascade bloom.** Extend `MetalPlate` (`drums.rs:345/454`): per
  mode a `u8` band index vs edges 1.5/4/8 kHz; 3-stage step-driven one-pole cascade
  (`c0+=atk(1-c0); c1+=atk(c0-c1); c2+=atk(c1-c2)`); band multipliers `m_b=f_b+D_b·c_{b-1}`
  (m0≡1); **multiply** (not replace) the intrinsic `mode.amp*=mode.decay`; accumulate per
  band. Smoothstep velocity gate `g=smoothstep((velnorm-0.25)/0.75)`; `D_b=(1-f_b)·g^{b/3}`;
  `τ=21-6g` ms (crash). Per-key floors/τ from the HLD profile table (crash 0.40/0.18/0.10;
  ride 0.92/0.88/0.85 ×0.15 depth — ride CANNOT become a crash by construction). f₃≤0.12
  hard. No back-reaction. Oracle: BLOOM-1..4b, VEL-1, RIDE-1/2, GONG-1 (see HLD). ° = pin
  vs SYN + printed current values. Re-capture GOLDEN. Commit.
- [ ] **C2 — P-C2 wash-follow.** Drive the crash wash swell env from `c2` (not c3):
  `env=floor_w+(1-floor_w)·c2`, `floor_w=1-0.55·g^0.8`. Soft crash wash no longer swells
  (intended, audible — flag for the listen check). Oracle: the >6 kHz bloom peak stays in
  [15,90] ms with the wash contributing. Re-capture GOLDEN. Commit.
- [ ] **C3 — P-C3 density (twins + micro-AM).** Split MetalPlate mid/high modes into
  detuned PAIRS (pairs not triplets; low band stays legacy-dense — allocation + Δf law
  `Δf=4.5+0.0011·f Hz ×(1+0.25·white)` in the HLD); per-band micro-AM = one
  `OnePole::lowpass` on the SHARED white, cutoffs 30/45/60 Hz, depth 0.12, clamp
  [0.5,1.5]. Keep total partials ≤48 = legacy counts. Oracle: DENS-1/2/3 (DENS-2 note:
  `env_autocorr_peak` detrends at 15 Hz — target 9–22 Hz beats; if mushy use the in-repo
  `twin_beat_cv` DR-O4 pattern at `drums.rs:2091`). Must keep flatness≥0.18 / narrow-line
  ≤0.45. Re-capture GOLDEN. Commit.
- [ ] **C4 — P-C4 amplitude glide.** Track band-0 strike env E; retune `f_i=f_i0(1+β·E)`
  β≈0.02–0.04 via `Sine::set_freq` every K≈64 samples; clamp f<0.49·sr. Oracle:
  `peak_locate` a strong low mode [0,20ms] vs [300,400ms] sharp-then-flat, 10–80 cents,
  <1 semitone. Re-capture GOLDEN if moved. Commit.

### Phase D — Finalize.
- [ ] **D1 — version bump.** ferrosintesis 0.13.1 → **0.14.0** (feature set; minor).
  Lockstep `crates/ferrosintesis/Cargo.toml` + `Cargo.lock`. Commit.
- [ ] **D2 — full gate green.** `cargo fmt --check`; `deltic timeout 480 cargo clippy
  --workspace --all-targets -- -D warnings`; `$null | deltic timeout 600 cargo test
  --workspace`; `deltic timeout 480 cargo build --release -p ferrosintesis-cli`. Fix any
  fallout. (No commit unless fixes needed.)
- [ ] **D3 — render-diff inventory.** Build a baseline binary in a throwaway
  `git worktree add <tmp> fa08886` (pre-change origin/main), build the new
  ferrosintesis-cli, render every `render_opus.py::ALBUMS` MIDI with BOTH, `cmp`. EXPECTED
  diffs: only albums whose ch-10 MIDI reaches 38/40/41/43/45/47/48/50/49/51/52/55/57/59 on
  Kit V3/Brush. UNEXPECTED diff (piano-only album, a hat 42/44/46 or bell 53 diff from a
  MetalPlate-only change, DC on a silent channel) = BUG → stop (fork a) and investigate.
  Record the inventory in this journal.
- [ ] **D4 — asset refresh.** Build `ferrosintesis-cli --release`; refresh ONLY the
  affected committed `listening/*.opus` (per D3 inventory; needs `ropusenc` on PATH from
  the sibling `ropus` repo). Assert no NEW listening files
  (`git ls-files --others --exclude-standard listening` prints nothing). Stage temp
  outputs under `target/` in the worktree (os.replace can't move across drives). Commit
  the refreshed assets.
- [ ] **D5 — full-mix spot-checks.** For Sub Rosa, The Burning Meridian, The Ninth Bell,
  Tuxedo Noir, Ignition Court (Synth Feature Showcase): compare before/after peak +
  integrated RMS; peak finite; RMS within ±3 dB unless noted after a listen. Record here.
  Also note the intended soft-crash-wash change (C2) needs one human listen before final
  acceptance — flag it in the terminal report.
- [ ] **D6 — terminal report.** Confirm the gate was observed green + version bumped +
  assets refreshed. STOP, report to Arthur, ask for the integration go. Do NOT push.

## NEXT

**NEXT UNIT: A2 — P-S1 PhISEM collision-grain gate.** A1 landed (kurtosis helper +
oracle-0 calibration, green). Read HLD §P-S1 for the grain-gate detail.

## Log

- 2026-07-11: journal created; baseline `cargo test -p ferrosintesis` = 288 passed;
  version 0.13.1; branch based on origin/main fa08886 (HLD present).
- 2026-07-11: A1 landed — `kurtosis` helper + oracle-0 calibration
  (`kurtosis_flags_grain_over_smooth_noise`) green. testutil.rs only; golden untouched.
