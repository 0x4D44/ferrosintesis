# JRN — Electric Guitar v2 (pickup circuit, two-stage amp + sag, sustainer)

Task branch: `task/20260711-DEV-HUM-electric-guitar-v2` (worktree
`D:\worktrees\midi-music\20260711-DEV-HUM-electric-guitar-v2`, forked from trunk
`bfd77a9`, ferrosintesis 0.14.4).

## 2026.07.11 — investigation + design

- Strategy investigation (6-lens workflow, main session): current chain, LA sampler
  constraints, design history incl. the T16 "wooden glockenspiel" verdicts, album
  usage survey (26/27/29/30 load-bearing; 31 unused), Sullivan/Jaffe-Smith/JAES
  pickup literature, CC0 sample-source landscape (Karoryfer/FreePats viable;
  Iowa MIS has no electric — dead end).
- Key diagnosis: missing pickup RLC resonance; single static tanh with no gain
  recovery; dissipative KS loop (high-note harmonic starvation). Sum-distortion and
  two-polarization decay are already right.
- Found the T16 sustaining-lead branch had been integrated as `bfd77a9` while we
  scoped — DRIVE_LEAD + CC0 alt-bank are on trunk; design baselines on it.
- Wrote `wrk_docs/2026.07.11 - HLD - electric guitar v2 pickup amp sustain.md`
  (units A pickup RLC / B 26-27 split / C Drive v2 + sag / D in-voice sustainer).
  Chose in-voice AGC over Sullivan channel feedback: no Voice-trait change, bounded
  stability, defers howl as future character. LA samples explicitly out of scope
  (Stage 3, separate task).
- Adversarial review round 1 (3 lenses: DSP-physics, architecture, oracle/blast)
  found two genuine spec bugs — the sustainer clamp ignored the in-loop damper
  (|H(f0)| makes a bare 1.004 scalar clamp unable to hold anything above ~A4;
  oracle unpassable as written) and the sag law had inverted temporal polarity at
  onsets (T/env slams the pick transient at G_MAX after silence). Also: JAZZ comb
  constant wrong under the ×2 pickup convention (voices.rs:1980), DRIVE_LEAD
  contract unspecified (AGC would ATTENUATE it), altbank PIZZ compile break,
  golden-fixture recapture unplanned, Bright Matter missing from the blast radius,
  three non-diagnostic oracles (sag absolute bound, octave-above leg, RMS-only
  sustain hold).
- Cross-refutation round (same 3 agents on each other's findings): every core
  finding survived; refuted/softened — "Big Weather exists nowhere" (it's in-flight
  in its own worktree), the ×54 composite-gain arithmetic (AGC self-limits), "no
  in-suite canary" (golden guard exists, tolerance-based). Notable convergence:
  all three independently derived the same |H(659)|=0.991 / |H(1319)|=0.966 damper
  numbers.
- HLD rev 2 written: damper-normalized clamp, corrected sag law (fast-attack/
  slow-release + upward slew + idle reset), DRIVE_LEAD re-spec retiring the amp-1.5
  hack, sine-into-tanh steady state stated + post-Drive harmonic pin, V0 FNV byte
  canaries, differential sag oracle, exact 20-opus enumeration + Bright Matter
  MIDI-only, chunked-commit plan, altbank/testutil/README rows in file impact.
- Codex external review (gpt-5.6-sol, xhigh): three invocation failures first —
  (1) mdtimeout parsed pwsh's -NoProfile as its own flag (needs `--`), (2) Git Bash
  expanded `$p` inside a double-quoted -Command (use a .ps1 file), (3) the embedded
  1124-line prompt as an argv blew Windows' command-line length cap (pipe the
  prompt via stdin instead). Also: codex's read-only sandbox helper
  (codex-windows-sandbox-setup.exe) is MISSING on this box — it cannot exec/read
  files at all, so external reviews must embed all content in the prompt.
  → surfaced to Arthur as a deltic/fleet issue.
- Codex found 8 findings, all adopted (HLD rev 3). Critical: rev 2's f0-normalized
  whole-loop clamp is unstable at DC (|H_damp(0)|=1; excitation retains ~10% DC),
  and a proportional AGC has no stable equilibrium (at env=target requested gain
  is 1, decay resumes) and would suppress the early note. Redesign: band-limited
  regeneration — additive k_fb·bandpass(s) at the loop input (dsp.rs bandpass is
  RBJ constant-0dB-peak, zero phase at center; retune_bandpass preserves state),
  headroom clamp per polarization loop, min across glide endpoints — plus a
  one-way hold latch (k_fb=0 until env decays to sustain×reference, ramp toward
  headroom, never below 0). Oracles V2/V5/V6/V7/V8/V9 hardened (temporal sag legs,
  upper+lower hold bounds over 8 s, release-after-hold, pure-sine-into-Drive alias
  test with enumerated folds). dsp.rs added to file impact (state-snap denormal
  guard, closed-form OnePole magnitude).
- Critical findings ⇒ the process's single targeted re-review is running on the
  revised §3.D + oracles (one pass, no fan-out).
- Next: fold re-review verdict, then Arthur's sign-off gate. No code before
  sign-off.

- Targeted re-review (Codex, single pass on revised §3.D + oracles) returned 5
  criticals, all adopted (HLD rev 4): a LINEAR bandpassed driver at net 1.002 is
  supercritical with nothing to arrest growth, and the latch's 1.1× ease-down was
  switched control with no equilibrium → replaced both with a SATURATING driver
  (k_fb·softsat(bp(s), L)) — the classical Sullivan/e-bow stabilization: the
  saturator IS the equilibrium, ease-down deleted. Also: discrete skew coupling is
  not energy-neutral (|λ|=sqrt(a²+k²)) → boundedness argument reworked + V8b margin
  unit test + correct the "adds no energy" comment; V7 gained a +50 ms
  post-release lower bound (instant-kill detector); V6 gained an engaged-glide leg.
- Design committed at 46c7840 (rev 3); rev 4 committed next. Review chain done:
  3-lens adversarial + cross-refutation, external Codex pass, one targeted
  re-review (per the lean process — no further rounds). Presenting for sign-off.
