# JRN — Rock/pop ten-track album (Fable 5)

Task worktree: `D:\worktrees\midi-music\20260711-DEV-HUM-rockpop-ten-track-album`
Branch: `task/20260711-DEV-HUM-rockpop-ten-track-album`

## Brief (Arthur, 2026-07-11)

10 upbeat, catchy rock/pop tracks for ferrosintesis, composed by Fable 5:
- melodic bass guitar; drum solos with drums spread across the stereo stage
- layered complex choral vocals; distorted/overdriven guitars; piano parts
- builds and drops; clear verse / pre-chorus / chorus / middle-8 song forms
- orchestral elements entering as pieces build (strings, brass, timpani)
- advanced MIDI features for realism; introspective slow passages allowed
- must sound great on ferrosintesis specifically

Process: lean (≤2–3 helper agents per step), design → adversarial review →
Codex external review → auto-approved sign-off → implement → code review →
validate. Design doc in wrk_docs/.

## Log

- 2026-07-11: Worktree minted via `deltic start-task` (DEV). Read
  lessons_learnt.md in full. Launched two Explore agents: (a) fable5 album
  conventions / engine API / oracle patterns, (b) ferrosintesis rock/pop
  capability map (guitars/drive, drums+pan, choir, brass, CC surface, mix
  architecture). Orientation ls: fable5 has 10 albums; opus4-8 and
  gpt5-3-spark use the tracks/ shape; fable5 uses movements/ shape.
- 2026-07-11: Conventions agent reported. Template = **Through Lines**
  (newest fable5 album, federated multi-track shape): conductor.py REGISTRY
  of (number, module_stem, title, midi_file, seed); one self-contained
  movements/tNN_*.py per song with PART (intra-song sections = verse/chorus
  etc.), BUILDERS, 7 verification-config constants, oracles(), optional
  audio_checks(ctx). build.py --verify exits nonzero on any failure.
  analyze.py: generic click scan (max step ≤22000) + mono-loss ≤2 dB, plus
  per-track audio_checks with ctx.bar_window/rms/db; Goertzel is a per-track
  idiom. Manifest is generated — never hand-write. render_opus.py needs one
  ALBUMS line; artist "Claude Fable 5"; lyrics/NN - Title.txt sidecars become
  LYRICS tags. Engine: PPQ 480, ch9 drums, note(jt,jv) humanisation,
  _resolve_overlaps clamp, rich CC helpers (vibrato/wah/leslie/vowel/
  bend_range/portamento/aftertouch/sostenuto/soft_pedal/morse).
- 2026-07-11: Synth agent reported. Rock surface is real: GM29/30 have a
  per-channel Drive insert (biased tanh + 5-biquad cab, 2× oversample,
  power-chord intermodulation); GM28 dedicated palm-mute; GM33 the melodic
  "McCartney" bass; GM18 rock organ with tanh drive + CC1 Leslie; choir
  52-54 = ChoirV2 8-singer SATB with CC70 vowel morph; timpani = MELODIC
  GM program 47 (ch10 key 47 is a tom). Drums pan per-instrument in the
  synth (hats 0.33L … china 0.75R); CC10 offsets the whole kit. Avoid:
  GM86/87 (placeholder), 55 sustained, 119/120-127. GM19 default =
  cathedral organ → cathedral reverb.
- 2026-07-11: HLD written: wrk_docs/2026.07.11 - HLD - Big Weather rockpop
  album.md. Album "Big Weather", 10 tracks, Through-Lines shape, 5 shared
  data-driven song-oracles + drum-solo spread check + audio mirrors.
  Launched 3 adversarial reviewers (musical / oracle / synth-pipeline
  lenses); cross-refutation round to follow.
- 2026-07-11: Adversarial review + full cross-refutation done. 26 raw
  findings → survivors folded into HLD v2 (see doc §11). Big design moves:
  duration-weighted energy metric with explicit pairwise rules (no global
  max); drum solos foreground the kit's WIDE voices (cymbals/hats/high-tom;
  mid toms are only ±0.08 off centre) with raised density floors (≥12/bar,
  32nd bursts) and a within-window side/mid audio ratio (no verse baseline —
  guitar confound); feature coverage counts only ON-TARGET CCs (program must
  honor them) + 2 headline features per track get audio probes; form policy
  7 full-grammar + 3 bespoke; choir required on ≥6 declared tracks, not all;
  guitars at moderate 48/80 (mono gate); t09 drop is produced (drone+swell)
  with a re-entry oracle; orchestral-entry oracle reframed as boundary RMS
  rise; stale-render guard added to analyze.py plan. Rejected after
  refutation: B10 (premise misread), A7 (0x05 "displayed humming" is repo
  convention), A8 (claps/tambourine are homed ch9 keys).
- 2026-07-11: Codex external review launched (gpt-5.5, xhigh, read-only,
  deltic timeout 1500) → target/codex-hld-review.txt.
- 2026-07-11: Codex attempt 1 BLOCKED — missing codex-windows-sandbox-setup
  .exe on this box (no local file access; ~100k tokens burned probing).
  Re-ran the single pass with the HLD embedded in the prompt →
  target/codex-hld-review2.txt. Verdict SOUND-WITH-FIXES; 5 findings, all
  accepted and folded (freeze rule, program timeline, RNG model clarified,
  exact wide drum-key set, t09 grammar note). Codex's finding-4 arithmetic
  was itself off (0.62 → 0.12 from centre, fails the 0.17 wide test) but the
  fix direction was right and applied. HLD now v3, Status: SIGNED-OFF
  (pre-approved). NOTE for Arthur: the codex Windows sandbox helper is
  broken on this machine — fleet/tooling issue, not repo.
- 2026-07-11: → IMPLEMENT phase A (scaffold).
- 2026-07-11: Phase A done: album scaffold (conductor REGISTRY, build/
  verify/analyze, 6 shared song-oracles + DRUM_PAN pinned copy + WIDE_KEYS,
  FEATURE_PROGRAMS honoring table, stale-render guard) + 10 honest stubs.
  --check smoke run: machinery green, stubs fail honestly.
- 2026-07-11: Phase B done: T01 "First Light Freeway" composed as the
  reference track. All 17 structural oracles + 2 audio oracles green FIRST
  render (mono loss 0.17 dB, max step 16973). Committed (97d0606).
  Fixes found during self-review: final chorus bass now sings the hook
  only (walk suppressed), tonic pickups give the hook section its 6th
  distinct pitch.
- 2026-07-11: Phase C wave 1 launched: 3 composer agents (t02 Paper Kites,
  t03 Run the Rooftops drum-feature, t04 Glass Anthem ballad/build), each
  bound by target/composer-brief.md (ownership, composer loop, jt=0
  boundary, centred beds, on-target features, measure-then-assert).
- 2026-07-11: Wave 1 hit the Fable 5 usage limit mid-study (3 agents died;
  t02 left a partial draft). Briefly relaunched on Opus, then Arthur set
  the routing: **Fable composes, Opus does the legwork** — stopped the
  Opus composers and relaunched all three on Fable (t02 with a
  verify-and-complete-the-draft note). Opus reserved for review/validation
  /doc chores.
- 2026-07-11: Wave 1 GREEN and committed (2459483). t02 Paper Kites (3.8 dB
  lift, choir-morph probes), t03 Run the Rooftops (solos 18.1/21.6 hits/bar,
  9 pan groups, side/mid spread probe; ch11 = trumpet 57 deviation), t04
  Glass Anthem (C→D key-lift, 13-channel layer_build oracle, 16.4 dB
  measured build; ~4:25 vs ~5:00 brief — accepted). I spot-re-ran all six
  gates myself: green. Watch: t02 click max step 21398/22000.
- 2026-07-11: Wave 2 launched on Fable: t05 Static & Sparks (whisper drop +
  re-entry probes, Leslie choreography), t06 Half Past Summer (bass
  countermelody + fretless GM35 middle-8 cadenza), t07 The Getaway Choir
  (3-channel SATB, vamp antiphony + tag-purity oracles, key-lift).
- 2026-07-11: Wave 2 interrupted (session limit + ECONNRESET); relaunched
  after reset; t05 resumed mid-flight via SendMessage (kept its draft).
  Meanwhile registered the album in render_opus.py::ALBUMS (93 MIDIs all
  claimed) + album README, committed (c346652).
- 2026-07-11: Wave 2 GREEN, spot-re-verified, committed (5b6156c). Notable:
  t05's Leslie probe uses AM-flux on an exposed coda chord and wah uses an
  FFT band fraction (Goertzel bins too narrow for vibrato'd harmonics —
  lesson candidate); t06 caught its bass-band probe measuring backwards and
  swapped to RMS-contrast (measured-direction lesson applied); t07 proved
  the vowel morph with a formant-band ratio (3.07x) and fixed a click-cap
  transient at the arrangement layer.
- 2026-07-11: Wave 3 launched on Fable: t08 Neon Cathedral (GM19 confined
  to prelude, mono watched, GM16 fallback per R5), t09 Ten Thousand Watts
  (trading fours, unaccompanied breakdown solo, produced drop + slam), t10
  Big Weather (tutti closer, bookend title hook, ritardando, morse "BW").
- 2026-07-11: Wave 3 interrupted twice (limits/timeouts), resumed via
  SendMessage each time — t08's on-disk draft survived and was completed.
  All three GREEN; spot-re-verified; committed with the generated manifest
  (00d93aa). Album total 41.64 min; tracks 01-07 rebuilt byte-identical.
- 2026-07-11: PHASE D ASSEMBLY GREEN: full build.py --verify (all ~170
  checks) PASS; all ten re-rendered; python analyze.py — 10/10 PASS
  (mono 0.04-0.17 dB, no clicks). Gotcha logged: running render_all.sh
  under `deltic timeout` handed the script to a bash that can't exec the
  D:/ exe path ("No such file or directory" from the exported function) —
  run it via the harness Git Bash directly; the first failure was
  misattributed to a concurrent cargo relink. Lesson candidate.
- 2026-07-11: Phase E: Opus legwork agent sweeping PROVISIONAL→FROZEN
  threshold annotations (with byte-identity + verify gates). I wrote
  ALBUM.md (track table, 41:38) and all ten lyrics/ sidecars
  (why/listening-guide/techniques style); render_opus sidecar validation
  OK (10/10 matched).
- 2026-07-11: Listening assets rendered: render_opus.py --album green,
  10/10 tagged .opus (34.9 MB) into listening/Claude Fable 5/Big Weather/.
- 2026-07-11: CODE REVIEW (Opus, independent review-and-apply): 8 MIDI-
  inert fixes applied — headline: WIDE_KEYS float boundary (|0.33−0.5| =
  0.16999… silently dropped the hats from the wide set; 1e-9 tolerance
  restores the HLD's named 11-key set) — plus vacuity guards
  (late-channels silent-channel, empty drum-solo windows, empty riff
  windows), unknown-op failure in ENERGY_RULES, CC67 pedal-up not counted,
  hook-window boundary epsilon, analyze.py empty-WAV guard. Deferred items
  triaged by me: t01/t09 lift pins re-set per the freeze rule's ≥1.5 dB
  slack with measured values annotated (t01 lift 2.81 dB, rise 1.64 dB;
  t09 lift 2.43 dB); BANK_SELECTS ordering + mid-section-meter latents →
  scratchpad. SIMPLIFY pass (Opus): dead params (_program_timeline/
  _detect_features module arg), dead constants (t04 BPM, t08 G3), unused
  loop vars → _deg; all AST-scanned, byte-inert, 172 checks green.
- 2026-07-11: FINAL GATES: fresh render ×10, build.py --verify all green,
  analyze.py 10/10 PASS. Album complete; awaiting integration go.
- 2026-07-11: INTEGRATION: rebased onto origin/main (trunk had moved a lot:
  ferrosintesis 0.13.x -> 0.14.4 — V3 drums, brass rasp, deeper cathedral
  reverb, sustaining leads; conflicts resolved in lessons_learnt.md and
  scratchpad.md, union semantics). Post-rebase revalidation on 0.14.4:
  MIDIs byte-identical, 172 structural + 33 audio oracles ALL GREEN with
  no threshold re-pin — the freeze-rule slack absorbed a real synth
  upgrade. Re-rendered the .opus on 0.14.4.
- 2026-07-11: Pushed in chunks (single ~70 MB pack hit HTTP 408 on the
  low-bandwidth link; the "Everything up-to-date" tail after a 408 is a
  LIE — always fetch + rev-parse to verify). origin/main = cac5efd:
  full album source, MIDIs, docs, code integrated. DEFERRED per Arthur:
  the two listening commits (00d8117 opus + 5f12df0 re-render on 0.14.4,
  ~70 MB) stay on this branch until high bandwidth returns — sync later
  with `git push origin HEAD:main` from this worktree (rebase first if
  trunk moves; re-render opus if the synth moves again). Keep this
  worktree until then.
