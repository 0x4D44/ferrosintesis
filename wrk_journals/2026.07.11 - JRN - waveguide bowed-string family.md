# JRN — Waveguide bowed-string family (2026.07.11)

Design: `wrk_docs/2026.07.11 - HLD - waveguide bowed-string family.md`.

## The arc so far

- Arthur reported the **GM43 contrabass** "low buzzing." Root cause: the shared
  subtractive `Bowed` voice (saw → static body EQ), voiced for a violin, collapses
  into a **"transformer hum"** at ~65 Hz.
- Iterated a fix over several ear-in-the-loop rounds (I can't hear — Arthur is the
  oracle): subtractive re-EQ → "still hums"; a **bowed-string waveguide** (delay-line
  string + stick-slip friction) → "better but synthesised"; added bolted-on grit →
  "brushes on top"; **integrated the grit into the friction** → good; then **LA
  synthesis** — a real cello-section arco (`celens`) sampled attack over the
  waveguide sustain → the winner. Bite tuned to 0.29 by ear.
- **Multi-agent collision:** while I worked, a **Codex** effort landed on trunk
  (`386290f` deepen default solo bowed strings, `6c7d70f` bound contrabass low end,
  `5449617` tests) — the *same problem*, solved with **per-program body-EQ presets**
  on the saw model, plus a **samples-into-crates** split (`bd9a85c`).
- **A/B (Fault Lines, Arthur's ear):** the waveguide+LA contrabass **won** over the
  trunk Codex contrabass.

## Decision (Arthur)

Waveguide+LA becomes the **default** bowed voice, extended across the family in
**ear-gated stages**; Codex's set moves to the **CC0 alt bank**; the **v0.9** alt
voicings are discarded. Refresh policy: **audibly-changed assets only**.

## Reconcile

Started **fresh off current trunk** (`8118570`) rather than rebasing the stale
`task/20260710-TSK-HUM-contrabass-low-string-voicing` branch through Codex's refactor
+ the samples-crate split. `BowedString` ports directly; the LA bank uses the samples
crate via the existing `bank!` macro (`embedded_wav`).

## Stage 1 status (contrabass 43)

- **1a DONE, committed `eb833ad`:** `BowedString` + `contrabass_bank` LA layer, routed
  as the DEFAULT GM43 in `voices::make`. Renders match the approved version (Fault
  Lines solo: opening 0.137 / riff 0.169 / collision 0.168, peak 0.44). clippy clean.
- **Render-diff (audio-delta vs trunk):** exactly **43 GM43 tracks audibly changed**,
  **0 codegen drift**, 46 byte-identical, **no clipping** anywhere (incl. the
  pathological sub-bass gpt5 albums). Perfectly scoped.
- **Asset refresh:** 32 `.opus` across 7 albums (Ninth Bell, Iron Tide, Through Lines
  ×2, The Spark ×12, Hours After Rain ×12, Atlas ×3, Synth Showcase ×1). VIGIL /
  opus4-8's 11 changed tracks are MIDI-only → no refresh.
- **Version:** bumped 0.13.5 → **0.14.0** (Cargo.toml ×2, CLAUDE.md; Cargo.lock on
  next build).
- **PENDING:** rebuild + gate (fmt/clippy/test) + commit assets/version + integrate.

## Not done yet (follow-ups)

- **Alt-bank reorg** (Codex → CC0 alt, discard v0.9): de-risked (no `engine.rs`
  coupling — `strings_vib_base` only touches 48–51), but **audibly inert** (no album
  selects the alt bowed bank) and test-only-verifiable. Recommend a **separate,
  test-driven** change — a strong candidate to **hand to Codex** (owns
  `bowed_preset`/`altbank`/its tests).
- **Stage 2:** cello 42 waveguide + A/B (expected to win).
- **Stage 3:** violin 40 / viola 41 waveguide + A/B **vs Codex** — may stay Codex
  default (the waveguide's advantage is register-dependent; Codex already carries a
  real LA attack up there).

## Gotchas hit

- Trunk moved twice mid-session (`3c8e7ee` → `8118570`); re-check `origin/main` before
  integrate. The bowed family is actively worked by the fleet — the HLD + claimed
  branch are the collision signal.
- `deltic timeout bash -c '... python ...'` loses PATH (python not found) and eats the
  loop var — call `deltic timeout python …` directly instead of via a nested `bash -c`.
