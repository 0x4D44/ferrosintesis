# Scratchpad review — 2026-07-12

## Previous-pass sweep

Retired nine resolved scratchpad items:

- Actioned fixes: raised four low contrabass lanes, repinned the SawStack canary,
  removed two drum hot-path/latent-noise issues, consolidated alt-bank constants and
  fold-key tests, and authored the Through Lines cymbal choke explicitly.
- Promoted defects: synth-showcase audio-oracle failures (`MM-BUG-KILN-00002`) and
  velocity-insensitive brush-slap re-excitation (`MM-BUG-KILN-00001`).
- Refuted cleanup: defaulting `CymSpec` and collapsing explicit `ReedPreset` tables
  would obscure required acoustic choices for little benefit.
- Already fixed or documented: the open-hat match was exhaustive, and reverse-cymbal
  note-off behaviour was already documented.

## Current pass

- Wind/Bowed vibrato: Bowed was already fixed. During integration, Stage 1 commit
  `48a7e71` fixed Wind and `ede3347` refreshed all 32 affected listening assets;
  the duplicate scratchpad item is retired. `MM-BUG-KILN-00003` remains open for
  its independent two-eyes closure.
- SawStack canary: reproduced the two known fingerprints. New trunk evidence
  showed the split is cross-machine as well as profile-dependent, so the canary
  accepts that exact known set and still rejects any third render. Portable
  metric-based canaries remain separate follow-up work.
- Golden drum row: `print_golden_fixture` confirmed −22.30 dB / 685.2 Hz on current
  trunk. Re-pinned only channel 9; the balance guard remains green.
- Distorted-guitar quality review: superseded by the integrated guitar-v2 build.
  The shipped design implements the note's e-bow, compressor/sag, cabinet, and
  richer electric-pickup options, with focused lifecycle and stability oracles.
- Catalog freshness: the old full-render branch remains 20 commits behind and
  differs from current trunk in 52 listening files, including files for albums
  documented as MIDI-only. Promoted the durable policy and refresh work to Draft
  requirement `MM-REQ-KILN-00018` (proposed `Should`, `heavy`).
- Guitar sustainer cap: confirmed the engine pushes unlimited active voices and
  driven GM 29/30 Plucks sustain until effective release. Left open for Arthur's
  policy decision; recommend an eight-voice per-channel cap with oldest release.
- Cello listening assets: confirmed stale on trunk. The existing cello task has
  already rendered 36 GM42 assets across seven commits; it needs a rebase from its
  14-commit-behind base, not duplicate work in this pass.
- Big Weather latents: confirmed bank-select priority 2 follows program priority
  1, while both energy functions divide by the start meter. Big Weather authors
  no bank selects and all ten tracks are 4/4, so neither affects current output.
  Left open for the two policy choices recorded in `scratchpad.md`.
