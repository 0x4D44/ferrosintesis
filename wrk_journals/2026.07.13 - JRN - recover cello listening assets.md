# Recover cello listening assets

Recovered `task/20260711-TSK-claude-cello-waveguide`, the stranded Stage 2 GM42
listening refresh, and reconciled it onto current `origin/main`.

## Recovery

- The branch was clean, seven asset commits ahead and 43 commits behind trunk.
- Its merge base already contained the shipped waveguide cello implementation,
  sampler wiring, version 0.14.5 bump, and `render_opus.py --only-list`; only the
  36 committed listening assets remained stranded.
- Rebasing preserved all seven commits. Ten binary conflicts overlapped later
  trunk refreshes; the current trunk containers were retained during the rebase.

## Fresh blast-radius scan

The current 104-MIDI catalog contains 47 default-bank GM42 tracks. Thirty-six
have committed Opus assets and eleven VIGIL tracks remain intentionally MIDI-only.
The committed-asset set is exactly the original 36-file Stage 2 list, so no newer
album expanded the listening blast radius.

## Current-synth refresh

- Built the rebased v0.15.3 release synth.
- Surgically rendered all 36 tracks with `render_opus.py --only-list`.
- Nine assets were already current through later trunk refreshes: Burning
  Meridian ×3, Through Lines / Estuary Suite, and Atlas of Becoming ×5.
- The final branch delta is the remaining 27 GM42 listening assets. No unlisted
  file changed.

The parallel render exposed container variance: one Atlas output changed Opus
hash on a single-worker repeat, but decoding both encodes to float PCM produced
the same SHA-256. The same decoded-PCM check showed six post-render working
changes were container-only, so their newer trunk containers were retained.
Iron Tide and Skyline Brass Reactor had genuine decoded-audio changes and keep
their fresh current-synth renders.

## Verification

- `cargo build --release -p ferrosintesis-cli`: green at v0.15.3.
- Surgical render: 36/36 successful.
- Current MIDI program-state scan: 47 GM42 tracks, 36 committed Opus, no missing
  or unexpected target.
- Final Git delta: 27 listening assets, all members of the 36-file GM42 set.
- Container-only reconciliation used decoded float-PCM SHA-256, not raw Opus
  bytes.
