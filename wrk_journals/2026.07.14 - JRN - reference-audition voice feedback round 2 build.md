# JRN — reference-audition voice feedback round 2 (build)

Worktree: `D:/worktrees/midi-music/20260714-DEV-HUM-audition-voice-feedback-round2`
Branch: `task/20260714-DEV-HUM-audition-voice-feedback-round2`
Spec (root causes + fixes + oracles): `wrk_docs/2026.07.14 - CR - reference-audition voice
feedback round 2 investigation.md` (committed 1b6b7f4). Read it before touching a voice.

## Arthur's decisions (2026.07.14)
- **Scope:** full loop-build now, gated per-voice (oracle fail-before/pass-after + render-diff),
  amounts set to sensible defaults; Arthur listens to a fresh render and tunes ears-only amounts.
- **GM001/002/003:** split ALL THREE into distinct voices (bright acoustic, electric grand,
  honky-tonk), un-alias from GM000.
- **GM014 tam-tam:** KEEP the voice (load-bearing album gong); fix the audition only
  (drop `ALT_BANK[14]` so it's not A/B'd against tubular bells; give it its own slot).

## My defaults for the un-asked calls
- **GM044:** drop the redundant CC0 alt (both banks are the same violin-bodied tremolo).
- **GM076 blown bottle:** leave as-is (in-spec Helmholtz).
- **GM019:** default → legacy Leslie drawbar (3 edits: voices dispatch + engine
  `organ_leslie_family` + `cathedral_organ`). Keep CC0-GM19 = `legacy_church_organ` UNCHANGED so
  The Ninth Bell renders byte-identical (default now equals CC0 voice → drop the redundant GM19
  audition alt too). `CathedralOrgan` becomes unused → delete it + support + its tests
  (Arthur called it "terrible"); if the delete is too broad mid-build, `#[allow(dead_code)]` and
  flag it. Verify the Leslie by render.

## Build discipline
- Regression-first: add the oracle, `cargo test <name>` → expect FAIL on current code, implement,
  → expect PASS. Commit voice + oracle together.
- Scope tests small (`cargo test -p ferrosintesis <name> --release`? — audio oracles are heavy;
  run the single named test, stdin closed: `$null | cargo test ...`).
- Demo (`demos/ferrosintesis_reference`) `python build.py --verify` must stay green; rebuild the
  audition (`python build.py`) after rig changes (regenerates midi/lyrics/manifest). Run demo
  builds/renders in THIS worktree, never the main clone.
- **Render-diff inventory** once at the end (baseline binary in a throwaway `git worktree add
  <path> HEAD` = pre-change trunk; render every ALBUMS-table MIDI with both binaries; cmp).
  Expected diffs only on the intended programs; unexpected diff = bug.
- **One** version bump (`Cargo.toml` 0.19.0 → 0.20.0 or patch as fits) at the end, lockstep lock.
- Baseline binary already built at `target/release/ferrosintesis.exe` (pre-change). Reference
  renders + probes in `scratch_render/` (probe.py onset, metrics.py bands, timbale.py, evolve.py).

## Progress log (2026.07.14)
Base: rebased worktree onto origin/main = `ae3d15a` (was branched off the older b481405;
one trivial style commit ahead). Investigation reads (main clone @ ae3d15a) now match the
worktree exactly. Baseline binary for render-diff must be rebuilt from `ae3d15a` (the pre-
change trunk); the scratch_render/*.wav were rendered from the b481405 build (fine for the
investigation, re-render for the final render-diff).

LANDED (green, committed; full `cargo test -p ferrosintesis --lib` passed after timpani):
- timbale 65/66 split (0e93923) — `timbales_hi_vs_lo_distinct_pitch`
- recorder open-pipe ladder (4b6eb50) — `wd_recorder_is_an_open_pipe_not_a_sine`
- harp shimmer (e718551) + level-hold amp trim (e687e09) — `harp_46_shimmers`
- timpani de-boom (64dbf23) — `timpani_47_tail_stays_pitched_not_a_boom`
- lyrics marker→onset (bb387ae) — demo rebuilt + verify green

KEY LESSONS this session (heed before the rest):
- **Run a BROAD test scope per voice, not just its own oracle.** The harp brightening broke
  THREE bowed-family level oracles (`bowed_family_level_match`, `default_bowed_family_level_
  and_numerical_safety`, `default_bowed_body_bands_and_fiddle_identity`) because the HARP
  pluck is the early-window LEVEL REFERENCE for the pizz/bowed family (BW-O9). Fix pattern:
  brighten via `bright` (sustain shimmer, ratio-safe) + trim `amp` to hold the early-window
  level (amp is ratio-invariant so it doesn't touch centroid/soundboard differentials).
  I committed the harp before running the broad scope and shipped a red intermediate commit
  (e718551) — squash the per-voice commits at integration (round-1 did this) so trunk stays
  green commit-by-commit.
- **Even the broad scope can miss a signature golden with a TOLERANCE BAND.** The ensemble
  breath commit (cb59e5f) shipped RED: `sawstack_family_signatures_are_stable` pins strings(48)'s
  centroid ±2%, and breath 0.015 stayed inside the band (my scoped run passed) but the shipped
  0.025 drifted it to +3.7% → only the FULL suite caught it. Fixed by re-pinning the golden's
  expected centroid 683→708 (17f3351). LESSON: run the FULL `cargo test -p ferrosintesis --lib`
  before committing any voices.rs/engine.rs/drums.rs change, not just a name-filtered scope.
- **GM019 anchors verified (engine.rs):** `organ_leslie_family` (:95-96) `matches!(program,16..=18)
  || (program==19 && alt)` → make it `16..=19`; `cathedral_organ` (:99-100) `program==19 && !alt`
  → false for 19; pins at engine.rs:7066-7067 `assert!(!organ_leslie_family(19,false))` /
  `assert!(organ_leslie_family(19,true))` → invert the first. voices.rs make() arm 19 folds into
  the organ() arm. altbank.rs 19 = legacy_church_organ stays (The Ninth Bell byte-identical).
- **EDIT THE WORKTREE, not the main clone.** I twice-fat-fingered `d:\language\midi-music`
  (the sacred trunk-holder). Always `D:/worktrees/.../...`.

## DECISION NEEDED FROM ARTHUR — GM041 viola (deferred, NOT fixed)
"alt better / maybe replace" is fundamentally the **sample layer**, which is an INTENTIONAL
design choice with two pinned tests defending it:
- `default_bowed_articulations_and_sample_routing` asserts **"GM41 must skip the sample
  layer"** (41 is deliberately the only bare solo-bowed default; violin/cello/bass are
  LA-wrapped, and so is the CC0 *alt* of 41 — which is why the alt sounds better).
- `default_bowed_body_bands_and_fiddle_identity` pins the viola's **220 Hz body mode ≥ 1.10×
  violin** for instrument distinctness — the EQ revert (7.5→5.0 the alt uses) drops it to
  ~1.05× and FAILS this. So the alt's calmer EQ conflicts with the distinctness oracle.
Reversing the model-only design (give GM041 the violin sample layer + re-pin both tests) is
an architectural fork per doctrine — surfaced, not hacked. I reverted my EQ+routing attempt;
viola is back to shipped state. **Ask Arthur:** reverse the model-only design for GM041
(sample layer as default), accept a modest EQ, and re-pin the two tests? Or leave it.

## Progress checklist (tick as landed, each its own commit)
Tier 1 — clear wins:
- [x] timbale 65/66 split (0e93923) — `timbales_hi_vs_lo_distinct_pitch` (hi 377 / lo 278 Hz)
- [x] recorder harm ladder [0.09..]→[0.16,0.08,0.02] (4b6eb50) — `wd_recorder_is_an_open_pipe...`
- [x] harp bright 3000→6000, pick_lp 1800→3400, amp 0.62→0.56 (e718551 + e687e09) — `harp_46_shimmers`
- [x] timpani TIMPANI[0].T60 3.0→1.9 (64dbf23) — `timpani_47_tail_stays_pitched_not_a_boom`
- [x] viola — Arthur chose "give the default the sample layer". Applied: make() 41 routed to the
      LA-wrapped arm (Bowed(41) + LA_VIOLIN + violin_bank), BODY_VIOLA kept at +7.5 (distinctness
      oracle needs it), `default_bowed_articulations_and_sample_routing` re-pinned (41 moved from
      the skip-samples group to the carry-samples group), doc updated. All 8 `default_bowed*` +
      `bowed_family_level_match` green. (Uncommitted pending the full-suite check — see lesson.)
- [x] ensemble strings() breath 0.0→0.025 (cb59e5f) — `string_ensemble_48_has_bow_noise_air`
- [x] lyrics marker→onset (bb387ae) — demo rebuilt + verify green
- [ ] tam-tam audition de-weird — drop `ALT_BANK[14]` + `REGISTER_MAY_DIVERGE[14]` in programs.py
      (KEEP the synth voice — album-critical). Then rebuild demo + `build.py --verify`. NOTE:
      check_coverage_alt iterates ALT_BANK, so removing 14 also removes its coverage requirement
      (good). Arthur wanted "its own slot" — a standalone-alt audition slot needs new plumbing in
      melodic_slots; if too big, drop the A/B only and document.
Tier 2 — constants + oracle, tune by ear:
- [~] sax de-growl — ATTEMPTED + REVERTED (hard; Fable/focused-iteration candidate). Findings:
      * A flat convex power law (p^2.5) does NOT clean up mf: g~7 at mf is still deep in the tanh,
        mf/ff skirt only 0.46→0.38 (the skirt/rms ratio saturates). Not enough.
      * A smoothstep BLOOM gate on g (`g = g0 + g_span·p·smoothstep(LO,1.0,p)`) DOES clean mf
        (mf/ff 0.46→0.21 at LO=0.55) and preserves ff/G3 — BUT the clean→dirty knee is a discrete
        harmonic-content transition that BREAKS the tight O-PRESSURE invariants: at LO=0.55 P2
        (within-note brightening rise ≥1.35) fails 1.34; lowering to LO=0.45 fixes P2 but P5
        (honk band-winner monotone across velocities, ±1.5%) fails — alto peak jumps 1108→830→1108
        Hz at vel 80 (the gate-engagement velocity). Genuine multi-objective tension: preserve ff
        (gate must reach full by p=1) + clean mf (steep knee near mf) + O-PRESSURE monotonicity
        (no sharp knee) can't all hold with a simple smoothstep-on-g.
      * UNTRIED IDEA for next attempt: gate the rasp OUTPUT MIX (crossfade dry↔rasped by pressure)
        instead of the drive g. The rasp then runs at full internal drive at every pressure (so
        the honk peak location, P5, never jumps — it's just mixed lower at mf), while mf hears less
        rasp. May preserve P2/P5 monotonicity AND clean mf. Try this before spending Fable.
      * Alternative (taste): uniform g_span cut + relax G3's −40 dB ff floor — makes ff LESS raspy
        too (contradicts the design intent that ff is deliberately raspy). Arthur's ear call.
- [ ] guitar drive gains: engine.rs Drive::new tuple (~:259-266) raise g1 for 29 (2.5→~6-8) / 30
      (4.5→~8-10), re-match `post` for the −9.9/−11.6 dBFS loud-point. Oracle `driven_sustain_stays
      _distorted` (THD h2..16/f0 in the deep-hold window ≥ −12 dB; fail-first ~−20). Guarded by
      `drive_alias_floor` (−40 dB) — keep it green.
- [ ] GM019 Leslie default (3 edits): voices.rs make() fold 19 into the organ() arm (delete the
      `19 => CathedralOrgan` arm); engine.rs `organ_leslie_family` → include 19; engine.rs
      `cathedral_organ` → false for 19. LEAVE altbank.rs 19 = legacy_church_organ (The Ninth Bell
      renders byte-identical). INVERT the pinning tests at engine.rs:6921-6926. Oracle: default-bank
      GM19 CC1 sweep → tremulant AM rate ramps up. CathedralOrgan then unused → delete it +
      cathedral_registration + RankPipe + their tests (Arthur called it "terrible") OR
      #[allow(dead_code)] and flag. Default==CC0 now → drop the GM19 audition alt (programs.py).
Tier 3 — new voice code:
- [ ] choir voicing bypass: ChoirV2::render (~voices.rs:5230-5247) add a lowpassed (~500-650 Hz)
      copy of the dry pre-tract source blended in parallel (Klatt voicing branch) + a chest_lp
      field + small chest_gain. Oracle: low note A2 (key45), sustain band_rms(80-500)/rms > ~0.05
      (fail-first ~0.003). Default-on timbre change → render-diff + the version bump.
- [ ] EP pickup shaper: add `Option<PickupShaper>` to struct Modal (branch OUTSIDE the sample loop
      so every other Modal is byte-identical), `.with_pickup_drive(drive, asym)` builder, call from
      electric_piano_1/_2 scaled by vn + one-pole DC blocker on the shaped signal. Oracle: mag_at(4f)/
      mag_at(f) at 4f (absent from the EP1 table): <0.01 before, >~0.03 at vel120 AND rising with
      velocity; DC guard |mean|<1e-3.
Rig/scope:
- [ ] split GM001/002/003 (Arthur: split ALL THREE): give make() distinct arms — 1 bright acoustic
      (brighter EQ over acoustic_piano), 2 electric grand (CP-ish, nearer EP), 3 honky-tonk (detuned
      unison). Remove {1,2,3} from programs.py ALIAS. is_acoustic_piano (voices.rs:966) currently
      true for 0..=3 (applies LA piano bank) — decide sample routing per new voice. check_alias_
      dispatch + check_coverage_melodic must stay green (1/2/3 now audition as distinct slots).
      allowlisted_collapses_are_really_clones guard will force any stale ALLOW delete.
- [ ] drop GM044 redundant alt: remove ALT_BANK[44] (programs.py) — both banks are the same
      Bowed(44). altbank.rs 44 arm can stay (harmless) or be removed; check_coverage_alt drops the
      requirement automatically. (GM041 alt: leave pending the viola decision.)
Finish:
- [ ] render-diff inventory: baseline binary from a throwaway `git worktree add <p> ae3d15a`, render
      every render-catalog ALBUMS MIDI with both binaries, cmp. Expected diffs ONLY on touched
      programs (14/47/46/48/49/64-67/29/30/19/52-54/0-3 + drums 65/66). Unexpected diff = bug.
- [ ] version bump (Cargo.toml + lock lockstep) — ONE bump for the whole task (trunk is 0.19.1).
- [ ] full gate: demo `build.py --verify`; `cargo fmt --check`; `clippy --workspace --all-targets
      -D warnings`; `cargo test --workspace </dev/null`; `cargo build --release`.
- [ ] SQUASH the per-voice commits into a clean green-per-commit set at integration (round-1 did
      this) so trunk bisects clean. Then hand to Arthur for listen/tune + the viola decision.

## Round-2 remainder — BUILT (Fable session, 2026.07.14)

All eight remaining items landed, one green commit each (full `cargo test -p
ferrosintesis --lib` before every commit). Version bumped 0.19.1 → 0.20.0
(Cargo.toml + both dependents' requirement strings + Cargo.lock, one bump).

- [x] GM029/030 drive (f5a8b76) — `driven_sustain_stays_distorted`, fail-first
      −32.8/−35.8/−35.5/−35.4 dB → −25.6/−21.4/−30.1/−23.9 (29/30 × E2/A2,
      floors −29/−28/−33/−29). **The CR's operating-point model was refuted by
      measurement**: the voice fed the Drive ~24 dB below the tanh knee, so the
      briefed g1-raise alone moved the deep hold <2 dB. Landed: g1 9/8 (bias
      0.9 both — the old 0.45 washed out at the hotter swing), DRIVE.amp
      0.7→1.5, sustain 0.5→0.7, bright 4800→9000 (the DRIVE_LEAD lever),
      post re-matched END-TO-END (0.12/0.145). **Structural cap discovered:**
      a harder-clipped hold turns square and its windowed RMS overtakes the
      broadband attack (crest inversion) → the §2.6 sag-catcher fails at
      DRIVE.amp ≥ 2.0 — so "hold in HARD clip" is impossible by design; the
      hold rides the knee. Re-pins (values, never tolerances): golden ch 4
      −32.87/544.9; SUS_HOLD_REF_OFFSET_DB −11.6→−7.3 (bright 9000 releases
      the k_max clamp at E6); coupling peak bound 1.5→3.2 (amp ×2.14, same
      margin).
- [x] GM044 alt drop (156d40a) — programs.py only; verify green.
- [x] GM019 Leslie default (baf8dce) — the 3 edits + retired CathedralOrgan/
      RankPipe/registration/wind-chest/CathedralReverb + 13 tests (~2,100 lines
      deleted). New oracles: `default_gm19_cc1_ramps_the_leslie` (fail-first
      early 0.00/late 1.00 Hz — no ramp) and `default_gm19_is_the_legacy_
      drawbar_voice` (default ≡ CC0 bit-identical → the audition alt slot
      dropped). alt_bank_selects_distinct_voices drops 19 (intentional
      identity); gm_routing pin 19→"organ". The Ninth Bell's CC0 path
      untouched (legacy_buf routing + chorus defaults preserved).
- [x] tam-tam standalone slot (57fb038) — STANDALONE_ALT plumbing in
      melodic_slots (own register/gesture/display name, appended after the
      walk); check_ab_parity skips them; check_coverage_alt fixed to resolve
      the LATEST program at CC0 (the max-program shortcut only held for the
      ascending inline walk). Auditions as "GM 014 Tam-Tam [alt]", one ONESHOT
      crash at (36,47).
- [x] GM001/002/003 split (5989d13) — bright grand (grand_piano_variant,
      bright 0.55→0.67, 12×/4.8k hammer), CP electric grand (own construction:
      inharm 0.0011, t1 5.5, aftersound 0.07, no LA layer/no sym bus —
      is_acoustic_piano = {0,1,3}), honky-tonk (trichord ±12 cents at 0.85,
      k≤6, third string). Oracle = the distinctness matrix: 6 ALLOW entries
      deleted (fail-first: "GM 0 vs 1 scores 0.1786 ≥ EPS 0.03"); pairwise
      0.179-0.358, GM2 vs EP1/EP2 0.43/0.48. GM0 byte-identical.
- [x] GM052/053 chest branch (11a8abe) — 580 Hz 2-pole lowpassed dry pre-tract
      copy in parallel (chest_gain 52: 0.35, 53: 0.50, 54: 0 + bit-identity
      guard); amp re-trim 52 ×0.85 / 53 ×0.44 holds the strings-continuity
      window. **The CR's band-fraction oracle was refuted**: 80-500 Hz already
      collects formant skirts (0.20/0.64 on the DEFECTIVE code); the honest
      metric is h1 vs the note's loudest harmonic (fail-first −24.4/−19.6 dB →
      −9.5/−9.3, floor −12). Metric self-saturates ~−9 (the chest lifts h2-h4
      too) — pushing gain past 0.5 bought nothing and broke the vowel oracles.
- [x] GM004/005 pickup shaper (81d1c5b) — PickupShaper on Modal (bias-tanh +
      20 Hz DC blocker, Option branched outside the loop like bloom); EP1
      drive 0.6+3.2·vn bias 0.4, EP2 0.8+3.8·vn bias 0.45. Oracle h4/h1
      (4f absent from EP1's table): fail-first 0.0011 flat → GM4
      .0042/.0170/.0545, GM5 .0153/.0319/.0452 at vel 60/90/120; DC <1e-3
      (10 Hz let 1.1e-3 through on the Wurly → 20 Hz).
- [x] GM064-067 sax bloom (da8fe76) — **the untried mix idea, taken one layer
      deeper**: the audible mf dirt IS the noise-intermodulation skirts (the
      harmonic edge is already darkened by RD3 downstream), so gate the
      TURBULENCE INTENSITY only: n_eff ∝ p⁴ (was p²), one line in
      ReedRasp::set_drive. Harmonic path (g law, tent, honk, tilt) untouched
      at every p → P2/P5/P3 cannot regress (all 10 O-PRESSURE green,
      untouched); ff bit-identical → G3 floors identical (−38.5/−37.9/−37.4/
      −36.2), ff/p strengthened 3.2-6.3×. Oracle `reed_sax_rasp_blooms_with_
      pressure` (fail-first sop mf/ff 0.550): ff ≥ −40 dB AND (mf ≤ 0.4×ff OR
      mf ≤ −42 dB absolute — the RD_O12B audibility budget; the bari passes on
      the absolute clause at −43.3 dB, its 0.445 residue being the
      pressure-blind pulse-jitter floor).

Ear-tunable defaults for Arthur (all named constants):
- Drive: g1 9.0/8.0, bias 0.9/0.9, post 0.12/0.145; DRIVE amp 1.5,
  sustain 0.7, bright 9000. Deep-hold THD tops out ≈ −21..−30 dB (knee, not
  rail) — deeper needs relaxing the §2.6 no-late-bloom guard (Arthur's call).
- Honky-tonk detune ±12 cents / 0.85; bright-grand +0.12 brightness;
  CP inharm 0.0011 / t1 5.5.
- Choir chest_gain 0.35/0.50 + amp trims 0.85/0.44.
- EP pickup drive/bias per program above.
- Sax turbulence exponent p⁴ (steeper = more ff-gated rasp).

## Verification — technical-lead sign-off (2026.07.14, post-Fable)

Fable built all 8 remaining items; I own the gate, the render-diff interpretation, and
the integration handoff. Reviewed the four spec-deviating / golden-re-pinning commits by
understanding (not just re-running):
- **sax (da8fe76)** — gates turbulence INTENSITY (`n_eff ∝ p⁴`, was p²), leaving the whole
  harmonic path bit-identical at every pressure, so O-PRESSURE P2/P5/P3 cannot regress by
  construction and ff is unchanged (`window(1)=1`). This is the logged output-mix idea taken
  one layer deeper (gate the noise source, not the drive g). Sound.
- **guitar (f5a8b76)** — re-pins are expected-VALUES not tolerances (golden ch4 centroid
  814→545 Hz is real cabinet-rolloff darkening of a harder-clipped tone); the coupling
  safety bound 1.5→3.2 is a proportional rescale (amp ×2.14 → peak 0.73→1.57, same 2×
  margin, still catches runaway). Legit; darkness is an ear call for Arthur.
- **choir (11a8abe)** — Fable correctly REFUTED my CR band-fraction oracle as a false-pass
  (80–500 Hz already reads 0.20/0.64 on the defective code); replaced with h1-vs-loudest-
  harmonic (amp-invariant, fail-first −24.4/−19.6 → −9.5/−9.3 dB). GM54 bit-identical via
  the `chest_gain==0` guard. Better oracle than I specified.
- **GM019 (baf8dce)** — retires CathedralOrgan (~1350 lines) once GM19 stops routing there;
  The Ninth Bell's CC0-GM19 pinned byte-identical. In-scope but a reversible deletion —
  surfaced to Arthur (restore path = revert one commit).

Gate (all mine, all green): `fmt --check`; `clippy --workspace --all-targets -D warnings`
(0 warnings); `cargo test --workspace` **446 passed / 0 failed** (ferrosintesis lib 421,
render-catalog parameter-parity 16 — CathedralOrgan deletion left no dangling param);
`cargo build --release` clean (the os-error-5 was only the render-diff holding the exe);
demo `build.py --verify` PASS.

Render-diff — INDEPENDENTLY re-verified contamination, not just trusting Fable's bespoke
script. Scanned all 114 corpus MIDIs (albums + demos) for touched-set membership; exactly
**4** contain no render-changing touched program/key (the only places contamination could
hide) — Three Flights Up (GM0-incl.), bass_demo, mccartney_bass, and the FX demo (GM96–127)
— and **all 4 render byte-identical** baseline(ae3d15a) vs task. Contamination = 0, and GM0
is render-confirmed byte-identical (positive control). The 4 no-touch files also carry drum
keys 36–51 but not 65/66, confirming the timbale arm-split left other keys bit-identical.

**render_diff.py blind spot (durable finding):** the harness keys on the program BYTE, so it
cannot distinguish default-bank GM19 from CC0-bank GM19. With `--program 19`, The Ninth Bell
(CC0-GM19, byte-identical) would show as a FALSE not-reached. For bank-dependent voice
changes, adjudicate by bank, not program number alone (Fable's per-file inventory + the
CC0 bit-identity pin cover it here).

Deliverable for Arthur's listen: rendered all 6 reference-audition tracks to WAV in
`round2_audition_wav/` (git-ignored). Housekeeping: retired the `tmp-render-baseline`
worktree; `scratch_render/` (Fable's analysis scratch) left untracked. State: **done, green,
v0.20.0, NOT pushed, unsquashed** — awaiting Arthur's listen, the GM019-deletion decision,
and the integration go (squash per-item commits at integration).
