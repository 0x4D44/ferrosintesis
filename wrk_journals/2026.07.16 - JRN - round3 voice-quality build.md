# 2026.07.16 - JRN - round3 voice-quality build

Task mode: **Build** (self-paced /loop). Worktree
`D:/worktrees/midi-music/20260716-FEAT-HUM-round3-voice-quality-build`, branch
`task/20260716-FEAT-HUM-round3-voice-quality-build`, base 33579ac (origin/main after the
round-3 docs branch was docs-lane integrated 2026-07-16).

Authoritative design:
- `wrk_docs/2026.07.15 - HLD - ferrosintesis round-3 voice-quality plan.md` (the plan; §7-9 addenda)
- `wrk_docs/2026.07.16 - HLD - perceptual distinctness oracle.md` (Wave-0 spec, ACCEPTED, incl. §7 two-tier)

**Scope cap:** Tier-1 (CC0) / CC-BY assets + model fixes only. GPL steel crate PAUSED.
No CC-BY-NC / Sampling-Plus / Iowa ever.

**Discipline per loop prompt:** oracle-first (RED on HEAD observed, then GREEN); render-diff
inventory (branch-point baseline vs HEAD, all ALBUMS MIDIs, cmp) for any
voices.rs/engine.rs/sampler.rs/drums.rs change; atomic commits, explicit paths,
Co-Authored-By trailer; ONE version bump per shipped unit (U0 is test-only → no bump);
amount-tuning / ear calls go to the EAR-A/B LIST, never block the loop.

## Ordered unit list

- [x] **U0 — Wave 0: `perceptual_distinctness` module** (testutil.rs, alongside the old
  matrix). Two-tier metric per oracle HLD §2-§7: 7-dim passport, saturating bounded-influence
  sum, samples-on, keys {48,72}, 5 windows, SHORT/LONG blend; `is_shared_onset` tier split
  (Tier-2 credit = D1/D3/D4 on W3-W5 only); 6 tests + `print_perceptual_matrix` (#[ignore]);
  gated `#[cfg(all(test, feature = "embedded-samples"))]`. RED-first observation = empty-ALLOW
  run flags exactly (0,1),(0,3),(4,5),(29,30); commit GREEN with those four `Collapse`
  entries. Calibration per §2.4 + per-tier §7; tripwires T1-T9. **Test-only → NO bump.**
- [x] **U1 — GM7 clavinet bug fixes** (plan §3.3, Wave 1): resurrect dead `attack_noise`
  (voices.rs ~3434/3439 if/else-if — `click_post` and `attack_noise` must coexist);
  strengthen/relocate `grit` so metallic buzz survives into the sustain. Oracles: pickup
  shelf ~4.5 kHz + odd-harmonic sustain grit (0.2-0.5 s) + broadband onset vs forced-off.
  Bump.
- [x] **U2 — GM29/30 drive main≠alt** (plan §3.6, Wave 1): cut `DRIVE` e-bow (floor sustain
  ~0.15 OR migrate the ~5 `&DRIVE` sustainer oracles to `DRIVE_LEAD`); `DRIVE_LEAD` swelling
  onset. MAIN≠ALT differential oracle (both banks, sustain-index gap + onset gap); move
  distorted-tail THD onto DRIVE_LEAD. Then delete the (29,30) Collapse entry (U0 test 2
  forces it) — but (29,30) byte-identity test 4 must be updated in the same change. Bump.
- [ ] **U3 — GM0/1/3 piano sample-DSP in LaVoice** (plan §3.1, Wave 1): default-inert
  per-program params — GM1 high-shelf tilt (+~4 dB >1.8 kHz) on the sample readout; GM3
  honky — NOTE T4/M1: must earn Tier-2 credit via D1/D3 tail timbre (genuine tack-piano:
  brighter/harder/shorter), detune-alone cannot pass the two-tier metric. GM0 + all other
  wrapped programs byte-identical. Delete (0,1),(0,3) Collapse entries. Bump.
- [ ] **U4 — GM6 harpsichord quill excitation** (plan §3.3, Wave 2): per-preset opt-in
  excitation shape on `PluckPreset` (default = today's noise burst); bright buzzy quill
  pulse; 4′ choir stays OPTIONAL (H3: oracle must hold with 8′ only). Oracle: persistent
  high-centroid vs GM0 control + attack-noise presence. Bump.
- [ ] **U5 — GM4/5 e-piano split** (plan §3.2, Wave 2): GM5 → genuine 2-op FM (new dsp.rs
  voice; sheen persists into sustain); GM4 → Modal with raised bell/tine partials (bark
  0.4-0.6× f0 at onset, knock-then-bloom). M2: keep `electric_piano_pickup_bark_grows_with_
  velocity` green for BOTH progs 4 and 5 (h4/h1 monotone, DC<1e-3) or revise in-change.
  Mandatory loudness re-check (GM4 in 4 committed albums). Delete (4,5) entry. Bump.
- [ ] **U6 — GM25 steel S-levers** (plan §3.5, Wave 2): body re-voice (low-mid scoop ~350 Hz,
  presence 2800 Hz +3-4 dB, ~4.5 kHz sparkle); sharpen flatpick (`click_hp`→~2500,
  `attack_noise` 0.05-0.08); A/B the CC0 nylon-bank pick-attack borrow (license-clean, no new
  asset). Gate on the 3-5 kHz sparkle-presence oracle (H2); inharmonicity oracle ONLY if the
  dispersion spike ever ships (spike itself → EAR list, not this loop). Mandolin double-duty
  (Hollow Hill, The Signal Fire) → EAR list per-use listening. Bump.
- [ ] **U7 — GM21/23 accordions + GM19 16′** (plan §3.4, Wave 3): musette spread across
  harmonics (detuned pairs H2/H3/H4); reed brightness (`drive` 0→0.15-0.3 + formant tilt);
  bellows chiff; REMOVE the coherent global tremolo (the oracle cheat); GM23 = drier/sharper
  bandoneon (narrower musette than GM21); GM19 add missing 16′ foundation partial only.
  Oracle: per-partial complex-demodulate/Hilbert AM beating-sidebands, Δh scales with h,
  window ≥0.5-1 s (H4); GM23 drier-than-GM21; GM19 f0/2 band_rms present; keep Leslie ramp
  green. GM19 further presence lever → EAR list. Bump.
- [ ] **U8 — GM4 `rhodes_bank` jRhodes CC0 LA attack layer** (plan §7.2/§7.4): genuine
  onset-only LA layer, reachable with today's `LaVoice` (contrabass_bank is the wiring
  template). New Tier-1 pin (jRhodes CC0 GM subset — CC0 covers the WAV bytes; the
  velocity-bark morph layer is CC-BY-NC → REJECTED, velocity→brightness stays in the model).
  prepare.py pipeline + samples-crate growth + provenance. Bump.
- [ ] **U9 — GM14 tam-tam → ARCHITECTURAL FORK, STOP & ASK** (plan §3.7): the preferred
  route needs a NEW full one-shot melodic player subsystem in sampler.rs (VSCO-2-CE CC0 gong,
  ~36 layers, already-pinned source) — the exact fork the loop prompt reserves for Arthur.
  Present at the stop together with: (a) the GM6 VCSL release-sample-layer subsystem (same
  class of sampler engineering, new VCSL pin), (b) the dense-mode-cloud model fallback,
  (c) the *Ten Metres of Air* final-chord re-voice follow-on. Do NOT build unprompted.

**NEXT: U3**

## Decisions

- 2026-07-16: docs branch `task/20260715-DOC-HUM-round3-voice-quality-plan` rebased onto
  b503241 and docs-lane ff-pushed (b503241..33579ac); worktree + branch reaped. Build
  worktree minted off 33579ac.
- 2026-07-16: unit order = Wave 0 → Wave 1 (clav bugs, drive, pianos) → Wave 2 (harpsi, EP,
  steel) → Wave 3 (organ/accordion) → Tier-1 asset unit (rhodes) → GM14 fork stop. GM14 and
  the GM6 release-layer are deliberately LAST so maximum value lands before the
  architectural stop.

## EAR-A/B LIST (batched, non-blocking — one human pass at the end)

- Shared-bank pair adjudications the two-tier oracle cannot decide (oracle HLD §6/§7):
  **(40,41)** tail 0.1307 (suspect defect: viola = repitched-violin proxy) and **(48,49)**
  tail 0.0602 — both under BAR_TAIL 0.76, landed as `Why::EarPending` ALLOW entries. One
  same/different listen each → `EarAccepted` or a voice-fix req (viola bank = Stage 3).
- **(4,5) UNDER-REDS the perceptual matrix** — full-tier score 1.0236 vs min(POS) 0.1071:
  the §2.4 calibration gap is empty and 15×-inverted, beyond the one sanctioned JND
  revision (not spent). At matched pitch our GM4/5 genuinely differ in envelope (D1≈2.4)
  and band energy (D7≈1.0-1.3); the "very similar" ear verdict is instrument-CLASS
  identity — the oracle HLD's §6.1 honest limit. Listen once to confirm the complaint
  still stands post-U5; U5 carries its own fail-first oracles (bark, FM sheen, sustain
  distinctness) and its before/after jump in this matrix (baseline 1.0236) is reported.
- (40,44) full 1.2026 and (72,73) full 0.3280 both PASS the full bar — no gate action;
  informational listens only if Arthur wants ((72,73) classified independent-onset: the
  0.06 s flute fade lets the models diverge inside W1 — investigated, not a fallback).
- (16,17) full 0.3160 — organ 1/2, the family's tightest non-flagged pair; passes the
  frozen bar with wide margin but was the closest "surprisingly tight" observation.
- **U1 clavinet amounts** (mechanisms landed at defensible defaults, one listen each):
  (a) sustained yarn/fret buzz — preset `buzz: 0.22` through the contact-gated HF stage
  (×8 internal make-up) lands the sustain buzz at ~−15 dB HF share (0.17 vs real-clav
  "prominent"); too little/too much is one constant. (b) tangent scrape — `attack_noise`
  0.55 (was 0.22, but that value dated from when the param was DEAD, never ear-set);
  sits ~−16 dB under the onset next to the gain-2.0 click.
- **U2 drive amounts**: the default 29/30 bank now DECAYS on held notes (25 albums move —
  the intended default-on timbre change); the alt lead swells over 30 ms. One listen on a
  held-drive passage (Big Weather / Bright Matter) confirms the decay reads as "guitar",
  and on an alt-bank lead that the swell isn't too slow/fast.

## Log

- 2026-07-16 bootstrap: docs integrated, worktree + journal created. Starting U0.
- 2026-07-16 **U0 DONE (gated-green, test-only, no bump).** `perceptual_distinctness`
  module in testutil.rs: 7-dim passport, two-tier (§7) metric, 6 oracle tests + tier pin +
  `print_perceptual_matrix`; T7 fade guard in voices.rs. RED-on-HEAD observed with empty
  ALLOW: tail tier flagged exactly (0,1),(0,3),(1,3),(29,30) + the two shared-bank
  adjudication candidates; committed GREEN with 4 Collapse + 2 EarPending entries.
  FROZEN: BAR_FULL=0.075 (POS-side only — no NEG_full anchor survived), BAR_TAIL=0.76
  (1.25·max(NEG_tail)=1.25·0.6043). Deviations from the HLD, each measured + documented
  in-code: (a) test 6 uses W1 waveform rel-diff (>0.05) instead of ">0.5 dB RMS" — the
  la_level_continuity contract level-matches engagements (GM68 measured −0.24 dB while
  fully engaged), so RMS is blind to a correct engagement; (b) (4,5) ear-adjudicated per
  §2.4's fallback (see EAR list); (c) (72,73) independent-onset (early fade), full tier.
  Runtime +37 s debug (256 3-s renders, OnceLock-shared). Full crate suite 454 green;
  clippy -D warnings + fmt clean.
  GREEN-transition contracts for later units: U2 deletes the (29,30) Collapse entry AND
  updates `byte_identical_arms_score_zero` + the tier pin in the same change; U3 deletes
  the three piano Collapse entries + re-pins tiers (the piano/honky fixes un-share the
  onsets → pairs reclassify to the full tier, where D2/D5/D6/D7 credit them; T4 guards
  flange-vs-beat via the d5≈0 check at key 72).
- 2026-07-16 **U1 DONE (gated-green, 0.21.2→0.21.3).** Clavinet bug A: dead `attack_noise`
  resurrected via a dedicated `scrape` post-out slot (only the click_post+attack_noise
  combination builds it — every other preset byte-identical); level raised 0.22→0.55 (the
  old value was never audible-calibrated — it was dead since birth). Bug B: sustained
  yarn/fret buzz as a contact-GATED highpassed injection (n = y over a 40 ms-tracked
  envelope; gate > 0.45 opens the top third of every swing; deterministic, no rng draw,
  pre-pickup). Two dead ends measured + documented in-code: an envelope-normalized CUBIC
  (smooth waveshaper cannot make real HF from a dark sustain: frac 0.063→0.076) and a
  1-sample snap train (energy-starved, −38 dB). Landed: sustain HF frac 0.063→0.171,
  ring-persistence 0.37×→0.58×. Oracles RED→GREEN: `clavinet_tangent_scrape_speaks`
  (byte-identical proof + click-free lesion diff ≥ −24 dB), `clavinet_buzz_survives_into_
  the_sustain` (audible ≥0.12 + late≥0.5·mid + differential ≥1.5×). Full crate suite 456
  green; fmt+clippy clean. Render-diff inventory (33579ac baseline vs U1, all 99 album
  MIDIs): **SAME 99 / DIFF 0 / FAIL 0** — no committed album uses GM7, and the shared
  PluckPreset/scrape/buzz plumbing leaked nowhere. Oracle-metric lesson: with the click
  alive, the scrape's with/without band read drowns in click rng-realization (bursts
  interleave draws on the shared voice rng) — assert on a click-free lesion; and Hann-
  windowed spectral_band_rms zeroes t≈0 content (use time-domain band_rms for onsets).
- 2026-07-16 **U2 DONE (gated-green, 0.21.3→0.21.4).** Drive main≠alt restored (plan
  §3.6): DRIVE.sustain 0.7→0.0 (the default bank is a DECAYING overdriven pluck again —
  DEFAULTS-standard config, no formula edge cases) and DRIVE_LEAD gets a 30 ms swell
  (attack_s — deliberately sized INSIDE the sustainer's 80 ms spoken-level capture; a
  60 ms swell deflated the captured reference and broke the V6a hold-calibration chain).
  New fail-first oracle `driven_main_and_alt_banks_diverge` (RED at HEAD: gap −0.6 dB —
  the MAIN held louder than the lead; GREEN: sustain-index gap ≥6 dB + ≥25 ms swell gap).
  The five &DRIVE sustainer oracles (V6a/V6c/release/self-osc/sus_probe) migrated to
  &DRIVE_LEAD; SUS_HOLD_REF_OFFSET_DB re-captured −7.3→−3.9 for the lead's softer click
  + swell (E5 −12.0 / E6 −4.6 rel-ref, provenance in-code); V6a's crest ref widened to
  [0.02,0.20] (the swell moves the crest per pitch). Engine THD pins
  (driven_sustain_stays_distorted, sustained_lead_stays_harmonic_through_drive) follow
  the hold to the alt bank via CC0=1 — floors unchanged, cleared at 1.4-4.1 dB margins.
  Golden mix fixture re-captured ch-4-only (−32.87→−32.96 dB, centroid 545→808 — decaying
  tails are brighter on average); all other rows + canaries BIT-EXACT. Per plan review M3
  the (29,30) perceptual-matrix Collapse entry STAYS (the complaint was main-vs-alt; the
  programs deliberately still share the arm) — U0's "U2 deletes it" note was wrong, ALLOW
  comment corrected. Full suite 457 green; clippy+fmt clean. Render-diff (56da136
  baseline): SAME 74 / DIFF 25 / FAIL 0 — the 25 movers are exactly the albums that PLAY
  GM29/30; the 26th candidate (Winter Guests Part One) declares GM30 on ch13 with zero
  note-ons (verified) — correct non-reach. Contamination 0.
