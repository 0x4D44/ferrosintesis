# 2026.07.16 - JRN - voice-quality r3, Stage 1 (flue breath flutter + sax de-fuzz)

Task mode: **Build**. Worktree `20260716-FIX-HUM-voice-r3-stage1-flue-sax` off origin/main.
Implements Stage 1 of `wrk_docs/2026.07.16 - PLN - voice-quality round 3 (15 voices)
roadmap.md` — the two quick wins (Arthur's most vivid complaints).

Process: lean design→build (Arthur's brief). At most 2-3 helper agents per step, one
external cross-family review (Codex), no wider fan-out. Fable retainer held in reserve —
Stage 1 is constants + one deterministic LFO, cheaply verifiable, so it does not meet the
"hard + high-leverage + hard-to-verify" bar.

## Intent (one line)

Kill the flue pipes' "shaking sshsshssh" (a deep pitch-vibrato-locked breath AM at the
~4 Hz fluctuation-strength peak) and the saxes' "fuzz on top" (p⁴ rasp turbulence ~200×
the real module), by model tuning only — no new samples, no other families.

## STEP 1 — UNDERSTAND (code read, worktree)

- Flue breath AM: `voices.rs:6778` `breath_mod = 1.0 + 0.5*self.vib_val`, applied to all
  three noise components (helm jet 6788, bed 6794, muraiki 6799). `vib_val` is the pitch
  vibrato LFO (control-rate, preset rate 4.5-5.5 Hz) → the AM is coherent with pitch and
  deep (±50%). Presets carry the breath level: PAN_FLUTE 0.24 (6405), SHAKUHACHI 0.30
  (6455), BLOWN_BOTTLE 0.80 (helm), WHISTLE 0.30, RECORDER 0.055, FLUTE 0.09.
- RNG alignment: the render loop keeps a FIXED `rng.white()` draw count per sample so the
  WD-O5 differential seam (`wd_o5_breath_fraction_bands_and_ordering`, 18035, renders
  full vs breath-zeroed with the same seed and subtracts) stays aligned. `control_lfo`
  (voices.rs:103) draws one `rng.white()` for its rate jitter → a flutter LFO must use a
  SEPARATE throwaway `Rng`, not `self.rng`.
- Sax rasp: `ReedPreset.rasp = (g_base, g_span, noise_base)` = `(0.5, 15.5, X)`; X per sax
  SOP 0.12 (7767) / ALTO 0.085 (7785) / TENOR 0.058 (7803) / BARI 0.040 (7821). Turbulence
  rms = `noise_base·p⁴·(1+span·growl)` (`ReedRasp::set_drive`, 8152). Only `noise_base`
  (X) needs cutting; the tanh harmonic drive `g = 0.5+15.5·p` stays (that IS the reedy
  edge). Oracle `reed_sax_rasp_blooms_with_pressure` (17313) asserts `ff_db >= -40` — will
  drop below with a smaller rasp; re-pin the floor (bloom-with-pressure intent survives).

See the HLD for the design + acceptance criteria.

## Log

### Baselines (current origin/main via release binary + probe A/B)

Sax inharmonic fraction (vel96, key60), ours vs SC-55 — confirms the reviewers' numbers:
- soprano 0.0227 / 0.0001; alto 0.0124 / 0.0004; tenor 0.0052 / 0.0009; bari 0.0022 / 0.0043.
- So bari is already CLEANER than the real SC-55 bari → do not cut bari. Cut sop/alto/tenor
  toward a uniform ~0.003 target. Sax body baseline (h2/h3, dB re f0): sop -3.4/-15.1,
  alto -2.9/-14.1, tenor -0.5/-16.1, bari -0.3/-19.6 (the h3 duty-null = Stage 2, not this).

Flue coherent-AM (new metric): bandpass a breath-dominated band, Hilbert envelope, envelope
spectrum. Current code — envelope peak sits at the pitch-vib rate (~5 Hz) for ALL flue
voices, and the vib-band (3.8-6 Hz) energy dominates the flutter band (1.6-3 Hz):
- pan E@vib/E@flut = 361/75 (4.8×); bottle 128100/521 (246×); shak 112/2.9 (38×);
  flute 340/4.65 (73×, but on a quiet bed — masked).
- Confirms: the "shaking" is a coherent AM locked to the vib rate. After the fix the peak
  should move to ~2.2 Hz and the vib-band energy collapse. This is the acceptance metric and
  the basis for the new Rust oracle (which will use the WD-O5 differential to isolate breath
  cleanly, avoiding the harmonic-band picking the Python probe needs).

### STEP 3 adversarial review — 2 reviewers + refutation (done)
Real survivors folded into the HLD: sax under-scoped the oracle work (missed
reed_g3_sax_rasp_skirts shipping gate; a 4-5× cut sinks both skirt oracles to the rasp-off
null) → moderate measured cut + re-pin both + re-check reed_o6/o7/o8a, o_pressure_p3; the
flue acceptance metric was wrong (broadband depth is noise-dominated) → coherent-AM-at-vib-
rate metric + new oracle; control_lfo gives rate- not phase-decorrelation → build the flutter
inline with a seed phase; shak 0.18→0.20; D6 reasoning corrected (WD-O5 measures RMS).

### STEP 4 external review (Codex, gpt-5.6-sol)
Codex read-only sandbox helper is missing on this box (`codex-windows-sandbox-setup.exe` not
found) → every filesystem read failed; ARG_MAX blocked a 58 KB self-contained prompt as an
arg; stdin-fed prompt produced no response. Third attempt worked: design doc (~13 KB) as arg,
codex reviewed the REASONING (no code access). Real survivors folded into the HLD: flutter
2.2→2.0 Hz (keep 2nd harmonic out of the vib band so the AM oracle stays clean); sax skirt
oracle re-anchored to a rasp-on-vs-off DIFFERENTIAL (absolute floor off my own output is
circular); new AM oracle over pan/shak/bottle+flute, band-based; multi-key/vel sax validation
+ harmonic-peak centroid; sharpen "self.rng draw-sequence identical". Reduced-fidelity pass
(no code) noted as a residual risk; the internal 2-reviewer pass carried the code rigor.

### STEP 5 CHECKPOINT — design SELF-APPROVED (2026-07-16)
Two review layers done, no architectural fork, no unresolved critical finding → proceeding to
implement. Not human SIGNED-OFF (integration is the human gate).

### STEP 6 IMPLEMENT + measured outcomes
Code (voices.rs): WD_FLUTTER_{HZ 2.0, DEPTH 0.12, JIT 0.08, SALT}; Wind gains `flutter: Sine`
+ `flutter_val`, built via control_lfo from `&mut Rng::new(seed ^ SALT)` (no self.rng draw);
breath_mod → `1 + 0.12*flutter_val`. PAN_FLUTE breath 0.24→0.12, SHAKUHACHI 0.30→0.20. Sax
noise_base: sop 0.12→0.044, alto 0.085→0.042, tenor 0.058→0.044, bari 0.040 (kept). Version
0.21.1→0.21.2.

Measured with the new release binary (probe A/B):
- SAX inharm (vel96 key60): sop 0.0227→**0.0042**, alto 0.0124→**0.0038**, tenor 0.0052→**0.0033**,
  bari 0.0022 (all in the SC-55 range 0.0001-0.0043); h2/h3 body preserved (Δ<0.2 dB), centroid
  −4..−5 % (removing HF noise). Fuzz cleared.
- FLUE coherent breath-AM (differential-isolated, the new oracle): pan flutter/vib **10.5×**,
  shak **11.2×**, flute **5.5×** — the flutter dominates the vib rate; envelope peak moved
  5 Hz→2 Hz. (Python bandpass proxy was confounded by harmonic FM — codex called it — so the
  Rust differential is the definitive metric.)

Oracle re-pins (all RED-before/GREEN-after): WD-O5 pan [0.038,0.085]→[0.021,0.037], shak
[0.048,0.105]→[0.040,0.062] (ordering + shak>3·fl still hold: pan 0.0277, shak 0.0484, fl
0.0143); reed_g3 re-anchored to a rasp-ON−rasp-OFF differential (ff must be ≥4 dB over the
null — measured +7.0/+8.0/+10.6/+7.1 dB, non-circular per codex); bloom oracle's redundant
−40 dB floor removed (presence now guarded by reed_g3). New oracle
`wd_breath_am_flutters_off_the_pitch_vibrato`. Full lib suite: **446 passed, 0 failed**.

### STEP 7 CODE REVIEW (1 independent reviewer) + simplify
Reviewer verified: zero self.rng draws (seam intact), zero-mean breath_mod, differential
isolation sound, reed_g3 differential non-circular. Findings folded: (1) cargo fmt was RED →
`cargo fmt`; (2) stale bloom-oracle comment → rewritten; (3) bottle coverage gap; (4) comment
nit → tightened. Simplify: hoisted a double render in reed_g3; refactored the AM oracle to a
shared `am` closure (no duplication).
- On (3), tried to guard the bottle by killing its vibrato — but it STILL failed (ratio 0.85):
  the Helmholtz **resonator** narrows the jet into a narrowband signal whose envelope
  fluctuates randomly across all low frequencies, swamping any coherent-AM read (the confound
  is the resonator, NOT the vibrato). The bottle is genuinely not measurable this way; it rides
  the SAME breath_mod on its jet, so the pan/shak/flute oracle guards the shared mechanism.
  Documented in the oracle. (This is a deviation from the design's "oracle covers …bottle".)

### STEP 8 VALIDATE
cargo fmt --check clean; clippy --workspace --all-targets -D warnings clean; cargo test
--workspace green (ferrosintesis 446, samples 2/5/2, render-catalog 16 incl. param-parity +
ropusenc-argv goldens).

Render-diff inventory (baseline 7b78a11 = merge-base, over all 99 album MIDIs, touched GM
64/65/66/72-79): **CONTAMINATION 0, NOT-REACHED 0** — 37 albums changed (all use a touched
voice: 21 via GM73 flute, rest sax 65 / pipes 72/74/75/77/78), 62 byte-identical. Perfect
scope: the RNG seam held (no untouched album moved) and the fix reached every album that
uses a touched voice. The 21 GM73 flute hits are expected (the flutter is shared by all
winds) and inaudible — RNG-identical noise, only the AM envelope shallowed (new oracle:
flute 5.5×; WD-O5 flute fraction 0.0143, in its tight band).

### DONE — ready to integrate (pending Arthur's go)
All acceptance criteria met, all gates green, one version bump (0.21.2). Not pushed.
