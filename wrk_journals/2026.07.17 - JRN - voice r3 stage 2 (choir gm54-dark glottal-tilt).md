# 2026.07.17 — JRN — Voice R3 Stage 2: choir GM52/53/54 (aahs/oohs/synth-voice)

Brass-loop slice 4 of 4 (LAST). Lean 8-step process, SC-55-anchored physical realism.

## The complaint / root cause (roadmap PLN §Choir)
- **GM52 Choir Aahs:** centroid TOO BRIGHT (2479 vs SC-55 1829) — always-on singer's-formant +
  raw −6 dB/oct saw source (real glottal flow is −12).
- **GM53 Voice Oohs:** h2 ~10 dB too hot vs SC-55's fundamental-only ooh.
- **GM54 Synth Voice: INVERTED** — ours is the BRIGHTEST (2962) but both real modules render it
  the DARKEST (1023-1168).
- Narrow-Q PARALLEL formant bank carves deep notches BETWEEN formants (h3/h4 −21/−29 dB where
  the modules PEAK) — the "hollow/synthetic vowel".
Cheap fix: dark GM54, glottal-tilt source −12 dB/oct, widen formant Qs to fill notches, recal the
defect-encoding CH2-O2 oracle (pins GM54 bright). **PARKED FORK: a full Klatt glottal cascade —
if the cheap fix can't reach SC-55 without it, STOP + escalate.**

## STEP 0 — worktree + baseline (done)
`task/20260717-DEV-HUM-voice-r3-stage-2-choir-gm54-dark-glottal` off origin/main @ 9444af4.
Baseline binary saved `/tmp/ferro_baseline_choir.exe`.

## STEP 1 — UNDERSTAND + MEASURE + KLATT-FORK ASSESSMENT (in progress)
Delegated to a subagent: render-path map (ChoirV2? source/formant/singer's-formant, how 52/53/54
differ), GM52/53/54 measurement vs SC-55 + MU-80, the Klatt-fork verdict (cheap fix suffices vs
needs Klatt → escalate), and the oracle inventory (CH2-O2 defect-encoder + golden fixture).

### Findings (measured) + KLATT VERDICT: CHEAP FIX SUFFICES (proceed, Klatt parked)
- Dispatch `52..=54 => choir()` (voices.rs:10799); struct `ChoirV2` (5892), 8 singers → 4 tracts,
  each = 5 parallel Biquad bandpass (3 CC70 vowel formants + 2 fixed singer's-formant). Source =
  raw BlepSaw (−6 dB/oct). A chest branch (LP dry source, 6154) already blends a parallel source.
- Measured (k60/k55 centroid Hz; ours vs SC-55 vs MU-80): GM52 1990 vs 1078/1056 (~1.85× bright);
  GM53 h2 −11/−5 vs SC-55 −20 (~9 dB hot); **GM54 2718/2323 (brightest of ours) vs SC-55 1003/763,
  MU-80 827/899 (DARKEST) — 2.7-3.3× too bright, the flagship**; inter-formant notch GM52 h3/h4
  −37/−37 vs SC-55 −12/−20, MU-80 −22/+1 (17-38 dB too deep).
- **Fix (5 knobs, choir-scoped, NO RNG draws):** (1) glottal source tilt −6→−12 dB/oct — add
  `tilt_lp:[OnePole;4]` (~120-250 Hz corner) on the voiced sum @6135; (2) widen vowel Qs @5971
  [9,10,9]→~[5,6,5] (fill notches); (3) trim singer's-formant sf_gains on 52/54; (4) re-voice GM54
  dark (F2 1900→~1050, vgains/sf down, chest 0→~0.45, re-trim amp); (5) GM53 vgains[1] 0.45→~0.30 (h2).
- Targets: GM52/54 centroid ~1050-1100, GM53 ~700-1000, notch h3/h4 +15-25 dB, ordering GM54 darkest.
- **Oracles:** CH2-O2 (11887, defect-encoder — asserts eh@1900 bright + permits eh brightest) → RECAL
  to dark-54 (F2~1050, pin GM54 DARKEST by centroid). Re-verify/re-pin: CH2-O3 singer's-cluster
  (11930, trim lowers ring), CH2-O6 level (12087, re-trim amp), CH2-O4 breath onset (12008). CH2-O5
  shimmer (RNG) untouched. **GOLDEN mix fixture = ropusenc-argv capture, timbre-INVARIANT → no regen.**

## STEP 2 — DESIGN
See `wrk_docs/2026.07.17 - HLD - voice r3 stage 2 (choir gm54-dark glottal-tilt).md`.

## STEP 3/4 REVIEWS → ESCALATE (the notch needs the parked Klatt fork)

Two adversarial + Codex converged on a BLOCKING split: the choir has TWO defects with different fixability.
- **FIXABLE cheaply (brightness/inversion):** GM54 dark re-voice (the FLAGSHIP inversion fix — a pure
  preset move: F2 1900→1050, gains down, chest 0→0.45; all 3 reviewers confirm it lands, centroid
  ~2700→~1100, no notch downside) + GM53 ooh trim. These are clean, high-value, per-preset.
- **NOT fixable cheaply (the "hollow vowel" = the inter-formant notch, h3/h4 −37 vs SC-55 −12):**
  - Q-widen 9→5 lifts the notch only ~5 dB (not 25). The notch is likely a PHASE-CANCELLATION zero
    between adjacent parallel bandpasses (opposite phases) — Q-widen can move/deepen it, not fill it
    (Codex). It needs SIGNED/alternating formant residues or a Klatt CASCADE.
  - The glottal tilt (OnePole @180 Hz on the source) DEEPENS the re-f0 notch by ~8 dB (attenuates
    h3=786 more than f0=262) — the tilt-for-brightness and the notch-fill pull OPPOSITE directions.
  - Net of the two headline knobs on the re-f0 notch: ~−3 to −4 dB (WORSE). +15 dB needed. Impossible
    with a parallel bank. Honest P(still hollow after 5 knobs) ≈ 50-70%.
- **Design flaws the reviews exposed** (for the next attempt): CH2-O2 recal was CONTRADICTORY —
  "GM54 darkest" conflicts the kept `ooh<0.8·eh` clause AND the data (GM53 is the darkest; GM54 is
  only "no longer brightest"); the new eh@1050 F2 probe collides with GM52's F2@1120 (q5 band 210 Hz);
  the notch gate `h3≥−22` was MU-80's value, not SC-55's −12 (laundered target); CH2-O3 re-pin had no
  cited SC-55 cluster ratio (laundering risk); the tilt attenuates f0+chest → chest_gain/amp re-tune.
- **Verified OK:** RNG-neutral (choir has own rng), choir-scoped (only 52/53/54), golden fixture
  timbre-invariant, tilt_lp[4] shape correct.

**DECISION: STOP + ESCALATE to Arthur** (per the loop's explicit Klatt-fork gate — the cheap fix
cannot reach SC-55 on the hollow-notch without a fork). NO code changed; NO integration. The flagship
GM54-inversion + GM53 fixes ARE achievable as a smaller preset-only slice; the hollow-notch is a
Stage-5 fork (Klatt cascade OR a signed-parallel-residue redesign). Handed back with this decomposition
+ the review findings so the next attempt doesn't repeat the contradictory oracle / weaker-reference gate.

## FORK TAKEN — Arthur chose "the Klatt/signed-residue fork" (do the full hollow-notch fix)

The cheap-half is NOT the path; the notch (the perceptual heart of the synthetic-vowel) gets the
dedicated fix. Deep-dive in progress: confirm the phase-cancellation mechanism (|ΣH| vs Σ|H| at the
GM52 notch), prototype the SIGNED-RESIDUE cure (Klatt's parallel branch alternates adjacent formant
signs +F1/−F2/+F3 so they ADD in the valley instead of cancelling — the standard, small fix), measure
whether it fills the notch to SC-55's −12 with vowels preserved, and decide signed-residues vs a full
series cascade. Then design → 2 adversarial + Codex → implement → validate → integrate → A/B.

## DEEP-DIVE VERDICT: SERIES CASCADE (not signed residues)
- Phase cancellation is only ~4.6 dB of the notch (prior "deep zero" hypothesis wrong). The notch is
  the structural inter-formant valley of a PARALLEL bank + the chest branch inflating h1.
- **Signed residues [+,−,+]** (RNG-neutral) fills only ~4 of 10 dB AND HARMS ooh (pushes ooh h3
  −31.8→−21.9, AWAY from SC-55's −37.9) — doesn't cure "hollow", harms dark vowels. REJECTED.
- **Klatt all-pole SERIES CASCADE (prototyped, RNG-neutral) NAILS it:** aah k60 h3 −14.7 (SC-55 −14.4),
  centroid 1667→847 (SC-55 940), ooh stays dark (−30.4), GM54 darkens (2129→930). All-pole formants
  MULTIPLY into a connected vocal-tract envelope (no inter-formant zeros); unity-DC resonators pass
  the fundamental → the chest crutch becomes redundant (bonus for CH2-O4). Prototype coefficients are
  rough (eh under-differentiated, some keys overshoot bright) — the RETUNING is the real work.
- **Fix:** add `Biquad::resonator` (all-pole, unity-DC) to dsp.rs; the 3 vowel formants become a 3-
  resonator CASCADE (singer's-formant + chest stay parallel); retune per-vowel freqs/Qs/amps (eh
  brighter than aah) + recal CH2_AMP + reduce chest; calibrate to SC-55 at keys 55+60. Choir-scoped,
  RNG-neutral. Oracles: recal CH2-O2/O3/O4/O6 (re-pin from SC-55, not prototype) + NEW notch oracle
  (two-sided, SC-55-anchored, fixed ~780 Hz band, keys 55+60).

## STEP 2 — DESIGN (cascade fork)
See `wrk_docs/2026.07.17 - HLD - voice r3 stage 2 (choir Klatt cascade fork).md`.

## VERSION: pre-bump ferrosintesis 0.21.11 → 0.21.12 on this branch (deltic doesn't add one).

## STEP 3/4 REVIEWS (cascade fork) — 2 adversarial + Codex CONVERGE on a structural ceiling
Three independent reviews (adversarial `ae6974…` DSP/retune, adversarial `a882d0…` blast/oracle,
Codex `gpt-5.6-sol` xhigh) agree — NOT "looks fine". The convergent findings:
1. **Pure all-pole cascade cannot set formant amplitudes independently** — each higher formant sits on
   the falling skirts of the lower ones, so relative formant level is fixed by Q+spacing, and a single
   `vowel_out` scalar only scales the whole cascade. **GM54-"darkest" is structurally UNREACHABLE by the
   cascade** (eh F2=1900 is 2.25 oct above F1=400 → keeps a bright shelf → prototype eh 930 > aah 847,
   INVERTED vs the SC-55 target). The HLD silently relaxes §3 "GM54 darkest" → §5 "impossible" mid-doc.
2. **Single per-vowel table across both registers is over-determined — THE most-likely failure.** The
   prototype ALREADY proves it: aah **k55 h3 = +3.0 (blares)** vs **k60 = −14.7**, SAME table — h3 is
   588 Hz at k55 (on F1's rising edge) vs 786 Hz at k60 (in the target notch). A fixed formant envelope
   can't hold a moving harmonic to one target at both registers. All three estimate ~30-40% (LOW) that
   one table converges; all three say **treat per-register escalation as the BASE case**, not the tail.
3. **Oracle laundering near-certain** — CH2-O2's `cent(ooh)<0.8·cent(aah)` gate is impossible vs SC-55
   (0.85); CH2-O7 §5 says both "SC-55-anchored −14" AND "pinned from the built cascade" (contradiction).
   Must FREEZE an immutable SC-55 acceptance matrix (centroid + notch + fundamental, per program, per key
   across a register span) BEFORE coding; derive every threshold from it, never the render.
4. Coded gaps the plan missed: `vgains`→scalar deletes `sf_open`'s drive input (`sf_open=vgains[2]/ref`,
   6103) → CH2-O3(b) breaks; chest removal is unmeasured at CH2-O4's key-45 test point (one-sided floor
   invites laundering — KEEP chest, stage removal separately); 3 unity-DC resonators multiply peak gains
   (~+21 dB F1 alone) → whistle risk → peak-normalize each SOS + lower Qs + clamp + crossfade retunes;
   CH2-O7 measured DRY misses the cascade+cluster antiphase null at the F3↔cluster crossover.
- **Verified OK by all:** RNG-neutral (ChoirV2 owns its rng, 5913), choir-scoped (52..=54 only, alt-bank
  is a separate SawStack), golden fixture timbre-invariant (ropusenc argv), resonator formula stable.

## DECISION: run the DECISIVE offline analytic fit BEFORE any production Rust (reviewers' unanimous ask)
Rather than escalate on reviewer *probabilities*, convert 30-40% → a measured yes/no the cheap way the
reviews prescribe ("offline constrained fit of the analytic cascade before production code"). The cascade
transfer function is analytic (product of three 2-pole |H|); the notch-re-f0 metric is scale-invariant
(peak-norm vs unity-DC cancels) → I can search for a SINGLE per-vowel (F1..F3,Q1..Q3) table that puts the
SC-55 valley + centroid + ordering in-window at EVERY key 52-64 for all 3 vowels, with NO rendering.
- **Freeze the SC-55 matrix first** (subagent w/ mdmidiemu): GM52/53/54 valley depth + centroid +
  h1/peak at keys 52,55,57,60,62,64. Immutable acceptance target; every oracle derives from it.
- **Offline fit** (stdlib Python): source (BlepSaw 1/h × tilt) × analytic 3-reso cascade, sampled at
  harmonics per key; grid+local search for one table per vowel meeting the frozen matrix at ALL keys.
- **GO/NO-GO:** converges at all keys (notch two-sided, ordering, fundamental) → build it in Rust (peak-
  normed SOS, keep chest, decouple sf_open, per-vowel prom oracles pinned to the matrix) → validate →
  integrate. **Does NOT converge (the reviewers' expected case) → STOP + escalate to Arthur with the fit
  evidence + options (per-register tables / hybrid cascade+series-peak / honestly-relaxed target).** Do
  NOT ship a laundered relaxation unilaterally — that's the §8 + guardrail trigger.

## PIVOT — the full-register SC-55 measurement INVALIDATES the fork's premise (STOP + ESCALATE)
Before writing production Rust I ran the full-register SC-55 acceptance-matrix measurement (subagent,
repo harness, keys 52-64, 3 vowels) + baseline apples-to-apples. Two findings demolish the fork's basis:

**1. The "hollow inter-formant notch" (h3 re h1) is an UNSTABLE MEASUREMENT ARTIFACT, not a defect.**
SC-55's OWN aah notch_h3 swings **+26.1 (k52) → −2.0 (k55) → −12.1 (k60) → +14.0 (k64)** — it goes
POSITIVE at the register ends (h3 louder than h1) because the metric is dominated by which harmonic
happens to land on a formant, not by timbre. Baseline ferro swings identically (−36.7/−8.1/−44.0). So
**the entire "fill the notch to SC-55's −14" framing — including the deep-dive prototype's celebrated
"−14.7 ≈ SC-55 −14.4 at k60 validation" — was a k60 snapshot of a ±40 dB-swinging artifact.** There is
no stable −14 notch in SC-55 to hit. The Klatt cascade was greenlit to cure a phantom.

**2. The REAL, STABLE defect is uniform over-BRIGHTNESS (centroid), and GM54 inversion.** Measured
apples-to-apples (harness fmax=5000 Hann-FFT centroid, --no-samples --wet 0):
| vowel | ferro centroid (k52..k64) | SC-55 | ratio |
|---|---|---|---|
| GM52 aah | 1320-1866 | 918-1055 | ~1.6× bright |
| GM53 ooh | 787-1369 | 503-991 (rises w/ pitch) | ~1.4× bright |
| GM54 synth | 2003-2338 | 638-789 (dark, FLAT) | **~3× bright, INVERTED** |
Brightness driver identified: `CH2_HUM_LP` open cutoff = **8000 Hz** (mouth-open lowpass, voices.rs:5839)
passes all HF incl. the always-on 2900/3250 singer's-formant cluster. A preset/darkening problem, NOT a
cascade problem.

**3. "GM54 darkest" cannot be frozen.** SC-55 centroid ordering INVERTS across the register: GM53 is
darkest at k52/55/57, GM54 only at k60/62/64 (crossover ~k58). The HLD §3 premise held solely because it
was anchored at k60. A single frozen ordering is unpinnable — exactly the CH2-O2 contradiction all three
reviewers flagged. GM54's defensible target is "dark & FLAT ~700", not a strict ordering.

**4. A one-shot darkening PROTOTYPE proves the cheap fix reaches SC-55 (no cascade).** Edits: CH2_HUM_LP
open 8000→3200; GM54 F2 1900→1050, F3 2600→2200, vgains[1,2]→0.30/0.12, sf→0.10/0.05, chest 0→0.30;
GM52 sf→0.15/0.08, vgains[2]→0.15; GM53 sf→0.10/0.05, vgains[2]→0.12. Result (ratio to SC-55):
GM52 1.6×→**~1.14×**; GM53 1.4×→**~1.0× (matched)**; GM54 3×→**~1.5×, inversion gone**. One rough pass
cut the defect ~½-⅔ with NO new DSP; GM52/GM54 close further with a second refinement pass. (Prototype
reverted — evidence only; voices.rs is clean.)

**DECISION: STOP + ESCALATE to Arthur.** The fork he explicitly chose (twice) was greenlit on invalid
info (the phantom notch). The correct fix is a targeted preset darkening (the cheap-fix family he steered
away from — but on the now-invalidated notch argument). This reverses an explicit, deliberate user
decision AND raises a reference-philosophy question (how dark/flat should "synth voice" track SC-55; the
ordering can't be frozen) — both squarely Arthur's call, not mine to make unilaterally. Handing back with
measured evidence + recommendation (do the darkening fix; drop the cascade). Reusable tools left:
tools/choir_measure.py, tools/choir_probe_mid.py, tools/choir_cascade_fit.py (offline model); SC-55
matrix + darkening recipe above.

## ARTHUR'S CALL: "Darkening fix, drop cascade (Recommended)" — IMPLEMENTED
The darkening fix, tuned to the SC-55 centroid (the STABLE metric) across keys 52-64. NO new DSP, NO
cascade — preset constants + the mouth-cutoff + oracle re-anchoring, all choir-scoped (52..=54).

### Final constants (voices.rs)
- `CH2_HUM_LP` open cutoff **8000 → 3400** Hz — the dominant darkening lever (the "open mouth" was far
  too bright; SC-55's open vowel is much darker). Also caps the 3.25 kHz cluster band.
- GM52 aah: vgains `[1,0.55,0.28]→[1,0.45,0.15]`, sf `[0.30,0.18]→[0.20,0.09]`, chest `0.35→0.45`,
  amp_mul `0.85→0.75`. Keeps its singer's formant (SC-55 gives ONLY aah a real ring); vgains[2]=0.15
  is deliberately OFF the floor so the CC70 cluster morph still differentiates (see below).
- GM53 ooh: vgains[2] `0.20→0.12`, sf `[0.20,0.12]→[0.06,0.03]` (SC-55 ooh has no ring).
- GM54 synth: F `[400,1900,2600]→[400,900,1800]` (F2 1900→900 is THE inversion fix), vgains
  `[1,0.70,0.40]→[1,0.28,0.08]`, sf `[0.35,0.22]→[0.04,0.02]`, chest `0.0→0.42`, amp_mul `1.0→0.70`.

### Result (centroid ratio to SC-55, harness fmax=5000, keys 52-64 mean)
GM52 aah **1.11×** (was 1.6×) · GM53 ooh **0.95×** (was 1.4×) · GM54 **1.17×** (was ~3× & inverted).
The flagship GM54 inversion is fixed (now darker than aah, the SC-55 ordering) and the cluster/total
profile matches SC-55 per-vowel (aah has the ring ~0.04, ooh/GM54 ~0.01-0.02).

### Oracle re-anchoring (SC-55-derived, fail-first on the old defect, never weakened)
- **CH2-O2 formant_placement** — rewrote from the old "eh F2@1900 brightest / ooh strictly darkest"
  (both wrong vs hardware) to: aah brightest (SC-55, every key); GM54 dark not inverted (`cent(eh) <
  0.95·cent(aah)` — fails on old eh@1477); GM54's F2 now LOW (`P900 > 1.5·P1900` — the fix, fails on
  old bright-F2); aah F2@1120 tooth; ooh dark. No strict ooh/GM54 ordering (they cross ~k58).
- **CH2-O3 singer's cluster** — re-scoped to aah ONLY (SC-55 gives ooh/GM54 no ring, ratio ≤1.4×); the
  CC70 mm-shade test moved to prog 52 with F3-gain 0.03 (below aah's 0.15 default) → sf_open ~0.4.
- **CH2-O4 breath onset** — the "onset darker than sustain" relative gate is unachievable once the
  vowels are voiced dark (dark ooh/GM54 sustains are no brighter than a closed mouth); replaced with an
  ABSOLUTE closed-lips ceiling (onset centroid < 650) + the unchanged quieter/breath/harmonic clauses.
- **CH2-O6 level** — amp_mul re-trims (GM52 0.85→0.75, GM54 1.0→0.70) hold the ±2.4× strings window
  (GM54 k69 was 3.24× after the added chest; now in-window). Pure level, invariant to the centroid.
- **CC70 cluster-morph coupling (engine.rs cc70_vowel_morph_opens_formants)** — the F5 review finding:
  `sf_open = vgains[2]/sf_ref_g3` saturates the cluster morph when the default F3 gain is on the floor.
  Fixed by keeping aah's default vgains[2] at 0.15 (not 0.08) → morph differentiates mm(0.10→0.67) from
  ah(0.35→1.3); the >1500 Hz open-ratio recovered 1.42→1.67. No engine change needed.

### Validation (green)
fmt clean · clippy -D warnings clean · `cargo test --workspace` 475 lib + all crate tests pass (choir
13/13, cc70 2/2) · render-diff 52/53/54 (running) · version pre-bumped 0.21.11 → 0.21.12.

### Known limitation (parked for a future CC70 slice, not this one)
The `sf_open = vgains[2]/sf_ref_g3` coupling (F5) is worked-around here (keep the reference off the
floor), not decoupled. A dedicated CC70 slice could give sf_open its own state driven by an explicit
cluster-open control, so the singer's-formant shade is independent of the F3 formant gain. Logged to
scratchpad.
