# 2026.07.17 — JRN — Voice R3 Stage 2: recorder even/odd inversion (GM74)

Brass-loop slice 3 of 4. Lean 8-step process, SC-55-anchored physical realism.

## The complaint / root cause (roadmap PLN)
Recorder even/odd balance INVERTED: ours even-dominant, dies after ~h4; real SC-55 is
ODD-tilted to ~h10 (a recorder ≈ a stopped/cylindrical pipe → odd-harmonic). Plus a
**defect-encoding oracle** (WD-O1 / recorder_*) HARD-ASSERTS the wrong `h2 ≥ h3`, rewarding
the even-dominant defect. Fix: shift the Wind additive bank toward odd harmonics (recorder-
scoped), extend the odd tail, and FLIP the oracle to SC-55-faithful (odd-tilted), STRONGER not
weaker.

## STEP 0 — worktree + baseline (done)
`task/20260717-DEV-HUM-voice-r3-stage-2-recorder-even-odd-tilt` off origin/main @ ca32b7b.
Baseline binary saved `/tmp/ferro_baseline_recorder.exe`.

## STEP 1 — UNDERSTAND + MEASURE (in progress)
Recorder is a Wind voice (GM72-79, additive sine bank). Blast-radius question: does it share
the bank/preset with flute 73 / pan flute 75? The fix must be recorder-scoped (render-diff:
only GM74 moves). Delegated the render-path map + h1..h10 measurement + oracle inventory to a
subagent.

### Findings (measured)
- Recorder = Wind voice; `RECORDER.harm[6]` (h2..h7) IS the spectrum (additive bank, no
  waveshaper). Current `[0.16, 0.08, 0.02, 0, 0, 0]` — even-dominant (h2>h3>h4), dead after h4.
- Measured overtone even/odd (h1 excluded): ferro 2-3×, SC-55 0.29-0.36 — inverts ~7-10×.
  h2/h3: ferro 1.8-2.8, SC-55 0.14-0.74. SC-55 carries an odd ladder to ~h10.
- **The even-rich balance was a DELIBERATE round-2 "open pipe" choice** (comment voices.rs:6688).
  Round-3 roadmap + SC-55 reverse it: a recorder's low register is odd-emphasized (cylindrical
  fipple ≈ stopped pipe). Reversal is roadmap-sanctioned + SC-55-faithful, not unilateral.
- **Defect-encoding oracle `wd_recorder_is_an_open_pipe_not_a_sine` (19311)** asserts `h2>=0.14`
  + `h2>=h3` ("evens must stay alive, open pipe") — rewards the defect. FLIP to odd-tilt.
- Fix = rewrite RECORDER.harm ONLY (per-preset; zero→nonzero h5/h7 draws a phase either way →
  no RNG shift → other 7 winds byte-identical). h8-h10 extension DEFERRED (needs bigger arrays →
  RNG draws for all winds → breaks byte-identity + WD-O5 seam; SC-55 h8-h10 are −50..−75 dB).
- Verify/re-pin: FLUE_PRESSURE_TABLE RECORDER=1.22 (19934, highest risk — new h5/h7 raises
  centroid), WD-O6b vel-tilt (19632), WD-O11 loudness (19877), WD-O5 breath (19463).

## STEP 2 — DESIGN + STEP 3/4 REVIEWS
See `wrk_docs/2026.07.17 - HLD - voice r3 stage 2 (recorder odd-tilt).md`.
Reviews (2 adversarial + Codex) sharpened it substantially:
- First-cut harm over-brightened h5/h7 ~18 dB (matched aggregate not per-harmonic) → use SC-55
  per-harmonic dB `[0.009,0.036,0.005,0.005,0.001,0.0025]`.
- **WD-O6b (vel-tilt ≤1.25) will structurally flip** (h2→h3 = bright²→bright³); the "keep vel_bright"
  reasoning was wrong. → HARD gate; escalate if SC-55 tuning can't hold ≤1.25.
- Oracle admitted a degenerate single-h3 pass + measured at the rectangular-window leakage floor →
  Hann-windowed, "h3 loudest overtone" + h5≥0.4·h3 + odd/even band ≥2.5 + h2 present-not-dead.
- Physics wrong (open pipe, not stopped — odd-tilt is SC-55 voicing). Fixed framing; direction is
  ROADMAP-sanctioned (not a fresh fork); degree needs Arthur's ear (most ear-dependent slice).
- FLUE_PRESSURE ≥1.22 is a lower bound → gate unchanged, never re-pin DOWN.
- RNG byte-identity VERIFIED (phase drawn per slot unconditionally) → recorder-scoped.
Design → SELF-APPROVED (direction sanctioned; escalate on WD-O6b/FLUE hard-gate failure).

## STEP 6 — IMPLEMENT (tests-first)
- Rewrote `RECORDER.harm` → `[0.010, 0.036, 0.005, 0.006, 0.001, 0.0025]` (SC-55 per-harmonic dB) +
  corrected the comment (open pipe, SC-55 voicing odd-tilt, not stopped). `harm` is a per-preset
  value array → recorder-scoped by construction.
- Replaced the oracle with `wd_recorder_matches_sc55_odd_tilt` + a `wd_mag_hann` (Hann-windowed
  mag_at) helper: (1) h3 the loudest overtone, (2) odd/even ≥2.5, (3) h5≥0.1·h3, (4) h2<flute & h2≤h3.
  Two-sided, leakage-robust, fail-first on the even-dominant spectrum, no degenerate pass.
- **Both HARD gates PASSED with the SC-55-faithful harm, no escalation:** measured odd-tilt oracle
  h2 0.010 / h3 0.036 / h5 0.006 (matches SC-55), odd/even **2.78**; **WD-O6b tilt 1.136** (≤1.25 —
  the DSP reviewer's 1.24 hand-calc overestimated); **FLUE recorder rise 1.296** (≥1.22, target
  untouched). The odd-tilt's HF partials ADDED velocity sensitivity → the rise went UP not down.
- (Fixed my first h5≥0.4·h3 clause — too strict; SC-55 h5/h3 ≈ 0.14, so 0.1·h3.)
- Gate: 24 wind/pressure tests green; full workspace **475 pass, 0 fail**; fmt + clippy clean;
  golden fixture untouched (no wind programs). Version pre-bumped 0.21.10 → 0.21.11.

## STEP 7/8 — CODE REVIEW + VALIDATE (render-diff + code review running)

## VERSION: pre-bump ferrosintesis 0.21.10 → 0.21.11 on this branch (deltic doesn't add one).
