# JRN — GM0 acoustic-grand alternate banks (audition)

## Task
Arthur dislikes the current GM0 grand (Salamander Yamaha C5). Wire a curated set
of alternative free grand-piano sample banks as **permanent CC0-selectable
alternates**, render each on a torture-test MIDI + the opening of Tubular Bells,
so Arthur can audition and rank them. Design in `wrk_docs/2026.07.18 - PLN - ...`.

## What happened

### Research (workflow, 48 agents, license-verified)
Ranked shortlist produced earlier. Decisions locked with Arthur: permanent shipped
alt-banks; curated grands + anchors; torture-test + real track; real track = first
few bars of Tubular Bells (his pick — an exposed solo-piano ostinato, ideal).

### Sourcing (subagent, ground-truth)
- **VCSL Steinway B** — CC0, `sgossner/VCSL`, `Chordophones/Zithers/Grand Piano, Steinway B/Sus/`,
  `JHPiano_Sus_Close_<Note><Oct>_vl<N>_rr1.wav`, whole-tone, vl2/3/4, 24-bit stereo WAV. SOLID.
- **VCSL Kawai** — CC0, same repo, `Grand Piano, Kawai/Sustains/`, `GPiano_sus_<Note><Oct>_v<N>_rr1_Player.wav`,
  v1-4, 16-bit stereo WAV. SOLID.
- **Headroom "Intimate" (CLOSE)** — CC-BY 4.0, `sfzinstruments/BengtNilsson.HeadroomPiano`,
  `Samples/HEADROOM PIANO LEVEL<1-5> CLOSE <MIDI>.flac`, minor-thirds MIDI 21..108, 16-bit stereo FLAC. SOLID (needs ffmpeg).
- **MuseScore_General.sf3** — MIT, `ftp.osuosl.org/pub/musescore/soundfont/MuseScore_General/MuseScore_General.sf3`
  (39,900,972 bytes; NO published sha — length is the anchor). SOLID (SF3 preset extract, ffmpeg Ogg).
- **Sofia_MZ — DEAD.** wixsite 404, no live mirror, license unverifiable. DROPPED (can't source, can't ship).
- **Noct-Salamander — FRAGILE.** 1.6 GB Google Drive behind virus-scan, needs non-stdlib `gdown`,
  version-pinned file ID. Unfit for a reproducible SHIPPED prepare.py fetch.

### Decision: Noct → synthetic "darkened Salamander"
Rather than the fragile GDrive Noct, substitute a **darkened Salamander** bank derived
from the ALREADY-fetched Salamander source (CC-BY 3.0, reproducible, stdlib, shippable).
Serves the SAME diagnostic — "is Arthur's complaint fixable by EQ, not a new instrument?" —
and is itself a cheap shippable win if he likes it. FLAGGED to Arthur. (Real Noct remains
an option if he wants the actual remaster.)

**Final roster:** bank 0 Salamander (anchor) · model (`--no-samples`) · 1 VCSL Steinway B ·
2 VCSL Kawai · 3 Headroom C3 · 4 MuseScore_General grand · 5 Salamander-darkened.

## Done this session (all in worktree; main clone kept clean)
- `voices.rs`: `acoustic_grand_with_bank()` helper; default GM0 routed through it.
  17 la_/piano/grand oracles green — behaviour-preserving. (render-diff still owed.)
- `tools/gm0-audition/`: `make_torture_midi.py` + `prep_audition.py` (CC0 injector +
  tempo-aware trim). Tested: torture MIDI OK; Tubular Bells → CC0=N on ch0, trim OK.
- `tools/ferrosintesis-samples/gen_crate_lib.py`: generic sample-crate lib.rs generator.
- `altbank.rs`: GM0 bank-dispatch arm (bank 1 = steinwayb; extend per candidate).
- **VCSL Steinway B (bank 1) vertical slice IN PROGRESS:** prepare.py `steinwayb`
  family (source map + `ensure_direct_sources` + KEEP_FAM/F0_RANGE/FAMILY_PACKAGE +
  main wiring); crate `ferrosintesis-samples-vcsl-steinway` (Cargo/NOTICE/PROVENANCE/
  README); workspace + .gitignore + ferrosintesis deps/features + sampler get() chain.
  Bake running. STILL TODO: sampler.rs steinwayb_* zone tables (measured roots from
  bake) + `steinwayb_bank`; build; render.

## Key facts / gotchas
- Reference MIDIs (`test-corpus/`) are UNTRACKED — only in the main clone. Render from
  `D:/language/midi-music/test-corpus/reference-midi/mike-oldfield/01-tubular-bells-part-one.mid`.
- Tubular Bells: **channel 0 = program 0 piano** (the ostinato), no existing CC0. For a
  clean timbre A/B render with `--solo 0` (opening is not solo piano in this GM arrangement).
- VCSL Steinway B labels are TRUE sounding pitch (probed C2=65.7/C4=262/C6=1050 Hz) —
  the octave-mislabel trap is VCSL keyboard/ocarina, NOT this piano. Still MEASURE each root.
- New sample crate WAVs are gitignored until `!crates/<crate>/samples/*.wav` is added
  to `.gitignore` (done for vcsl-steinway).
- Engine passes real `samples` to `altbank::make` (engine.rs:1492) — alternates DO get
  their sample layer. CC0 sets `Strip.alt_bank`/`alt_bank_value` (engine.rs:1606).

## Next (recipe per remaining candidate: Kawai, Headroom, MuseScore, darkened-Salamander)
1. prepare.py: source map + fetch/bake path (Kawai=plain WAV; Headroom=FLAC via ffmpeg;
   MuseScore=SF3 preset extract like `_bake_sf_onset` but KEEP 1.5s body; darkened=derive
   from grand src + a low-pass/high-shelf-cut). Register family in KEEP_FAM/F0_RANGE/FAMILY_PACKAGE.
2. Crate skeleton (+ .gitignore, workspace, ferrosintesis dep/feature, get() chain).
3. Bake; `gen_crate_lib.py` → lib.rs; sampler.rs zone tables (MEASURED roots) + `<name>_bank`.
4. altbank.rs: add bank N arm.
Then: build, render matrix (torture + Tubular Bells `--solo 0`, all banks incl. `--no-samples`),
hand to Arthur to rank. Post-ranking: final CC0 order; render-diff (must be byte-identical
on all albums); workspace tests; integration + single version bump.

## Round 3 (Arthur, 2026.07.18): FINAL arrangement — re-voice GM0/GM1/GM3 slots
Distribute the winners across the 3 acoustic-piano program slots (not all on GM0):
- **GM 0 Acoustic Grand = VSCO upright** (its ORIGINAL voicing, reverted from Salamander).
  CC0 alts: 1 Salamander · 2 Steinway B · 3 Headroom · 4 dark-Salamander (LEFTOVER, ask
  Arthur keep/remove — not in his 4-option list; wired to avoid dead code).
- **GM 1 Bright Acoustic = VCSL Kawai over the BRIGHT model.** The bright model (a) fits
  the slot and (b) is REQUIRED — without it GM0/GM1 are byte-identical modeled-only
  (both acoustic_piano), which the anti-clone matrix rejects. Only changes GM1's SUSTAIN
  vs the plain-model Kawai Arthur auditioned (attack = Kawai either way). CC0 alts: 1 YDP
  bright grand · 2 MuseScore grand.
- **GM 3 Honky-tonk = real honky-tonk sample over the detuned honky_tonk_piano model**
  (Arthur confirmed: detuned through the sustain). Faked LaFx cues removed.
- Committed 7a7da97. acoustic_grand_with_bank simplified to one `bright` flag; the
  exploratory wide-blend lever B (LA_PIANO_WIDE) DROPPED (not adopted). Oracles updated
  (not weakened): bright_piano_tilts -> model-only gm1_bright_model_is_brighter_than_gm0;
  harpsichord control moved back to GM0 (the upright, 1.75x). 487 tests green.
- **render-diff running** (origin/main Salamander-GM0 vs re-voiced, programs 0/1/3): shows
  the album impact. render_audition.sh now uses a STALE CC0 encoding (audition-era) — needs
  updating to the final arrangement or retiring.
- RESOLVED (Arthur, 2026.07.18): (1) KEEP dark-Salamander as a 5th GM0 option (CC0=4);
  (2) KEEP GM1's bright model (brighter sustain OK). Both already match committed 7a7da97 —
  no code change, just comment/journal cleanup. So GM0 = 5 options (upright + Salamander/
  Steinway B/Headroom/dark-Salamander), GM1 = 3 (Kawai + YDP/MuseScore), GM3 = honky.
  render-diff CLEAN (baseline = pre-revoicing c9e7e7b, same branch): 0 contamination,
  74 melodic piano tracks changed (GM0-heavy, expected). The 22 "not reached" are a
  harness artifact — the --program heuristic counts CHANNEL 9 (drum) program numbers
  (e.g. Slipstream prog-1 = a drum kit, Riverwake prog-0 = standard kit), not melodic
  pianos. (NB the FIRST render-diff vs current origin/main showed false "contamination" —
  origin/main is ~7 versions ahead with violin/viola/sax/drum changes my branch predates.)
  TERMINAL: focused-green, ready for integration (rebase onto origin/main 0.21.26 +
  single version bump owed at the integration step). Awaiting Arthur's integration go.

## Round 2 (Arthur, 2026.07.18): levers A + B + C
Finding that drove it: measured attack(0-0.15s)=source vs sustain(1.0-2.2s)=1.5% for EVERY
bank incl. model — the sustain is 100% our MODEL; the source only colours the attack. Arthur
(the ears) confirmed the attacks "sound very very different" — so source matters perceptually
even where the HF metric said the energy converges. He wants all 3 levers.
- **B (wide blend) DONE + committed (7a0d0ce):** `LA_PIANO_WIDE`=(1.0,(0.30,1.30)); source
  persists ~0.4s longer (verified: upright mid-window HF 1.5%->2.1%). Bounded by the 1.5s bake.
- **C (bright model) DONE + committed (7a0d0ce):** reuses GM1 `bright_acoustic_piano`. SUBTLE
  on sampled banks (+0.1%) because the sample owns 0-0.85s and the model's high partials have
  decayed by the tail it owns — C shows mainly in the --no-samples model reference. If Arthur
  wants more, push bright_add higher and/or slow high-partial decay `t`.
- **Mechanism:** CC0 decoded as ones=source(0-9), tens=lever(1 wide,2 bright,3 both). Wired in
  `acoustic_grand_with_bank(...,wide,bright)` + altbank GM0 dispatch. Default GM0 byte-identical.
- **A LANDED (banks 7,8) + full A×B×C matrix rendered.** Bank 7 FreePats YDP bright grand
  (a9073bc, CC-BY 3.0, SF2 raw-PCM extract — confirmed BRIGHTEST source, torture HF 6.4%).
  Bank 8 FreePats honky-tonk (24c9c96, CC0, 7z per-note FLAC — distinctive detune). Osiris
  (bank 9, soft) DEFERRED (Arthur wants bright; offered as follow-up). render_audition.sh now
  covers sources 0-8 + a B/C demo (16/26 upright, 17/27 ydp) + model normal/bright refs.
  TERMINAL: awaiting Arthur's ranking + integration go. (render-diff still owed at integration.)
- **A (brighter/distinctive sources) — research shortlist (built 7,8; 9 deferred):**
  - **Bank 7 = FreePats YDP Grand** (CC-BY 3.0, Zenph Yamaha Disklavier) — THE bright grand
    (harder/present hammer). SF2 (~36MiB tar.bz2), extract preset 0. Attribution NOTICE.
  - **Bank 8 = FreePats Honky-tonk** (CC0, Frances Bacon player piano) — DISTINCTIVE tack/jangle,
    a NEW category. SF2 or SFZ+WAV/FLAC. CC0. (Watch: detuned unisons may confuse the onset
    pitch-tracker — sanity-check roots.)
  - **Bank 9 = Osiris Piano** (CC0, Karoryfer/Versilian) — soft una-corda character (NOT bright).
    FLAC, use the "noisy" set for hammer/air noise. Lower priority (Arthur wants bright).
  - Skipped: FluidR3 GM grand (bright but overlaps our MuseScore/FluidR3 lineage), VCSL Keys
    (overlaps our Steinway B/Kawai). A subagent is fetching exact URLs/formats/structures.
  - Reserve ones-digit 7/8/9 for these (altbank match arm `bank % 10`).
  - After building: extend render_audition.sh (sources 0-9 + a curated B/C set e.g. 11/13/16,
    21/23/26, 31/36; + --no-samples model normal/bright), render full A×B×C matrix, present.

## CONFIRMED (Arthur, 2026.07.18) + progress
- **Bank 6 added: the OLD GM0 = CC0 VSCO "Upright Nr1"** (`piano_bank`, still GM1/3's
  bank), routed through `acoustic_grand_with_bank` so it reproduces the historical GM0
  exactly. No new crate (already shipped in `-core`). Rendered, distinct. Arthur asked
  for it as an A/B after learning GM0 used to be that upright pre-Salamander (68b55fa).
- Noct slot → **synthetic darkened-Salamander** (bank 5): derive from the already-fetched
  Salamander grand source + a low-pass/high-shelf-cut. Reproducible, stdlib, shippable,
  CC-BY 3.0 (inherits Salamander). Tests the "is it EQ not instrument?" question AND is a
  cheap shippable win if Arthur likes it. (Real Noct not shipped.)
- Cadence → **build the whole lot, then Arthur ranks.** Drive to done-green.
- **ALL 5 ALTERNATE BANKS LANDED green + committed.** Bank 3 Headroom (52d4f43),
  bank 4 MuseScore_General grand (f088287, single-velocity MF multisample, 25 zones),
  bank 5 darkened-Salamander (ee5c87f, high-shelf EQ on the grand crate, 54 zones).
  FINAL STEP: release build + full audition render (banks 0-5 + `--no-samples`), then
  report render paths to Arthur for best→worst ranking + the integration go.
- **Bank 3 (Headroom C3) LANDED green** (CC-BY 4.0, 9 zones, FLAC via ffmpeg;
  `ensure_flac_sources` generic added; attribution NOTICE credits Bengt Nilsson).
- **Bank 2 (VCSL Kawai) LANDED green + committed** (fb4dcc5): CC0, 8 zones. Octave-label
  trap hit + fixed via measurement (A2 read 55 Hz → remapped). Reusable `gen_zones.py`
  (stdin roots → Rust zone tables) added.
- **Bank 1 (VCSL Steinway B) LANDED green + committed** (d51d3a6): pipeline proven
  end-to-end (bake→crate→sampler→altbank CC0 dispatch→render); bank 1 audibly differs
  from bank 0. fmt/clippy/59 tests green.

Remaining banks to build (each: prepare.py family + crate + sampler table + altbank arm;
then extend `render_audition.sh` BANKS + re-render):
- **Bank 2 VCSL Kawai** (CC0) — `Grand Piano, Kawai/Sustains/GPiano_sus_<Note><Oct>_v<N>_rr1_Player.wav`,
  16-bit stereo WAV. SAME easy path as Steinway (plain WAV via `ensure_direct_sources`). Note
  octaves include negative (`A#-1`); PROBE roots (VCSL keyboard octave-label trap does NOT hit
  the Steinway but CHECK Kawai). Map v1..v4 → pp/mf/f + RR2 adjacent layer.
- **Bank 3 Headroom C3** (CC-BY 4.0) — `sfzinstruments/BengtNilsson.HeadroomPiano`,
  `Samples/HEADROOM PIANO LEVEL<1-5> CLOSE <MIDI>.flac`, minor-thirds MIDI 21..108, 16-bit
  stereo FLAC. Needs an ffmpeg FLAC→PCM transcode-at-fetch (mirror `ensure_banjo_sources`),
  then the main loop bakes. Pick zones near MIDI {36,42,48,54,60,66,72,78,84} (C2..C6-ish),
  levels → pp/mf/f. NOTICE must credit "Bengt Nilsson" + keep instrument name (CC-BY).
- **Bank 4 MuseScore_General grand** (MIT) — fetch `MuseScore_General.sf3` (39,900,972 bytes,
  no sha published — pin the length) from ftp.osuosl.org. Extract preset 0 (Acoustic Grand)
  zones like `_sf_preset_zones`/`_bake_sf_onset` BUT keep ~1.5 s body (not onset-only) — write
  a `_bake_sf_body` variant, Ogg-decode via ffmpeg. Retain FluidR3/Collins copyright NOTICE (MIT).
- **Bank 5 darkened-Salamander** (CC-BY 3.0) — read the baked `grand_*.wav` (or re-derive from
  Salamander src), apply a warmth EQ (e.g. one-pole low-pass ~3–4 kHz or a high-shelf cut),
  write `darkgrand_*.wav`, SAME roots as grand. Own family `darkgrand`. EAR-TUNABLE (box has
  no ears) — pick a defensible default and let Arthur judge. Reuses the grand crate's licence.

Then: render full matrix (torture + Tubular Bells `--solo 0`, banks 0..5 + `--no-samples`),
hand to Arthur to rank. Post-ranking: he sets final CC0 order (best→worst); run render-diff
(MUST be byte-identical on all albums — no album sends CC0 on a GM0 channel); workspace tests;
integration + single version bump.

## Fable
Not engaged — mechanical/well-understood build. Retainer intact.
