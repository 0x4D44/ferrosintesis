# JRN 2026.07.18 — LA sample layers: harp, timpani, pipes, sitar, banjo

Design + full pointers: `wrk_docs/2026.07.18 - PLN - LA sample layers harp timpani pipes sitar banjo.md`.
Read the PLN once per fresh context, then use the **NEXT** pointer below.

## Live state

**DONE (committed green):**
- Unit 1 (566d4d3) — `_sf_preset_zones(sf3, preset=7)`; GM7 byte-identical; SF3 75/76/77/104 present.
- Unit 2 (211289d) — **Harp (GM 46)**. New CC0 crate `-orchestral2` (11 zones G1–F7,
  forte layer, roots measured). prepare.py `--only=<family>` filter added (+ gong gated).
  Wrapped `Pluck(&HARP)` with `LA_HARP=(0.38,(0.05,0.22))`. 46 added to LA_PROGRAMS +
  LA_HARP to the fade guard. All harp oracles green (release); clippy clean.
- Unit 3 (f59192d) — **Ocarina (GM 79)**. 3 zones E4/G#4/C5 (roots 329.0/414.5/519.2)
  in -orchestral2. `LA_OCARINA` = flute's (0.55,(0.06,0.24)). The `72..=79` arm refactored
  to a per-program bank/LA match (flute 72/73, ocarina 79; slot ready for 74/75/76/77;
  78 stays modeled). 79 in LA_PROGRAMS + fade guard. Oracles green; clippy clean.
- Unit 4 (4c00780) — **Recorder (GM 74)**, priority instrument. 7 zones F3–C6 (roots
  measured, all within ~10¢) from VCSL Baroque alto+soprano, one combined bank. Added
  prepare.py `TWO_F_STRONG` + per-note ceiling cap (label×1.5) — the recorder is strongly
  2f-dominant across 2.5 octaves, so a fixed ceiling can't work; this generalizes the
  ocarina lesson. `LA_RECORDER`=flute profile; 74 in the arm match + LA_PROGRAMS + fade
  guard. Oracles green (sampled recorder in tune at E5); clippy clean.
- Unit 5 (d773212) — **Timpani (GM 47)**, STRUCK. 5 zones A#1–F3 in -orchestral2, roots
  MEASURED (57/88/106/143/173 Hz). VCSL source names carry no note → probed each hit's pitch
  offline, named dest by measured pitch, picked a high-confidence spread from ~34 tuned hits.
  `LA_TIMPANI=(0.50,(0.05,0.30))`; wrapped arm 47; 47 in LA_PROGRAMS + fade guard. The
  **modal** o_pitch oracle (mc(47,45,TIMPANI)) passes on the sampled leg. clippy clean.

**PROVEN PIPELINE (reuse verbatim for each remaining instrument):**
1. `gh api "repos/<owner>/<repo>/contents/<path>?ref=<PIN>" --jq '.[].name'` to get EXACT
   source filenames (never guess VCSL take-suffixes).
2. prepare.py: add `<FAM>_URLS`/zone table (mirror HARP_URLS), `F0_RANGE["<fam>"]`,
   `KEEP_FAM` ONLY if plucked/struck (winds use the 0.62 default), `FAMILY_PACKAGE["<fam>"]
   = "ferrosintesis-samples-orchestral2"`, and add the URL dict to BOTH the fetch loop and
   the `sorted(... | <FAM>_URLS ...)` union in main().
3. `python prepare.py --only=<fam>` → generates WAVs into orchestral2/samples/.
4. **`.gitignore`: add `!crates/ferrosintesis-samples-orchestral2/samples/*.wav`** — ALREADY
   DONE for orchestral2 (covers all its families), so no per-instrument gitignore edit now.
5. `sampler.rs`: add `<fam>()` Zone bank with MEASURED roots + `pub fn <fam>_bank()` + prewarm.
6. orchestral2 `src/lib.rs`: add the new `("<fam>_X.wav", include_bytes!(...))` entries.
7. `voices.rs`: `LA_<FAM>` const (fade.1<0.90) + wrap the make() arm; add program to
   LA_PROGRAMS + the fade-guard array (o_pitch case usually already exists — check the table).
8. Verify (release): `-- la_level_continuity la_fade_ends_before_model_owned_window
   o_pitch_melodic_programs_sampled`; orchestral2 crate tests; clippy -D warnings; fmt. Commit
   (stage explicit paths INCLUDING samples/*.wav + Cargo.lock).

**NEXT (single unit to advance):**
- Unit 6 — **Sitar (GM 104)**, first SF3 ONSET-bake unit. Source: MS Basic SF3 preset 104
  (8 zones E3–G6, all sr 44100), via `_sf_preset_zones(sf3, 104)`. Write a NEW reusable
  `_bake_sf_onset(src, preset, prefix, dest_crate, keep_s, fade_s, f0_range)` that mirrors
  `_bake_clavinet`'s Ogg-slice + ffmpeg-decode (read `_bake_clavinet` ~:954 for the exact
  subprocess call), then `resample` per-zone to 44.1k (use the zone's sr `z[5]`; sitar is
  44100 but pipes aren't) + `trim_to_onset(keep 0.9 plucked, fade 0.30)` — NOT `_bake_clavinet_note`
  (that's the decay/loop bake). Dest name `sitar_<_midi_name(root_midi)>.wav` (root from SF3
  `h[40]`, trustworthy). **Route to the MIT `-clavinet` crate** (MS Basic = MIT, its NOTICE
  already covers the chain) — add entries to that crate's lib.rs + samples/; its .gitignore is
  whitelisted. Gate the bake with `want("sitar")` in main(). `F0_RANGE["sitar"]` (E3 165–G6 1568,
  fundamental-strong plucked; verify per 2f-lesson). Rust: `sampler.rs` sitar bank (measured
  roots) + prewarm; wrap arm `104 => Pluck(&SITAR)` with `LA_SITAR` (~LA_GUITAR (0.42,(0.05,0.20)),
  the jawari buzz is in the sample); 104 → LA_PROGRAMS + fade guard. o_pitch hc(104,64,…) exists
  (`:20841`). Update the `-clavinet` crate README/PROVENANCE: it now holds sitar (+ pipes next),
  not just the clavinet. embedded_wav already chains to clavinet.

**AFTER (ordered units — see PLN §Ordering):**
7 banjo(105, NEW ganjo CC0 fetch — pin commit SHA, probe WAV fmt; → orchestral2) ·
8 pipes 75/76/77 (SF3 via `_bake_sf_onset` → -clavinet; 75=8 zones mixed-sr so resample,
76/77 single-zone thin; wind keep 0.62; likely `TWO_F_STRONG`) · 9 sweep: fix pre-existing
LA_PROGRAMS staleness (add 25,41; `2` is model-only), reconcile perceptual-oracle list, full
render-diff inventory, README/DESIGN/tools-README docs, lessons nugget · 10 terminal report.

## Decisions already taken (don't re-litigate)

- LA = onset crossfade into the EXISTING model for all 9 (LaVoice::wrap), not a
  model replacement. Bake = `trim_to_onset`; keep_s ~0.9 s plucked/struck, ~0.6 s winds.
- Crates: CC0 new → `-orchestral2`; MS-Basic MIT (sitar+pipes) → reuse existing
  `-clavinet` crate. Crate name reversible pre-publish; NOT a fork.
- GM 78 whistle stays modeled (no usable source) — non-goal.
- Roots MEASURED (print in prepare.py, hardcode in sampler.rs) — VCSL labels lie.
- Integration owns the version bump — do NOT bump on this branch.

## Env notes

- Worktree: `D:/worktrees/midi-music/20260718-TSK-HUM-la-sample-layers-harp-timpani-pipes-sita`.
- Toolchain OK: python 3.14, ffmpeg 8.1, cargo 1.95, gh. `7z` at
  `C:\Program Files\7-Zip\7z.exe` (not on Git Bash PATH; not needed — no new .7z sources).
- Run tests stdin-closed: `$null | cargo test …` (PowerShell). Bound long gates with `mdtimeout`.
- Render-diff: baseline binary from `git worktree add <tmp> HEAD` before edits, both render `ALBUMS`, `cmp`.

## Surprises / dead ends
- **Near-sine winds (ocarina) are 2f-STRONG → measure_f0 grabs the 2nd harmonic** if the
  F0 ceiling admits 2f, and the octave-snap then "confirms" the wrong octave (it can't tell a
  true fundamental from a 2f that lands on a label×2 candidate). Recording labels were sounding-
  accurate; the misread was purely the ceiling. Fix: keep the zone span UNDER one octave and set
  the single ceiling above every fundamental but below the lowest 2f. ALWAYS probe roots at a
  tight per-note ceiling (`measure_f0(x,sr,150,label*1.5)`) before trusting the default-ceiling
  print. Applies to the SF3 pipes next (pan flute etc. may be 2f-strong too). GENERALIZED in
  Unit 4: `prepare.py::TWO_F_STRONG` families get a per-note ceiling cap (label×1.5) in main(),
  so a >1-octave 2f-dominant instrument (recorder) works without narrowing the span. Add a
  family to that set rather than hand-narrowing when you hit this again.
- **`.gitignore` silently drops new sample-crate WAVs.** `*.wav` is ignored except an
  explicit per-crate `!crates/…/samples/*.wav` whitelist. A new crate's samples are
  ignored until whitelisted, so `git add <crate>` commits the crate WITHOUT its WAVs and
  the build breaks from a clean checkout. Fixed for `-orchestral2` (whitelist added in
  211289d, covers all its future families). ALWAYS `git ls-files <crate>/samples` after
  committing a sample crate to confirm the WAVs are tracked.
- **SF3 pipe presets are THIN + mixed sample-rate** (probed 2026.07.18): GM75 pan flute
  = 8 zones F#3–C7 but sample rates vary (18000/24000/28000/30000/44100) → the SF3 bake
  MUST resample each zone to 44.1k (`prepare.py::resample`, using `z[5]`); the clavinet
  bake got away without it because all its zones are 44100. GM76 blown bottle = **1 zone
  (C6)**; GM77 shakuhachi = **1 zone (C5, 38000)**. Single-zone presets mean the LA onset
  is only credible within the `LaVoice::wrap` `step` clamp (~±1 octave of the sample root);
  outside that the wrap falls back to bare model — graceful, but 76/77 onsets cover only a
  narrow register. Acceptable (Arthur pre-approved soundfont sourcing); flagged for the
  final report. Sitar (104) = 8 zones E3–G6, all 44100 — clean.
