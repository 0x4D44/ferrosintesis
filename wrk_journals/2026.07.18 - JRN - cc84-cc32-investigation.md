# JRN — CC84 portamento + CC32 XG bank-LSB (investigation)

## CC84 (portamento control) — found low value for THIS file
- ch10 is the only channel sending CC84 (6 events, source keys 32/37). NO channel sends
  CC5 (porta time) or CC65 (porta on) anywhere → glide falls back to the 5 ms default =
  ~instant. So implementing CC84 correctly changes 6 note-ons by an inaudible scoop.
  Legit MIDI-correctness fix for the general player; ~no effect on Incantations.

## CC32 (XG bank-LSB variations) — premise appears largely FALSE
- File's LSB values: 96,112,113,115,117,3,19,41,45,65,98 — scattered/high, per channel.
- XG voice list (Bank MSB=0) I could access: these LSBs "not listed" for these programs.
- Yamaha/community docs: **an XG synth falls back to the BASIC (GM) voice for undefined
  banks.** ferrosintesis already plays the basic GM voice → **for these channels we are
  very likely ALREADY rendering exactly what a real XG synth would.** The "fidelity gap"
  is largely illusory.
- Corollary (correctness): modeling "variations" for UNDEFINED banks would make
  ferrosintesis DIVERGE from a real XG synth (which plays base GM) — i.e. LESS faithful.
  So the correct behavior for undefined banks is precisely what we already do.
- Could not get a definitive complete XG voice map cheaply: angelfire host unreachable,
  manualowl 403, official Yamaha PLG100XGE.pdf won't parse via WebFetch and this box has
  no poppler to render it. A few LSB-96/98 banks *might* be real ethnic/synth voices; not
  confirmed.

## Decision needed (surfaced to Arthur)
- Premise ("this file needs XG variation voices") is mostly false; ferrosintesis already
  matches real XG for undefined banks. Options: drop; or get a reliable XG voice map and
  model only the confirmed-real variations (likely a small handful, if any); or build the
  CC32 dispatch infra anyway (but with no real variations it's a no-op = premature).

## CORRECTION — read the PDF (PyMuPDF); premise is PARTLY real after all
- Extracted the official Yamaha PLG100-XG voice list (pages 13-16) with fitz.
- Max defined XG LSB bank = 101. So file's LSB 112/113/115/117 = UNDEFINED -> base GM
  (11 channel-programs; ferrosintesis already correct; modeling them would be WRONG).
- Defined banks the file hits (verified against raw table rows):
    ch0  Nylon(24) + LSB96 = **Ukulele**       (distinct instrument)
    ch11 Taiko(116)+ LSB96 = **Gr.Cassa**       (orchestral bass drum, distinct)
    ch13 Banjo(105)+ LSB98 = **Oud**            (Arabic lute, distinct)
    ch5  Strings(48)+LSB3  = S.Strngs (slow strings)      [subtle]
    ch3  Atmosphr(99)+LSB19= HollwRls                     [subtle]
    ch8  Dist.Gtr(30)+LSB41= FeedbkG2                     [subtle]
    ch10 FngrBass(33)+LSB45= FngBass2                     [subtle]
    ch11 Syn.Str1(50)+LSB65= Str5 (synth-str variant)    [subtle]
- => The task IS justified: 3 genuinely-distinct instruments to model. My earlier
  "premise false, drop it" was WRONG (couldn't read the PDF then).

## Refined scope (increment-1)
- CC32 bank-LSB tracking + variation dispatch (program, LSB) -> variation voice | None->base GM.
- Model the 3 distinct instruments: Ukulele, Gran Cassa, Oud (preset/param tweaks on
  existing Pluck / drum primitives).
- Undefined banks (112/113/115/117) keep falling back to base GM (correct; no work).
- Defer the 5 subtle variations (cheap table-adds later once the infra exists).
- CC84: separate small correctness fix; include or split? -> decide in design.

## Design + reviews (SELF-APPROVED)
- HLD written: CC32 infra (Strip.bank_lsb; dispatch in the existing !is_drum/!alt_bank
  else branch via voices::make_variation -> Option, unwrap_or base) + CC84 + the 8 variations.
- Adversarial review (2): folded — reuse over new voices (Slow Strings = strings(49) slow
  path; Feedback Gtr2 = DRIVE_LEAD; Gran Cassa = membrane_drum; Oud/Uke = NYLON presets);
  no variations.rs module (make_variation in voices.rs); no bank_msb field (MSB=0 implied);
  CC84 take() hoisted; model-vs-model oracles with correct bases (Oud vs NYLON); clear
  porta_control in RAC.
- Codex external: MSB gate VERIFIED correct (CC0 handler sets alt_bank for all nonzero
  MSB!=127); CC84 consume moved after early-returns; REFUTED "drop Fingered Bass2/Str5"
  (defined banks -> base-GM falsely undefined; minimal delta more faithful). Bank applies
  immediately (matches existing alt_bank convention).
- Scope: all 8 variations (per Arthur) + CC32 infra + CC84. Weak pair modeled minimally.

## Next: implement (tests-first), voice by voice (loop if needed) -> code review -> validate.

## Implementation log

### Unit 1 — CC32 infra + CC84 (DONE, green)
- engine.rs: `Strip.bank_lsb: u8` + `Strip.porta_control: Option<u8>` (init 0 / None).
- CC handlers: `32 => s.bank_lsb = val`, `84 => s.porta_control = Some(val)`.
- note_on dispatch (the `!is_drum && !alt_bank` else branch) now calls
  `voices::make_variation(prog, bank_lsb, …).unwrap_or_else(|| voices::make(prog, …))`
  — same `seed` on both paths, so an undefined pair is bit-identical to base.
- CC84 consumption: `let porta_ctrl = self.strips[ci].porta_control.take();` hoisted to
  after the bagpipe/legato/tremolo early-returns (consumes only when a voice actually
  spawns), before the `let s = &self.strips[ci]` borrow. Glide source merged:
  `porta_ctrl.map(key_freq).or(if s.porta_on { porta_from } else { None })` — CC84 glides
  regardless of CC65, honoring CC5 porta_time; falls through to the CC65 path otherwise.
- RAC (CC121) clears `porta_control`; `bank_lsb` PERSISTS (bank select is not a RAC controller).
- voices.rs: `pub fn make_variation(program, bank_lsb, key, vel, sr, seed, samples) ->
  Option<Box<dyn Voice>>` — skeleton returns None (RNG-pure); variation arms land per-unit.
- Tests (engine::tests): `cc32_undefined_bank_falls_back_byte_identical` (LSB 113 undefined
  for all progs -> byte-identical to no-CC32, forward-safe as variations get added);
  `cc84_glides_from_source_without_porta_on_and_is_consumed_once` (source-pitch `mag_at`
  energy: note-1 scoops from key 48 without CC65, note-2 doesn't -> consumed once).
- Green: 2 tests pass; `clippy -p ferrosintesis --all-targets -D warnings` clean; fmt clean.

### Unit 2 — Ukulele (24, 96) (DONE, green)
- `UKULELE` PluckPreset from `..NYLON`: t60 3.8->1.8, bright 3800->4800, pick_lp 2500->3400,
  rel_t60 1.1->0.55, body raised (260/520/900 Hz — tiny soundbox), click_hp 1000->1100.
- make_variation arm `(24, 96) => Pluck::new(&UKULELE, …)`. Modeled-only (no uke sample bank;
  base 24's LA guitar layer would sound like a guitar, so the variation is pure model).
- Oracle `ukulele_variation_is_brighter_and_shorter_than_nylon` (koto-test idiom, 3 seeds):
  centroid(UKU) > 1.10*centroid(NYLON) AND t60_of(UKU) < 0.80*t60_of(NYLON); + dispatch pins
  (24,96)->Some, (24,113)->None. Green; clippy/fmt clean.

### Unit 3 — Oud (105, 98) (DONE, green)
- `OUD` PluckPreset from `..NYLON` (its timbral base is nylon/gut, NOT the banjo drumhead):
  bright 3800->2200, pick_lp 2500->1500, out_lp 3200 (mellow rolloff), low warm bowl body
  (90/160/300 Hz), + the dulcimer double-course params (detune 1.0042, course_t60 0.85,
  course_mix .56/.44, couple 0.002, wound_key_split false).
- make_variation arm `(105, 98) => Pluck::new(&OUD, …)`.
- Oracle `oud_variation_is_warmer_than_nylon` (3 seeds): centroid(OUD) < 0.90*centroid(NYLON).
  First pass came in at ~0.91 (only 9% warmer) — deepened the model (lower pick_lp + out_lp
  3200) to clear the 10% bar rather than weaken the oracle; the oud is genuinely mellow. Green.

### Unit 4 — Gran Cassa (116, 96) (DONE, green)
- `GRAN_CASSA_MODES` (fundamental-dominated, one faint fast 2nd mode; no 3rd mode) + `gran_cassa`
  fn beside taiko_drum: darker soft strike (noise 0.45, lp120 vs taiko 0.95/lp260), longer
  release (8.5 vs 6.0), gentle pitch drop (1.0 vs 2.8). make_variation arm `(116, 96)`.
- Oracle `gran_cassa_variation_is_darker_and_longer_than_taiko` (3 seeds, model-vs-model):
  t60_of(cassa) > 1.15*t60_of(taiko) AND upper-mode band [2f,6f]/fundamental < 0.60*taiko's.
  METRIC LESSON: a log-binned centroid could NOT separate two bass-dominated drums (262 vs 260 Hz)
  — both floored near the fundamental; nor could hp_rms(200)/rms (soft-skirt fundamental leakage,
  only 0.31 vs 0.40). The taiko's ringing 3rd mode (2.14f) is the real difference, so measure the
  2f..6f band relative to the fundamental. Deepened the model too (killed noise>120Hz). Green.

### Unit 5 — Slow Strings (48, 3) (DONE, green)
- make_variation arm `(48, 3) => strings(49, …)` — reuses the existing program-49 slow voicing
  (attack 0.45, darker cutoff) verbatim; no new preset, no change to the base voice.
- Oracle `slow_strings_variation_has_longer_attack_than_base` (3 seeds, model-vs-model): a
  late-steady-window attack-rise metric (0.9..1.3s reference, NOT the built-in attack_rise_s's
  0.25-0.45s window — too early for a 0.45s attack). slow > 2.0*fast AND slow > fast+0.05s.
  NOTE: attack is velocity-scaled, so measured at vel 64 (a fortissimo hit shortens every
  attack); at vel 64 slow~0.12s vs fast~0.04s (3x). Reused strings(49) as designed (can't
  alter the base voice), so the gap is what the slow path gives — a real, audible 3x. Green.

### Unit 6 — Feedback Gtr 2 (30, 41) (DONE, green)
- make_variation arm `(30, 41) => Pluck::new(&DRIVE_LEAD, …)` — reuses the CC0 alt-bank driven
  lead (amp-feedback e-bow hold) vs the hard-picked base DRIVE (program 30).
- Oracle `feedback_gtr2_variation_sustains_longer_than_base_drive` (3 seeds): routes through
  the REAL make_variation/make dispatch (not just presets — tests the wiring), held 3s, and
  asserts sus_index(fb) >= sus_index(base) + 3.0 dB (late held level / early spoken level, the
  metric driven_main_and_alt_banks_diverge uses). Per-seed gap measured 4.4/14.8/11.7 dB;
  3.0 dB sits safely below the worst seed. Green; clippy/fmt clean.

### Unit 7 — Hollow Release (99, 19) (DONE, green)
- `FX_HOLLOW_RELEASE = FxSpec { saw_env: (0.02,0.7,0.30,3.0), ..FX_ATMOSPHERE }` — atmosphere
  with the amplitude release 1.4->3.0s, nothing else. make_variation arm `(99, 19) =>
  Fx::from_spec(&FX_HOLLOW_RELEASE, …)`. "Hollow" spectral colour is a documented non-goal
  (unverifiable without ears) — release length only.
- Oracle `hollow_release_variation_has_longer_tail_than_base` (3 seeds): render held 0.5s ->
  note_off -> capture 7s tail; t60_of(hollow) > 1.4*t60_of(base 99). Green first pass.

### Unit 8 — Fingered Bass 2 (33, 45) (DONE, green, subtle-but-kept)
- `FINGERED_BASS2 = PluckPreset { t60: 4.0, ..BASS }` — the ONLY delta is a longer ring
  (3.2->4.0). Deliberately does NOT brighten (the review caveat): bright/pick_lp/out_lp/body
  all inherited from the muffled flatwound BASS. make_variation arm `(33, 45)`.
- Oracle `fingered_bass2_variation_rings_longer_without_brightening` (3 seeds): t60_of(bass2)
  > 1.15*t60_of(base) AND centroid(bass2) <= 1.02*centroid(base) (not brighter). Green first
  pass — the delta cleanly exceeds seed noise, so kept (not fallen back to base).

### Unit 9 — Str5 (50, 65) (DONE, green, subtle-but-kept)
- make_variation arm `(50, 65) => synth_strings(51, …)`. DEVIATION FROM HLD WORDING (noted):
  the HLD said "a bespoke minimal detune delta"; instead REUSED the existing program-51 lush
  machine (wider detune, darker) — non-invented and guaranteed-distinct (a bespoke tiny delta
  risked not clearing seed noise, the HLD's own fallback caveat). Same reuse pattern as Slow
  Strings (49) / Feedback Gtr (DRIVE_LEAD).
- Oracle `str5_variation_is_a_darker_synth_string_than_base` (3 seeds, real dispatch):
  hf_frac = hp_rms(2800)/rms; str5 < 0.90*base (the differentiator synth_strings_2_is_the_
  darker_variant uses, threshold 0.85->0.90: one of my seeds gave a 14.3% gap, so 0.90 is a
  robust bar — still a clear audible darkening). Green.

## All 8 variations + CC32 infra + CC84 landed (units 1-9). Next: unit 10 validate.

### Unit 10 — Validate (DONE, all green)
- Full workspace gate: `cargo fmt --all --check` clean; `clippy --workspace --all-targets
  -D warnings` clean; `cargo test --workspace </dev/null` = 507 passed / 0 failed (+ all
  sample/cli/render-catalog crates green); release build clean.
- Added engine integration test `cc32_defined_bank_selects_the_variation`: all 8 defined
  (program, LSB) pairs route through the FULL engine path (CC32 handler -> bank_lsb -> note_on
  make_variation dispatch) and render distinctly from the base program (diff/base > 0.01).
  This closes the gap left by cc32_undefined (which only covered the fallback path). Green.
- RENDER-DIFF (the mandated inventory): baseline = c3267a0 (= origin/main code, pre-task) in
  a throwaway worktree; NO --program/--key declared (the change is controller-gated — new
  voices reachable only via CC32, which no album authors — so ANY diff = contamination = bug).
  Result over all 109 album MIDIs: EXPECTED same 109, CONTAMINATION 0, NOT REACHED 0. Every
  album is byte-identical -> the byte-identity invariant holds empirically. Both binaries
  verified fresh (mtimes at build time).
- END-TO-END CLI smoke (synthetic MIDIs): for each of the 8 variations, a MIDI with CC32=LSB
  renders DIFFERENTLY from the base and an undefined bank (113) renders IDENTICALLY to base;
  a CC84 glide MIDI renders differently from plain. Confirms the whole pipeline (MIDI bytes ->
  parser -> CC handlers -> dispatch -> audio) through the release binary.

## STATUS: focused-green, ready to integrate. NO version bump on branch (integration owns it);
## NOT pushed. Awaiting explicit "go integrate".
