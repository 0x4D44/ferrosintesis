# 2026.07.18 — JRN — drum-key mapping fixes (KILN-00008, -00022) + close -00011

Task: verify+close MM-BUG-KILN-00011, then fix the sampled-drum batch 00008 / 00009 / 00022.

## MM-BUG-KILN-00011 — Closed (verify only, no code)
CC7/CC10 slew (already Fixed on trunk, `a9fd965`). Independent re-verify with fresh
context (fixer was the 1M-context session; two-eyes holds). All four oracles green.
**Fails-before observed, not asserted:** temporarily emulated the old instant set
(snap current→target each block) → `cc7_cc10_post_prime_change_slews_one_pole_monotone`
fails at the one-pole-step assertion; real slew passes. Closed on `origin/main` via the
docs lane (`45291e2`).

## MM-BUG-KILN-00008 — Fixed (`d03e33d`)
Sampled kit mapped both key 38 (acoustic) and key 40 (electric) to `kit::SNARE`@1.0
with equal level → bit-identical bar the round-robin. The modeled path already
differentiates (drums.rs `center_mul 1.15`), but that arm is dead when samples are on.
Fix: repitch the sampled key 40 up 1.15× on the same bank (parity with the model).
Centroid 2333 vs 2023 Hz (=1.15×); `sampled_drum_level_parity` still within 3 dB.

## MM-BUG-KILN-00022 — Fixed (`055a849`)
GM2 ext keys 27–34 / 83–87 fell through the `_` arm → generic 1 kHz tick. Aliased each
to its nearest modeled voice via a key remap placed AFTER the sampled-kit check (these
keys are never sampled). Surdos 86/87 got a dedicated ~82 Hz membrane — no GM tom sits
that low — mute damped / open ringing (~85 Hz measured).

## MM-BUG-KILN-00009 — NOT done; architectural fork for Arthur
The drumkit crate ships only two tom recordings (`tomhi` 181 Hz, `tomlo` 113.5 Hz; 12
takes each). Key 50 is driven 1.94× (~+11.5 st) off the rack tom → chipmunk/thinning.
The clean fix needs a NEW public-domain mid/high-tom recording added to a **published**
asset crate (new dependency + provenance + level pin) — discuss-first. The cheap
alternative (repitch cap) trades chipmunk for pitch-collapse at the top of fills — a
perceptual tradeoff this earless box can't judge. Left Open; surfaced to Arthur with
both options.

## render-diff inventory (mandated for drums.rs/sampler.rs)
Baseline = `ccb8171` (see gotcha below); `--key 40,27..34,83..87 --rate 11025`.
- **0 contamination** (decisive), 4 EXPECTED changed (V3-kit key-40 albums), 102 same.
- 3 NOT-REACHED all send **ch10 Program 40 → Brush kit**, where key 40 is a brush swirl
  (drums.rs:1432), before the sampled path — so the 00008 fix correctly leaves them
  unmoved. Verified by scanning each MIDI for a ch9 prog-40 change; not assumed.
- 00022 moved nothing (no album sounds those keys) — exactly right for a foreign-GM fix.

## Gotchas worth remembering
- **origin/main moved mid-task.** It advanced 45291e2 → `ccb8171` (XG bank-LSB / CC84,
  +13 commits touching engine.rs/voices.rs) while I was reading. Rebased the FIX branch
  onto it BEFORE building the baseline, so baseline and new differ only by my drum edits
  — else the XG voices would have shown as false contamination.
- **NOT-REACHED ≠ broken wiring.** The render-diff predicts by key, blind to the ch10
  program-40 brush switch. A key-40 album on brushes never reaches the sampled snare.
