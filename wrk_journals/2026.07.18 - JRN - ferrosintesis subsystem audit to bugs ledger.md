# JRN 2026.07.18 — ferrosintesis subsystem audit → bugs ledger

## What this was

Arthur asked where the synth is strongest / weakest / where to improve next. I ran
a read-only subsystem audit (9-agent fan-out over voices / engine-mix / drums /
sampler / dsp-reverb / coverage-live / oracles / history, plus a synthesis), and
cross-checked it against my own read of `README.md`, `DESIGN.md`, the round-3
voice-quality roadmap, the SC-55 level-audit CR, and the perceptual-distinctness
HLD. Arthur then asked to raise the findings as tracked bugs so we can work through
them.

## Routing decision (don't re-litigate)

I proposed splitting genuine defects → `bugs/` and enhancements/new-oracles →
`reqs/` (reqs being the `/loop` work queue). **Arthur chose: everything into
`bugs/`, meaningful items only, park trivia in `scratchpad.md`.** So the LA-bank
campaigns, the Passport-oracle work and the brass spectral-balance item are filed
as bugs here despite being enhancements — that is deliberate, per his call.

## Landed (this branch — docs/ledger only)

- **18 bugs, MM-BUG-KILN-00005 … 00022** (host KILN, next id was 00005; dedup'd —
  none duplicate the 4 existing bugs or 18 reqs). Severity spread:
  - High: 00005 viola 41≡40 shared onset; 00006 no class-identity (Passport) oracle.
  - Medium defects: 00007 linear sample-interp aliasing; 00008 elec-snare 40≡38;
    00009 tom 6-key stretch; 00010 shaker triplet; 00011 CC7/CC10 zipper;
    00012 contrabass wolf-band; 00013 no live poly cap; 00014 48/49 --no-samples.
  - Medium enhancements (into bugs per routing): 00015 chromatic-perc LA;
    00016 bass LA; 00017 e-piano LA; 00018 brass h2–h5 spectral balance;
    00019 second loudness-trim pass.
  - Low: 00020 anti-clone oracle vanishes under --no-default-features;
    00021 poly-AT dropped live; 00022 GM2 perc → generic tick.
- **10 trivia parked** at the top of `scratchpad.md` (closed/pedal-hat dup, ride
  bell modeled, blown bottle, cathedral-send HP, BusGlue keying, Sine renorm,
  denormal FTZ, embedded_wav guard, dead drumkit banks, doc/count drift).

## Recommended work order (my view, not binding)

Reuse-only, highest leverage first: **00005 viola bank** (source pinned, ~37–51
albums) → **00007 cubic sample interpolation** (reuse `tap_cubic`, lifts every
sampled instrument) → **00006 Passport oracles** (the force-multiplier — would
catch realism drift like the viola clone automatically). Then the cheap default-path
drum/controller fixes (00008/00010/00011), then the net-new sampling campaigns
(00015/00016/00017).

## State

Docs/ledger-only branch, ready. Line-number pointers in the bug files are indicative
(`~:NNNN` + symbol) — they were gathered from origin/main HEAD at audit time; confirm
against the tree before fixing. No code touched, no version bump.
