# 2026.07.18 — JRN — Guitar block two (strum helpers, high-key damper, commuted r2)

Task: design + implement, as one sequential block (same lean flow as the
guitar-realism block that shipped as 0.21.36):

1. **Strum/idiom composition helpers** — per-string strum offsets, stroke
   direction, playable 6-string voicings, legato idiom — as a tested,
   self-contained reference module for FUTURE albums (no committed album
   touched; copy-per-album convention).
2. **Steel high-key damper fix** — the documented handover cliff (KS one-pole
   damper magnitude at f0 — B5 ≈ −390 dB/s lesson); guitar-scoped; restore
   the excluded seam-oracle rows.
3. **Commuted excitation round 2** — reconstruct the parked prototype
   (`park/20260718-commuted-excitation-prototype`) on current trunk and apply
   the (1−g)-normalized harmonic-projection seed + boundary handling; the
   FROZEN round-1 gates decide; ship-or-repark; Arthur's audition gate.

Worktree: `D:\worktrees\ferrosintesis\20260718-DEV-HUM-guitar-block-two-strum-damper-commuted`
(branch `task/20260718-DEV-HUM-guitar-block-two-strum-damper-commuted`, off
origin/main post-0.21.36).

Proposed read stated at kickoff (Arthur may correct): strum helpers land in
the repo's copy-per-album convention at the least-surprising reference spot
(scout decides), unit-tested, with a demo probe; existing albums' builds stay
byte-identical.

## Log

- Worktree created (deltic start-task, type DEV). Two UNDERSTAND scouts
  spawned: `scout-compose` (engine.py landscape, guitar-part prior art,
  helper-module home, byte-identity rules) and `scout-synth` (damper math at
  high keys, XG-variation coupling — trunk now derives UKULELE from NYLON —
  pinning tests, park-branch machinery inventory).
- **Scout results** (both strong): a basic strum() already exists (~40 call
  sites) — item 1 is an EXTENSION; adding unused defs is byte-identity-safe
  (per-Score rng, drawn only in note()); The Remaining = newest lineage with
  a unittest test_verify.py. Damper decay table computed (damper share 89-97%
  at E6; nylon worse than steel); fix idiom = damper-corner lift (DRIVE
  comment precedent); UKULELE/OUD inherit ..NYLON (must handle explicitly);
  park branch re-applies cleanly (anchors byte-identical, saved diffs).
- **HLD v1 → adversarial review (3 lenses) → full cross-refutation.**
  13 findings; 11 upheld (some strengthened), 2 corrected in mechanism, none
  spurious. Standouts:
  - **D-F5 (CRITICAL, the round's best find)**: my (1−g) fold normalizer was
    wrong twice — omits the in-loop damper (per-trip is g·|H|; tail models
    |H| — inconsistent) and a truncated fold never converges. NUMERIC
    CONFLICT in refutation (dsp: −15..−17 dB under at k52; scope: −0.5 dB) —
    I arbitrated by direct computation: g(k52)=0.9906, K≈19.8, 1−g^K=0.17 →
    dsp right, scope's g≈0.87 was arithmetic error. Fix (dsp-validated
    numerically): normalize by the ACTUAL accumulated weight sum with
    (g·|H(f0)|)^k weights — |H| shifts the sum −11 dB at k88 and makes the
    fold converge where plain g^k is truncation-dependent.
  - **S-F1/D-F7 (unanimous)**: v1's rake t^1.6 was convex — bunched strings
    at the START, contradicting its own prose AND oracle → concave
    1−(1−t)^rake.
  - **S-F2 (strengthened)**: the pattern-string DSL provably could not
    reproduce Big Weather t10's stroke table (non-uniform beats, per-stroke
    durations, per-stroke dv all lost) → DSL cut; strum_seq takes stroke
    tuples (a true superset); AC = reproduce _gtr_strum stroke-for-stroke.
  - **S-F3 (modified)**: hammer_run ~90% overlaps run(legato=True) — but
    run is DIATONIC-only (verified), so the surviving delta is a pitches=
    override ON run, not a second helper.
  - **S-F4 → arbitrated**: demo-local home WITHDRAWN in refutation
    (oracle-first needs the unittest home); survives as a stated-decision
    condition — The Remaining designated the seed engine explicitly.
  - **I-M1 (verified)**: v1's damper-law insertion spec was unimplementable
    (the sr·0.45 clamp precedes f's existence); fix = fold factor into the
    wound line :3640 + re-clamp. The feared tuning drift was REFUTED (both
    delay_for and OnePole clamp internally at 0.49) — fix kept for corner
    consistency.
  - **D-F1/D-F2/D-F3**: honest limits documented — totals (not "held")
    steel −40/−49, nylon −65/−67 at vel100; vel-40 cliff persists (canary
    row added, fix out of scope); working window k71..~86 (clamp), dry-out
    keeps skip(1).
  - **S-F6/I-M4**: item ordering re-labeled risk-ordering (all commuted
    gates probe key 52 < anchor → item-2-inert; all gates relative; no
    absolute thresholds to invalidate).
- **HLD v2 written** folding all survivors. Next: external Codex pass.
- **External Codex pass ran** (gpt-5.6-sol, xhigh, read-only; pwsh wrapper
  recipe, worked first try). SEVEN findings, all folded, none rejected
  (HLD §9). Decorrelation paid again — catches the internal panel missed:
  - C4: altbank.rs PIZZ spells out every preset field without ..DEFAULTS —
    the new field literally won't compile without touching it (file was
    absent from the impact table).
  - C3: Codex ran `git apply --check` itself — the park patch ALREADY fails
    a hunk (XG dispatch landed at the insertion point) → re-land is a
    semantic transplant with a journaled checklist, not an apply.
  - C5: note_off's G6 release-darkening glide (~36 ms toward 600/700 Hz)
    overrides any treble hold in release → SCOPE DECIDED: released notes
    stay G6-governed (that's G6's shipped design); decay oracle specified
    as model-only HELD note with fixed estimator/windows/seeds; short-note
    behavior goes to the audition stems.
  - C1: frozen gates probed one zone/key while authorizing all → population
    expanded to every zone's nearest key + 88, thresholds untouched.
  - C2: scalar (g·|H(f0)|)^k still not the true per-harmonic operator →
    upgraded to an OPERATOR fold (per-pass g·OnePole, Horner), classified
    heuristic, gates validate.
  - C6/C7: voicing oracle tightened (set-equality, all-12-barre-root sweep,
    per-string mapping, precise units); `sweep_span` rename + duration law.
- **HLD v3 stamped SELF-APPROVED** (not SIGNED-OFF); proceeding to
  implementation: item 1 → 2 → 3.

## Implementation log

- **Item 1 built** (212c32d): strum_seq + voicing + run(pitches=) in The
  Remaining's engine.py; 10 unittest oracles green FIRST RUN (incl. the
  byte-for-byte _gtr_strum reproduction and the 36-case barre sweep);
  album rebuild proven byte-identical; demo written + .mid committed
  (demo .mids are a repo convention, 15 exist).
- **Item 2 built** (32ac634): treble_hold_hz + law at the wound line with
  re-clamp; altbank PIZZ compile-fix landed exactly as Codex C4 predicted;
  decay oracle green first run (steel/nylon 76/88 vel100 in band; vel-40
  canary froze at [−450,−40]); k76 seam rows scoped BY MEASUREMENT
  (probe_k76_seam_excess): nylon k76 1.72×/1.61× at vel 72/100 (rows
  added), 3.22× at vel 40 (documented limit), steel k76 3.6–4.0× at ALL
  vels — a velocity-independent take-vs-model LEVEL parity gap →
  scratchpadded as its own feature (old cliff entry closed, split).
  Full suite 527 green.
- **Code review (4 angles)**: no substantive bugs; 8 findings survived, 4
  applied (duplicated k76 probe cut — the A4 drift risk re-introduced;
  strum_seq dir char + rake now fail-loud ValueErrors; voicing min(key=)),
  4 skipped with rationale (pitches= overload kept as pinned DRY win; k76
  vel-scoping confirmed honest; alt-bank high-key pin policy-expected;
  taper-constant dup tiny). Tracer PROVED the .min re-clamp is a no-op for
  every hold=0 preset at all rates.
- **Validation** (rebased clean onto 0.21.37 — XG amp-sim landed upstream,
  no conflicts): workspace 544/0; clippy/fmt clean; py oracles 10/10; album
  byte-identity holds. **Render-diff** (fresh verified binaries): 114 MIDIs
  → 11 EXPECTED-changed (all GM24/25), 101 same, **0 CONTAMINATION**;
  2 "NOT REACHED" rows investigated and CORRECT — Through Lines t03 (max
  guitar key 64) and Winter Guests p1 (max 69) never exceed the key-71
  anchor, so the hold is exactly 1.0 there: the max(1,·) bit-inertness
  proven end-to-end on real albums.
- **Audition kit** (`target/audition/`): item1_strum_demo.wav (musicality
  gate), item2 nylon/steel high-key A/B pairs (held vel 100/40 + short
  notes for the G6 release decision), README listening guide.
- **Final state: items 1–2 focused-green and ready to integrate; item 3
  re-parked on `park/20260719-commuted-excitation-r2`. No version bump on
  the branch (integration owns it). BASELINE worktree left for the
  integrator to reap.**
- **Item 3 RE-PARKED** (park/20260719-commuted-excitation-r2, commit
  0daabd8; task branch rewound via switch+rename — no revert pair, no
  reset --hard, nothing discarded). The C1 gate expansion earned its keep:
  restrike+determinism passed at k52, but zone E2/key 40 failed seam
  (−10.4 dB) and profile (10.3 vs 6.1) — the fold's K scales with f0 so
  the seed drifts per zone; fixing it = mechanism change = out of budget
  per the preregistration. Round-3 lead journaled in the HLD as-built
  note (fixed-time-span fold / per-seed f0-parity normalization).
