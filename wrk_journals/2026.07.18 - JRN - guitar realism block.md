# 2026.07.18 — JRN — Guitar realism block (items 1–5)

Task: design + implement, as one sequential block, the top five guitar-improvement
items from the 2026.07.18 guitar SoTA analysis
(`wrk_docs/2026.07.18 - CR - guitar voicing, LA usage, and SoTA comparison.md`):

1. RR/velocity variation on the acoustic LA banks (kill the machine-gun tell)
2. GM29/30 held-note decay latch — investigate/fix
3. IR cabinet for the Drive insert
4. Guitar mechanical noise (preset authoring) + modest LA fade widen
5. Commuted-synthesis prototype for the acoustics (tightly scoped)

Process: lean end-to-end flow (design → adversarial review → external Codex review
→ SELF-APPROVED checkpoint → implement → code review → validate). Max 2–3 helper
agents per step. Worktree: `D:\worktrees\ferrosintesis\20260718-DEV-HUM-guitar-realism-block`
(branch `task/20260718-DEV-HUM-guitar-realism-block` off origin/main @ 0.21.26).

## Log

- **Worktree created** via `deltic start-task` (type DEV). Base: 7ff9044 (0.21.26).
- **Prior evidence**: a 5-agent investigation workflow (run earlier this session)
  produced file:line maps of the LA layer, Pluck model, Drive insert, prior-art
  decisions, and SoTA/asset research. Key inputs to the design:
  - Guitar LA is onset-only BY DESIGN (2026.07.10 LA HLD §1; lessons_learnt
    2026.07.16) — model owns sustain for CC expressiveness.
  - Both shipped guitar banks are single-take (source limitation, not policy);
    the LA HLD §3 originally *proposed* 2 vel layers + RR2.
  - Karoryfer free libraries relicensed to CC0 (Shinyguitar 4vel×4RR,
    Emilyguitar 4vel×3RR) — but instrument-match vs our nylon/steel needs
    verification (archtop/electric ≠ classical/dreadnought).
  - No FFT/convolution in shipped code; a 5–50 ms cab IR as direct FIR is
    ~1–2 % of a core — feasible without FFT. Public-domain IR sourcing needs
    licence verification (Soundwoofer).
  - Electrics excluded from LA sampling by design (LA HLD §5) — not revisited.
- **Item 2 re-diagnosis (own read of voices.rs)**: `DRIVE.sustain = 0.0` is a
  DELIBERATE round-3 U2 decision (voices.rs:2503-2511): default GM29/30 =
  decaying overdriven pluck; the sustaining voice is the alt bank's DRIVE_LEAD
  (sustain 0.6); `driven_main_and_alt_banks_diverge` pins the contrast. The
  lessons_learnt 2026.07.18 "opposite of design intent" note measured against
  the superseded guitar-v2 intent. → Item 2 is a RECORD CORRECTION, not a code
  change. To fold into the HLD decision log; verify oracle name via code scout.
- Spawned 2 design-phase helpers: `asset-scout` (web: CC0 guitar sample sets
  with vel/RR; verifiable public-domain cab IRs) and `code-scout` (plumbing:
  bank tables/RR selection, new-crate wiring, latch code, cab oracles,
  mechanical-noise preset fields, excitation path, LA guards).
- **Own read**: `Pluck::render` has a per-sample `inject` path into both KS
  loops, fed from the `hammer` buffer (voices.rs:3797-3801) — an arbitrary-
  length excitation CAN be trickled in over time. Item 5 needs no loop-
  architecture change.
- **asset-scout report — two pivots forced:**
  1. *No CC0 acoustic-guitar set with RR or velocity layers exists.* Karoryfer
     (CC0, verified LICENSE) are all ELECTRIC (Shinyguitar = archtop jazz box;
     Emily = solid-body; Black&Green = hollowbodies) — poor substitutes for a
     classical/dreadnought. FreePats steel-acoustic is GPLv3 (licence trap).
     Pianobook non-redistributable. VCSL guitar content thin/unverified.
     → Item 1 pivots from "re-source with real takes" to **synthetic variation
     of the existing single takes**: velocity-tracking brightness on the sample
     (mirrors the model's own pick_lp velocity law; fixes the model/sample
     brightness mismatch at the crossfade) + seeded per-note decorrelation
     (rate ±cents, spectral tilt, micro-comb — DrumJitter/LaFx precedent). No
     new assets, no new crate, no licence work.
  2. *Soundwoofer "public domain" is a community/marketing claim — no licence
     deed found*; jfsantos ir.wav merely re-asserts it. Embedding is a
     provenance risk our strict CC0+SHA-pinned culture shouldn't take.
     → Item 3 pivots from "embed a measured IR" to a **modal cabinet**: a
     parallel bank of damped resonators (+1-2 early-reflection taps) authored
     from published cab-response character — dense interference structure an
     IR gives, zero assets, zero FFT (a damped-cosine mode IS a resonant
     biquad), licence-clean by construction. Machinery would accept a real
     measured IR later if a clean one ever appears.
- Consequence: the whole block is now **asset-free**. Item 5's excitation can
  also be derived at RUNTIME from the already-embedded onset WAVs (string-
  canceller inverse filter at bank init) — no new baked assets there either.
- **code-scout report**: full plumbing map (bank tables, LaVoice no-seed, latch
  gates, cab oracles + numbers, preset noise fields, hammer/inject port,
  prepare.py routing, LA guards). Confirmed item 2 = deliberate design
  (`class_identity_ranges_hold` pins plucked 24-37 sustain_db ≤ −10).
- **HLD v1 written**; adversarial review ran: 3 reviewers (rev-dsp DSP/physics,
  rev-integ integration/oracles, rev-scope simplicity/scope) then a full
  cross-refutation round (each judged the other two's findings in code).
  31 findings raised; none fully spurious; 4 corrected in scope. Standouts:
  - **I2 (elevated CRITICAL, all upheld)**: STEEL is also the `_` fallback
    preset (voices.rs:11172) — authoring on it would leak to unmapped programs
    and falsify the render-diff canary. → GM25/fallback preset split.
  - **D1 (CRITICAL, upheld)**: KsLoop::tick delays injected energy one loop
    period — commuted excitation must preload its first period as KS init
    state, inject only the tail; benefit reframed to body colour + restrikes
    (LaVoice discards the model before fade_start anyway).
  - **D6-vs-I5 conflict settled with numbers**: measured alias floor −54 dB vs
    −40 bar (~14 dB margin even with FIR after cliff); resolution = place the
    MicroCab FIR BEFORE the cab lowpasses (unconditional safety, free).
  - **D3 premise refuted**: Zone.root is already the autocorrelation-measured
    recording f0 (sampler.rs:76-77) — "derive N from measured f0" collapses.
  - **S1/S2/S3 (upheld)**: green oracles prove properties, not musicality —
    added the §10 audition checkpoint (restores the v2 HLD listen gate);
    item 5 ships on Arthur's ears, not numerics; AC1 demoted to presence
    canary (HAT lesson: NCC 0.494 still machine-gunned).
  - **D5 (upheld, measured)**: steel_B5.wav = 0.706 s (all others 0.908) →
    fade widen capped at 0.30 s (0.30×2.05 = 0.615 clears every zone);
    GM25 rows added to la_level_continuity (steel was unguarded).
  - Other survivors folded: locked sample+model per-note detune (D7),
    guitar-only wrap_var factoring (S4/D8, 2 touch points not 16), FNV
    byte-identity pins (S5), per-item-commit render-diff (S6), seam-RMS
    parity calibration (D2), (zone,preset) OnceLock cache + prewarm (D10/I3),
    retrigger wiring for commuted restrikes (S8, premise corrected — retrigger
    already re-injects noise), pick_click band-above-corner + differential
    wound test (I1/I9), vel≥100 exact bypass (I10), tap ≤0.55 ms (I8),
    band-average normalization (D9), CR "portable here" correction (S11).
- **HLD v2 written** folding all survivors. Next: external Codex review.
- **External review ran** (codex exec, gpt-5.6-sol, xhigh, read-only; first
  attempt failed — `deltic timeout … bash` resolved to WSL bash without node;
  retried via `pwsh` wrapper, worked). 9 findings, ALL folded, none rejected
  (HLD §14 triage). The two that overturned internal conclusions, both
  verified in code before folding:
  - **C2**: the `_ => STEEL` arm is UNREACHABLE for valid MIDI — enumerated
    all match arms: 128/128 GM programs explicitly covered. The internal
    panel's I2 "preset split" (upheld by both refuters, one elevated it to
    CRITICAL) was wrong. Split dropped; D8 retracted.
  - **C1**: authored attack mechanics (attack_noise/click_hp) are INAUDIBLE on
    the default sampled path — LaVoice zeroes the model before the 50 ms fade
    start and the bursts die in 3-4 ms; the recorded onsets already carry the
    real mechanics. Item 4a reduced to stop_thump only (audible at note-off,
    all paths) — which also dissolves the risky pick_click/wound oracle
    recalibrations entirely.
  - Rest: C3 detune composition rule + ±10 c musical budget; C4 low-vel seam
    rows; C5 combined MicroCab+biquads oracle; C6 body-EQ bypass when
    commuted + 3-band seam parity; C7 per-strike restrike variation; C8
    source-domain guard + end taper + stepped residual read; C9 preregister
    item-5 oracles from baseline+counterfactual before mechanism.
  - Lesson for the fleet doctrine: same-family review (3 lenses + refutation)
    still shared two blind spots a cross-family pass caught in one shot.
- **Item 2 + S11 docs edits applied** (lessons_learnt GM29/30 correction; CR
  "portable here" correction).
- **HLD v3 stamped SELF-APPROVED** (not SIGNED-OFF — that marker is Arthur's);
  proceeding to implementation per the lean flow. Integration + audition
  (§10) remain Arthur's gates.
