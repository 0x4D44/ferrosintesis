# 2026.07.18 — JRN — overnight code-review pass (ferrosintesis core)

Read-only overnight code-review pass, team-lead. Docs-lane branch
`task/20260718-CR-HUM-overnight-code-review-pass`.

## What I did

- Created the coverage ledger `wrk_docs/review-coverage.md` (first run) with all 20
  `crates/*/` areas at `(never)`. Cargo workspace → areas are crate dirs.
- Picked `crates/ferrosintesis/` (NEVER-REVIEWED, highest-risk: the core, crates.io-
  published synth library). ~49k lines — too big to review uniformly in one bounded
  pass, so I scoped the deep slice to the ~17k-line correctness/safety core (parser,
  engine, sampler, dsp, reverb, loudness, wav, live, offline, error, lib) where
  objectively-confirmable defects live, rather than the ear-gated timbre tables.
- Ran the `deep-review` workflow (7 lenses + 3-skeptic adversarial verify) over that
  slice with a brief that fenced off by-design audio aesthetics and the sealed API.
  27 agents, 0 errors, 7/7 lenses ran. 10 findings → 7 confirmed, 3 refuted.
- Independently re-confirmed every reported finding against source (didn't take the
  workflow on faith): read the parser key-read sites, the two engine array-index
  sites, the brightness oracle + `render_default_bowed` + `make()` routing.
- Filed 2 bugs, wrote the CR report, upserted the ledger, journaled.

## Decisions / judgement calls

- **Excluded the parser running-status-after-meta issue** I spotted early: commit
  history shows it is an **in-flight fix** on `task/20260717-FIX-CDX-fix-smf-running-
  status-across-meta-event` (checked the diff — it adds the exact regression test).
  Not a duplicate to file.
- **Filed as bugs (genuine defects):**
  - `MM-BUG-KILN-00025` — renderer panics on note key ≥ 128 (parser doesn't clamp the
    key byte; engine indexes `[_;128]` arrays by it). Reproduced by execution. The
    flagship finding. Panel corrected High → Medium (availability-only, safe panic).
  - `MM-BUG-KILN-00026` — GM42/43 brightness guarded by the wrong struct: brightness
    oracles render `Bowed::new` (CC0 alt-bank), not the shipping `BowedString`, and
    assert no direction. Recurrence of the **Closed** `MM-BUG-KILN-00004` wrong-struct
    trap — 00004 fixed the *vibrato* oracle (migrated to `make()`); the *brightness*
    oracles were left behind, and today's `41d2bc8` brightness commit walked into it.
    Distinct from 00004's closure, so not a reopen — a new instance.
- **Kept in the CR report only (not filed):** CR-3 (stale comment, Low), CR-5 (GM43
  depth-floor not raised, Low), CR-6 (chorus de-send unpinned, Low), CR-7 (GM42 no
  cents-depth oracle, Low) — low-severity nits/oracle gaps on ear-gated constants,
  code correct today. Per "be conservative; nits stay in the CR report".
- **No reqs captured** — nothing here is a structural refactor / missing abstraction;
  the tracked items are the 2 bugs, the rest are CR-report record.
- **Ledger honesty:** the whole crate row is stamped 2026-07-18 though only ~17k of
  49k lines were deep-reviewed. `voices.rs`/`sampler.rs` internals change often, so the
  **MOVED** tier will re-trigger the crate naturally — noted in the CR report.

## Surprises

- A deep-review lens reproduced the key≥128 panic by actually building and running a
  crafted MIDI through the binary — stronger evidence than static reasoning.
- One refuted finding was killed *empirically* (a skeptic reverted both vibrato-depth
  constants, ran the suite, saw the rate oracle fail, restored the file). The rate
  oracle turns out to be an implicit depth guard for GM42 — good adversarial verify.

## Next pass

All other crate areas are NEVER-REVIEWED. Order: `crates/ferrosintesis-cli/` →
`crates/render-catalog/` → the `ferrosintesis-samples-*` asset crates. A dedicated
`voices.rs`/`sampler.rs` sub-pass is worthwhile later (MOVED tier will surface it).
