# 2026.07.18 — JRN — overnight code-review pass 13 (samples-orchestral2)

Read-only overnight review loop, pass 13, team-lead. Docs-lane branch
`task/20260718-CR-HUM-rev-orchestral2`.

## What I did

- Picked `crates/ferrosintesis-samples-orchestral2/`. Grepped declarations: `SAMPLES`
  slice, `FILE_COUNT = SAMPLES.len()` (derived), `get()` (212), 2 tests — pure-inventory
  pattern with a slice (not fixed-array) table. No decode/Bank/root logic.

## Findings

- **None** (no bug/req). Minor CR-report observation: this crate's size test
  (`every_sample_is_a_nonempty_wav`) lacks the `EXPECTED_BYTES` aggregate-size canary
  that -core/-orchestral/-sax/-drumkit use; low-value cross-crate test-rigor
  inconsistency, and `inventory_matches_packaged_wavs` still pins the file set.

## Next pass

6 more `samples-*` crates remain NEVER-REVIEWED. Next: `-strings` (204), then
`-musescore-grand`, `-musescore`, `-clavinet`, `-honkytonk`, `-ydp-grand`, `-gong`.
