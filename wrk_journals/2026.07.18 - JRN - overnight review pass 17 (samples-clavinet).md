# 2026.07.18 — JRN — overnight code-review pass 17 (samples-clavinet)

Read-only overnight review loop, pass 17, team-lead. Docs-lane branch
`task/20260718-CR-HUM-rev-clavinet`.

## What I did

- Picked `crates/ferrosintesis-samples-clavinet/`. Grepped declarations: `FILE_COUNT=11`,
  fixed-array `SAMPLES`, `get()` (63), 2 tests (with `EXPECTED_BYTES`) — pure-inventory
  pattern. No decode/Bank/root logic.

## Findings

- **None.** Pure asset-inventory crate. The SF3 clavinet loop-point extraction
  (lessons_learnt) is in `prepare.py` (`_bake_clavinet_note`), not this crate.

## Next pass

3 more `samples-*` crates remain NEVER-REVIEWED. Next: `-honkytonk` (100), then
`-ydp-grand` (100), `-gong` (83). After those, coverage is current and the loop stops.
