# 2026.07.18 — JRN — overnight code-review pass 3 (render-catalog)

Read-only overnight review loop, pass 3, team-lead. Docs-lane branch
`task/20260718-CR-HUM-overnight-review-pass-render-catalog`.

## What I did

- Refreshed main clone; ledger shows `ferrosintesis` + `ferrosintesis-cli` reviewed
  2026-07-18. Picked `crates/render-catalog/` (last NEVER-REVIEWED real-code crate,
  higher-risk than the asset crates).
- Read the whole `main.rs` (1144 lines) across all seven lenses. Analysed the
  subprocess (`ropusenc`), the `thread::scope` + `AtomicUsize` parallelism, temp-file
  handling, path handling, and the argv construction directly rather than spawning the
  27-agent fan-out — the file is a single cohesive, exceptionally well-tested unit and
  the fan-out was not where effort pays.
- Appended Pass 3 to the day's CR report, upserted the ledger, journaled.

## Findings

- **CR-9 (Low, defensive, currently unreachable):** the success tally uses
  `results.len()` (surviving-worker outcomes) as the denominator, and `join()` errors
  are swallowed by `unwrap_or_default()` (main.rs:823, :828-831). A worker panicking
  *outside* `render_one`'s `catch_unwind` would silently drop its tracks and the tool
  could print "N/N rendered" + exit 0. No realistic panic path exists (render_one is
  fully catch_unwind'd; the worker loop only does guarded indexing), so it's a
  defensive-hardening note, not a live defect. → CR report, not a bug.
- **No bugs, no reqs.** Clean, defensively written, unusually well-tested crate.

## Decisions / judgement calls

- **Direct review over fan-out:** proportionate for one well-tested file where I could
  fully reason about the concurrency/subprocess/path surface. "No findings" is a valid
  outcome; CR-9 is the one honest, low, defensive note — explicitly labelled unreachable
  rather than dressed up as a live bug (the task forbids plausible-but-wrong findings).
- **Verified the security surface rather than assuming:** `ropusenc` via `Command::args`
  (no shell), all argv content repo-controlled (compile-time ALBUMS + sidecars), fixed
  argv order → no argument-injection. Not a finding.
- **Verified the concurrency:** cursor partition-tested, Relaxed ordering correct for a
  unique-index counter, unique temp files per track, immutable shared borrows. Not a
  finding.

## Next pass

Only the `crates/ferrosintesis-samples-*` asset crates remain NEVER-REVIEWED (17 of
them). Next: `crates/ferrosintesis-samples-drumkit/` (1144-line mapping crate, the
largest), then `-orchestral`, `-sax`, `-core`, and the rest. Scope each to the crate's
`lib.rs` mapping logic, not the embedded WAV bytes. Both real-code crates are now done.
