# 2026.07.18 — JRN — overnight code-review pass 4 (samples-drumkit)

Read-only overnight review loop, pass 4, team-lead. Docs-lane branch
`task/20260718-CR-HUM-overnight-review-pass-samples-drumkit`.

## What I did

- Refreshed; ledger shows the 3 real-code crates reviewed. Picked
  `crates/ferrosintesis-samples-drumkit/` — first of the 17 NEVER-REVIEWED asset
  crates, tie-broken to the largest (1144-line) / highest-risk one, and the area
  nearest the open drum-mapping bugs.
- Read `lib.rs` directly (asset crate, no fan-out): the 188-entry inventory table plus
  `Bank`/`layer_for_velocity`/`file_name`/`wav`/`pcm`/`get`/`decode_wav`.

## Findings

- **None.** Well-tested data/asset crate; minimal, correct helper logic.

## Decisions / judgement calls

- **Scoped to `lib.rs` logic, not the WAV bytes** (source assets, out of scope per the
  brief and the repo's "samples are source" rule).
- **Confirmed the panics are unreachable, didn't file them:** `decode_wav`'s length
  assumptions and `wav`/`pcm`'s `.expect` apply only to compile-time embedded,
  test-validated assets — by-design, and pinned by the crate's own tests.
- **`pcm()` eager decode-all-on-first-use is intentional** (documented) — not a finding.
- **Confirmed the drum-key→bank mapping is NOT here:** the open drum bugs
  MM-BUG-KILN-00008/-00009 live in the consumer (`ferrosintesis` drums/sampler), so
  correctly out of scope for this crate.

## Next pass

15 more `ferrosintesis-samples-*` asset crates remain NEVER-REVIEWED. Next:
`crates/ferrosintesis-samples-orchestral/` (555 lines), then `-sax`, `-core`, and the
rest down to `-gong` (83). These are structurally uniform inventory + root/mapping
crates; scope each to its `lib.rs` logic. All are low-risk generated code, so expect
mostly clean passes — verify the root-pitch/zone mapping tables (the historical trap
per lessons_learnt: 2f-dominant roots, octave mislabels) where present.
