# 2026.07.18 — JRN — overnight code-review pass 5 (samples-orchestral)

Read-only overnight review loop, pass 5, team-lead. Docs-lane branch
`task/20260718-CR-HUM-overnight-review-pass-samples-orchestral`.

## What I did

- Picked `crates/ferrosintesis-samples-orchestral/` (next NEVER-REVIEWED asset crate).
- Read `lib.rs` (555 lines) directly. It is a **pure inventory crate**: the 157-entry
  `SAMPLES` table + `get()` + two tests. No decode, no mapping/root logic.

## Findings

- **None.** Pure asset-inventory crate. Sample roots are in the filenames; the parsing
  and root-labelling (the historical 2f/octave trap) live in the consumer
  (`ferrosintesis` `sampler.rs`), not here.

## Decisions / judgement calls

- **Confirmed the structural pattern:** these `samples-*` crates are pure `[(name,bytes)]`
  + `get()` inventory crates; `-drumkit` (with `decode_wav`/`Bank`) was the exception.
  The remaining 15 will be fast clean confirmations unless one carries extra logic
  (decode/Bank/root table) — recorded a loop-note in the CR report to that effect.
- **Fable 5 retainer available this session but NOT engaged:** a read-only review of a
  pure inventory crate is nowhere near the Fable bar (hard + high-leverage +
  hard-to-verify). Holding it in reserve; the nearest Fable-worthy item is designing the
  GM42/43 brightness-ordering oracle, but that is the *fix* for MM-BUG-KILN-00026, which
  this read-only loop does not perform (the fix-open-bugs loop owns it).

## Next pass

14 more `samples-*` crates remain NEVER-REVIEWED. Next: `-sax` (375), then `-core`
(343), `-dark-salamander`, `-headroom`, `-grand`, and the rest down to `-gong`.
