# 2026.07.18 — JRN — overnight code-review pass 6 (samples-sax)

Read-only overnight review loop, pass 6, team-lead. Docs-lane branch
`task/20260718-CR-HUM-overnight-review-pass-samples-sax`.

## What I did

- Picked `crates/ferrosintesis-samples-sax/`. Grepped for non-inventory logic (only
  `get()` + 2 tests), then read the head + `get()`/test region directly to confirm.
- It is a pure inventory crate: `FILE_COUNT=74`, `SAMPLES` table, trivial `get()`,
  inventory + header/aggregate-size tests.

## Findings

- **None.** Pure asset-inventory crate. The sax looped-sustain / `SAX_LOOP_GAIN` logic
  (per lessons_learnt) lives in the consumer `sampler.rs`, not here.

## Decisions / judgement calls

- **CC BY 4.0 licence (not CC0):** attribution obligations are documented in
  `NOTICE`/`PROVENANCE.md` and referenced from the crate doc-comment. That's a
  provenance obligation, not a code defect; whether attribution propagates correctly
  into distributed builds is a compliance matter outside this crate's code — noted in
  the CR report, not filed.
- **Fable 5:** not engaged (trivial pass).

## Next pass

13 more `samples-*` crates remain NEVER-REVIEWED. Next: `-core` (343), then
`-dark-salamander`, `-headroom`, `-grand`, `-vcsl-steinway`, `-vcsl-kawai`,
`-orchestral2`, `-strings`, `-musescore-grand`, `-musescore`, `-clavinet`,
`-honkytonk`, `-ydp-grand`, `-gong`.
