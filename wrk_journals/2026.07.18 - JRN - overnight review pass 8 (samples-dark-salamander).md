# 2026.07.18 — JRN — overnight code-review pass 8 (samples-dark-salamander)

Read-only overnight review loop, pass 8, team-lead. Docs-lane branch
`task/20260718-CR-HUM-overnight-review-pass-samples-dark-salam` (deltic truncated the
slug to `-dark-salam`).

## What I did

- Picked `crates/ferrosintesis-samples-dark-salamander/`. Grepped declarations:
  `FILE_COUNT=54`, `SAMPLES`, `get()` (232), 2 tests — pure-inventory pattern, no
  decode/Bank/root logic.

## Findings

- **None.** Pure asset-inventory crate.

## Note

- deltic caps the worktree slug length, so this task's worktree is
  `...-samples-dark-salam` (not `-dark-salamander`); a first `cd` to the full name
  failed. Use `git worktree list` to get the real path when a slug is long.

## Next pass

11 more `samples-*` crates remain NEVER-REVIEWED. Next: `-headroom` (280), then
`-grand`, `-vcsl-steinway`, `-vcsl-kawai`, `-orchestral2`, `-strings`,
`-musescore-grand`, `-musescore`, `-clavinet`, `-honkytonk`, `-ydp-grand`, `-gong`.
