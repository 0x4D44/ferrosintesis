# JRN — XG drum-kit bank routing (CC0=127)

Task: `task/20260718-TSK-HUM-xg-drum-bank-routing`

## Diagnosis (pre-worktree, on main clone)
- Symptom: `04-incantations-part-iv-xg.mid` degrades at ~2:40.
- Parsed the MIDI: at **t=162.4s** ch14 (idx 13) sends `CC0=127` + `program=16` (XG
  Rock drum kit) — a 2nd percussion part (congas/bongos/shaker, keys 58–64/82).
- ferrosintesis routes drums by `ch==9` only, and treats any `CC0!=0` as alt bank →
  program 16 falls through to GM Drawbar Organ. Congas render as a sustained
  D4/Eb4/E4 organ cluster.
- Render confirmed: ch13-solo sustained pitched energy at Eb4/D4/D3; full mix +6 dB
  at the break. Root cause solid.

## Design
- Wrote HLD `wrk_docs/2026.07.18 - HLD - XG drum-kit bank routing (CC0=127).md`.
- Enumerated all `ch==9`/`ch!=9` sites in engine.rs; classified drum-routing gates
  (must change) vs inert melodic gates (leave).
- Key architecture fact: ch9 drum gain is baked into bus master
  `g9 = strips[9].gain·DRUM_FORWARD` + optional drum wah — entangled, doesn't
  generalize.
- ch13 authors **435 CC11 expression events** → losing its dynamics would be audible,
  so chose **A2**: XG-drum voices take a small parallel path (own channel gain/pan →
  master mix, DRUM_FORWARD + own CC91 reverb + shared drum room), leaving ch9's bus
  path literally untouched (byte-identical albums by construction).
- Rejected A (share ch9 bus — loses ch13 dynamics) and B (refactor ch9 gain path —
  album-render risk).

## Adversarial review (2 reviewers) — survivors folded
- No critical findings; both confirmed byte-identical-albums holds by construction and
  A2 is the right altitude (A cross-couples ch9's fader onto ch13 congas).
- Folded: (1) guard the channel-AT `set_pitch` loop (~2116) with `!a.is_drum` — removes
  the "inert by luck" fragility; (2) rewrote acceptance test #1 — single isolated hit,
  `assert_eq!(ch9,xg)` + `assert_ne!(organ)` + Goertzel@D4, with a pinned note that the
  parallel path must mirror ch9's `(scratch·ul)·g` FP association; (3) explicit borrow
  fix (pre-copy `pan_off`, move `strips[9]` mut-borrow after the closure); (4) documented
  sustain/choke XG-vs-ch9 divergences as accepted minors.

## External review (Codex gpt-5.6-sol)
- First two runs failed: xhigh timed out (10 min); high hit a broken Windows sandbox
  helper (`codex-windows-sandbox-setup.exe` missing) so it couldn't read files. Third
  run with all context INLINED succeeded.
- Verdict: A2 is the right minimal architecture; no new critical defect. Its points map
  to already-documented non-goals/minors (kit-not-mapped, sustain divergence, wah
  bypass, RR coupling, bank-commits-on-PC). Folded two refinements: (1) the bug-proof
  test should compare SUSTAINED TAIL energy (organ sustains, conga decays), not raw D4
  magnitude; (2) noted CC0=127-commits-immediately matches existing alt_bank convention.
- Codex's "High" (kit selection) was mis-attributed but usefully forced me to VERIFY
  drums.rs voices all of ch13's keys (58/61/62/63/64/82) — it does, with the default V3
  kit. Fix is complete, not half.

## Checkpoint: design SELF-APPROVED
- No architectural fork, no unresolved critical finding → proceeding to implement
  without waiting (per lean-flow checkpoint rule).

## Implementation (commits 348b353 feat, + refactor)
- Added `Strip.xg_drum` (set by CC0==127, cleared otherwise), `Active.is_drum`
  (= ch9 || xg_drum, frozen at spawn). Gated voice spawn, both mix passes, the
  program=128 sentinel, and the channel-AT set_pitch loop on it. New parallel branch
  in the drum pass: XG-drum voices scaled by their own gain/pan → master mix
  (DRUM_FORWARD + own CC91 reverb + shared drum room), ch9 bus path untouched.
- Found + fixed a 2nd `Active` constructor (bagpipe drone → is_drum:false).
- Test `xg_bank_127_routes_channel_to_drums`: single hit, xg13==drum9 (byte-equal),
  != organ, tail@D4 ≪ organ. PASSES.
- Verified drums::make voices all reference keys (58/61/62/63/64/82) with V3.
- Verified CC121 reset_all_controllers leaves bank flags alone → xg_drum persists
  like alt_bank (MIDI-correct).

## Code review + simplify
- /code-review high: no correctness defects. One cleanup: dedup the drum-pass pan
  computation across ch9/XG branches → hoisted (byte-identical), dropped pan_off.
  Committed as a refactor. Re-ran XG + 76 drum tests: green.

## Validation
- fmt clean; clippy (-D warnings via CI, ran plain) clean; full lib suite 488 passed.
- **origin/main moved to 0.21.23 (3 sax commits) during the task** → render-diff
  baseline is my parent 9adbd1b, not current trunk (else the sax commits read as
  false contamination). Branch needs a rebase onto origin/main at integration.
- Baseline binary built at 9adbd1b; confirmed genuinely different from the new binary
  (SHA 6f4608b2 vs f1f56ae4) — not a stale/contaminated baseline.
- Render-diff over all 109 album MIDIs: **0 changed, 109 same, 0 contamination, 0
  not-reached** — every committed album byte-identical. (Criterion 3 ✅.)
- ferrosintesis-cli + render-catalog tests: 16/16 green (incl. param-parity + ropusenc
  goldens). fmt + clippy clean. (Criterion 5 ✅.)
- Before/after Incantations (ch13 solo, baseline vs new binary), organ-cluster region
  168–176 s:
  - D4 293.7 Hz: 1.06e5 → 3.09e3 (↓34×); Eb4 311.1 Hz: 2.34e5 → 4.38e2 (↓534×).
  - New energy sits at D3/A2 (conga/bongo bodies): the sustained organ cluster is gone,
    the region reads as percussion. (Criterion 4 ✅.)

## Result: all acceptance criteria green; awaiting integration go
- TERMINAL at focused-green. Branch is un-bumped (integration owns the single bump) and
  3 commits behind origin/main (0.21.23) — needs a rebase onto trunk at integration,
  then re-run the affected gate before ff-push. NOT pushed (needs Arthur's go).
- Follow-ups parked in scratchpad: GS rhythm-part SysEx (extra drum channels the GS way)
  and XG SFX-kit bank 126.
- Cleaned up the throwaway BASELINE-xgdrum worktree.
