# JRN — XG effect SysEx (reverb / chorus / variation) for Incantations Part IV

Task: honor the XG effect-block SysEx the reference file
`test-corpus/reference-midi/mike-oldfield/04-incantations-part-iv-xg.mid` sends but
ferrosintesis currently drops. Arthur chose FULL scope (reverb + chorus + the variation
effect), knowing it's global one-time setup (won't explain the 2:40 degradation) and
unverifiable here by ear (needs his render + listen).

## What the file sends (12 SysEx, all once at the start)
- 1× GM System On (universal 7E).
- `00 00 7E = 00` — XG System On.
- Effect1 block (addr `02 01 xx`), 10 messages:
  | addr | data | (working interpretation — being confirmed by spec research) |
  |------|------|-------------|
  | 02 01 00 | 01 00 | Reverb Type = **Hall 1** |
  | 02 01 0C | 40    | Reverb Pan? (0x40 = center) |
  | 02 01 20 | 41 00 | Chorus Type = **Chorus 1** |
  | 02 01 2C | 40    | Chorus Pan? |
  | 02 01 40 | 4B 11 | Variation Type = **Amp Simulator** (MSB 0x4B=75 confirmed via XG spec), LSB 0x11 |
  | 02 01 42 | 00 18 | Variation Parameter 1 |
  | 02 01 54 | 00 64 | Variation Return? |
  | 02 01 56 | 40    | Variation Pan / Connection? |
  | 02 01 5A | 00    | Variation Part? (routing) |
  | 02 01 5B | 0E    | MW Variation Control Depth? |

Routing (insertion vs system, and which part) + the exact Amp-Sim params + the Hall1/Chorus1
defaults are being decoded by a research subagent against the MU100/MU128 XG spec. HELD until
that returns — those facts are load-bearing for the design.

## Current parser (midi.rs)
- SysEx handled at parse():198-232. Decodes ONLY two GS messages (DrumMode, GsReset); **every
  other SysEx is ignored** (comment at :201). XG effect SysEx (0x43…) is dropped.
- Clean precedent: a recognized SysEx pattern → a specific `EvKind` (DrumMode/GsReset), consumed
  by the engine. XG effect SysEx follows the same shape.

## Current engine effect architecture (engine.rs)
- THREE fixed global buses: hall reverb (Freeverb), stereo chorus, tempo-derived ping-pong echo.
  Reverb is CC91-authored per-channel (`reverb_send`); chorus/echo sends per-program via
  `fx_profile` (:383). Global amounts: `Options.wet` (reverb), `Options.delay_s` (echo) — set by
  the CLI/render-catalog, NOT by SysEx.
- Reverb *type* is effectively hard-coded (one Freeverb config); only wet + sends vary.
- **No "variation effect" / insertion-effect concept at all.**

## Reuse story (the key finding — the variation effect is mostly REUSE)
- `reverb::Reverb::new(sr, room, damp, wet)` + `with_predelay(...)` is FULLY parameterized →
  mapping XG Reverb Type (Hall 1) onto room/damp/predelay is a bounded param mapping, not new DSP.
- `Chorus` struct (engine.rs:483) is tunable (rate/depth) → Chorus 1 is a param mapping.
- **`struct Drive` (engine.rs:275) IS an amp simulator already**: pre-HPF, voicing peak EQ, two
  biased-tanh gain stages (g1/g2 + bias, asymmetric), tilt EQ, DC blocker, and a **5-biquad
  cabinet** (`cab_biquads`), all at 2× oversampling. Currently applied per-VOICE for the
  distortion-guitar programs 29/30 (via `needs_drive`). The XG Amp Simulator variation = a
  parameterized, per-PART (channel insert) version of this same DSP. So the variation effect is
  largely REUSE + a new routing (channel insert vs program-gated), not a from-scratch effect.

## Design shape (pre-decode, to firm up once the spec returns)
- Parser: decode the XG Effect1 SysEx → new `EvKind`(s) (reverb type, chorus type, variation
  type + params, returns, connection, part). Follow the GS DrumMode precedent.
- Engine: map reverb/chorus type → the existing Freeverb/Chorus params; add a variation
  insertion effect = a parameterized `Drive`-style insert on the target part's voices.
- Byte-identity invariant (as with CC32/CC84): NO album sends XG effect SysEx, so the change must
  be inert without it → render-diff over all albums must stay 0. Confirm.
- Open architectural questions for the checkpoint: (a) insertion routing (per-channel insert on
  the variation Part) vs system-send; (b) how SysEx effect settings interact with the
  CLI's `Options.wet`/`delay_s`; (c) how faithfully to model the Amp-Sim LSB 0x11 variant.

## AUTHORITATIVE DECODE (research subagent, sourced to Yamaha XG Parameter Change Table v1.35)
- `02 01 00 = 01 00` Reverb Type = **Hall 1** (high conf). `02 01 0C = 40` Reverb **Return** = 0 dB
  unity (0C is RETURN, not pan — pan is 0D).
- `02 01 20 = 41 00` Chorus Type = **Chorus 1** (high conf). `02 01 2C = 40` Chorus **Return** = unity.
- `02 01 40 = 4B 11` Variation Type = **Amp Simulator** (MSB 0x4B, high conf). LSB 0x11 is UNDOCUMENTED
  in the MU100/MU128 map (only 4B/00 exists) → treat as basic Amp Sim, ignore the LSB.
- Variation params (2-byte pairs at 42..55): `42 = 0x0018` = **Drive 24** (of 127 — MILD overdrive);
  `54 = 0x0064` = **Dry/Wet 100** (wet-leaning; 64=equal). AMP Type / LPF(cab) / Output / Edge are
  NOT sent → power-on defaults.
- ROUTING (the key facts): `5A = 00` = **INSERTION** (not a system send); `5B = 0E` = **Variation
  Part 15 = MIDI ch 15 = 0-based channel 14**. `56 = 40` Variation Return (inert in insertion mode).
- **Target channel 14 (0-based) plays program 29 = Overdriven Guitar** (325 notes, CC0=0, CC32=112
  undefined→base GM). So the amp-sim insert lands on the electric-guitar channel — as expected — which
  ferrosintesis ALREADY drives (needs_drive covers 29/30). The XG insert would STACK on the voice's
  own overdrive.
- Reverb/chorus defaults (Hall1 time ~1.8-2s etc.) are OCR-sourced = moderate confidence; the address
  MAP and NAMES are certain, the exact default NUMBERS are not.

## HONEST re-scoping (post-decode) — surfaced to Arthur before building
- The ENTIRE audible effect of this file's XG SysEx = a MILD amp-sim (Drive 24) on ONE already-
  overdriven guitar channel + reverb Hall1/chorus Chorus1 with UNITY returns ≈ the engine's current
  hall/chorus (near-inert). It is global (not the 2:40 fix) and unverifiable here by ear.
- So "full XG effect engine" collapses to: a new per-channel INSERTION-effect slot (reusing the Drive
  amp/cab DSP) applied to the variation Part channel, plus recognizing reverb/chorus type + returns.
  Contained — but the payoff is subtle. Checkpoint question for Arthur: is the per-channel-insertion
  architecture worth it for that payoff, or right-size (e.g. honor returns, skip/simplify the insert)?

## Arthur's steer: FULL scope + FAITHFUL reverb/chorus (re-tune Hall1/Chorus1 to XG params).

## Design + reviews (SELF-APPROVED)
- HLD written (wrk_docs). Two adversarial reviewers (general-purpose, cheaper — NOT Fable; both
  load-bearing calls are followable + cheaply verifiable, so the Fable retainer stays reserved):
- ARCH review: flipped **stack → REPLACE** (the XG Amp Sim = the amp+cab stage = the strip `Drive`,
  not the voice; replacing keeps program-29's driven voice, honors one authored amp, avoids two
  5-biquad cabinets in series). Cut 3 over-built pieces: return-scaling, reverb/chorus type *tables*
  (→ two-arm Hall1/Chorus1 match), variation SYSTEM bus (file is INSERTION).
- CORRECTNESS review: empirically confirmed byte-identity — **0/109 album .mid files carry ANY
  SysEx**; seed is spawn-counter-based → provably inert. Killed a realtime leak: "Reverb Return scales
  opt.wet" is wrong (opt.wet is construction-only; mutating it corrupts hall+cathedral+drum_room and
  survives hard_reset in live.rs). Confirmed `Reverb::reconfigure(room,damp)` sound in place but
  predelay is NOT (construction-baked) → drop it. Dry/Wet must blend at the apply site (Drive has no
  blend); keep Drive::process byte-identical. Reset must touch ONLY XgEffects, never hard_reset/opt.
- Folded ALL survivors into the HLD (SELF-APPROVED). Net design: 2 EvKinds (XgReset, XgEffectParam),
  an XgEffects state block, a two-arm Hall1/Chorus1 recognizer + reconfigure(room,damp), and a
  param-driven amp/cab insert that REPLACES the program drive on the target channel with a base-rate
  dry/wet blend at the apply site. Provably inert on every album.

## Implementation plan (tests-first, unit-by-unit, each green + committed)
1. Parser: XG/GM System On → XgReset; XG Effect1 → XgEffectParam. Round-trip + GS-unchanged tests.
2. Byte-identity: XgEffects state defaulting to current + no-XG-song-bit-identical test.
3. Reverb::reconfigure(room,damp) + Hall1 recognizer + mechanism/wiring tests.
4. Chorus::reconfigure + Chorus1 recognizer + mechanism test.
5. Drive::from_stages + Drive::amp_sim; Strip.xg_insert; apply-site REPLACE + base-rate dry/wet blend;
   variation resolve/install. Routing + REPLACE + drive/drywet oracles.
6. Validate: full gate + render-diff (0) + synthetic + reference render for Arthur's listen.

## Status: SELF-APPROVED, implementing.
