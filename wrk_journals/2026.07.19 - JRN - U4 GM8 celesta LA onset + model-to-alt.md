# JRN — U4: GM8 celesta LA onset + model-to-alt (MM-BUG-KILN-00015 batch 2)

- **Date:** 2026-07-19
- **Author:** Claude Opus 4.8 (1M)
- **HLD:** `wrk_docs/2026.07.19 - HLD - LA sample sourcing plus model-to-alt (GM4-8-10-11-14-15, 32-37).md`
- **Unit:** U4 (last fully-autonomous already-pinned unit). **Status: focused-green**, ready to integrate. **No bump**
  (Arthur: continue unbumped; reconciled later).

## What landed
GM 8 celesta gains a real MS Basic bell strike as the **default**, pure `bell(CELESTA)` model preserved as the
**CC0≠0 alt** (free slot → the plain `8 | 11 | 32 => voices::make(.., false)` arm).

- **Source (already-pinned, MIT):** MuseScore "MS Basic" SF3 preset 8 (celesta), via `_bake_sf_onset` — the same
  self-contained SF3-extract path as the sitar/pipes, **zero external download** and no WAV-fetch config
  (F0_RANGE/KEEP_FAM/etc. don't apply). → the MIT **-musescore** crate (existing NOTICE covers the MS Basic source).
- **prepare.py:** one `if want("celesta"): _bake_sf_onset(ms_src, 8, "celesta", "…-musescore", 0.9, 0.24)` block.
- **Measured roots (all clean — SF3 originalPitch-windowed):** F#3 184.77, C4 261.14, F#4 370.10, C5 523.20,
  F#5 739.76, C6 1052.66, F#6 1481.45, C7 2098.53 (cents −3.2..+10.2, conf 0.97–1.00). **No drops** — first family
  with a fully clean bake (the SF3 originalPitch is trustworthy, unlike the autocorr-on-raw-WAV low-note traps).
- **sampler.rs:** `celesta()` + `celesta_bank()` (8 zones F#3–C7). **voices.rs:** GM8 wraps the model in
  `LaVoice::wrap` under `if samples` (byte-identical `--no-samples`); `LA_CELESTA` + registry. **altbank.rs:**
  `8 | 11 | 32 => voices::make(.., false)`. **-musescore lib.rs:** 8 celesta entries + doc header updated.
- **Oracle:** appended `(8, 72, 1)` to `altbank_sampled_programs_preserve_pure_model_and_default_layers`.

## Validation (green)
- `cargo build -p ferrosintesis` clean; `cargo test -p ferrosintesis -p ferrosintesis-samples-musescore` —
  **568 + oracle pass, 0 failed**; musescore parity green; `cargo fmt --check` + `clippy -D warnings` clean.

## Remaining for U4 close-out
- **Render-diff --program 8** as the pre-integration check (baseline = branch fork point).
- **No bump.** MM-BUG-KILN-00015 batch 2 now has vibraphone(11)+tubular(14)+celesta(8); still needs **music box(10)
  + dulcimer(15)** (the Freesound-gated U6/U8) before the bug can flip Fixed.
