# 2026.07.19 — JRN — Commuted excitation round 3

Task: third attempt at commuted synthesis for the acoustic guitars (GM24/25).
Round 1 (0.21.36 block) parked the prototype; round 2 (0.21.38 block) fixed the
(1−g) normalizer with an operator fold, passed key 52, then FAILED the expanded
gate population at zone E2/key 40 (seam −10.4 dB, profile 10.3 vs 6.1) — the
fold's block count K scales with f0, so the seed drifts per zone in level AND
spectrum; re-parked on `park/20260719-commuted-excitation-r2` (0daabd8).

Round 3 mandate (Arthur: "Let's go do round 3 and see where we get to!"):
reconstruct the r2 machinery on current trunk (0.21.38) and replace the
f0-scaling calibration with a measurement-grounded one. The two journaled
leads: (A) fixed-time-span fold (block count independent of f0), (B) per-seed
f0-parity normalization against the burst-path reference. **Gate thresholds
and population stay FROZEN** (every zone's nearest key + key 88; same numbers
as r2) — round 3 may change the *mechanism*, never the *judge*. Ship-or-repark;
Arthur's audition is the final gate on ship.

Worktree: `D:\worktrees\ferrosintesis\20260719-DEV-HUM-commuted-excitation-round-3`
(branch `task/20260719-DEV-HUM-commuted-excitation-round-3`, off origin/main
@ 0.21.38).

Known transplant hazard, stated up front: trunk gained brass fixes in
voices.rs (body-EQ ring) after the park diff was cut — the r2 delta may not
apply clean; expect a semantic transplant, not a `git apply` (the r2 lesson).

## Log

- Worktree created (deltic start-task, DEV). Two UNDERSTAND scouts spawned:
  `r3-scout-park` (r2 delta inventory, gate-failure record, transplant
  feasibility vs the brass-touched trunk, zone ground truth) and
  `r3-scout-math` (numeric model of the fold's zone drift; feasibility of
  leads A and B against BOTH gates — seam level parity and profile spectral
  distance; recommendation with left-open failure modes).
- **scout-park report** (key facts):
  - **Transplant is CLEAN**: `git apply --check` of the r2 delta
    (32ac634..0daabd8, saved to `target/scout/r2-delta.patch`) exits 0
    against current trunk — every hunk lands at a +106 line offset. The
    brass fixes touch only the Brass region (~9274–10146 + its tests),
    fully disjoint from every r2 touch point (Pluck ~3570–4340, commuted
    block ~11008, make() arms ~11141, tests ~15633). All compile deps
    verified present on trunk (`OnePole::lowpass_mag` dsp.rs:271,
    `key_freq`, `testutil::band_rms`, sampler symbols). The feared
    semantic-transplant risk is RETIRED; round 3 = clean apply + redesign
    the calibration.
  - Neither enabling edit exists on trunk (Zone fields still private,
    prewarm hook absent) — the patch provides both.
  - **Frozen gate population** (nearest_key per zone + 88):
    nylon {40,47,52,58,64,70,76,88}, steel {40,46,52,58,64,71,77,83,88}.
    Failing zone E2 ≈ 82 Hz exists in BOTH banks. Bank = 44.1 kHz mono,
    single flat take per zone.
  - Gate-failure record confirmed: r2 passed gate 4 (restrike/determinism,
    k52) and failed at k40 only — seam −10.4 dB (±2 budget), profile 10.3
    vs burst 6.1 (must be strictly closer). K ≈ 10 blocks at E2 vs ~20 at
    k52.
  - Full machinery inventory captured (CommutedExc, operator-fold core,
    inject-port tail, retrigger jitter, body-EQ bypass, residual gate
    −12 dB with per-zone burst fallback, 4 tests + probe).
- **scout-math report** (the decisive one — model VALIDATED against the r2
  failure: predicts steel k40 seam −10.80 dB vs the measured −10.4):
  - Root cause refined: the drift lives in the NUMERATOR |Σ w^b·X_b| (the
    residual's per-period coherence), not the wsum denominator; it is
    TWO-SIDED — k40 under-divides (front-loaded residual ≈ 2-3 coherent
    blocks vs wsum≈9.5) and k88 degenerates to a spike-flat first period
    over-divided by wsum≈104 (round 1's failure reborn). Seam spread across
    zones: nylon 60.3 dB, steel 25.0 dB; tilt spread 37/28 dB.
  - **Lead A numerically REFUTED**: forcing K constant (6..12) leaves seam
    spread 57–70 dB — it equalizes the wrong term. Do not spend the round.
  - **Lead B exactly useless for gate 2**: the profile metric normalizes by
    the f0 magnitude → scale-invariant → a scalar moves it 0.00 dB. And at
    k40, f0=82 Hz sits below all three seam bands, so B's seam fix misses
    the worst zone too.
  - Recommended mechanism (adopted into HLD v1): (1) per-harmonic
    recording-matched seed seed_k = rec_k·(1−w_k) against the loop's known
    steady-state gain (all w_k<1 verified); (2) loop-OUTPUT seam-band
    calibration per zone (the restrike peak-norm lesson: measured/output-
    pinned normalization is the only part of r2 that passed everywhere);
    (3) coverage fallback — nylon 88 is +11.94 st above its top zone root,
    a missing-DATA problem no seed construction fixes.
  - Scripts preserved: target/scout-math/{fold_model,fold_leads,loop_gain}.py.
- **HLD v1 written**: "wrk_docs/2026.07.19 - HLD - commuted excitation round
  3 (recording-matched seed).md". Mechanism per scout-math; judges frozen;
  two preregistered honesty amendments — A1 (fallback rows ASSERT the
  fallback engaged, never silently skip) and A2 (gate 3 named as
  quasi-tautological once part 2 calibrates toward it; independent judges =
  gate 2, gate 4, residual gate, audition). Honest renaming (D8):
  "recording-matched excitation + commuted residual tail".
- Adversarial review spawned, 3 lenses: b3-rev-dsp (physics/math of the
  1/(1−w_k) construction, tail-pollution risk in gate windows, detuning),
  b3-rev-integ (implementability, prewarm ordering, determinism, A1 test
  shape, +12 st numbers), b3-rev-scope (worth-it/null-hypothesis, is the
  fallback gate-shopping — argue both sides, cost, magic numbers).
- **Adversarial review returned — 18 findings, the criticals forced a
  redesign (HLD v2).** The big ones:
  - **D-F2 (CRITICAL)**: the kept r2 tail is CONTINUOUSLY injected → IT
    gets the 1/(1−w) drive gain (not the seed), persists into both gate
    windows, tilts the spectrum up, one-sided vs the burst twin — the r2
    failure mode rebuilt under a "by construction" banner. Numerics:
    seed-only profile distance 3.37 dB → 12.43 dB with tail (nylon 52).
  - **D-F1 (MAJOR)**: 1/(1−w_k) is the CONTINUOUS-drive gain; the seed is
    an initial condition that decays as w_k^(t·f0). v1's (1−w_k) factor was
    a coincidental pre-emphasis. Correct law: decay compensation toward
    the gate window (validated: 0.10–0.59 dB seed-only distances).
  - **S-F1 + I-M1 (CRITICAL, independent double-find)**: nylon 88 =
    +11.94 st → the "+12 st" fallback NEVER fires for its one target case;
    valid separating window (5.15, 11.94). Moving the constant to straddle
    the failing row = gate-shopping (scope's charge upheld).
  - I-M2/I-M3: calib needs two-phase 'static derivation on the FULL model
    with direct Pluck constructors (make() → OnceLock reentrancy deadlock).
  - D-F4: calibrate to the exact gate twin (seed 5), not a multi-seed mean
    (which leaves ±1.5 of the ±2 dB budget on the table).
  - D-F3: harmonic die-off — above ~E3 most gated harmonics don't survive
    to the gate-2 window; steel 88 is a ~1-harmonic race; coverage should
    key off LOOP sustain, not take coverage.
  - S-F2: gate 2 is as construct-adjacent as gate 3 — ship evidence is
    gate 4 + render-diff + audition. S-F3: prototype the densest register
    (steel 65–76, 14,748 album notes) first, early stems, preregistered
    kill condition. S-F5: per-note additive cost must be phase-recurrence.
  - Album ground truth (scope): nylon never below key 47 in any committed
    album; steel densest 65–76, real content 38–46 and above 88.
  - Non-findings (claims verified fine): detuning common-mode ≤0.4 dB;
    phases gate-invariant; sealed API untouched; transplant clean.
- **My arbitrations** (recorded in HLD v2 §8): tail CUT with the whole
  residual path (D4 reversed — retires inverse comb + residual gate;
  revisit trigger = audition wants restrike body); seed law = relative IC
  decay compensation rec_k·(w_k/w_1)^(−t_c·f0) capped +18 dB; coverage =
  loop-sustain criterion (≥2 of h1..6 with t60_k ≥ 0.35 s) with the map
  computed and FROZEN before the first gate run; S-F2's proposed
  decay-rate oracle REJECTED as vacuous (both paths ring the same loop —
  identical decay rates by physics; amplitudes are the only DOF); D-F4
  adopted with a seed-5-vs-mean deviation diagnostic so an outlier twin
  stays visible.
- **HLD v2 written** (same file, Status: DRAFT v2). Targeted re-review
  (one pass, all three warm reviewers) next; then external Codex pass.
- **Targeted re-review returned: all three lenses verdict FOLDED, zero
  blockers.** Deltas folded into v2.1:
  - dsp: relative seed law proven equivalent to the validated form (rel-f0
    difference 0.000000 dB — the w_1 factor is k-independent and cancels);
    tail critical fully retired (seed is a pure IC, no external continuous
    input); provisional engagement map = ALL gate keys engage (nylon
    survivor counts 6/6/6/6/3/2/2/2, steel 6/6/6/6/5/3/3/3/3); asks: a
    synthetic high-key disengage test (steel ~100) so the fallback branch
    isn't dead-reckoned, and early stems aimed at the 30–150 ms early-body
    transition (the model onset is transient-free until LA hands over).
  - integ: I-M2b PARTIAL → fixed by threading `calib` as an explicit
    constructor parameter (phase 2 passes 1.0; make() reads the static and
    passes through; new_impl never reads a half-built static). New MINOR:
    three "kept" items shed residual semantics (`.filter(ce.ok)` → the
    engagement check; gate-2's `assert!(ce.ok)` removed; probe repurposed
    to the AC1 diagnostics). Watch-item: integ's note-t60-law reasoning
    suggests nylon 88 may DISENGAGE in real code, contradicting dsp's
    provisional model — resolved by protocol: the in-code probe is
    authoritative, nylon 88 is an explicitly-checked line, and if it
    engages-and-fails that is a repark (no fallback rescue exists).
  - scope: freeze protocol ruled adequate (0.35 s is pinned BY the frozen
    gate window; raising "≥2" visibly weakens coverage so it can't quietly
    dodge a fail — plus a sensitivity disclosure at freeze time); D13
    rejection ACCEPTED and strengthened (decay rates are loop properties;
    in the gate window the note is below any tanh knee, so both paths'
    decay converges — no cheap independent sustain oracle exists; audition
    really is the sole quality judge); residual-gate retirement ruled
    HONEST, not a freeze violation (it lowers no surviving mechanism's
    bar), conditioned on Arthur's explicit assent at ship + code review
    confirming nothing still reads a residual; worth-it re-verdict on the
    smaller design: YES, now proportionate ("the simplest lever that can
    move a linear loop's sustain profile — its initial condition"). New
    MINOR S-F7: kept symbols must rename (`new_matched`, `recmatch_for`) —
    shipped names must not assert "commuted".
- **HLD v2.1 stamped** (D15–D17 added). Next: external Codex pass, then
  SELF-APPROVED and implementation.
- **External Codex pass BLOCKED**: `codex exec` (gpt-5.6-sol, xhigh,
  read-only, the block-two recipe) failed instantly — OpenAI usage limit
  exhausted until Jul 25. Not retryable this round. Substitute: one
  COLD-CONTEXT review by a different Anthropic model (Opus, no session
  context, same prompt file `target/codex/REVIEW_PROMPT.md`) — partial
  decorrelation (different model, same family), honestly labeled as a
  substitute, surfaced to Arthur in the checkpoint report. Worktree
  baseline verified green meanwhile (`cargo check -p ferrosintesis`
  clean at 0.21.38).
- Opus substitute ALSO limit-blocked (session limit, resets 11:30 London);
  second substitute = cold-context Sonnet. **It delivered — 5 findings,
  including the round's best catch:**
  - **C1 (CRITICAL, lead-verified)**: "past-cap harmonics decay to the
    floor regardless" was asserted, never computed — and it's FALSE for
    mid rows: nylon 52 k6 needs ≈+22 dB (lead's independent computation;
    C1 said +23.1) against the +18 cap while t60_6 ≈ 0.95 s (fully
    "engaged"). Surviving ≠ shapeable-within-cap. Also the v2.1 spec was
    ambiguous (clamp vs disable — up to 18 dB apart). Fix folded: CLAMP
    semantics (saturating, continuous — also retires dsp's binary-flip
    micro-note); needed-comp-vs-cap table at k2..6 + gate-3 band
    positions joins the freeze record with "engaged-but-past-cap" a named
    category.
  - C2 (MAJOR): D14's "engineering picks" left t_c/+18/8kHz OUTSIDE the
    freeze — a reopened S-F1 hole. Fix: all five constants freeze
    together, pre-gate-run.
  - C3 (MAJOR): restrike peak-norm discards calib → spawn/restrike level
    laws diverge, gate 4 blind to it. ARBITRATED: keep the peak-norm (it
    is the SHIPPED restrike law for burst too — consistency with the
    twinned behavior; redesigning shipped restrike semantics is out of
    scope), with a spawn-vs-restrike RMS diagnostic in the freeze record
    + a restrike passage in the stems.
  - C4 (MAJOR): residual-gate retirement left ZERO checks that touch the
    recording itself. Fix: per-zone profile sanity test (finite, rec_1
    floor).
  - C5 (MINOR): KsLoop's ceil(target)+1 fill wraps r2's rounded-period
    exc mid-period — inaudible for noise, not for phase-exact
    resynthesis. Fix: synthesize continuously over the actual fill
    length; seam retired.
  - SOUND verdicts: IC-decay physics confirmed against KsLoop code
    (guitars provably drive-free — sustain 0.0 never overridden);
    freeze-protocol mechanics fine apart from C2.
- **HLD v2.2 stamped SELF-APPROVED** (D18–D23; explicitly NOT
  SIGNED-OFF — Arthur's gates: early-stems kill, AC4 audition, assent to
  the residual-gate retirement). Proceeding to implementation.

## Implementation log

- **Machinery landed** (compiles clean first try): sampler.rs enabling
  edits; `RecMatchProfile` + complex-Goertzel profile measurement with the
  C4 sanity flag; `recmatch_seed` (continuous fill-length synthesis per
  C5, phase-recurrence rotation, relative IC decay compensation with the
  D18 saturating clamp, restrike phase/amp jitter); `recmatch_ref_params`
  + `recmatch_engaged` (§2.3, lockstep-commented against new_impl);
  two-phase calib (`recmatch_calib`, direct constructors, calib as an
  explicit `new_matched` parameter per I-M2b); four statics;
  `recmatch_for` (engagement → profiles → calibs → zone pick → sanity);
  `recmatch_prewarm`; Pluck `matched` field + constructor split + body-EQ
  bypass + make() arms + restrike fork (shipped peak-norm law kept, D20).
- Probe iteration 1: four zones' calib pinned at my 0.05 sanity-clamp
  floor — the TRUE calibration sits below it (the compensated seed rings
  the seam bands ≥26 dB hotter than the burst twin). Widened the
  implementation guard to [0.005, 100] (NOT one of the five frozen
  constants; changed pre-freeze, noted here honestly).

### FROZEN MAP RECORD (recmatch_freeze_probe, 2026.07.19, pre-gate-run)

The five mechanism constants are HEREBY FROZEN (D19): survivors floor
0.35 s, min survivors ≥2, t_c = 0.475 s, comp cap +18 dB, harmonic cap
8 kHz. After this point: any gate red ⇒ repark.

- **Engagement map: EVERY frozen gate key ENGAGES, both banks** (in-code
  probe agrees with the dsp lens's provisional model; integ's nylon-88-
  disengages hypothesis is refuted by the authoritative computation).
  Survivor counts (h1..6 with t60_k ≥ 0.35 s):
  nylon 40/47/52/58 = 6, 64 = 3, 70/76/88 = 2;
  steel 40/46/52/58 = 6, 64 = 5, 71/77/83 = 3, 88 = 3.
- **First disengaged key above the population: nylon 93, steel 94** —
  the A1 synthetic fallback assertions target these (real album content
  above key 93 exists for steel, so the disengage branch is live shipped
  behavior).
- **Sensitivity disclosure**: floor 0.35→0.40 s flips ONLY nylon 76
  (2→1 survivors → would disengage); min ≥2→≥3 would disengage nylon
  70/76/88 (all at 2). Steel rows are insensitive to both perturbations.
- **Needed-compensation vs the +18 dB cap** (external C1's table,
  authoritative): cap binds from nylon k52 h6 (+23.1), k58 h4+, k64 h3+,
  k70/76/88 h2+; steel k58 h5+, k64 h4+, k71/77/88 h2+, k83 h3+.
  "Engaged-but-past-cap" rows are therefore MOST of the upper population —
  the gates now judge whether clamped compensation suffices there.
- **Calib per zone** (dB): nylon −31.8/−14.3/−14.6/−9.7/−14.6/−10.9/−7.7;
  steel −23.0/−12.8/−32.2/−18.1/−17.5/−26.2/−45.0/−15.3. Wildly
  zone-dependent — the r2 lesson (no scalar fits all zones) confirmed by
  measurement; steel 77's −45 dB flags a very hot compensated seed there
  (watch gate 2 and the stems at that zone).
- **Seed-5 twin vs multi-seed burst mean**: deviations up to +4.8 dB
  (nylon 76) and +4.3 dB (nylon 70) — the gate twin is NOT always typical;
  calibrating to the exact twin (D-F4) was the right call and the
  deviation is now on the record.
- **Spawn-vs-restrike level diagnostic** (nylon 52): matched −0.19 dB
  (restrike ≈ spawn — internally consistent); burst +5.45 dB (the shipped
  peak-norm law makes burst restrikes hotter than their spawn). The
  matched path is the MORE consistent of the two; C3's concern recorded,
  no change to the shipped law (D20).

### First gate run + diagnosis (2026.07.19)

- **Gate 4 PASS. Profile sanity PASS. Frozen map (incl. synthetic
  disengage at nylon 93 / steel 94) PASS.**
- **Gate 2: matched wins 12/17 rows, loses 5** (nylon 88; steel
  52/64/83/88 — margins −1.0 to −3.8 dB). The wins at the bottom are
  emphatic: nylon 40 = 1.27 vs 6.10 (THE r2 killer row, fixed); steel 58 =
  7.9 vs 28.3. **Gate 3: 9/17 rows out of ±2 dB** — a consistent tilt
  (matched hot at 300 Hz, cold at 3000) plus a wild steel-77 outlier.
- **Localization** (probe, encoded → bare → wrapped at steel 52 / nylon 40
  / steel 77): the seed law + loop physics are NEARLY PERFECT bare (steel
  52: encoded +3.9/+11.7/+0.4/−12.8/−15.0 → bare
  +3.8/+11.2/+0.7/−11.8/−12.1) — **the collapse happens inside the LA
  wrap**, increasing with harmonic number (h6 −12.8 dB).
- **Root cause, quantified**: `wrap_var` detunes the wrapped MODEL by the
  per-note intonation scatter (`la.sustain.set_pitch(var_mult)`,
  sampler.rs:2392; GUITAR_VAR = ±5 c strata ± 1 c white; seed 5 draws
  −5 c). Gate 2's frozen estimator measures at EXACT k·f over a 0.25 s
  window: predicted sinc under-read at −5 c = −0.8/−1.9/−3.6/−5.7/−9.0 dB
  at h2..6 — matches the measured wrapped-minus-bare
  −1.2/−3.8/−7.7/−11.7/−12.8 in shape and scale. Both paths suffer it
  (same seed → same var_mult), so the artifact is common-mode in the
  COMPARISON but masks the true difference — at the top half the row
  outcome is essentially noise. r2 passed key 52 under the same artifact
  because its fold seed was noise-like to begin with.
- **Steel-77-class rows** (bare already collapsed above h2): the
  engaged-but-past-cap category doing exactly what the freeze record
  predicted — the loop cannot hold those harmonics; matched still WINS
  gate 2 there (38.9 vs 61.9) because burst is even further away.
- **Gate-pair structural tension** (design-level discovery): gate 3
  demands per-band ±2 dB parity with the BURST twin at 0.25–0.35 s; gate
  2 demands the spectrum move TOWARD THE RECORDING at 0.35–0.60 s. The
  windows are adjacent and the loop's spectrum cannot change materially
  between them — so wherever the recording's sustain spectrum differs
  from the burst's by ≫2 dB in a band (i.e., wherever the mechanism
  SUCCEEDS at its purpose), the frozen pair encodes "change the timbre
  without changing the timbre." The 9 seam reds are dominated by this,
  not by level error (calib holds the mean at 0 by construction).
- **Options considered and refused**: encoding anti-sinc pre-emphasis
  into the seed (teaching to the test); un-detuning the matched model in
  the shipped arms (ships a feature regression AND biases the gate);
  per-zone λ-blend toward burst spectrum (fixes seam rows but cannot flip
  the 5 gate-2 losses — λ→0 is a tie, and the strict `<` needs a win
  through the masking noise); narrowing engagement to green rows
  (population-narrowing by outcome — S-F1's exact sin).
- **VERDICT: the preregistered repark trigger has fired** — the gates
  cannot all go green without touching a frozen judge or gaming the
  estimator. RE-PARK round 3, with the mechanism itself VALIDATED bare
  and the failure attributed to the judges' blind spots, not the physics.
  (Note: an earlier line in this journal over-compressed the HLD's
  trigger as "any red ⇒ repark"; the HLD's actual §3 trigger — cannot go
  green without touching frozen things — is the one applied, and it fired
  after sanctioned bring-up iteration was exhausted.)
- Round-4 lead (needs Arthur-signed re-preregistration, per the
  discipline): judges that measure the actual goal through the shipped
  path — profile Goertzels at the render's OWN per-note detune
  (k·f·var_mult — objective, not gamed), seam parity as total-energy or
  vs-the-RECORDING rather than per-band burst parity, thresholds set from
  this round's published tables. The stems (below) are the decision basis
  for whether round 4 is worth it.
