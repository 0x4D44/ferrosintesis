# JRN — natural pluck excitation redesign

Task: `task/20260719-TSK-HUM-natural-pluck-excitation-redesign`
Design: `wrk_docs/2026.07.19 - HLD - natural pluck excitation redesign.md` (APPROVED, Fable-designed).
Execution: self-paced `/loop`, per-phase A/B check-in with Arthur.

Complaint: plucked voices sound FIERCE ("digging into the strings"), sustain too quiet vs the pluck.
Diagnosis (measured): attack-to-sustain ENERGY imbalance (bare model 2.8–5.1 vs FluidR3 ~1.3), not
spectral harshness. Fix = attack-side taming (excitation redistribution + click disposal + sustain-band
normalization). Two-pronged: voices.rs excitation + sampler.rs LA layer (Tripwire 8, Phase 4).

Baseline: branch cut from `origin/main` 7acbbb0 (crate at v0.21.40 on trunk — I do NOT bump; integration owns it).

## Phase 0 — test tooling (test-only, no bump)

### Unit 0a — metric primitives + Oracle-0 calibration  ✅ (commit pending)
Added to `crates/ferrosintesis/src/testutil.rs` (all `#[cfg(test)]`, zero shipped-binary impact):
- `fft_inplace` — hand-rolled iterative radix-2 Cooley–Tukey FFT (f64 twiddles, bit-reversal).
- `fft_mag_hann(seg, n)` — Hann-windowed magnitude spectrum, normalised so an on-bin unit sine ≈ 1.0.
- `spectral_tilt_db_oct(seg, sr, lo, hi)` — least-squares dB/oct slope over log-spaced FFT probes.
- `crest(seg)` — peak/RMS (HLD G2).
- `att_sus_ratio(seg, sr, f0)` — the PRIMARY metric: RMS[0,max(15ms,1.5/f0)] ÷ RMS[100,250ms].
- `inter_harmonic_floor_db(seg, sr, f0)` — median inter-harmonic vs harmonic-peak magnitude, dB (G6).
- `max_slew_norm(seg)` (G5), `dc_offset(seg)` (G10).

Oracle-0 calibration tests (all green): FFT recovers a tone (peak bin + ≥40 dB octave leakage); tilt
orders white≈0 / one-pole≈−6 / two-pole≈−12 dB/oct (mean over 8 noise seeds to tame single-window
variance); crest √2 vs impulse ≫; att/sus reads 2.0 for a 2× front-loaded tone, 1.0 flat; inter-harmonic
floor deeply negative for a pure harmonic tone, rises with noise; slew ≈ 2πf/sr, DC recovered exactly.

Gate: `cargo test -p ferrosintesis calibration::` 20/20 green; `cargo fmt --check` clean; `clippy
-p ferrosintesis --all-targets -D warnings` clean.

### Unit 0b — HEAD baseline capture  ✅ (commit 03ec5fb)
`pluck_baseline` module in testutil.rs. A `#[ignore]` printer renders the BARE model
(samples off) for the 14 §2.6 presets over {keys 40,52,64}×{vels 50,100,120}×{seeds 3,7,11}
via shipped make/make_variation; `HEAD_BASELINE` freezes it (rms_db = G7 anchor; att_sus/tilt =
the "before"). `head_baseline_documents_the_fierceness_gap` validates + pins the gap.

Captured HEAD (confirms HLD §0): att/sus pervasively 1.5–6×, EXPLODES at high keys (string
decays before the sustain window) — PIZZ key64 up to 41×, OUD 12–19×, JAZZ/CLEAN 10–13×. STEEL
flattest onset tilt (−1.6 dB/oct = click-dominated). Seed spread up to 8–9 dB at key 52 (the
Rayleigh-h1 luck the deterministic backbone must collapse to ≤1.0 dB for G7).

### Unit 0c — FluidR3 differential goldens  ✅ (commit — this unit)
`tools/pluck-attack/gen_fluidr3_golden.py` (+ README, fluidsynth 2.5.5 / SF SHA 74594e8f…) renders
single notes dry (reverb+chorus off) and prints a paste-ready const; `FLUIDR3_GOLDEN` frozen in
testutil, hermetic (no fluidsynth at test time). `.gitignore` allowlisted `!tools/pluck-attack/`.
Chose a Rust const over JSON (zero-dep repo — no serde). D-oracle lands Phase 2.

FluidR3 att/sus far gentler: steel 0.97–1.54 (ours 2.7–8.1), nylon 1.15–2.26 (ours 1.7–8.2),
**pizz 0.48–0.89** (ours 4.35–41). Pizz BELOW 1.0 = FluidR3 loop-flattening below the physical
ring-down → validates anchoring the P-band on physics (R_phys), NOT on chasing FluidR3.

**Phase 0 COMPLETE** — all test-only, no version bump. Next: Phase 1 (shipped, existing knobs).

## Phase 1 — pick-click reduction (shipped, low risk)  — IN PROGRESS

Scope narrowed after reading the presets + KICK_LP_HZ=55 (a sub-bass thump, NOT a guitar
body-knock). The proper bandpass body-knock and pick_lp/pos moves need code/excitation work →
DEFERRED to Phase 2 (where the Shaped builder reworks the excitation anyway). Phase 1 = pure
CLICK REDUCTION, the safest first audible step, directly targeting Arthur's ch14 pick-tick.

Click cuts (voices.rs), existing knob only:
- Picked (35-50% of current): STEEL 2.0→0.9, CLEAN 1.8→0.8, JAZZ 1.2→0.55, DRIVE 2.2→1.0
  (prog29 = ch14 lead), DRIVE_LEAD 1.3→0.6 (tracks DRIVE, keeps lead<main), PICK 1.6→0.72,
  DULCIMER 1.7→0.77.
- Finger: NYLON 0.9→0.3 (UKULELE inherits), OUD 0.8→0.3.
- Untouched: BASS/FRETLESS/UPRIGHT/HARP (click=0, excitation-driven → Phase 2); PIZZ (click 0.6,
  ratio is decay-dominated/physical → Phase 2); MUTED/HARMONIC (not §2.6); SLAP/SLAP_POP/SITAR/
  BANJO/HARPSICHORD/CLAVINET (Legacy-exempt).

KEY FINDING — att/sus is EXCITATION-dominated; the click barely moves it (CLEAN: <0.1% att/sus
change from a 1.8→0.8 cut, sign-unstable). The click's effect lives in the ONSET HF band (the
pick "tick"), where the click IS the source. So the Phase-1 metric is onset-HF, not att/sus —
which confirms the HLD: Phase 1 gentles the pick-tick, Phase 2's excitation redesign owns the
front-load proper.

Tests:
- NEW `phase1_click_cut_reduces_onset_hf` (voices.rs): strict per-key onset-HF reduction for the
  loud clicks (STEEL, DRIVE) + ≥10% aggregate reduction across the whole cut set. Green.
- RE-BASELINED `pick_click_is_audible` (HLD §7, announced): STEEL click-on/off onset-HF boost
  fell 1.3x→~1.09x at click 0.9; bar 1.3→1.05 (RED at no-click 1.0, GREEN at 1.09). Widens in
  Phase 2 as the excitation's own onset HF drops.
- Tripwire 7 CHECKED, does NOT fire: steel_string_has_pick_sparkle green at 0.0981 ≥ 0.085 (16%
  margin) — the sparkle rides the body EQ + pick_lp, not the click.

Committed d0cd7b8; full ferrosintesis lib suite 566 passed / 0 failed (the +1 vs Phase 0 is the
new guard; golden_mix_balance_holds + driven_* oracles all still green).

RENDER-DIFF (baseline _p1-baseline @184ca3c vs new, 124 albums, --rate 11025, touched GM
15/24/25/26/27/29/30/34/105): **CONTAMINATION 0** (Tripwire 6 clear — no pluck-free album moved),
**NOT REACHED 0**, 53 changed / 71 same. Driven guitar (GM29/30) dominates the changes — the
ch14-relevant DRIVE cut lands. Perfect scope: the change reached exactly the plucked albums.

A/B renders (durable in D:/worktrees/midi-music/, git-ignored):
- _ab-p1_incantations-iv-{OLD,NEW}.wav — the reference (ch14 driven lead, Arthur's complaint).
- _ab-p1_hollow-hill-1-{OLD,NEW}.wav — fable5, nylon+steel+clean+driven (fullest plucked palette).
Both baseline+new binaries include the XG SysEx (baseline @184ca3c is post-v0.21.37), so the A/B
isolates ONLY the Phase-1 click change.

STOP here (Phase 1 boundary) for Arthur's A/B verdict before structural Phase 2.

## Phase 1 A/B follow-up — driven-lead soft onset (Arthur: "much better")

Arthur judged the ch14 driven-lead solo "still quite plucky" after the click cut. Diagnosis: the
click is the MINOR lever; the driven pluckiness is the excitation front-load, which the amp-sim then
accentuates (loud pick attack clips hardest). Rendered a 3-way ch14 solo A/B; Arthur approved the
soft-onset take ("much better").

DRIVE (prog 29/30) change: `attack_s: 0.0 -> 0.015` (a ~15 ms soft onset eases the string front-load
into the amp-sim so it clips the transient far less — the lead sings in rather than snapping) +
`click 1.0 -> 0.7`. Kept above DRIVE_LEAD's 0.6 so the main still has a firmer pick than the alt lead.

Design consequence + resolution: softening the DEFAULT bank's attack collapsed the attack-swell gap
with the CC0 DRIVE_LEAD (a_main 20 ms vs a_lead 29 ms = 9 ms, < the oracle's 25 ms). DRIVE_LEAD
CANNOT swell further to restore it — its attack_s 0.03 is sized to complete inside the sustainer's
80 ms capture window (a longer swell deflates the held reference; documented in the preset). So:
- driven_main_and_alt_banks_diverge clause (2) RE-BASELINED 25 ms -> 5 ms (announced): the attack
  contrast is now the MINOR axis; clause (1) — the >=6 dB SUSTAIN gap (main decays, lead holds),
  UNCHANGED — carries the bank distinction. Bar 5 ms still RED if the main softens to swell as much
  as the lead.
- phase1_click_cut_reduces_onset_hf aggregate 10% -> 5% (announced): DRIVE's soft onset ramps its
  string front-load in, trimming the click's onset-HF share, so the aggregate click-reduction eased
  ~10% -> ~9%. 5% robustly GREEN, still RED if clicks restored.
All driven/attack oracles green (o_attack_struck_attack_dominates, driven_sustain_stays_distorted,
sustained_lead_stays_harmonic_through_drive). This globally softens the driven-guitar attack across
the 53 driven albums (a deliberate, Arthur-approved voice change).

## Load-bearing reminders (from the HLD)
- Attack-side only; never touch t60/bright/release (compositional + pinned).
- Per-preset `exc_model: Legacy | Shaped` opt-in; un-migrated presets BIT-IDENTICAL.
- Normalize on the SUSTAIN BAND (h1..h4 = K_SUS·v), NOT peak — the anti-re-leveling core; G7 (±1.0 dB).
- Render-diff every phase; baseline = the commit rebased ONTO; expected scope = plucked albums only
  (a pluck-free album diff is a BUG — Tripwire 6).
- Legacy-exempt: HARPSICHORD, CLAVINET, SLAP, SLAP_POP, SITAR, BANJO.
- STOP at the Phase 1 boundary for Arthur's A/B before structural Phase 2.

## Phase 2 — Shaped excitation (structural)  — IN PROGRESS

Scope = Arthur's ear-driven picks (NOT the HLD's finger-first): PIZZ, STEEL, JAZZ, PICK, DULCIMER.
Everything else stays Legacy/bit-identical. Fresh worktree off trunk ba57db9 (v0.21.44 integrated).

### Unit A — Shaped excitation builder + opt-in scaffold  ✅ (commit pending)
- New `enum ExcModel { Legacy, Shaped }` + 4 PluckPreset fields (exc_model, slope, noise_mix, exc_trim);
  DEFAULTS = Legacy/0 → every un-migrated preset BIT-IDENTICAL (existing oracles confirm: golden mix,
  velocity_preserves_tuning, harp_46_shimmers, pick_click_is_audible all green unchanged).
- `shaped_excitation()` (voices.rs, before impl Pluck): deterministic harmonic backbone
  w_k=|sin(kπβ)|/k^s·P(k·f0) (β=jittered pos, s=slope, P=existing pick_lp one-pole magnitude), φ₁=0 /
  φ_{k≥2}=±0.4 rad, amp jitter k≥2 only (h1 deterministic), energy-domain blend √(1−ρ)·bb+√ρ·noise,
  zero-meaned (G10), sustain-band normalized. `dft_bin_mag()` helper (exact harmonic bin).
- Pluck::new branches on p.exc_model (Legacy = historic peak-normalize, no extra rng draws → bit-id;
  Shaped = the builder). altbank.rs frozen PIZZ pinned to Legacy.

**§2.4/§4 RESOLUTION (load-bearing):** HLD §2.4 says "h1..h4 ENERGY = K_SUS·v", but §4 says "level
unchanged (v=vn^1.6)" and G7 enforces loudness parity across velocities. Energy∝v would make sustain
amplitude ∝√v — HALF the velocity dynamic range, breaking the level law + G7. So I normalize the
sustain-band AMPLITUDE to K_SUS·v (amplitude ∝ v, energy ∝ v²), preserving the level law. G7 will
verify. K_SUS=0.30 is a PLACEHOLDER — Unit C calibrates it against HEAD_BASELINE.

- `shaped_excitation_builder_mechanism` test: zero-mean, sustain-band amp = K_SUS·v, steeper slope =
  less HF, deterministic, Shaped preset renders + differs from Legacy. Shaped variant #[allow(dead_code)]
  for the scaffold (Unit B migration removes it). fmt + clippy -D warnings clean.

### Unit B — migrate the 5 presets to Shaped (initial s/ρ) + re-baseline entangled oracles — NEXT
### Unit C — calibrate K_SUS + per-preset s/ρ/exc_trim (parallel Workflow) + P/D/G oracles — TODO
### Unit D — render-diff (only GM 15/25/26/34/45) + corrected showcase A/B — TODO

### Unit B/C — migration + K_SUS calibration (Tripwire 1 → Fable → resolved)  — 4/5 DONE
Migrated STEEL/JAZZ/DULCIMER/PIZZ to Shaped; PICK DEFERRED to Legacy.

TRIPWIRE 1 hit: global K_SUS + ≤±1.5 dB trims can't hold G7 (5 presets span 5.4 dB in Legacy
sustain-per-excitation-h1h4). Arthur chose "loop in Fable". Fable verdict (verified, HLD amendment
2026.07.20): per-preset trims are UNAVOIDABLE (G7's target is per-preset frozen historical data; the
trim IS that measured transfer constant); amplitude ∝ v confirmed (STEEL 12.23 dB velocity span);
**the §2.4 peak guard is the PICK bug — DELETED** (it rescales the calibrated level crest-dependently
and makes exc_trim a no-op when engaged); level = contract (exact normalization), crest = envelope
(G2/G5 oracles), phase dispersion for k>k_c if crest runs high. Guard deleted.

Result: K_SUS=0.15 + per-preset exc_trim → EXACT 0.00 dB loudness parity for all 4 migrated presets
across every key×vel (STEEL −1.51, JAZZ +1.95, DULCIMER −1.39, PIZZ +3.16). Deterministic → seed
spread collapses. `print_shaped_loudness_offset` measures it.

PICK DEFERRED: after guard deletion its offset is KEY-dependent (+5.3 dB low → +0.8 dB high,
velocity-flat) — a single scalar trim can't hold G7 across keys. Beyond the per-preset-scalar model
(dark/wound bass: excitation→sustain gain varies with pitch). Reverted to Legacy (bit-identical);
needs a key-aware target (follow-up / possible Fable re-consult).

### REMAINING (next iterations): land Fable's oracle set (G7 per-cell + velocity-flatness span +
Legacy canary; builder exactness + v-linearity; trim lint) + re-baseline entangled oracles
(steel_string_has_pick_sparkle 0.098→0.076 spectral drop, golden ch1 STEEL) + G2/G5 check + P/D/G
suite + render-diff (GM 25/26/15/45 only) + corrected showcase A/B. Then PICK's key-aware fix.

### Unit C cont. — G7 oracle landed (7405b11)  + KEY FINDING
Fable's G7 oracle landed, calibrated to measured reality (shaped_g7_mean_parity_and_seed_bound).
KEY FINDING (walks back the earlier "exact 0.00 parity" claim — that was the MEAN): the
excitation→sustain loop gain is PITCH- AND VELOCITY-dependent, so the per-preset SCALAR exc_trim
holds the per-preset MEAN (±0.5 dB, the tight anti-re-leveling guarantee) but leaves ~±2.5 dB
DETERMINISTIC per-note variation. This is a strict improvement over Legacy's ±9 dB RANDOM seed chaos
(same note now always same loudness) but NOT perfectly flat. Fable's velocity-flatness clause assumed
a pure dB shift (wrong) → dropped; the builder h-band-exactness test is the real limiter-creep guard.
Oracle bars: mean ≤0.5, seed spread ≤2.5, per-cell ≤max(2.5, legacy_spread), NYLON canary ±0.05.
FOLLOW-UP: a key/velocity-aware target would flatten the ±2.5 AND unblock PICK. Surface the ±2.5 to
Arthur at the A/B (it's a loudness characteristic he should know, though inaudible-ish + better than
Legacy). REMAINING: builder-mechanism guard-cell + v-linearity; trim lint; re-baseline steel sparkle +
golden ch1; G2/G5; P/D; full suite + render-diff; corrected showcase A/B.

### Unit C cont. — full-suite triage (7 failures, STEEL-concentrated); Arthur: "push through STEEL"
JAZZ/DULCIMER/PIZZ clean. Failures:
- steel_string_has_pick_sparkle: 0.0757 < 0.085 → re-baseline bar (gentler attack, HLD §7). ratio 2.34.
- golden_mix_balance_holds: ch1 STEEL rms/centroid → re-capture ch1 ONLY (canaries ch7/ch8 must stay bit-exact).
- dulcimer_level_vs_harp: GM15 +2.48 dB vs harp (the ±2.5 per-note variation) → re-baseline/verify.
- velocity_opens_pluck_timbre: STEEL 1.30 (bar >1.4; ABOVE Tripwire-3 floor 1.25) → re-baseline (gentler opens less).
- wound_strings_darker: wound 641 vs plain 692 Hz → INVESTIGATE (still darker? margin?) — real interaction, not just re-baseline.
- guitar_release_thump_speaks_low: STEEL diff 0.0020 vs ring 0.0088 = 0.23 (bar 0.25) → marginal, key-variation.
- la_level_continuity: STEEL LA crossfade seam steps 2.57x (~8 dB) — the Shaped model's fade-window level moved. REAL
  FIX (recalibrate the steel LA gain/fade so sample↔model is continuous; HLD §7 anticipated >0.5 dB → recalibrate).
Decision: push through STEEL (LA-seam fix + announced re-baselines), then A/B all 4. STEEL is the LA-wrapped/wound/
reference-song preset — most entangled (like PICK). Handed to a fresh loop iteration.

### Unit C cont. — STEEL push-through: LA-seam fix + 5 announced re-baselines all green; G2 crest finding (2026.07.20)
Arthur chose "push through STEEL". All 7 STEEL-concentrated failures resolved; full ferrosintesis
suite 571 green (the only red is the NEW G2 crest oracle, deliberately #[ignore]'d — see below).

REAL FIX — la_level_continuity (LA crossfade seam). STEEL (GM25) is LA-wrapped; going Shaped dropped
the model's RMS in the crossfade window (0.05-0.28 s) ~6.5 dB (0.098->0.046 at key 52), so the
unchanged 0.42 sample transient overshot 2.57x. Nylon (24) shares LA_GUITAR and is Legacy (bit-exact),
so added a steel-specific LA_STEEL=(0.20,(0.05,0.28)) — gain tracks the drop (0.42·10^(-6.5/20)≈0.20).
Seam continuous; nylon/harp/harpsichord bit-exact (non_guitar_la_render_is_pinned + guitar seam tests
pass). HLD §7 anticipated this (recalibrate if the fade-window model level moves >0.5 dB; it moved 6.5).

5 announced re-baselines (all BARE model — render_program uses samples=false, so LA_STEEL doesn't touch
them; pure Shaped consequences):
- steel_string_has_pick_sparkle 0.085->0.065 (gentler onset, 3-5k sparkle 0.098->0.0757; ratio 2.34 kept).
- velocity_opens_pluck_timbre STEEL >1.4->>1.25 (the Tripwire-3 FLOOR, not arbitrary; 1.76->1.30, above floor).
- wound_strings_darker 0.9->0.95. INVESTIGATED: wound law intact (scales bright+pick_lp, both feed the
  Shaped backbone P(f)); gentler attack carries less HF for BOTH wound+plain, so at key 57 (weak
  wound_factor) the gap compresses 10%->7.5% (641 vs 693 Hz). Direction robustly preserved.
- dulcimer_level_vs_harp ±2->±3 dB (+2.48 is the ±2.5 per-note DETERMINISTIC variation; mean HEAD-matched via G7).
- guitar_release_thump_speaks_low STEEL 0.25->0.20 (NYLON stays 0.25). Probe: Shaped ring +7.6 dB louder
  (slope-1.4 low-harmonic concentration decays slower), thump differential LOWER (0.0033->0.0020, rides a
  louder signal into the output nonlinearity) but PRESENT + LOW-BAND (hf 0.081). Quieter RELATIVE to a louder ring.

golden_mix_balance_holds — recaptured ch1(STEEL)+ch2(JAZZ)+master only. Reference song has BOTH migrated
presets. ch1 2030->803 Hz, ch2 855->540 Hz. Verified NO contamination by capturing the FULL golden at the
branch base (963def2): ch7 piano bit-exact everywhere; ch8 strings base==HEAD (2139) — old row (2381) was
stale drift already present at base; ch0 nylon (819) + ch4 drive (846) likewise base==HEAD → pre-existing
patchwork drift, independent of this branch (Legacy Pluck path is RNG-identical; solo drops other channels'
notes so non-pluck ch8 is independent). Left the 3 stale rows untouched (still in ±20% tol) — scratchpad'd.

FINDING — G2 crest FAILS for STEEL + DULCIMER (added shaped_g2_g5_envelope_bounds + print_shaped_envelope).
Attack-window crest(v120) (same window as gen_fluidr3_golden.py): STEEL 6.27, DULCIMER 3.90 (bar 3.3), and
crucially SPIKIER than their own Legacy twins (STEEL 3.43, DULCIMER 3.04). PIZZ fails G5 (slew 1.43× Legacy).
JAZZ/PIZZ crest pass. ROOT CAUSE: the backbone's φ₁=0 phase coherence (every harmonic aligned at the pick
instant — the physical-displacement snapshot) is amplified by the loop+body into a sharp rendered peak that
Legacy's random-phase noise never had. att/sus DROPPED for all four (energy gentled) but the PEAK sharpened —
a real regression on the redesign's own goal. This is the HLD's anticipated tripwire C; the prescribed lever
is deterministic phase dispersion for k>k_c≈8 in shaped_excitation's backbone (magnitude-invariant, so every
re-baselined MAGNITUDE oracle above is unaffected — safe to commit this checkpoint first). G2 oracle #[ignore]'d
pending that fix. NEXT: implement phase dispersion → re-enable G2/G5 → re-run G7+builder → render-diff → P/D → A/B.
