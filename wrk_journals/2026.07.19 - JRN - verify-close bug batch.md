# 2026.07.19 — JRN — Verify & close the Fixed-awaiting-closure bug batch

**Mode:** verify-only (two-eyes closure), docs-lane. No shipped code changed.

## Task

Drain the Fixed-awaiting-closure queue in `bugs/` with an independent two-eyes
review, closing only bugs fixed by a *different* actor than me (Claude Opus
4.8 (1M context)).

## Two-eyes attribution

Ten bugs were in state `Fixed`. I read each fix commit's `Co-Authored-By` trailer:

- Fixed by **Claude Opus 4.8** (my own actor) → **SKIPPED** (cannot two-eyes my own
  fix): MM-BUG-KILN-00008 (d03e33d), MM-BUG-KILN-00022 (055a849).
- Fixed by **Codex GPT-5 / gpt-5@xhigh** (a different actor) → **ELIGIBLE** (8):
  00002 (b88d381), 00012 (ab602c4), 00014 (83be8e8), 00018 (5b5d80a+6220e71),
  00020 (c58e791→18664a3), 00023 (2d8f0b3), 00025 (a0d4995→00a5d94), 00026 (c9634f5).

(00020 and 00025 landed on `origin/main` under rebased SHAs — same subjects, content
present on trunk.)

## Method

Worktree off `origin/main` (0cc8e7f), which contains all eight fixes. For each bug I
(a) read the named regression test to confirm it is a genuine *directional* oracle
asserting the corrected behaviour (not a vacuous check), (b) re-ran the original
recorded observation and/or the regression test on the fixed build, and (c) ran the
repo gates.

**Gates (fixed build), all green:** `cargo fmt --check`; `cargo clippy --workspace
--all-targets -- -D warnings`; `cargo test --workspace` = **548 passed / 0 failed**
(15 ignored); release build of `ferrosintesis-cli`.

## Per-bug verdicts — all CONFIRMED → Closed

- **00002** (showcase audio oracles): `build.py --verify` 37/37, `test_analyze.py`
  4/4, and `analyze.py` PASS on all five *release* renders — the four original
  analyze.py failures (01 wah-bite, 04 vowel-drop, 05 dynamic-arc, 05 shanai-pressure)
  are gone.
- **00012** (contrabass wolf-band): `bowed_string_wolf_band_holds_fundamental` green;
  fundamental within 30¢ of f0 across GM42/43 keys 46–50 ×3 seeds. +1639¢ 3·f0
  mode-lock gone.
- **00014** (samples-off string collapse): `string_section_model_has_bow_catch_onset`
  green in **both** default and `--no-default-features` runs (early bow-catch noise
  >1.5× settled, via shipping `make()`).
- **00018** (natural-brass body ring): `brass_o17_low_mid_formant_ring_matches_sc55`
  and `brass_o17_synth_brass_body_eq_is_inert` green — h2–h5 within SC-55 bands for
  GM56–61, GM62/63 inert.
- **00020** (perceptual oracle vanishes samples-off): `cargo test -p ferrosintesis
  --no-default-features` now **fails loudly** on the deliberate
  `perceptual_distinctness_requires_embedded_samples` guard; default suite keeps the
  samples-on perceptual module green.
- **00023** (BAR_FULL negative anchor): `bar_full_rejects_near_clone_negative_control`
  green — deterministic near-clone scores 0.025–0.030, below BAR_FULL 0.075.
- **00025** (key≥128 render panic): crafted 34-byte SMFs with NoteOn key=200 (melodic)
  and key=163 (channel-10 drum) render **exit-0** to valid non-silent WAVs on the
  release binary — the pre-fix exit-101 index-out-of-bounds panic is gone; both
  `note_key`/`malformed_note_keys` regressions green.
- **00026** (wrong-struct brightness oracle): `bowed_string_cello_is_brighter_than_
  contrabass` green; confirmed by source read that it routes GM42/43 through
  `render_program_sampled → make() → BowedString` (the shipping voice, not the old
  `Bowed::new` alt-bank) and asserts a directional cello>contrabass×1.05. Both root
  causes (wrong struct + no direction) closed.

No residual gaps and no reopens — every eligible bug was a clean Confirmed close.

## Note — samples-off failures unrelated to this batch

`cargo test -p ferrosintesis --no-default-features` reports four failures: the
intended 00020 guard, plus three **pre-existing** positive sample-engagement controls
that hard-code `samples=true` and cannot pass with the sample bank compiled out —
`gm0_grand_and_gm1_upright_are_distinct_instruments` (voices.rs:14185),
`keyboard_voices_programs_4_7_do_not_use_acoustic_piano_voice` (voices.rs:13092),
`wd_o10_routing_sample_policy_and_lifecycle` (voices.rs:21290). None touch this
batch's code; the default-features gate is unaffected. Logged to scratchpad, not
filed (verify-only pass finds no new bugs).

## Landing

Pure docs/ledger branch (only `bugs/*.md` + this journal + a scratchpad line) →
docs lane via `deltic integrate`.
