# 2026.07.20 — JRN — GM sweep build queue (loop handoff)

Live handoff for the 5-unit build loop from the 2026-07-20 GM instrument sweep.
Each unit: own worktree, oracle-first, focused green, render-diff, committed and
HELD (never pushed); ledger flips + this journal ride the docs lane. Integration
of the code branches waits for Arthur's explicit go.

## Done

### U1 — GM 86 parallel fifth + GM 87 sub-octave (MM-REQ-KILN-00019/00020) ✅
- **Held branch:** `task/20260720-DEV-HUM-gm86-fifth-gm87-sub-octave` @ `7255802`
  (worktree `D:/worktrees/midi-music/20260720-DEV-HUM-gm86-fifth-gm87-sub-octave`,
  based on `2b791e6`).
- Mechanism: `LeadSpec.interval` (ratio, gain) + `SawStack::push_interval_layer`
  — per-layer `gain` (×1.0 exact elsewhere) + Σgain normalizer (== old len
  divide bit-exactly), salted fresh RNG, layer rides `base_f·ratio·bend` so
  bends/CC1/CC68 keep the interval. 86: (2^(7/12), 0.85); 87: (0.5, 0.90).
- Evidence: 3 anchored oracles observed red→green (first sub oracle PASSED with
  the sub deleted — ratio-of-skirts trap, MM-BUG-KILN-00023 shape — rewritten
  anchored on the voice's own fundamental); SawStack byte-stability canaries,
  golden mix, determinism, distinctness/class matrices (27), clippy -D, fmt all
  green. Render-diff over 124 MIDIs: **2 expected (GM87 = Heliopause Pts 1&2),
  122 same, 0 contamination, 0 not reached**. Heliopause GM87 stem (ch 10, solo)
  moves **−1.72 dB RMS** (quieter — normalizer outweighs added sub; peak
  0.15→0.13). NOTE: Heliopause authors GM 87 — the sweep audit's "zero in-repo
  usage of 86/87" claim was WRONG; trust the MIDI scan, not the audit.

## U2 journal anchor (superseded — see Done)
No worktree minted yet. `JawariSpec` on the Pluck preset (grep `JawariSpec` in
voices.rs; only SITAR authors it). Mirror `sitar_jawari_buzz_survives_decay` /
`sitar_jawari_*_stability` oracle shapes; tune gentler/darker than sitar; GM 106
has zero in-repo album usage (verified by MIDI program scan 2026-07-20), so
render-diff expects ZERO album diffs — any diff is a bug.

## Queue after that
- U3 bagpipe zone fill + RR2 (MM-REQ-KILN-00025) + LoopVoice drift
  (MM-REQ-KILN-00026) — prepare.py `BAGPIPE_SOURCES` + sampler.rs; lessons:
  24-bit WAVs (sw==3), wrap_error_db scoring, short windows; drift only (bp_o1
  pins constant amplitude). Render-diff: bagpipe material only.
- U4 steel high-key wrap-gain taper (MM-REQ-KILN-00027) — calibrate ≥76 vs the
  model; extend `la_level_continuity`. Render-diff: steel material only.
- U5 --solo 8 hang of "Hollow Hill Pt 1" (MM-BUG-KILN-00027) — reproduce
  bounded, then engine.rs solo path / voice reap + sampler.rs, one hypothesis
  at a time.

## Invariants (from the loop contract)
Oracle-first with an observed red; stdin-closed test runs; clippy -D warnings;
render-diff baseline = the branch's own base commit, binaries mtime-verified;
no version bumps (integration owns the one bump); reqs/bugs never touched on a
code branch; docs-lane branches may auto-integrate; code branches NEVER pushed.


## Update 2026-07-21 — U1 + U2 INTEGRATED; authorization; three gotchas

**Standing authorization:** Arthur (2026-07-20, in-session): each build-queue
unit's code branch is approved for integration once done-and-verified. Applied.

### U1 integrated — a3e0781 + f1106b1 (+ catch-up bump 0.21.51)
First integrate attempt FAILED the full-suite gate: o_pitch_melodic_programs_model
read GM 87 an octave low (dominant line f0/2 at sub gain 0.90). Fix at the
VOICING layer (gain 0.55 — req wants bass UNDER the lead), not an oracle
exception; 18/18 affected oracles green incl. o_pitch; re-swept render-diff:
2 expected (Heliopause GM87) / 0 contamination; tuned stem delta −1.73 dB.

### U2 integrated — 495f85d (+ catch-up bump 0.21.52)
Sawari via JawariSpec on SHAMISEN; oracle = band-decay-rate differential
(3.8 dB slowing vs jawari:None control; below sitar on t60-scaled windows);
red observed 0.00 dB; 29/29 suite; render-diff 124 same / 0 contamination.

### Gotchas for future iterations
1. **No `.deltic-integrate.toml` in this repo → `deltic integrate --push`
   gates and pushes but SKIPS the version bump** (unsupported lane). The task
   owns the raw-lane bump. U1/U2 landed un-bumped; repaired by catch-up bumps
   6c06cbc (0.21.51) + 4d8e8a7 (0.21.52). Future units: bump on the branch
   immediately before integrating, one bump per task.
2. **A `shutil.move` restore keeps the backup's OLDER mtime — cargo reruns the
   stale test binary** and a red-check "sticks". `touch` the file after any
   backup-restore before trusting a test run.
3. **Ledger ID race is real**: a concurrent agent minted KILN-00027/00028 and
   the collision resolved by renumbering theirs to 00029/00030 (070e525
   restored ours). Check `git log --oneline` for ledger renames after any
   integration race.

## Next: U3 — bagpipe zone fill + RR2 (MM-REQ-KILN-00025) + LoopVoice drift (MM-REQ-KILN-00026)
No worktree yet. prepare.py BAGPIPE_SOURCES bakes 8 of 26 FreePats WAVs — fill
A#4/B4/C#5/D#5/E5/F5/F#5 + a `_32` RR2; loop-cut with the wrap_error_db-scored
find_loop (post-d851869). Lessons: sources are 24-bit (sw==3); short loop
windows; drift ONLY on LoopVoice (bp_o1 pins constant chanter amplitude —
±0.22 % SAX_DRIFT_MAX-style read-rate walk). If the pinned FreePats archive is
missing/un-fetchable: STOP AND ASK. Render-diff: bagpipe material only.
Then U4 steel taper (MM-REQ-KILN-00027), U5 --solo 8 hang (MM-BUG-KILN-00027).


## Update 2026-07-21 (b) — U3 scouting (worktree minted, no edits yet)

**Worktree:** `D:/worktrees/midi-music/20260721-DEV-HUM-bagpipe-zones-rr2-loop-drift`
(branch `task/20260721-DEV-HUM-bagpipe-zones-rr2-loop-drift`, clean — resume here).

Scouted facts for the builder:
- `prepare.py main()` supports `--only=fam[,fam]` — the bagpipe family prefix is
  `chanter` (comment at prepare.py:~2216); use `python prepare.py --only=chanter`
  to fetch+bake ONLY the bagpipe bank (pinned 7z, SHA-verified, needs `7z` on
  PATH or `C:\Program Files-Zipz.exe`; archive
  freepats.zenvoid.org/Ethnic/Bagpipe/Bagpipe-SFZ-20221204.7z). No local cache
  exists — the fetch is part of the sanctioned flow. Drones are prefix `drone`
  (extend `--only` if drone RRs are added — NOT in scope; chanter zones only).
- `BAGPIPE_SOURCES` (prepare.py:572) maps bank filename → archive member
  (`Bagpipe-SFZ-20221204/samples/<NOTE>_31.wav`); `_31` = take 1, `_32` = the
  RR2 takes per the scratchpad's archive inventory (24 chanter WAVs total).
  Add: A#4/B4/C#5/D#5/E5/F5/F#5 `_31` + `_32` for every chanter pitch that has
  one — CONFIRM against the actual 7z listing after extraction; do not assume.
- Bake path: `_bake_bagpipe`-style loop at prepare.py:~1571 — reads the SFZ loop
  starts, measures f0 (tight ±2-semitone window per file), `extract_loop` with
  `BAGPIPE_LOOP_S`/`BAGPIPE_MAX_WRAP_DB=-14.0`, writes via
  `sample_output_path(fn)` → the `-orchestral` crate (its pinned bank byte
  total will need a refresh — see commit d293fa8 precedent). Note token is
  parsed from the FILENAME (`chanter_A#4.wav` — check `NOTE_HZ` has sharp
  names; the note parser takes the `_`-split part starting with A-G).
- Rust side: `crates/ferrosintesis/src/sampler.rs:2179-2226` — `chanter()`
  returns `&'static [Zone]` with hard-coded measured roots (F4 341.37 …
  G5 771.47); new zones need rows with the roots prepare.py PRINTS at bake
  (`_print_sample_rows`). RR2 mechanism does NOT exist for LoopVoice zones yet:
  `bagpipe_chanter_loop` (sampler.rs:2327) → `LoopVoice::new` picks a zone by
  key; RR2 needs a per-note pick among equal/nearest-root candidates (seed- or
  counter-based) — mirror how sampled_drum does round robins; keep
  deterministic per (key, seed).
- Drift (MM-REQ-KILN-00026): port the `SAX_DRIFT_MAX` ±0.22% read-rate random
  walk from `SaxLoopVoice::render` into `LoopVoice::render` — drift ONLY;
  `bp_o1_bagpipe_chanter_is_constant_amplitude_saxes_keep_dynamics` pins
  constant amplitude and must stay green. Oracle sketch: read-rate variance > 0
  over a held chanter note + the existing loop/parity oracles green.
- Oracles to extend: `looped_sustain_banks_are_loopable` sweeps all baked loops
  automatically; add zone-coverage/pitch-integrity assertions for the new roots
  (mirror `la_*_pitch_integrity` shapes). Red-first where possible (coverage
  assertion listing expected zone count fails before the bake lands).
- Render-diff: scan which albums author GM 109 FIRST (MIDI program scan — the
  U1 lesson: trust the scan, not the audit) and set EXPECTED accordingly.
- Raw-lane reminder: bump on the branch before `deltic integrate --push`.
