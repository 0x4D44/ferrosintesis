# 2026.07.20 — JRN — GM sweep build queue (loop handoff)

Live handoff for the 5-unit build loop from the 2026-07-20 GM instrument sweep.
Each unit: own worktree, oracle-first, focused green, render-diff, committed and
HELD (never pushed); ledger flips + this journal ride the docs lane. Integration
of the code branches waits for Arthur's explicit go.

## Done

### U1 — GM 86 parallel fifth + GM 87 sub-octave (MM-REQ-KILN-00019/00020) ✅
- **Held branch:** `task/20260720-DEV-HUM-gm86-fifth-gm87-sub-octave` @ `7255802`
  (worktree `D:/worktrees/midi-music/20260720-DEV-HUM-gm86-fifth-gm87-sub-octave`,
  based on `2b791e6`).
- Mechanism: `LeadSpec.interval` (ratio, gain) + `SawStack::push_interval_layer`
  — per-layer `gain` (×1.0 exact elsewhere) + Σgain normalizer (== old len
  divide bit-exactly), salted fresh RNG, layer rides `base_f·ratio·bend` so
  bends/CC1/CC68 keep the interval. 86: (2^(7/12), 0.85); 87: (0.5, 0.90).
- Evidence: 3 anchored oracles observed red→green (first sub oracle PASSED with
  the sub deleted — ratio-of-skirts trap, MM-BUG-KILN-00023 shape — rewritten
  anchored on the voice's own fundamental); SawStack byte-stability canaries,
  golden mix, determinism, distinctness/class matrices (27), clippy -D, fmt all
  green. Render-diff over 124 MIDIs: **2 expected (GM87 = Heliopause Pts 1&2),
  122 same, 0 contamination, 0 not reached**. Heliopause GM87 stem (ch 10, solo)
  moves **−1.72 dB RMS** (quieter — normalizer outweighs added sub; peak
  0.15→0.13). NOTE: Heliopause authors GM 87 — the sweep audit's "zero in-repo
  usage of 86/87" claim was WRONG; trust the MIDI scan, not the audit.

## Next: U2 — shamisen sawari (MM-REQ-KILN-00024)
No worktree minted yet. `JawariSpec` on the Pluck preset (grep `JawariSpec` in
voices.rs; only SITAR authors it). Mirror `sitar_jawari_buzz_survives_decay` /
`sitar_jawari_*_stability` oracle shapes; tune gentler/darker than sitar; GM 106
has zero in-repo album usage (verified by MIDI program scan 2026-07-20), so
render-diff expects ZERO album diffs — any diff is a bug.

## Queue after that
- U3 bagpipe zone fill + RR2 (MM-REQ-KILN-00025) + LoopVoice drift
  (MM-REQ-KILN-00026) — prepare.py `BAGPIPE_SOURCES` + sampler.rs; lessons:
  24-bit WAVs (sw==3), wrap_error_db scoring, short windows; drift only (bp_o1
  pins constant amplitude). Render-diff: bagpipe material only.
- U4 steel high-key wrap-gain taper (MM-REQ-KILN-00027) — calibrate ≥76 vs the
  model; extend `la_level_continuity`. Render-diff: steel material only.
- U5 --solo 8 hang of "Hollow Hill Pt 1" (MM-BUG-KILN-00027) — reproduce
  bounded, then engine.rs solo path / voice reap + sampler.rs, one hypothesis
  at a time.

## Invariants (from the loop contract)
Oracle-first with an observed red; stdin-closed test runs; clippy -D warnings;
render-diff baseline = the branch's own base commit, binaries mtime-verified;
no version bumps (integration owns the one bump); reqs/bugs never touched on a
code branch; docs-lane branches may auto-integrate; code branches NEVER pushed.
