# JRN — U5: GM33/34/35 electric bass LA onset + model-to-alt (MM-BUG-KILN-00016)

- **Date:** 2026-07-20
- **Author:** Claude Opus 4.8 (1M)
- **HLD:** `wrk_docs/2026.07.19 - HLD - LA sample sourcing plus model-to-alt (GM4-8-10-11-14-15, 32-37).md`
- **Unit:** U5 — the first **new-crate + new-source** unit. **Status: focused-green**, ready to integrate. **No bump**
  (Arthur: continue unbumped; reconciled later).

## What landed
GM 33 fingered / 34 picked / 35 fretless electric bass gain a real FreePats RBX pluck onset as the **default**,
pure `Pluck` model preserved as the **CC0≠0 alt** (`8 | 11 | 32..=35 => voices::make(.., false)`).

- **NEW source (CC0):** FreePats "Clean Electric Bass" (`github.com/freepats/electric-bass-YR`, release 2019-09-30),
  a real Yamaha RBX by Andrea Biasior. **CC0 1.0** confirmed on the downloaded bytes (`LICENSE.txt` inside each
  archive) + the FreePats page. Two SFZ+WAV archives **SHA-256 pinned**: FingerBassYR (`7a8075f8…`) + PickedBassYR
  (`ba301f87…`), extracted with the existing `ensure_archive_sources` 7z helper (new `ensure_ebass_sources`).
- **NEW crate `ferrosintesis-samples-bass`** (CC0-1.0): Cargo.toml + README + PROVENANCE + generated `src/lib.rs`
  (via `gen_crate_lib.py`, 13 files, ~1.1 MB). Wired: `.gitignore` allowlist, workspace members, ferrosintesis
  `embedded-samples` feature + optional dep, and the `sampler::embedded_wav` lookup chain.
- **Roots (measured near the SFZ `key=`, all clean, no drops):** finger E1 41.22/F#1 46.84/G#1 52.12/A#1 58.69/
  C2 65.40/D2 73.27; pick E1 41.62/F#1 47.19/G#1 52.55/A#1 58.70/C2 65.83/D2 73.70/E2 82.13. The SFZ keys gave the
  exact pitch, so the one-octave no-octave-label filenames were named + rooted precisely (no autocorr guessing).
- **sampler.rs:** `finger_bass_bank()` (6 zones, GM33 + GM35) + `pick_bass_bank()` (7 zones, GM34). **voices.rs:**
  33/34/35 wrap their Pluck in `LaVoice::wrap` under `if samples` (byte-identical `--no-samples`); shared `LA_EBASS`
  + registry. GM35 rides the finger bank (no free CC0 fretless exists; the model carries the "mwah"). **altbank.rs:**
  `32..=35` in the samples-off arm. **Oracle:** appended (33,33,1)/(34,33,1)/(35,33,1).

## Validation (green)
- `cargo build -p ferrosintesis` clean (new crate compiles + wires); `cargo test -p ferrosintesis
  -p ferrosintesis-samples-bass` — **568 + oracle pass, 0 failed**; -bass parity (13 files) green; fmt +
  clippy `-D warnings` clean.

## Remaining for U5 close-out
- **Render-diff --program 33 34 35** as the pre-integration check (baseline = branch fork point).
- **No bump** (ferrosintesis stays 0.21.44; Cargo.lock only gains the new -bass 0.1.0 entry). MM-BUG-KILN-00016 now
  has acoustic(32)+finger(33)+pick(34)+fretless(35); **slap 36/37 deferred** (HLD decision 4) is all that remains.
- **Coverage note:** the FreePats source is one low octave (E1–E2); LaVoice ±1oct covers ~E0–E3. High bass notes
  above ~D#3 fall back to the pure model (benign — most electric-bass playing is in range).
