# 2026.07.21 — JRN — M-CAL instrument-balance: foundation proven (loop-build handoff)

Resuming the **levels** task (`2026.07.20 - HLD - instrument balance oracle + drum-forward
recalibration.md`, DESIGN AGREED). The velocity detour (k=2) is done + integrated; its
render-diff verify came back clean (124/124 albums changed as expected, 0 contamination —
see below); bug 00029 (bowed/EP turnover) diagnosed and **deferred** by Arthur (note landed
on trunk, a1406a2). This session established and **de-risked the M-CAL measurement chain
end-to-end**. Remaining work is the derivation + application, below.

## Worktree
`D:/worktrees/midi-music/20260721-DEV-HUM-m-cal-instrument-balance-derive-percissi`
(branch `task/20260721-DEV-HUM-m-cal-instrument-balance-derive-percussi`, off origin/main
`58e487e`/`a1406a2`). `_cal/` holds scratch probe + renders + levels (untracked; do NOT
commit — gitignore or leave). No production edits yet.

## Verify step (k=2 render-diff) — CLEAN, done
Baseline `5d45c7e` (pre-code) → after `f7d585a` (last velocity commit before the GM sweep).
Full catalogue render-diff (124 MIDIs, `--rate 11025`): **124 changed / 0 contamination /
0 not-reached**. Every changed album is dominated by velocity-sensitive voices; no
exempt-only album exists, so "everything changed" is exactly right. Exempt-voice isolation
rests on the shipped self-retiring oracles (a full-mix diff can't isolate them). Safe to
build M-CAL on top.

## THE PIPELINE IS PROVEN — exact chain (all four stages work)
1. **Probe:** `python tools/instrument-balance/mkprobe.py <set>` → `_cal/probe_<set>.mid` +
   `_cal/plan_<set>.tsv`. Sets: smoke(4) / preflight(12) / hot / velcurve / full(128) /
   chunk0-3. `velcurve` set uses 2 keys × 9 vels for the velocity guard.
2. **ferro render:** `./target/release/ferrosintesis.exe _cal/probe_<set>.mid -o
   _cal/ferro_<set>.wav --peak-normalize -q`. **`--peak-normalize` is LOAD-BEARING** — it is
   a single LINEAR per-track scale (no nonlinear −18 LUFS limiter), so relative per-program
   levels are preserved; the default loudness-normalize would let the true-peak limiter
   non-uniformly bite hot programs and corrupt the differential.
3. **SC-55 render:** `/c/apps/mdmidiemu.exe _cal/probe_<set>.mid --wav _cal/sc55_<set>.wav
   --synth sc55 --roms /d/language/mdsc55/roms/sc55`. **ROM PATH IS THE GOTCHA** — mdmidiemu
   defaults to `roms/sc55` relative to CWD (absent here); the ROMs live in the sibling
   `mdsc55` repo. Renders ~5× realtime. S-YXG50 (`--synth syxg50`) needs `--dll <VST>` —
   not yet located; SC-55 is the primary reference, add S-YXG50 for the agreement check.
4. **Meter:** `./target/release/examples/calmeter.exe _cal/<engine>_<set>.wav
   _cal/plan_<set>.tsv <ferro|sc55> > _cal/<engine>.levels.tsv`. Emits per-note
   `max_m` (max momentary block), `peak_block`, `clipped`, `sounded`.
   Build both: `cargo build --release -p ferrosintesis-cli` AND
   `... -p ferrosintesis-cli --example calmeter` (the `--example` flag builds ONLY the
   example; the main binary needs its own build).

### As-built note that supersedes the HLD
The HLD called the statistic **S95** (active-block 95th percentile). The as-built (calmeter.rs
doc §"The level statistic") is **max momentary block**, and it explains why: over ~9 blocks in
a 1.2 s window, nearest-rank p95 == max at n≤19, and max is correct for a single isolated
note. So "implement S95" is DONE (as max-momentary); do not re-add a percentile. (Do NOT reuse
`voices.rs::percentile` — its body returns the 2nd-largest at n=9; documented in calmeter.rs.)

### Derivation (proven on smoke; `_cal/an.py` is the throwaway prototype)
Per engine, level(p) = median over the program's notes; D_p = (ferro_p − ferro_median) −
(sc55_p − sc55_median); trim_p = round(clamp(−0.70·D_p, −6, 6)) with a 1 dB dead-band. For
percussive-family targets trim is currently 0.0, so a trims-ON render gives their raw level
directly (no CLI trim-disable flag needed). For the sustained cross-check, subtract the known
`PROGRAM_TRIM_DB[p]` to recover raw level.

### Smoke findings (4 programs — medians rough, arithmetic proven)
GM0 D +3.25 / GM6 D +2.32 / GM40 D −2.17 / GM118(SynDrum) D −8.89. Velocity spans v72→110:
GM6 **2.48 (ferro) vs 6.87 (SC-55)** → GM6 correctly trips the velocity guard (its vel_sense
compression is deliberate); GM118 percussive sits ~9 dB low. Confirms both the trim path and
the guard path work.

## REMAINING UNITS (the build)
1. **Derivation tool** — promote `_cal/an.py` to `tools/instrument-balance/derive_trims.py`:
   D_p, velocity guard (`|D_p(v110)−D_p(v72)| > 3` → EXCLUDE + flag; auto-excludes GM6/42/43/4),
   dead-band, clamp; emit the `PROGRAM_TRIM_DB` percussive rows; the sustained-consistency
   cross-check (S95 recompute must agree with shipped sustained trims within ~1.5 dB — a
   CALIBRATION GATE: if it fails, STOP, the metric disagrees on the easy case).
2. **Full measurement** — run `full` (128) + `velcurve` sets on ferro + SC-55 (+ S-YXG50 if
   the DLL is found) → stable medians → the real D_p distribution.
3. **Apply trims** — extend `PROGRAM_TRIM_DB` (engine.rs:637) to the percussive families
   (Piano, ChromPerc, Guitar, Bass, pizz/harp/timpani, orch-hit, Ethnic, Percussive), same
   0.70×/±6/dead-band. Sustained entries FROZEN. Never-trim complement shrinks to SynthFX
   96–103 + SoundFX 120–127.
4. **Remove DRUM_FORWARD** (Arthur's §11 decision) — engine.rs:74 `= 2.0` → `1.0`/delete;
   rewrite the engine.rs:68-74 comment (the backwards absorption model).
5. **M-MIX + O1** — leave-one-out `loo_delta`; oracle `drum_contribution_matches_reference`
   (programmatic groove, fail-first at DRUM_FORWARD=2.0). O1's band is **ear-confirmed by
   Arthur**.
6. **O2 evolve** `program_trim_scope_and_calibration` (engine.rs:3246) to the two-class
   (sustained frozen + percussive new) contract. **O3** catalogue LOO sweep tool (report).
7. **Migration** — render-diff (DRUM_FORWARD change → only ch-9/XG-drum albums; drum-free
   album moving = bug); re-pin golden `testutil.rs:1454` ch-9 row + `GOLDEN_MASTER_PEAK`;
   ch-7/8 canaries stay bit-exact. Re-render catalogue for one consistent listening state.

## ARTHUR-FACING DECISIONS (surface with data, don't inherit)
- **Damping 0.70× / clamp ±6 dB** — HLD §11 says RE-EXAMINE, not inherit. Settle once the
  full D_p distribution is in.
- **DRUM_FORWARD removal magnitude** — §10 catalogue is BIMODAL by album/composer (median
  −1.79 LU, 7/17 hot). Removing outright buries the already-quiet albums; the two-step plan
  (fix synth → re-mix albums) handles it, but confirm.
- **Ears** — O1 band; final A/B on **Paper Kites** (Big Weather) + **Three-Sixty-One**
  (Through Lines). Re-run `render-catalog` first (disk renders are in mixed synth states).

## Then: step 2 of Arthur's plan
Re-balance the album tracks (per-album ch-10 CC7 re-touch for cohort-2 albums mixed against
DRUM_FORWARD=2.0). Composition-layer follow-up, after the synth is calibrated.
