# 2026.07.21 — JRN — M-CAL v2 tooling build (guarded metric machinery)

Task: build candidate-2's guarded-metric *tooling* end-to-end via the lean process
(design → adversarial review → Codex external → self-approve → implement → review → validate).
Worktree `task/20260721-DEV-HUM-m-cal-instrument-balance-derive-percussi`. Continues from the
committed metric HLD (`d40a092`) and journal `2026.07.21 - JRN - M-CAL foundation proven.md`.

## Intent

Build the deterministic, testable core so the percussive re-derivation is one command once the
ear-gated timbre bugs are fixed: register-band probe, attack/body multi-window meter,
envelope+pitch-tilt+velocity guards, paired-difference anchor. **Out of scope:** the 6 timbre
bugs, the bass voice-gain fix, and applying any new trims (all ear-gated downstream).

## Key design decisions (see `wrk_docs/2026.07.21 - HLD - M-CAL v2 tooling implementation.md`)

- **No library change.** The envelope mismatch is visible in the existing 400 ms momentary
  block *trajectory* (onset-region max vs held-region max) — no sub-400 ms primitive, so the
  published crate is untouched.
- **Two windows (attack `[0,200)ms`, body `[400,900)ms`), not three.** The probe meters 1.2 s
  while holding 1.3 s to exclude the per-engine release; a tail window re-admits that variance.
  Attack-vs-body catches both named defects (GM28, GM33).
- **Register fix = per-family semitone transpose** of the 6-pitch-class set (basses −24, high
  pipes/perc +12), not sampler zone-roots; the per-key guard backstops residual fallback.
- **Guard is anchor-independent** (`Δattack − Δbody` cancels the common offset).
- **Provisional derivation only** — final numbers wait on the timbre bugs.

## Log

- Read the three tools + `loudness.rs` (block structure) + `offline` module (dev surface) +
  sampler zone shape. Confirmed `offline` re-exports `momentary_lufs`; `measure()` already
  computes the block series and picks the max valid block — the two-region extension is natural.
- Wrote impl HLD; launched lean adversarial review (Fable design consult + Claude reviewer).
- **Adversarial review (Fable + Claude, both code-grounded, refutation pass — none killed).**
  The attack/body/tail window scheme was wrong in shape: Fable F1 — it *falsely excludes
  matched short one-shots* (floor-vs-floor arithmetic) and can't decide the short class at
  400 ms resolution; F2 — `max`-over-body left GM33 0.55 dB from its own threshold. Fix
  (simpler): emit the per-note momentary-block **trajectory** and compare peak-normalised
  trajectories — one anchor-independent statistic, correct for both. Also: Percussive offset
  split 112–115/116–119 (F3, membrane drums); bass −24 verified (F5, C1<knee caveat); velocity
  guard anchor-free (F6); cohort hygiene (F7). Claude: parse TSV by header **name** (#1, else
  new columns silently drop every note); per-key `load()` (#2); onset-relative vs absolute block
  index hazard (#3); hoist guards for `--selftest` (#4); `#[cfg(test)]` in a cargo example DOES
  run under `--examples` (#6, empirically confirmed). Rewrote the HLD around the trajectory guard.
- **Codex external (gpt-5.6-sol xhigh; sandbox helper missing again → re-fired `--yolo`).** Four
  code-grounded correctness items, all folded: #1 **BusGlue** (ferro-only bus compressor
  `engine.rs:915`) breaks the linear differential on loud notes → precondition: verify BusGlue
  inert for probe notes; #2 `median_k(shape_dev)` masks a 2-register mismatch → **>1-key**
  exclusion; #3 the "mismatched-short is excluded" claim was **false** (sub-hop pair normalises
  identical → mis-trimmed) → **explicit sub-hop unmetrable exclusion** (block-1 floored); #4
  register table still hits fallback (GM10 music-box, GM76 bottle) and a matched fallback slips
  the guards → flag heterogeneous-family trims low-confidence, defer an in-crate `#[cfg(test)]`
  ground-truth emitter. Output: `_cal/codex_v2.md`.
- **Checkpoint: HLD SELF-APPROVED.** No architectural fork, no unresolved critical finding.
  Proceeding to implement (tests-first).
- **Implemented + validated on real data.** mkprobe FAMILY_OFFSET (bass −24, pipe/perc-112-115
  +12) — GM33 basses now metered at C1 not C3. calmeter emits the onset-relative block series
  `b0..b8` via a unit-tested `onset_blocks()` (3 tests green). derive_trims: parse-by-name +
  per-key trajectories + guards + `--selftest` (green).
- **Real-data surprise (the value of validating by observing).** First guardcheck run excluded
  the *entire sustained cohort* (GM48/49/50/89) → anchor collapsed to 1 program. Dumping the
  trajectories showed why: ferro's ensemble *decays* ~8 dB across the hold while SC-55 sustains
  flat, and the pad's attack ramps differ — so a trajectory-max `shape_dev` at the metric-HLD's
  3 dB threshold (a value meant for a two-window delta, not a 9-block max) flags gentle
  differences as catastrophes. **Fix:** measure `shape_dev` over the BODY region (b2..b8) only —
  excluding the legitimate fast-vs-slow attack ramp — trim on the body-median level, and set the
  threshold in the observed bimodal gap (gentle sustain/decay ≤~7 dB vs the GM28 collapse ~38 dB
  → SHAPE_DB=12). Re-ran: cohort forms (16,48,50,89, anchor 11.4 dB); every pathological voice
  correctly excluded — GM28 palm-mute (shape/short), GM55 orch-hit (short one-shot), GM107 koto
  (pitch-tilt 13), GM33 bass (pitch-tilt 7.7, a known voice-fix). Selftest still green (matched
  one-shot GM9 kept). guardcheck proposes no trims — correct, its percussive members are all
  deliberately pathological; the cohort just anchors.
- **BusGlue precondition (Codex #1) — OPEN.** ferro WAV peak −2.0 dBFS but that's post-−18-LUFS
  normalization, so it says nothing about the pre-BusGlue mix level; the CLI has `--lufs` but no
  no-normalize/raw flag, so BusGlue-inertness can't be verified within the no-library-change
  scope. The qualitative A4 conclusions are unaffected (a 250 ms compressor release can't cause
  GM48's 900 ms monotonic decay), but the precise trim *values* are provisional pending BusGlue
  verification (verify inert, or add a `with_bus_glue(false)` calibration flag = one bump) — this
  is step 1 of the trustworthy (post-timbre-bug) derivation.
- **Code review (Claude) found two real issues; one exposed a genuine anchor bug.**
  (1) A Greek Δ in the cross-check print **crashes** the tool under Windows cp1252 stdout when
  redirected (I'd masked it with `PYTHONIOENCODING=utf-8`) — fixed to ASCII (and defensively
  swept the other printed em-dashes). (2) The reviewer flagged the sustained cross-check as a
  broken diagnostic; tracing *why* found the real defect: **I was backing `SHIPPED[p]` out of the
  cohort before computing the anchor**, which un-does the frozen-good calibration that *defines*
  the reference frame — biasing every percussive trim by ~median(shipped) ≈ +3 dB. **Fix:** the
  anchor is `median(RENDERED ferro − SC)` over the cohort (their frozen trims are correct, so
  rendered already matches SC at the engine offset K); the shipped trim is subtracted **once**,
  explicitly, in `D_p = g − SHIPPED[p] − anchor`. This also makes the cross-check meaningful:
  re-run shows the metric now **reproduces** the shipped sustained trims (GM48 implies +5.2 vs
  shipped +5.5, GM50 +6.3 vs +6.0, GM89 +0.8 vs 0.0 — all <1.5 dB), flagging only GM16 organ as a
  frame outlier (implies −9.5 vs −4.5). calmeter.rs + mkprobe.py were clean on every point.
- **Validation gate GREEN (A1–A5).** cargo fmt --check; clippy -p ferrosintesis-cli --examples
  -D warnings; cargo test --examples (onset_blocks 3/3); derive_trims --selftest (incl. GM6
  shipped-back-out); mkprobe preflight+full; py_compile. cp1252 redirect now exits 0.
- **Status: DONE — focused validation green, ready to integrate.** No version bump (dev-only
  tooling: two Python tools + a Rust example + its tests). Downstream (ear-gated, next
  increments): close the BusGlue precondition, fix the 6 timbre bugs + bass voice-gain, then the
  trustworthy full-128 derivation and trim application.
