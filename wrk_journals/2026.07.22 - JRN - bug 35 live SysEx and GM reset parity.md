# JRN — MM-BUG-KILN-00035 live SysEx and GM reset parity

## 2026-07-22 — understand and reproduce

- Started `task/20260722-FIX-CDX-fix-bug-35` in its dedicated worktree from
  `origin/main` `a20f0b36`.
- Read the repo instructions, full lessons file, applicable skills,
  `bugs/README.md`, and bug 35. No `BUGFIX-GUIDE.md` exists.
- Confirmed SMF GM On -> `EvKind::XgReset` -> effects-only `xg_reset()`, while
  live GM On -> `ResetKind::GmSystemOn` -> `hard_reset()`.
- Confirmed the four-byte live capture marks every longer XG/GS payload overflowed.
- Pre-change focused tests were green:
  - `cargo test -p ferrosintesis parser_emits_gm_and_system_resets`.
  - `cargo test -p ferrosintesis xg_effect_sysex_decodes`.
- MIDI Association guidance confirms GM On restores the GM initial state. Chosen
  anchor: retain live hard-reset behavior and bring SMF playback into parity.
- Drafted the HLD: dedicated `GmReset`, one strict shared payload decoder, bounded
  nine-byte live capture, and one shared engine GM-reset method.

## 2026-07-22 — adversarial design review

- Three reviewers covered reset semantics, parser safety, and test altitude, then
  each ran a refutation pass against the combined findings.
- Folded survivors:
  - `gm_system_on()` must preserve cumulative public `Stats` around `hard_reset()`.
  - The shared decoder accepts only complete `F0...F7` SMF messages, rejects
    high-bit data, and never decodes standalone `F7` events.
  - Live malformed SysEx aborts and reprocesses another non-realtime status.
  - `F8..FE` are transparent; `FF` resets and clears partial capture.
  - Regression layers are decoder tables, live routing/recovery, direct core
    state+Stats, and one offline mid-file parse/render case.
- Partially rejected full SMF continuation assembly as wider than bug 35. Complete
  fixed messages gain correct parity; incomplete/standalone packets are ignored.
- No unresolved critical finding remains. The Stats issue was critical but has a
  small, explicit constructor-backed correction.

## 2026-07-22 — external design review and checkpoint

- Claude Opus reviewed the design once in read-only plan mode (no subagents).
- Folded same-tick reset priority, one shared `EvKind::GmReset` engine route,
  private seed-position decoupling from cumulative `Stats`, explicit payload
  lengths, honest render-diff evidence, comment updates, and residual hard-cut /
  callback-allocation risks.
- Rejected unrelated malformed-SMF running-status cleanup, a broad allocation-free
  reset refactor, and a seam-click gate. They do not cause bug 35; immediate hard
  cut is the established reset contract.
- Marked the HLD `SELF-APPROVED`. No architectural fork or unresolved critical
  finding remains.

## 2026-07-22 — tests-first implementation

- Added parser expectations before production code. The focused test failed to
  compile because `EvKind::GmReset` did not exist, proving the old event model
  could not express the required semantic.
- Implemented `decode_sysex_payload` once in `midi.rs` with explicit 4/7/7-8/9
  byte shapes, seven-bit validation, complete-F0-only SMF framing, and GM-first
  ordering within a tick.
- Expanded live capture from 4 to 9 bytes. All decoded SysEx now travels through
  `LiveCommand::Channel(EvKind)`; only raw `FF` remains a live-only reset. A
  malformed non-realtime status aborts capture and is reprocessed normally.
- Added `EngineCore::gm_system_on()`: constructor-backed hard reset with cumulative
  public `Stats` restored. Added a private resettable seed index so post-reset
  audio retains existing live semantics without coupling it to diagnostics.
- Focused green evidence:
  - GM-first format-1 ordering and strict malformed SMF cases.
  - Live XG/GS routing plus malformed-status recovery.
  - Direct core full-state reset with cumulative Stats and reset seed index.
  - Live GM On stops active voices through the shared engine event route.
  - Parsed mid-file SMF preserves two total voices but max polyphony remains one,
    proving the pre-reset held voice cannot overlap the post-reset voice.
- Ran `cargo fmt` and `git diff --check` successfully.

## 2026-07-22 — code review and simplicity pass

- One independent reviewer inspected the production and test diff without changing
  it. The review found no implementation defect.
- Applied its one Issue: added direct regressions for a payload longer than the
  nine-byte capture and for raw `FF` received during a partial SysEx. Both cases
  now prove that reset recognition cannot leak and ordinary channel traffic recovers.
- Review disposition: one change made, zero skipped, zero disagreements, and no
  finding to promote into the bug or requirement ledgers.
- The quality-only pass removed the obsolete single-variant `ResetKind` layer.
  Raw System Reset is now the direct `LiveCommand::SystemReset`; GM On remains a
  shared `EvKind` handled identically for live and SMF input.
- Kept the explicit paired diagnostics/seed increments at the three existing
  allocation sites. They express a small state invariant locally; a new helper
  would hide the exceptional tremolo seed-slot behavior rather than simplify it.

## 2026-07-22 — final validation

- `cargo fmt --check`: green.
- Focused acceptance regressions: six SysEx shape/routing/recovery tests, two GM
  engine/live reset tests, and the offline mid-file reset/Stats test all green.
- `cargo clippy --workspace --all-targets -- -D warnings`: green.
- `cargo test --workspace` with stdin closed: green; the primary library ran
  628 tests and every remaining workspace/doctest target completed without failure.
- Rebuilt release CLI binaries from the exact task base `a20f0b36` and the task
  tree, then ran the mandatory render inventory over all 124 catalog MIDIs at
  11,025 Hz. Result: 124 byte-identical, zero changed, zero contamination.
- Removed the clean detached baseline worktree after the comparison.
