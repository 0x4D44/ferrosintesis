# JRN — MM-BUG-KILN-00035 live SysEx and GM reset parity

## 2026-07-22 — understand and reproduce

- Started `task/20260722-FIX-CDX-fix-bug-35` in its dedicated worktree from
  `origin/main` `a20f0b36`.
- Read the repo instructions, full lessons file, applicable skills,
  `bugs/README.md`, and bug 35. No `BUGFIX-GUIDE.md` exists.
- Confirmed SMF GM On -> `EvKind::XgReset` -> effects-only `xg_reset()`, while
  live GM On -> `ResetKind::GmSystemOn` -> `hard_reset()`.
- Confirmed the four-byte live capture marks every longer XG/GS payload overflowed.
- Pre-change focused tests were green:
  - `cargo test -p ferrosintesis parser_emits_gm_and_system_resets`.
  - `cargo test -p ferrosintesis xg_effect_sysex_decodes`.
- MIDI Association guidance confirms GM On restores the GM initial state. Chosen
  anchor: retain live hard-reset behavior and bring SMF playback into parity.
- Drafted the HLD: dedicated `GmReset`, one strict shared payload decoder, bounded
  nine-byte live capture, and one shared engine GM-reset method.

## 2026-07-22 — adversarial design review

- Three reviewers covered reset semantics, parser safety, and test altitude, then
  each ran a refutation pass against the combined findings.
- Folded survivors:
  - `gm_system_on()` must preserve cumulative public `Stats` around `hard_reset()`.
  - The shared decoder accepts only complete `F0...F7` SMF messages, rejects
    high-bit data, and never decodes standalone `F7` events.
  - Live malformed SysEx aborts and reprocesses another non-realtime status.
  - `F8..FE` are transparent; `FF` resets and clears partial capture.
  - Regression layers are decoder tables, live routing/recovery, direct core
    state+Stats, and one offline mid-file parse/render case.
- Partially rejected full SMF continuation assembly as wider than bug 35. Complete
  fixed messages gain correct parity; incomplete/standalone packets are ignored.
- No unresolved critical finding remains. The Stats issue was critical but has a
  small, explicit constructor-backed correction.

## 2026-07-22 — external design review and checkpoint

- Claude Opus reviewed the design once in read-only plan mode (no subagents).
- Folded same-tick reset priority, one shared `EvKind::GmReset` engine route,
  private seed-position decoupling from cumulative `Stats`, explicit payload
  lengths, honest render-diff evidence, comment updates, and residual hard-cut /
  callback-allocation risks.
- Rejected unrelated malformed-SMF running-status cleanup, a broad allocation-free
  reset refactor, and a seam-click gate. They do not cause bug 35; immediate hard
  cut is the established reset contract.
- Marked the HLD `SELF-APPROVED`. No architectural fork or unresolved critical
  finding remains.
