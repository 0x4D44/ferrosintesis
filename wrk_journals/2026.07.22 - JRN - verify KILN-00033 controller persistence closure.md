# 2026.07.22 — JRN — verify KILN-00033 controller persistence closure

## Scope

Independent Fixed → Closed review of `MM-BUG-KILN-00033`. Claude Opus 4.8 authored the
fix and Fixed transition; this Codex session did not participate in that work.

## Before/after proof

The immediate pre-fix parent was `758cae6`; the fix-containing verification tree was
`c6e048d`.

- Transplanting `program_change_preserves_authored_effect_sends` onto the parent made it
  fail on the original symptom: authored CC93 `0.787402` became program default `0.35`.
- Transplanting the stronger CC121 assertions onto the parent failed because CC121 removed
  the authored CC74 filter.
- A temporary probe covered every persistent field named by the original observation.
  The parent reset CC93/94, bend range, fine tune, CC70/71/74, and the wah filter to their
  defaults. The same probe passed on the fix-containing tree. The probe was removed after
  the comparison and was not committed.
- The two recorded regressions and the corrected
  `gm22_cc1_is_harmonica_vibrato_not_leslie` control oracle passed with the fix.

## Gates and verdict

The exact repository gates passed on the fix-containing tree:

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --all-targets --locked -- -D warnings`
- `cargo test --workspace --locked` with stdin closed

Root-cause inspection agrees with the behavioural proof. `program_change` now treats
`fx_profile` as an unauthored default, and CC121 rebuilds only the drive insert while
preserving the recorded channel state. Verdict: **Closed**, with no residual to split.
