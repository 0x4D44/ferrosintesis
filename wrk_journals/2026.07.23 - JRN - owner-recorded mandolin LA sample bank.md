# 2026.07.23 — JRN — owner-recorded mandolin LA sample bank

Arthur recorded his own mandolin and asked for it to become a ferrosintesis voice. This is the
build log and the handoff for the remaining work.

## Where the mandolin belongs (settled by the code, not by preference)

General MIDI Level 1 has **no mandolin program**. GM2, Roland GS and Yamaha XG all place one as
a **bank-select variation of the Steel Guitar**, and two of the three put the variation in the
**LSB**, which is what `voices::make_variation` already implements.

- **CC0 (bank MSB) on GM 24–25 is already taken.** `altbank.rs:1128` maps `24..=25` to
  `voices::make(.., samples = false)` — CC0 ≠ 0 on the guitars already *means* "pure-model, no
  sample layer", pinned by `altbank_guitars_skip_sample_layer`. The mandolin cannot go there.
- **CC32 (bank LSB) is the correct seam.** Target cell: **`(25, 96)`**. That is the XG Mandolin
  slot, and it sits alongside the Ukulele the repo already has at `(24, 96)` — Yamaha uses LSB
  96 for the "related but different plucked instrument" on the guitars. The existing `(24, 96)`
  cell being a real XG voice is the in-repo corroboration for 96.
- Dispatch order at `engine.rs:2462` is `altbank::make` (CC0≠0) XOR
  `make_variation(CC32)` XOR `make`. A non-zero CC0 suppresses the CC32 path entirely.

### The one contract change

`make_variation` accepts `samples` and discards it (`let _ = samples;`, `voices.rs:11777`) with
the comment *"Variations are modeled-only (no dedicated sample banks)"*. That is a statement of
**current inventory, not a design invariant** — on any real GM module every voice is sampled,
variations included. The mandolin is the first sampled variation.

The invariant that *is* load-bearing in that function is RNG-purity: every arm draws only from
the passed `seed`, so the `None` path stays bit-identical to `make`. Plain `LaVoice::wrap` takes
no seed, so the minimal change preserves it. Keep all eight existing arms untouched.

## The recording

`DR0000_0198.wav`, 24-bit/48 kHz stereo, 2:45. Three passes over ten positions (open + 5th fret
on all four courses, plus 10th and 12th on the E course), at normal / soft / hard. Ten pitches:
G3 C4 D4 G4 A4 D5 E5 A5 D6 E6 = MIDI 55 60 62 67 69 74 76 81 86 88.

Positions were chosen so no two coincide: courses are 7 semitones apart, so the 7th fret would
have duplicated the next open string — open+5th and 10th+12th never collide.

## Findings worth keeping

- **Octave detection is the whole game on this instrument.** Autocorrelation reported D6 as D5
  and G3 as G4. A mandolin's low fundamentals are weak (small body radiates them poorly), so the
  lower note's harmonics contain the upper note's entire comb and a bare harmonic comb scores
  the octave *above* higher. Fixed with a **sub-harmonic veto**: a candidate with real energy one
  octave below itself is not the fundamental. Five of the ten zones are octave pairs, so this is
  not an edge case. `prepare.py` has the same hazard covered by `TWO_F_STRONG` — **the mandolin
  family must be listed there**, or the measured roots for the low zones will come out an octave
  high.
- **A 0.28 s onset refractory hides re-plucks.** Two soft takes (G3, G4) contained a fluff
  followed by the real note 0.17–0.23 s later — invisible to the detector that produced the cut,
  so the zone would have double-struck on every note. Caught by an independent oracle (envelope
  rise inside the kept window), *and* independently flagged by the timbre measurement as the only
  two notes whose soft take was brighter than its loud take. Two unrelated oracles converging is
  what promoted it from noise to a bug.
- **"Last take wins" needs a credibility guard.** Applied naively it re-anchored A5_p onto
  −49 dBFS finger noise 0.88 s later, turning the cut into near-silence. A real retake is played
  at a comparable dynamic; anything >12 dB below the original attack is handling noise.
- **The third dynamic layer was measured and dropped.** Normal vs hard: 6/10 sign consistency,
  median +0.09 harmonics — a coin flip. Soft vs loud: 8/10, median +0.56, sign test p≈0.055 —
  weak but consistent and in the physically expected direction. Shipping `_p` and `_f` only.
- **Intonation:** open strings −12 to +1 cents; fretted notes sharp, up to **+29 cents at the
  5th fret on the thickest course**. Harmless — roots are stored as measured — but it is why
  nominal pitches must never be used as zone roots here.

## Done

- Worktree `D:\worktrees\midi-music\20260723-TSK-HUM-mandolin-la-sample-bank-from-owner-recor`.
- 20 source cuts (10 zones × `_p`/`_f`), mono 16-bit 44.1 kHz, 3.31 MiB, in
  `tools/ferrosintesis-samples/mandolin-src/`. Verified: correct note (scored against octave
  neighbours), clean onset and tail, no clipping, no secondary attack inside the 0.9 s keep
  window.
- Crate scaffolding: `crates/ferrosintesis-samples-mandolin/{Cargo.toml,README.md,PROVENANCE.md}`
  plus empty `samples/` and `src/`. `.gitignore` re-includes added for both the source dir and
  the crate's `samples/`.

## Remaining

1. ~~**`prepare.py` wiring**~~ — **DONE** (uncommitted at time of writing). All seven points:
   `MANDOLIN_SRC`/`MANDOLIN_SOURCES` after `FREESOUND_SOURCES`; `ensure_mandolin_sources` after
   `ensure_freesound_sources`; `FAMILY_PACKAGE["mandolin"]`; `KEEP_FAM["mandolin"] = (0.9, 0.30)`;
   `F0_RANGE["mandolin"] = (170.0, 1450.0)`; `mandolin` added to `TWO_F_STRONG`; and the two
   `main()` lines (`want("mandolin") -> ensure_mandolin_sources(src)`, and `| MANDOLIN_SOURCES`
   in the bake union).
2. ~~**Run the bake**~~ — **DONE**. 20 files, 1.53 MiB, mono 16-bit 44.1 kHz, 0.908 s each,
   in `crates/ferrosintesis-samples-mandolin/samples/`. Nothing else in the tree was touched.

   **CLI gotcha, corrected — my earlier note here was wrong.** `prepare.py` has no argparse; it
   scans `sys.argv[1:]` directly. `--local-only` (`:2496`) regenerates ONLY the local *gong*
   intake and would skip the mandolin entirely — it is **not** the flag to use. The correct
   invocation is:
   `python tools/ferrosintesis-samples/prepare.py --only=mandolin`
   (`--only=fam` at `:2497` filters by sample-name prefix and skips every other family's
   fetches). Passing `--help`, or any bare unrecognised token, leaves `only = None`, which makes
   `want()` true for everything and **starts refetching every family from the network**.

   **The `TWO_F_STRONG` guard is confirmed working**, not merely configured: G3 measured
   196.09 Hz, not the 392 Hz octave-up it would otherwise have produced, and D6 1183.78 Hz not
   591 Hz. Confidence 0.92–1.00 on all twenty.

   **Measured roots for the `sampler.rs` zone tables (unit 5) — use THESE, not
   `PROVENANCE.md`'s pre-bake numbers.** The bake re-measures after trimming and normalising,
   and the two layers differ slightly, so each bank function needs its own column:

   | zone | `_p` root Hz | `_f` root Hz |
   |------|--------------|--------------|
   | G3   |  195.83 |  196.09 |
   | C4   |  265.96 |  266.08 |
   | D4   |  292.89 |  293.51 |
   | G4   |  394.71 |  393.78 |
   | A4   |  440.67 |  440.66 |
   | D5   |  594.24 |  591.60 |
   | E5   |  658.05 |  658.70 |
   | A5   |  885.88 |  885.43 |
   | D6   | 1182.41 | 1183.78 |
   | E6   | 1329.52 | 1326.84 |

   - Housekeeping done: the stray `bottle_G3.wav` the aborted run left in the orchestral crate
     was untracked (`git ls-files` confirmed it was unknown to git) and has been removed. The
     underlying observation still stands and is **not** part of this task: `bottle` is absent
     from `FAMILY_PACKAGE`, so a full bake routes it generically into the size-capped
     `-orchestral` crate whose `samples/*.wav` is git-re-included, while a `_bake_*` helper also
     places it in `-bottle`. That looks like a latent duplicate worth a scratchpad item.
3. ~~`gen_crate_lib.py`~~ — **DONE**. `src/lib.rs` generated: `FILE_COUNT = 20`, 1 602 560 bytes.
   Re-run it (with `--doc "…"`) whenever the WAV set changes — it also regenerates the two
   self-consistency tests, which pin the aggregate byte count.
4. ~~Workspace + dependency + `embedded_wav` chain~~ — **DONE**. Four edits: workspace
   `members`; `embedded-samples` feature list; the `=0.1.0` pinned optional dependency (keep it
   on ONE line — the file warns that a multi-line inline table is invalid); and the
   `.or_else(|| ferrosintesis_samples_mandolin::get(name))` arm in `embedded_wav`.
   Green: `cargo test -p ferrosintesis-samples-mandolin` (2 passed),
   `cargo check -p ferrosintesis --all-targets`, and
   `cargo clippy -p ferrosintesis -p ferrosintesis-samples-mandolin --all-targets -- -D warnings`.
5. ~~`sampler.rs` zone banks~~ — **DONE**. `mandolin_p()` / `mandolin_f()` after `banjo_bank()`,
   `pub fn mandolin_bank(vel)` on the `vel >= 80` split, both layers registered in `prewarm`.
   Roots are the bake's measured values (table above).
   Oracle added: `sampler::tests::mandolin_zone_roots_match_the_fretboard`. It deliberately does
   NOT re-type the roots (that would only prove the copy-paste). Instead it checks the two
   things a transcription error cannot fake: the semitone-step sequence fixed by the fretboard
   (5,2,5,2,5,2,5,5,2, ±60 cents to absorb the real intonation), and cross-layer agreement
   (<25 cents) between `_p` and `_f`, which are independent takes of the same ten notes. It also
   forces both banks to decode, exercising `parse_wav`'s 16-bit/mono/44100 assertion and proving
   `embedded_wav` resolves the names.
   **Mutation-checked, not merely passing:** octave-shifting `mandolin_G3_p` to 391.66 makes it
   fail with `p zones 0->1 span -6.70 semitones, expected 5`. Reverted.
   Green: 46 sampler tests, clippy `-D warnings` clean.
6. ~~`voices.rs` preset + arm~~ — **DONE**. `MANDOLIN` preset (after `STEEL`): steel-string
   picked excitation, short `t60: 3.2` (small archtop, ~1–2 s note), and the double course
   modelled the dulcimer's way (`course_detune: 1.0035` ≈ 6 cents, `course_mix: (0.5,0.5)`,
   `course_couple: 0.002`). `LA_MANDOLIN` constant (seeded from `LA_STEEL`). The `(25, 96)` arm
   in `make_variation` uses `LaVoice::wrap` over `mandolin_bank(vel)`; `let _ = samples;` removed
   and the comment corrected (the mandolin is now the one sampled variation; RNG-purity intact).
   **Ear-tunable constants flagged in code as NOT final** (no ears on this box): `LA_MANDOLIN`
   gain, `exc_trim`/`amp` level, `t60`, body EQ, exact `course_detune`. These want an Arthur
   listening pass — they are correct in KIND, unproven in DEGREE.
7. ~~Tests~~ — **DONE**. Three:
   - `sampler::tests::mandolin_zone_roots_match_the_fretboard` — checks the fretboard step
     sequence (5,2,5,2,5,2,5,5,2) and cross-layer agreement without re-typing roots.
     Mutation-proven (octave-shift → fail).
   - Cell `(25, 96, 60)` added to `engine::tests::cc32_defined_bank_selects_the_variation`;
     `cc32_undefined_bank_falls_back_byte_identical` still passes (the `None` path is unchanged).
   - `engine::tests::mandolin_variation_renders_at_the_played_pitch` — the full CC32→variation→
     sample→mix chain renders an audible A4 and the MODEL is not octave-broken.
   **Finding worth keeping (candidate lesson):** the render test does NOT reliably guard a
   sample-ROOT octave error, because a root stored an octave high drives `LaVoice`'s repitch
   `step` to ~0.5 — the edge of the `0.5..=2.05` window — so it falls back to the bare model,
   which plays the correct pitch and MASKS the error at render level. The classic "a fallback
   hides the thing you measure" trap. The zone-table oracle (which reads the floats directly) is
   the real octave guard; the render test's scope is documented down to match. Confirmed by
   mutation: doubling A4's root fails the table oracle, PASSES the render test.
   Green: full `cargo test -p ferrosintesis --lib` = 634 passed / 0 failed / 23 ignored;
   `cargo fmt -p ferrosintesis --check` clean; `cargo clippy -p ferrosintesis --all-targets
   -- -D warnings` clean.
8. **Render-diff inventory** (mandated for any `voices.rs`/`sampler.rs` change). Expected:
   **zero diffs across every album**, because no committed album authors CC32 on a GM25 channel.
   Any diff is a bug — most likely the `let _ = samples;` removal changing the `None` path.
9. Docs: `crates/ferrosintesis/README.md` Pluck family row (:97) and CC32 row (:131); add the
   cell to the table in `wrk_docs/2026.07.18 - HLD - XG bank-LSB variation voices (CC32) + CC84
   portamento.md`.
10. Version bump belongs to **integration**, not this branch.

## Open, for Arthur

- **LSB 96 is inferred, not cited.** It matches the repo's own `(24, 96)` Ukulele and my memory
  of the XG voice list. Worth one check against a primary XG voice list before this ships in a
  published crate. STILL OPEN.

---

# 2026.07.24 — PIVOT: 4-RR bank + real-sample tremolo (supersedes items 8–10 above)

Everything in "Remaining" items 1–7 above is DONE and committed (six commits, `--lib` 634 green).
Items 8–10 (render-diff, docs, bump) are **deferred** — they now apply to the bank shape below,
not the 1-RR one. The committed 1-RR bank is a working foundation whose *code* scaffolding is
reused wholesale; only the bank DATA, the sampler bank functions and the voices arm change.

## The finding that changed the design (verified in code, not inferred)

**Genuine tremolo never reaches the sample path at all.** A same-key restrike with IOI ≤ 100 ms
(`TREM_IOI_MAX_S`, `engine.rs:1951`) takes an early-return at `engine.rs:2392-2413`: it re-picks
the still-ringing voice via `Voice::retrigger` and **returns before `make_variation`**. And
`LaVoice::retrigger` (`sampler.rs:2993-3001`) deliberately does **NOT** replay the sampled
attack. Its comment is explicit:

> "a tremolo restrike re-picks the MODEL string; the sampled attack transient is NOT replayed
> (the identical PCM 13×/s is the machine-gun artifact the drum round-robins exist to avoid)"

So the mandolin's tremolo machine-gun was **already solved**, by suppressing the sample and
letting the model's per-stroke RNG burst carry each stroke — a tuned decision pinned by
`engine::tests::fast_same_key_restrikes_shimmer_not_click_train`. The premise behind the whole
round-robin plan ("RR fights the tremolo machine-gun") was WRONG. A construction-time round
robin — however sourced — is chosen once at the first strike and frozen; during tremolo the
sample is silent.

Corollaries worth keeping:
- The per-note `seed` comes from a **global** counter (`engine.rs:2444`,
  `voice_seed_index`), so `seed % N` only *decorrelates*; it does not guarantee adjacent-distinct.
  Piano's `rr2` is just `seed & 1` (`voices.rs:12114`) and inherits that weakness.
- The drums' `drum_rr[key]` (`engine.rs:2451-2452`) is a genuine **per-key counter** and does
  cycle 0,1,2,3,… — the clean N-way precedent, but drums have no restrike path so the
  "does it survive a re-excite" question never arose there.

## Arthur's decisions (2026.07.24)

1. **Single dynamic is final** — "very hard to get different dynamics on my mandolin". The bank
   drops the p/f split entirely. (The measurement already said it was marginal.)
2. **Option C chosen**: use the 4-RR recording AND **make tremolo replay the real rotating
   samples**, reversing the deliberate suppression above.

## Option C design — and the constraint that makes it safe

`LaVoice::retrigger` is shared by **every** sampled voice. Un-suppressing it globally would
machine-gun the steel guitar, nylon, banjo, harp… and GM 25 steel tremolo is used by
*Hollow Hill* and *The Signal Fire*, so it would also blow the render-diff and the pinned
`fast_same_key_restrikes_shimmer_not_click_train`.

**Therefore the un-suppression MUST be scoped to voices that actually carry a round-robin bank
(N ≥ 2).** Single-sample LA voices keep today's suppress-and-model path byte-identically. That
scoping is what makes the reversal safe, and it is also the coherent argument for it: rotating
N genuinely different takes avoids the very artifact the suppression was avoiding.

Design sketch:
- Bank gains a round-robin dimension: N zone-sets (`mandolin_rr(i)`), each a full 10-zone set.
- `LaVoice` holds the RR set + the played frequency + a take index. `retrigger` advances the
  index, re-selects the nearest zone in the next take-set, resets sample playback, and REPLAYS
  the onset — instead of returning early with the sample retired. When N < 2, behaviour is
  exactly as now (bit-identical).
- First-strike take selection uses a per-`(channel,key)` counter modelled on `drum_rr` (a dense
  counter, so consecutive fresh strikes cycle; RNG-pure, no new RNG), threaded through
  `make_variation`. NOT `seed % N` — the global seed counter only decorrelates.
- Cycle strictly (not random): Arthur played the four takes in pick-direction order, so strict
  rotation reproduces real down/up/down/up tremolo alternation.

## New bank shape

**10 zones × 4 round robins × 1 dynamic = 40 samples.** Replaces the committed 20 (2 dyn × 1 RR).
Source: `DR0000_0202.wav` (24-bit/48k stereo, 2:40, **zero clipping** — a cleaner capture).

## Re-cut status — report GREEN, write still pending

`rr_cut.py` (in `%TEMP%\mando_work`, disposable) reports all ten notes with four credible
keepers. Two things it needed, both fixed and worth knowing:
- **Octave-down correction for missing-fundamental low notes.** G3's four takes (t≈2.1, 5.9,
  9.1, 12.1 s) read as G4 because a small mandolin body barely radiates the 196 Hz fundamental,
  so the 2nd harmonic (389 Hz) was mistaken for it. The tell is strong energy at the candidate's
  **odd-half-harmonics** (1.5× and 2.5× f0) — those are the octave-below note's 3rd and 5th
  harmonics, which a genuine note at the picked pitch does not have. The plain sub-harmonic veto
  cannot catch this: there is nothing at f0/2 to detect when the fundamental is *missing*.
- A per-note credibility gate (drop takes >15 dB below that note's own strongest) to exclude
  detection junk from the "last four" rule.

**Before writing the 40 cuts, review the messy groups by hand** — G4, E5 and D6 have several
fluff-redos interspersed, and G4 shows two onsets ~0.09 s apart (55.99/56.08) which is below the
0.18 s refractory and may be one pluck detected twice. "Last four credible" is a good
approximation of Arthur's "last four are the keepers", but not proven for those three notes.

## Remaining (option C)

1. ~~**Re-cut**~~ — **DONE**. 40 cuts, mono 16-bit 44.1 kHz, in
   `tools/ferrosintesis-samples/mandolin-src/`. All 40 verified: right note, clean onset/tail,
   no secondary attack inside the 0.9 s keep window, no clipping.
   Three pipeline bugs found and fixed on the way, all worth knowing:
   - **Octave-up misread of G3.** Its four takes (t≈2.1/5.9/9.1/12.1 s) read as G4 — the body
     barely radiates the 196 Hz fundamental so the 2nd harmonic looked like it. Fixed by the
     odd-half-harmonic (1.5×/2.5×) correction; the plain f0/2 veto cannot catch a *missing*
     fundamental.
   - **Level measured in a fixed window bleeds the NEXT attack.** A quiet fluff 0.3 s before a
     loud pluck measured as loud (−9 dB instead of its true −40) and survived the credibility
     gate. Measure each onset in a window that ENDS before the next onset.
   - **The 0.176 s refractory can hide the real pluck.** When a quiet precursor fires first, the
     real attack lands inside the refractory and is never reported, so it cannot be collapsed
     away — the cut anchored on the precursor and buried a +42…+50 dB attack ~0.17 s in (3 files).
     Fixed by re-anchoring each keeper onto the loudest attack within 0.35 s.
   - A fourth was a bug in the VERIFIER, not the data: its octave correction demanded energy at
     both 1.5× and 2.5×, but a mandolin's 5th harmonic is often weak, so a real E5 with a soft
     fundamental scored as E6. Relaxed to the 1.5× test alone (specific enough: a genuine note
     has nothing at 1.5× its own f0).
2. ~~**Bake**~~ — **DONE**. 40 files, 3.2 MB embedded, confidence 0.96–1.00. `TWO_F_STRONG` held:
   G3 measured 195.6–195.8 Hz, not 392. Roots are in `PROVENANCE.md`'s table and in `sampler.rs`.
3. ~~**Sampler**~~ — **DONE**. `mandolin_rr1..rr4` (four 10-zone sets), `MANDOLIN_ROUND_ROBINS`,
   `mandolin_bank(rr)` selecting by `rr % 4`, all four in `prewarm`. Oracle updated: the
   fretboard-step check runs per take-set, cross-TAKE agreement (<30 cents) replaces cross-layer,
   and it asserts the index WRAPS so an unbounded strike counter is safe.
   NB `bank!` takes a `literal`, so a `concat!()`-generated filename does NOT compile — the four
   sets are written out explicitly, like every other bank in the file.
4. ~~**Engine**~~ — **DONE**. `pitched_rr: Vec<[u8; 128]>` (per channel AND key, unlike
   `drum_rr` which is key-only because drums are ch9-bound), read-and-advanced for every melodic
   note and threaded into `make_variation`. Inert for voices without an RR bank, so it cannot
   move an existing render. Deliberately NOT `seed % n` — the seed counter is global, so it only
   decorrelates.
   Green: full `--lib` 634 passed / 0 failed (unchanged count), clippy `-D warnings` clean.
   `fast_same_key_restrikes_shimmer_not_click_train` still passes — the steel path is untouched.
5. **LaVoice**: the scoped retrigger un-suppression (N ≥ 2 only) + take rotation. **NEXT UNIT.**
   `LaVoice` must hold the RR sets + the played frequency + a take index; `retrigger` advances
   the index, re-selects the nearest zone in the next set, resets sample playback and REPLAYS
   the onset. When the bank has < 2 round robins, behaviour must stay bit-identical to today
   (that is what protects steel/nylon/banjo/harp and the two steel-tremolo albums).
6. **Voices**: `(25, 96)` arm takes the RR index; `MANDOLIN` preset unchanged.
7. **Tests**: mandolin tremolo now replays rotating samples (new); `fast_same_key_restrikes_
   shimmer_not_click_train` MUST stay green (proves the steel path is untouched); the CC32 cells
   and the zone-root oracle updated.
8. **Render-diff inventory**: expect **ZERO** diffs across every album. Any diff is a bug — the
   most likely cause is the retrigger scoping leaking into single-sample LA voices, which would
   show up on the steel-tremolo albums (*Hollow Hill*, *The Signal Fire*) first.
9. Docs (README Pluck + CC32 rows, the CC32 HLD table). Version bump belongs to **integration**.

**Ear-tunable, still not final** (no ears on this box): `LA_MANDOLIN` gain, `exc_trim`/`amp`
level, `t60`, body EQ, `course_detune`. Correct in kind, unproven in degree.
