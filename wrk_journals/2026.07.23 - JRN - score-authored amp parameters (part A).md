# 2026.07.23 — JRN — score-authored amp parameters, Part A

Round 1 gave the CC0 alt bank its own lead amp+cab (`e2a78c1`). Arthur listened, then reframed the
whole request:

> "Maybe what I'm really asking for is some more knobs which we can use to control the distortion &
> overdrive... Has to be author — that's where the real magic will happen."

So the deliverable stops being *my* voicing choice and becomes a **control surface**. I argued for a
design first and Arthur agreed. This is that work.

## Why a design, and what it actually had to settle

Not ceremony. Exposing knobs is easy; three things are not:

1. `Drive` is **destroyed and rebuilt** on Program Change, CC0 and CC121 — a rebuild throws away nine
   biquad states, the `MicroCab` line, and heap-allocates. Any parameter feeding construction is
   either lost at the next rebuild or becomes a click generator.
2. This area has **already shipped two bugs**: KILN-00033 (authored CC93/94 discarded at Program
   Change — cost three Big Weather tracks minutes of lead echo) and KILN-00034 (an NRPN select
   corrupting the bend range to 24 semitones). This change edits the very guard added for 00034.
3. The surface is **permanent** — once an album authors it into a committed `.mid`, a wrong
   parameter number is baked into album source.

The design's load-bearing choice is that values are **offsets from the shipped voicing, 64 =
unchanged**. That makes the inertness invariant structural rather than a special case, and it means
the oracle-tuned 29/30 × main/alt matrix from `e2a78c1` composes with authoring instead of being
discarded by it. It also lets the change ship with a **0-of-124 render-diff**, which is the strongest
safety evidence available here.

## Reconnaissance findings that changed the design

**`retune_peak` and `retune_highpass` already exist** (`dsp.rs:544`, `:516`), alongside
`retune_lowpass` and `retune_bandpass` — added for the reed voices, which move formant gain with
blowing pressure at control rate. My draft had proposed adding them. So the DSP crate needs **no
change at all**, and the click-free in-place retune is a proven in-repo pattern rather than a new
mechanism. Good reminder to check before designing a helper.

**`Biquad`'s constructors clamp to `.min(0.49)` of the sample rate**, so an out-of-range parameter
cannot push a corner past Nyquist — the stability question for the Tightness knob's upper end is
already answered by construction at `dsp.rs:478`.

**The base voicing values live only as inline literals inside `Drive::new`.** `from_stages` takes
already-constructed `Biquad`s, so an offset applied later has nothing to offset *from*. Fix: `Drive`
stores its own base voicing, set once at construction, and `apply_params` becomes a pure function of
(base, offsets). One source of truth, no duplication between `new` and the apply path.

## Log

- **Design drafted** — `wrk_docs/2026.07.23 - HLD - score-authored amp parameters (part A, control
  surface and state).md`. Requirement raised as `MM-REQ-KILN-00028`.
- **Housekeeping**: the rejected reference-informed candidate (the SC-55/S-YXG50 chase Arthur turned
  down) was preserved as a `wip:` commit and immediately reverted, rather than discarded — `8301066`
  then `852a19d`, and on disk at `_cal/amp_ab/reference-candidate.patch`.

## Review — what the two passes changed

**Three-lens adversarial pass** (state / DSP / scope, then mutual refutation): 23 findings, 16
survived, 7 refuted. The load-bearing ones:

- **`post` was hand-waved.** All three lenses independently found that "level-compensated like
  `amp_sim`" is not a spec — `amp_sim`'s formula is a function of an absolute drive the program path
  doesn't have, and the four shipped `post` constants are hand-measured. Rewrote it as a trim anchored
  at the knob's neutral point so 64 is bit-identical, with the residual *measured*, not asserted.
- **Drive at 127 breaks the alias budget.** Measured the draft's `g1 ×4` pushing the worst fold bin
  to ≈−36 dB against the −40 dB floor the repo already asserts. So the alias oracle now *sets* the
  knob's top end — the whole reason to design before freezing it into album source.
- **Cut Bite.** `g1` and `bias` are one expression in the shaper, so Drive moves Bite's own measure
  further than Bite does — it fails separability by construction. And round 1 had already measured
  `bias` and refused to vary it. Spent the slot on **Cab Tone** (the HF cliff corner, downward-only),
  which is the real "which cab" axis and the only level-independent tone control.
- **`Biquad` can't be read back**, so an offset needs a stored base or it duplicates a table Part B is
  chartered to change. `Drive` now carries `DriveBase`.
- **The click detector can't see the zipper** the design names as its hazard. Scalars now slew
  per-sample; the oracle looks for a spectral line at `sr/BLOCK` = 689 Hz.

**Cross-family pass** (Codex, GPT-5.6-sol) caught four things the Claude panel missed:

- A **real arithmetic error**: my `post` ratio form divides by zero at `k ≈ 1.585` and inverts sign
  at `k = 2`, because I'd extended `amp_sim`'s non-negative-domain formula to negative `n`. Replaced
  with a strictly-positive exponential trim.
- **NRPN `0x30` is not collision-free** — no manufacturer namespace, and Casio's XW uses those exact
  numbers. Downgraded the claim to "contained" and recorded that the collision-free alternative
  (SysEx + manufacturer ID) is *unavailable* to a published crate, so NRPN is the best we can use.
- The `data_msb` fix **still leaked from the initial null/null state**; a Data Entry with no live
  selector must be dropped.
- **No priming for a same-tick rig** — a single primed bool would ramp five of six parameters up from
  64. Made per-parameter, and a rebuilt `Drive` seeds from persisted `amp_cur`.

Design marked **SELF-APPROVED** (not human-signed-off; the human gate is integration). No
architectural fork remained: the one either/or (NRPN vs SysEx) is settled because SysEx needs a
manufacturer ID we don't hold. Proceeding to implement Part A. Baseline binary for the render-diff
captured at `852a19d` (ferrosintesis v0.21.56).

## IMPLEMENT

The whole feature is `engine.rs`-only — `dsp.rs`'s `retune_peak`/`retune_highpass` already existed,
and there is no public-API change (MIDI-driven, `Options`/`RealtimeOptions` untouched). Shape:

- **`Strip`** gained `amp_params`/`amp_cur`/`amp_primed`/`amp_authored` and the `nrpn_msb/lsb`,
  `nrpn_data_msb` latch fields. `amp_authored` gates the entire path.
- **`Drive`** gained a `DriveBase` (its own shipped voicing, so an offset has a single readable
  source), block-target scalars, a per-sample `slew`, and an `active` flag. `process` branches on
  `active`: unauthored → the byte-for-byte original loop; authored → per-sample `g1/g2/post` slew.
- **`apply_params`** recomputes the affected coefficients from `base + offset` in place (state-
  preserving `retune_*`) and sets the scalar targets. Never allocates, never rebuilds.
- **`cab_biquads`/`cab_biquads_lead`** now wrap `cab_spec`/`cab_spec_lead` (the literals live once);
  the response-shape tests still pin `cab_biquads` and stayed green, proving the refactor is
  bit-identical.
- The CC 6/38/98/99/100/101 arms were rewritten for the mutually-exclusive latch and the scoped
  Data Entry; the three `Drive` rebuild sites (Program Change, CC0, CC121) re-apply the rig; CC121
  nulls both latches but keeps the values.

Two calibration constants were set BY MEASUREMENT, as the design demanded, not by taste:

- **`AMP_DRIVE_K = 1.2`** — the review predicted the draft's `k = 2` would breach the −40 dBc alias
  budget. `amp_drive_knob_holds_alias_floor` confirmed: at `k = 1.2` (g1 ×0.44…×2.28) the worst
  fold bin holds −40 dBc across both programs, both banks and three fundamentals at Drive 0/64/127.
- **`AMP_DRIVE_POST_C = 0.9`** — `post` is a *partial* trim by construction (§2.2). Measured
  residual is ~4 dB at playing level, 5.18 dB at a whisper (0.05) input; the loud-vs-quiet regimes
  pull `c` in opposite directions, so no static law flattens both. Reported, not asserted away.

## Oracles — 8 new gated tests + 1 diagnostic

`amp_neutral_is_bit_identical` (sample-exact across 4 prog×bank — the inertness invariant),
`amp_each_knob_moves_its_axis` (+ 64 = identity for all four combos), `amp_drive_knob_holds_alias_floor`,
`amp_drive_level_residual_bounded`, `amp_sweep_has_no_block_rate_zipper` (689 Hz sideband ≤12 dB),
`amp_single_step_does_not_click`, `amp_params_follow_the_state_table` (every reset row + the
rebuild-before-first-block ordering), `amp_nrpn_never_corrupts_the_rpn_bend_range` (KILN-00034 all
three directions), and `amp_coupling_matrix` (ignored diagnostic).

**Measured knob spreads** (the per-knob primary axis): Body 14.8–16.8 dB, Presence 14.5–15.8 dB,
Cab Tone 11.3–11.7 dB — the three post-clip cabinet knobs are strong and separable. Tone 1.7–3.3 dB
and Tightness 1.7–4.1 dB are the pre-clip knobs, deliberately subtle because a saturator swallows
pre-clip EQ (§2.1). The coupling matrix confirms Drive is the master gain (it lights up every band —
correct physics), and that Tone's band is moved more by Drive than by Tone itself. **Part B's first
question is whether Tone earns its slot or should be retargeted** — the design already flagged this;
the matrix now quantifies it.

## Reviews

- **/code-reviewer (independent, read-only):** verdict **clean** — no critical or major defects, all
  five focus areas confirmed. One minor fidelity nit: a rebuild firing after authoring but before the
  channel's first audio block left the Drive *gain* ramping from neutral (filters were fine).
  **Fixed** with `Strip::amp_prime_all`, which snaps un-primed knobs before the reseed; locked by a
  regression assertion in `amp_params_follow_the_state_table`.

## Verification

- **Inertness proven three ways.** (a) `amp_neutral_is_bit_identical` — sample-exact for all four
  program×bank; (b) 16/16 `demos/` renders byte-identical against the `852a19d` baseline, INCLUDING
  the XG-effect-SysEx + full-controller reference (which is the AC-3 XG amp-sim guarantee — that path
  shares `from_stages`/`cab_biquads` and no album exercises it); (c) structurally, `process` runs the
  original loop whenever `active == false`, and `active` only turns true inside `apply_params`, which
  is only reached for an `amp_authored` channel.
- **Full gate green:** `cargo fmt --check` clean, `cargo clippy --workspace --all-targets -D warnings`
  clean, **630 tests pass** (622 prior + 8 new; 22 ignored), release build clean.
- **Render-diff** (the repo mandates this for engine.rs changes; it is a *report, not a gate*, and
  the expected result for a pure controller feature is 0 changes). Confirmed 0, four independent ways:
  (a) the **sample-exact oracle** above — the actual Drive DSP, all four program×bank, byte-for-byte;
  (b) **`Slipstream/03 - Hammerhead`** — the heaviest alt-bank track (756 of its 1,432 driven notes in
  the lead register) — byte-identical between the `852a19d` baseline and the feature binary;
  (c) three more diverse driven tracks byte-identical — **Big Weather `01` (main-bank 29/30)**,
  **Through Lines `16 - Three-Sixty-One` (the other alt-bank album)**, **Bright Matter `02` (a
  different composer)**; (d) all 16 `demos/` byte-identical, INCLUDING the XG-effect-SysEx +
  full-controller reference (the AC-3 XG amp-sim guarantee). Only 41 of the 124 album MIDIs build a
  `Drive` insert at all — the other 83 cannot be touched by definition — and no composition engine
  emits an amp NRPN (census clean), so no album reaches the authored path. Each full render takes
  ~34 s (3–5 min tracks), and the box was under heavy contention from other agents' render processes,
  so the exhaustive 41-track sweep was not worth the wall-clock over the four-way proof above.
