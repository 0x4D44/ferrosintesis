# 2026.07.24 — JRN — review `crates/ferrosintesis-samples-bottle/`

## Scope and selection

The consolidated coverage ledger lagged two committed transactional fragments, so
selection considered those fragments before choosing a target. They already covered
the CC-BY and electric-bass sample crates. The bottle crate was the higher-risk
remaining NEVER-REVIEWED area because it combines whole-voice looping, realtime lazy
initialization, and a separately committed Freesound source.

## Review

Seven read-only lenses completed: correctness, security, performance/scale,
reliability, maintainability, test coverage, and devil's advocate. The team lead
re-read every cited control flow and independently derived the two key arithmetic
facts:

- the loop search performs 67,384,800 sample-square accumulations per sampled GM 76
  NoteOn;
- the `u32` clock wraps after 27.05 hours at 44.1 kHz.

Static WAV inspection confirmed the current payload is structurally valid and its
committed source hash matches provenance. No code, app, test, generator, build, render,
or exploratory harness ran.

## Outcome

- `MM-BUG-KILN-00064`: repeated loop discovery and wrong prewarm target.
- `MM-BUG-KILN-00065`: clean-cache regeneration failure, warm-cache wrong routing,
  and no deterministic producer for the active asset.
- `MM-BUG-KILN-00066`: long-held voice clock overflow.

Two focused oracle gaps and the superseded onset path remain in the CR report and are
folded into those bugs' fixes. No Draft requirement was created because it would
duplicate the regeneration bug's ownership repair.

The next NEVER-REVIEWED workspace area is
`crates/ferrosintesis-samples-rain/`.

