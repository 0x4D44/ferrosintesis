# 2026.07.24 — JRN — coverage review of `crates/amp-lab/`

Reviewed `crates/amp-lab/` as the highest-risk remaining NEVER-REVIEWED Cargo
area on the fresh driver-provided baseline `63f569b`. The pass was source-only:
no app, tests, build, audio, generator, exploratory harness, or git mutation ran.

Seven bounded lenses covered correctness, security, performance, reliability,
maintainability, tests, and implicit assumptions. Team-lead verification retained
nine defects:

- loop wrap overrides the selected program/bank;
- export emits a nonexistent authoring API;
- callback-boundary MIDI timing;
- a 32.983-beat “eight-bar” loop;
- unsound `Sync` auto-traits on SPSC endpoints;
- unsupported output format/buffer assumptions;
- callback allocations;
- silently dropped UI commands; and
- non-accumulating peak telemetry.

Filed `MM-BUG-KILN-00076` through `MM-BUG-KILN-00084`. Captured
`MM-REQ-KILN-00030` as Draft for a single machine-checked amp-protocol
definition. Current protocol values match; the requirement is preventive debt,
not another present defect.

The devil's advocate initially rejected the ring finding because the endpoints
are non-Clone. A targeted recheck overturned that: `Sync` permits safe shared
references and `Arc<Producer>`, so non-Clone does not enforce one caller. This is
the material adversarial decision from the pass.

The next coverage pass should review
`crates/ferrosintesis-samples-rain/`; mandolin remains NEVER-REVIEWED after it.
