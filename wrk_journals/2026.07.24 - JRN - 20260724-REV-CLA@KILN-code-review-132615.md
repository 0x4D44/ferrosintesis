# 2026.07.24 — JRN — rain sample-crate code review

Reviewed `crates/ferrosintesis-samples-rain/` as the next NEVER-REVIEWED Cargo
area after reconciling today's committed transactional coverage fragments.

The review covered the crate, its embedded WAV metadata, and the minimum loader,
prewarm, GM 96 voice, public sample-rate, and oracle sources. Six independent
lenses plus a devil's advocate completed, followed by source verification.

Confirmed:

- GM 96 reads one 44.1-kHz source sample per output frame, so non-44.1-kHz
  renders play the recording at the wrong speed. Raised
  `MM-BUG-KILN-00086`.
- First-use rain decoding remains reachable from the realtime callback. Existing
  `MM-BUG-KILN-00073` already tracks the exact prewarm/oracle gap, so no
  duplicate was raised.

Held rather than filed: velocity-independent bed gain and in-phase multi-note
stacking are implementation facts without an accepted behavior oracle. The
missing `NOTICE` reference, approximate provenance recipe, and one-file
generator formatting issue remain report-only; the formatting issue already
exists in `scratchpad.md`.

No app, build, test, render, generator, or exploratory harness ran. Only the
transactional report, coverage fragment, journal, and new bug entry were
written.

