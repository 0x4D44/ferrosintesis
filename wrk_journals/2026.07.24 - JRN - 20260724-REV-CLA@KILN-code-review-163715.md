# 2026.07.24 — JRN — mandolin sample-crate code review

Reviewed `crates/ferrosintesis-samples-mandolin/`, the sole remaining
NEVER-REVIEWED Cargo area after reconciling today's committed transactional
coverage fragments.

The review covered the package, its 40 embedded onsets, generator and provenance
boundary, and the minimum consumer routing, prewarm, retrigger, round-robin, and
oracle sources. Six independent lenses plus a devil's advocate completed,
followed by team-lead source verification.

Confirmed and filed:

- `MM-BUG-KILN-00087`: fast same-key bank changes can retrigger the voice
  selected by the previous bank.
- `MM-BUG-KILN-00088`: fresh spawns and fast retriggers own separate mandolin
  round-robin phases, causing a repeated take at phrase boundaries; unrelated
  voices also consume the engine phase.
- `MM-BUG-KILN-00089`: the two-dynamic→four-round-robin migration left public
  package, tooling, test prose, inventory, and legal-package surfaces stale.

Held as report-only debt: current tests do not bind each filename/root to its
PCM payload. The same cross-family gap was already documented by an earlier
coverage pass, and no wrong current payload was found, so no duplicate bug or
speculative requirement was raised.

No application, build, test, package, render, generator, audio, or exploratory
harness ran. Only the transactional report, coverage fragment, journal, and
three new bug files were written.

