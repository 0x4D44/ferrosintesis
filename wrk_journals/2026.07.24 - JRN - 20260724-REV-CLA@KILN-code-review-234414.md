# 2026.07.24 — JRN — review `crates/ferrosintesis-samples-fretnoise/`

## Scope and selection

The aggregate coverage table lagged six committed transactional fragments. After
counting those fragments, the fret-noise sample crate was the sole current Cargo
workspace member still NEVER-REVIEWED.

## Review

Seven read-only lenses completed: correctness, security, performance/scale,
reliability, maintainability, test coverage, and devil's advocate. The team lead
re-read every cited path and independently confirmed:

- the manifest names an absent package README;
- exact seed arithmetic produces six adjacent repeated take selections in the
  first 64 isolated GM120 voices;
- the bake uses an unpinned NumPy `Generator`, whose official compatibility
  contract does not preserve streams across versions;
- all twelve current WAVs are valid PCM mono 44.1 kHz/16-bit and total 1,021,972
  bytes.

No application, test, build, package dry-run, sample generator, audio render, or
exploratory harness ran.

## Outcome

- `MM-BUG-KILN-00093`: the declared package README is absent.
- `MM-BUG-KILN-00094`: seed hashing permits adjacent repeated takes despite the
  round-robin contract.
- `MM-BUG-KILN-00095`: byte-identical regeneration has no stable environment or
  output oracle.
- `MM-REQ-KILN-00031`: derive the fret-noise inventory and generated crate surface
  from one source of truth.

The missing CC0 legal text and direct-to-final WAV writes were deduplicated against
open `MM-BUG-KILN-00089` and `MM-BUG-KILN-00063`. Two consumer-oracle gaps and two
small documentation drifts remain report-only.

After this coverage fragment is consolidated, the next pass should review the
highest-risk MOVED area, `crates/ferrosintesis/`.
