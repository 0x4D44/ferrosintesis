# Fixed queue two-eyes closure pass — 2026-07-24

Independent verifier: Codex GPT-5.6-Sol. The Open → Fixed transitions below were
authored by Claude Opus 4.8, so all 23 items were eligible for two-eyes review.

The fixed build was `2b38cc3`. `origin/main` later advanced to `b1e2a6b` through
a docs-only review commit; shipped code remained identical.

## Shared post-fix evidence

The original observations were rechecked against the fixed sources and assets.
All regression suites passed:

- `$null | cargo test --release -p ferrosintesis --lib`: 666 passed, 0 failed,
  26 ignored.
- `$null | cargo test -p amp-lab`: 25 passed, 0 failed.
- `python -m unittest test_prepare` in `tools/ferrosintesis-samples`: 31 passed.

The repository gates were green with stdin closed for tests:

- `cargo fmt --all -- --check`
- `cargo clippy --workspace --exclude amp-lab --all-targets --locked -- -D warnings`
- `cargo clippy -p ferrosintesis --no-default-features --all-targets --locked -- -D warnings`
- `$null | cargo test --workspace --exclude amp-lab --locked`

The amp-lab-specific clippy gate also passed:

- `cargo clippy -p amp-lab --all-targets --locked -- -D warnings`

There were no known unrelated failures.

## Fails-before evidence

Each negative control used the fix commit (or its parent for a coverage/build
observation), retained the regression oracle, and mechanically restored the
relevant pre-fix production behaviour. The temporary worktree was clean after
every control.

- **MM-BUG-KILN-00040:** restoring the modeled GM 120 dispatch made
  `gm120_sampled_beats_the_modeled_burst_level` fail; sampled and modeled RMS
  were both `0.00112287`.
- **MM-BUG-KILN-00054:** the parent-source census found none of the six named
  kit-sensitive tests using `for_each_kit`; the fixed tests exercise both V1
  and V2. This is a test-coverage defect, so the missing second invocation is
  the failing observation rather than a production assertion.
- **MM-BUG-KILN-00061:** restoring output-rate-dependent LA eligibility made
  `la_engagement_never_depends_on_output_rate` fail and report all 186 affected
  program/key/rate combinations.
- **MM-BUG-KILN-00062:** restoring existence-only extraction-cache reuse made
  the current preparation suite fail with eight errors and one assertion
  failure across changed-pin, altered, truncated, legacy, and refetch cases.
- **MM-BUG-KILN-00064:** removing the sustain-loop prewarm additions made
  `prewarm_leaves_no_sustain_loop_unsearched` fail because no sustain loop had
  been resolved.
- **MM-BUG-KILN-00065:** restoring the preparation script without the bottle
  route and producer made all four `BottleLoopTest` cases fail.
- **MM-BUG-KILN-00069:** restoring the pre-fix packaged documentation made both
  inventory tests fail: five crates lacked packaged `PROVENANCE.md`, and 15
  sample families were undocumented.
- **MM-BUG-KILN-00070:** the parent tree's exact no-default-features clippy gate
  failed on two production and eight test dead-code errors.
- **MM-BUG-KILN-00071:** replacing the licensing documents with the old
  name-only treatment made two of three licensing oracles fail, identifying
  ten unstated licences and ten banks outside their licence sections.
- **MM-BUG-KILN-00073:** removing the `prewarm()` additions made
  `prewarm_covers_every_lazy_cache` fail in its pristine child, naming
  `rain_loop`, `chanter_rr2`, and `GONG_LAYERS`.
- **MM-BUG-KILN-00074:** restoring fixed-size delay-line constructors made
  `every_program_renders_at_every_common_rate` fail at a requested tap of 2062
  frames against capacity 2048.
- **MM-BUG-KILN-00075:** bypassing `ebass_seam_gain` made
  `la_ebass_seam_level_parity` fail at GM 32, key 20, velocity 48: sampled
  onset was 0.296× the model, or -10.6 dB.
- **MM-BUG-KILN-00076:** restoring the old backing asset made
  `backing_asset_leaves_the_ui_channel_voice_alone` fail on tick-zero channel
  1 bank-select and program-change messages.
- **MM-BUG-KILN-00077:** restoring the prose-only export made
  `export_snippet_reproduces_the_rig_bytes` fail with an empty parsed command
  stream instead of the rig bytes.
- **MM-BUG-KILN-00078:** restoring block-boundary event application made
  `note_starts_at_its_own_frame_not_the_block_boundary` fail 543 frames early
  for a 1024-frame callback.
- **MM-BUG-KILN-00079:** restoring the late End-of-Track marker made
  `the_backing_loop_is_exactly_eight_bars` fail at 32.9833 beats and 839,172
  frames instead of 814,154.
- **MM-BUG-KILN-00080:** removing the non-`Sync` marker made the compile-time
  assertions fail for both `Producer` and `Consumer`.
- **MM-BUG-KILN-00081:** restoring the 4096-frame rejection made
  `any_callback_size_produces_audio` fail at 4097 frames. Source review also
  confirmed that startup now selects a supported f32 configuration or returns
  the exact incompatibility, rather than binding an f32 callback to an
  arbitrary default format. No hardware-specific success is claimed.
- **MM-BUG-KILN-00082:** restoring zero-capacity pending storage and disabling
  setup reservation made `the_audio_callback_does_not_allocate_per_block`
  fail: a 64-message CC burst allocated four times. The separately tracked
  per-voice allocation remains MM-BUG-KILN-00092.
- **MM-BUG-KILN-00083:** ignoring enqueue failure made the outbox suite fail
  both dropped-knob recovery and atomic A/B recall; no recovered commands were
  delivered.
- **MM-BUG-KILN-00086:** restoring one source frame per output frame made
  `fx_o8_rain_96_bed_plays_at_its_recorded_44100_clock` fail at 48 kHz, with
  loop-lag NCC `-0.003` versus `0.998` at 44.1 kHz.
- **MM-BUG-KILN-00087:** omitting bank LSB from the tremolo routing identity
  made `a_routing_change_inside_the_tremolo_window_spawns_the_new_voice` fail
  for both steel-to-mandolin and mandolin-to-steel transitions.
- **MM-BUG-KILN-00088:** removing the retrigger phase hand-back made
  `the_mandolin_strike_phase_is_bank_scoped_and_survives_a_retrigger` fail:
  the post-gap voice replayed take 1 instead of advancing to take 2.

## Verdict

All 23 eligible Fixed items are confirmed for closure. Existing residual work
remains separately tracked as MM-BUG-KILN-00085, MM-BUG-KILN-00090,
MM-BUG-KILN-00091, and MM-BUG-KILN-00092. This pass found no untracked residual
that requires a new ID, and no original symptom persisted.
