# 2026.07.25 - JRN - Codex Open bug queue drain

Live handoff for the `/goal` run draining `bugs/` one bug per fresh worktree and
one authorized Deltic integration. Read this file and `deltic bugs --json` before
choosing the next pass; `origin/main` is the only completed-work authority.

## NEXT

Integrate MM-BUG-KILN-00072, refresh the ledger, then choose the next fixable
Open bug that does not overlap another live worktree. Keep newly authored fixes
at `Fixed`; independent eyes own closure.

## Completed on origin/main

- MM-BUG-KILN-00097 — CLI output/input alias protection.
- MM-BUG-KILN-00093 — fret-noise package README and provenance oracle.
- MM-BUG-KILN-00098 — calmeter low-rate nontermination.
- MM-BUG-KILN-00099 — strict shared WAV parsing and real sample-rate metering,
  integrated as `583d8d3`.
- MM-BUG-KILN-00106 — modeled GM 96 rain now stays alive and audible while held,
  integrated as `5c16189`.
- MM-BUG-KILN-00103 — alternate-piano sample calibration and damper physics
  independently verified and closed as `2f4ddd2`.
- MM-BUG-KILN-00104 — register-aware piano felt damper independently verified
  and closed as `37fd4b9`.

## Completed pass: MM-BUG-KILN-00106

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00106-gm96-rain-liveness`

- Red before: `cargo test -p ferrosintesis --no-default-features --lib
  fx_o7_rain_96_real_recording_bed -- --nocapture` failed because modeled rain
  died before a five-second hold.
- Fix: synthetic GM 96 grain wash now keeps the voice alive while the key is
  held; after note-off it yields to the core release. The real sampled bed keeps
  its existing envelope-driven lifetime.
- Focused default/no-default tests and strict clippy are green.
- Render inventory: baseline `origin/main` `583d8d3`, 124 catalog MIDIs at
  11.025 kHz, 124 byte-identical, zero contamination. No catalog MIDI sounds
  GM 96, so focused coverage proves reachability.
- Strengthened default/model-only FX suites, strict clippy, and the Rust 1.87
  focused check are green.
- Integrated on `origin/main` as `5c16189`.

## Completed pass: MM-BUG-KILN-00103

- The code fix had already landed in `f6ed468`, followed by the register-aware
  felt damper in `70b7067`; the ledger remained Open.
- Independent source inspection confirmed all five GM0 alternate mappings use
  legacy sample calibration with the shared GM0 felt damper.
- `gm0_damper_reaches_gm0_alternates_and_stops_there` and both focused felt
  damper tests pass. The release-control regression also passes on Rust 1.87.
- Closed on `origin/main` as `2f4ddd2`.

## Completed pass: MM-BUG-KILN-00104

- The register-aware felt damper had already landed in `70b7067`; the ledger
  remained Open.
- Independent source inspection confirmed the bounded key-dependent law, its
  undamped top register, and its routing to every shared piano slot.
- Four damper oracles pass on native Rust and 1.87. The rendered tail separates
  damped and undamped notes by 132.2 dB; the sampled/model register sweep matches
  the derived release expectations.
- Closed on `origin/main` as `37fd4b9`.

## Current pass: MM-BUG-KILN-00072

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00072-toml-literal-strin`

- Red before: the focused manifest oracle rejected valid
  `foo = { path = 'vendor/a#b' }`, proving the literal-string false positive.
- Fix: one scanner now tracks TOML basic and literal strings while counting
  structural braces and stopping at real comments. Its single-line scope is
  explicit; multi-line strings require a stateful lexer.
- Regressions cover literal hashes and braces plus a literal quote that used to
  hide a malformed inline table.
- Native and Rust 1.87 focused manifest suites pass; strict clippy is green.
- Remaining: commit the fix and integrate it.
