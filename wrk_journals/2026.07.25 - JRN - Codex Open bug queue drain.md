# 2026.07.25 - JRN - Codex Open bug queue drain

Live handoff for the `/goal` run draining `bugs/` one bug per fresh worktree and
one authorized Deltic integration. Read this file and `deltic bugs --json` before
choosing the next pass; `origin/main` is the only completed-work authority.

## NEXT

Integrate MM-BUG-KILN-00049, refresh the ledger, then choose the next fixable
Open bug that does not overlap another live worktree. Keep newly authored fixes
at `Fixed`; independent eyes own closure.

## Completed on origin/main

- MM-BUG-KILN-00097 — CLI output/input alias protection.
- MM-BUG-KILN-00093 — fret-noise package README and provenance oracle.
- MM-BUG-KILN-00098 — calmeter low-rate nontermination.
- MM-BUG-KILN-00099 — strict shared WAV parsing and real sample-rate metering,
  integrated as `583d8d3`.
- MM-BUG-KILN-00106 — modeled GM 96 rain now stays alive and audible while held,
  integrated as `5c16189`.
- MM-BUG-KILN-00103 — alternate-piano sample calibration and damper physics
  independently verified and closed as `2f4ddd2`.
- MM-BUG-KILN-00104 — register-aware piano felt damper independently verified
  and closed as `37fd4b9`.
- MM-BUG-KILN-00072 — TOML literal-string handling in the manifest oracle,
  integrated as `8241388`.
- MM-BUG-KILN-00063 — atomic sample-generator WAV publication, integrated as
  `88f55c4`.
- MM-BUG-KILN-00084 — amp-lab interval peak retention, integrated as
  `f122f2c`.
- MM-BUG-KILN-00055 — nearest-rank percentile convention and regression,
  integrated as `b8f8247`.

## Completed pass: MM-BUG-KILN-00106

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00106-gm96-rain-liveness`

- Red before: `cargo test -p ferrosintesis --no-default-features --lib
  fx_o7_rain_96_real_recording_bed -- --nocapture` failed because modeled rain
  died before a five-second hold.
- Fix: synthetic GM 96 grain wash now keeps the voice alive while the key is
  held; after note-off it yields to the core release. The real sampled bed keeps
  its existing envelope-driven lifetime.
- Focused default/no-default tests and strict clippy are green.
- Render inventory: baseline `origin/main` `583d8d3`, 124 catalog MIDIs at
  11.025 kHz, 124 byte-identical, zero contamination. No catalog MIDI sounds
  GM 96, so focused coverage proves reachability.
- Strengthened default/model-only FX suites, strict clippy, and the Rust 1.87
  focused check are green.
- Integrated on `origin/main` as `5c16189`.

## Completed pass: MM-BUG-KILN-00103

- The code fix had already landed in `f6ed468`, followed by the register-aware
  felt damper in `70b7067`; the ledger remained Open.
- Independent source inspection confirmed all five GM0 alternate mappings use
  legacy sample calibration with the shared GM0 felt damper.
- `gm0_damper_reaches_gm0_alternates_and_stops_there` and both focused felt
  damper tests pass. The release-control regression also passes on Rust 1.87.
- Closed on `origin/main` as `2f4ddd2`.

## Completed pass: MM-BUG-KILN-00104

- The register-aware felt damper had already landed in `70b7067`; the ledger
  remained Open.
- Independent source inspection confirmed the bounded key-dependent law, its
  undamped top register, and its routing to every shared piano slot.
- Four damper oracles pass on native Rust and 1.87. The rendered tail separates
  damped and undamped notes by 132.2 dB; the sampled/model register sweep matches
  the derived release expectations.
- Closed on `origin/main` as `37fd4b9`.

## Completed pass: MM-BUG-KILN-00072

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00072-toml-literal-strin`

- Red before: the focused manifest oracle rejected valid
  `foo = { path = 'vendor/a#b' }`, proving the literal-string false positive.
- Fix: one scanner now tracks TOML basic and literal strings while counting
  structural braces and stopping at real comments. Its single-line scope is
  explicit; multi-line strings require a stateful lexer.
- Regressions cover literal hashes and braces plus a literal quote that used to
  hide a malformed inline table.
- Native and Rust 1.87 focused manifest suites pass; strict clippy is green.
- The repository-controlled 724-test gate passed.
- Integrated on `origin/main` as `8241388`.

## Completed pass: MM-BUG-KILN-00063

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00063-atomic-sample-wav`

- Red before: an injected `Wave_write.writeframes()` failure replaced known
  destination bytes with a partial 44-byte WAV.
- Fix: `write_wav_mono()` writes and closes a sibling `.part`, then publishes
  it with `os.replace()`. Failure removes the staged file and preserves the
  destination.
- The focused durability regression and all 32 sample-preparation tests pass.
- The repository-controlled 724-test gate passed.
- Integrated on `origin/main` as `88f55c4`.

## Completed pass: MM-BUG-KILN-00084

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00084-amp-lab-interval-p`

- Red before: the interval regression could not compile because `Meters` had
  no publish/consume operations; production only exposed overwrite/load.
- Fix: callbacks retain the relaxed atomic maximum and the UI consumes it once
  with `swap(0)`.
- The focused regression, all 26 amp-lab tests, and strict clippy pass.
- Rust 1.87 cannot build the existing amp-lab dependency tree because
  `image 0.25.10` requires Rust 1.88; amp-lab is already excluded from that
  repository gate.
- The repository-controlled 724-test gate passed.
- Integrated on `origin/main` as `f122f2c`.

## Completed pass: MM-BUG-KILN-00055

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00055-percentile-convent`

- Red before: the documented nearest-rank p95 of nine sorted values returned
  7.0 instead of the required maximum, 8.0.
- Fix: `percentile()` now uses the one-based `ceil(q × n)` rank and a focused
  regression pins the distinguishing case.
- The focused regression passes natively and on Rust 1.87. Both live consumers
  pass through the brass sustain oracle; strict clippy is green.
- Render inventory: exact baseline `f122f2c`, all 124 catalog MIDIs at
  11,025 Hz; 124 byte-identical, zero changed, zero contamination.
- Baseline worktree retained at
  `D:\worktrees\midi-music\BASELINE-KILN55` because this run has no authority
  for recursive worktree deletion.
- Deltic rebased the fix over four concurrent non-voice commits. The rendered
  baseline and integrated parent have the identical `voices.rs` blob, and the
  pre-/post-rebase patch IDs match (`bd475aa`), so the inventory covers the
  integrated delta exactly.
- The repository-controlled 737-test gate passed.
- Integrated on `origin/main` as `b8f8247`.

## Completed pass: MM-BUG-KILN-00110

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00110-reject-self-credit`

- Red before: the exact gutted NOTICE accepted its quoted `ccby` substring as
  a distinctive licensor credit.
- Fix: extracted tokens are now rejected when derived from the crate name,
  project name, or accepted licence spellings.
- The adversarial fixture and all three end-to-end attribution oracles pass
  with default features, no default features, and Rust 1.87; strict clippy is
  green.
- The repository-controlled 738-test gate passed.
- Integrated on `origin/main` as `5adfce5`.

## Completed pass: MM-BUG-KILN-00111

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00111-cross-check-attrib`

- Red before: a CC0 manifest declaration still agreed with provenance that
  explicitly recorded CC BY 4.0 because the predicate compared the manifest
  only with itself.
- Fix: all three attribution oracles now derive the obligation from packaged
  provenance, then require the manifest declaration to agree before deciding
  whether the attribution checks apply.
- The derived vocabulary lists supported licences, not crates, so the guard
  has no hand-maintained attribution-bearing crate list.
- Five licensing tests pass with default features, no default features, and
  Rust 1.87; strict clippy is green.
- The repository-controlled 739-test gate passed.
- Integrated on `origin/main` as `56cf792`.

## Completed pass: MM-BUG-KILN-00016

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-block-mm-bug-kiln-00016-pending-licensed`

- GM32–35 already have real acoustic-pizzicato, fingered, and picked onsets.
  GM38/39 are synth basses and deliberately remain modeled.
- The only Open remainder is GM36/37 slap/pop. Repository search confirms
  there is no real slap-bass asset; the approved 2026-07-19 sourcing HLD
  explicitly defers it until a clean CC0 source is hand-curated.
- This is an external sourcing decision, not an unattended code fix. Route
  Open to Blocked until Arthur provides an owner recording or approves a
  discovered CC0/CC-BY source with retained provenance.
- Ledger parsing and `git diff --check` pass; only the bug record and loop
  journal changed.
- Routed to Blocked on `origin/main` as `0cadec1`.

## Completed pass: MM-BUG-KILN-00019

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-block-mm-bug-kiln-00019-pending-listenin`

- The bug itself records that current program spread matches both reference
  modules; the residual is not a measured defect.
- Changing the 0.70 damping/±6 dB clamp and retouching album CC7/CC11 values
  are ear-in-the-loop choices with no autonomous correctness target.
- Route Open to Blocked until Arthur chooses the trim law after listening and
  names the album renders that need a controller-balance pass.
- Ledger parsing and `git diff --check` pass; only the bug record and loop
  journal changed.
- Routed to Blocked on `origin/main` as `cb805cb`.

## Completed pass: MM-BUG-KILN-00029

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-block-mm-bug-kiln-00029-pending-bowed-an`

- The bug's completed diagnosis shows register- and velocity-specific chaotic
  pockets in GM42/43. A bow-speed clamp fixed some cells but left others and
  introduced a register discontinuity.
- The remaining path is output normalization plus register tuning; GM4 needs a
  separate pickup-shaper timbre retune. Both are voicing changes requiring ear
  validation, not safe autonomous scalar fixes.
- Route Open to Blocked until Arthur can audition the documented bowed grid
  and approve a GM4 bark curve through v127.
- Ledger parsing and `git diff --check` pass; only the bug record and loop
  journal changed.
- Routed to Blocked on `origin/main` as `d358e7d`.

## Completed pass: MM-BUG-KILN-00039

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-verify-and-close-mm-bug-kiln-00039-after`

- The bug's own correction identifies MM-BUG-KILN-00042 as the real shared
  decay-law defect; that independently closed fix measured koto register tilt
  improving from 14.1× to 1.0×.
- Current source confirms koto inherits the derived damper law and the later
  reference-matched `t60: 4.1`.
- The register-law guard, rendered identity guard, and koto-family
  distinctness oracle all pass on current trunk.
- Close the stale Open record directly with both the landed fix provenance
  and this independent verification; no code change is needed.
- Ledger parsing and `git diff --check` pass; only the bug record and loop
  journal changed.
- Closed on `origin/main` as `0cfe8eb`.

## Completed pass: MM-BUG-KILN-00043

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-block-mm-bug-kiln-00043-pending-clavinet`

- The 1.6-second cutoff is objectively wrong, but its fix is coupled to the
  still-too-fast baked decay: changing either alone leaves the body wrong or
  exposes an audible chop.
- The bug explicitly records an ear decision between physical Clavinet brevity
  and the longer GM-reference idiom, plus a source-bake versus runtime-loop
  design choice.
- Route Open to Blocked until Arthur chooses that decay target and runtime
  direction after audition.
- Ledger parsing and `git diff --check` pass; only the bug record and loop
  journal changed.
- Routed to Blocked on `origin/main` as `c676bf7`.

## Completed pass: MM-BUG-KILN-00044

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-verify-and-close-mm-bug-kiln-00044-accep`

- The landed `d1245e9` fix removed GM6's stale exponent correction and restored a
  monotone body response without changing Arthur's accepted `vel_sense: 0.15`.
- Arthur's A/B decision explicitly keeps the roughly 7 dB divergence from both
  reference modules because physical harpsichord behavior wins over GM convention.
- The corrected-program monotonicity guard, documented-exemption velocity guard,
  and held-body ratio guard all pass unchanged on current trunk.
- Close the stale Open record directly after independent verification; the
  remaining reference gap is a documented product choice, not a defect.
- Ledger parsing reports Closed with no automation notes; `git diff --check`
  passes and only the bug record and loop journal changed.
- Closed on `origin/main` as `b2a7000`.

## Current pass: MM-BUG-KILN-00049

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00049-e-bow-hold-level-c`

- Red before: enabling the intended derived damper alone reproduced the ledger
  exactly; key 88 climbed from +8.0 to +12.0 dB relative to its spoken reference.
- Fix: the bounded e-bow driver still owns loop sustain, while a bounded 50 ms
  output-domain scalar closes level after the pitch-dependent pickup/body
  projection. The path exists only for an authored e-bow plus derived damper.
- `DRIVE_LEAD` rejoins `DamperHold::Derived`, removing KILN-00042's sole e-bow
  exclusion. The failed knee-feedback prototype was rejected because a one-way
  driver cannot remove overshoot and pumped against the eight-second decay.
- The strengthened oracle covers five keys and two seeds. Every late window
  passes; worst register spread is 2.4 dB instead of 19 dB.
- Native and Rust 1.87 sustainer suites pass with and without samples. Focused
  derived-damper, bank-distinction, and XG variation tests pass; strict clippy
  and formatting are clean.
- Exact `b2a7000` render inventory: 11 intended alt-bank tracks changed, 113
  unrelated tracks stayed byte-identical. A bank-aware census exactly matches
  the changed set; `render-diff`'s bank-blind false-contamination limitation is
  parked in `scratchpad.md`.
- Ledger parsing reports Fixed with no automation notes; formatting, strict
  clippy, and `git diff --check` pass on the final source tree.

## Current pass: MM-BUG-KILN-00056

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00056-same-pitch-overlap`

- A new catalog-wide serialized-MIDI oracle reproduced the ledger's exact
  10,398 positional same-pitch overlaps before the fix.
- Back-ported the established note-off clamp to the six stale writer lineages,
  including the standalone Iron Tide writer and the Opus writer shared by
  Vigil and Riverwake.
- Regenerated all 41 owned outputs. Thirty-nine committed MIDIs changed and
  the global oracle now reports zero overlaps.
- Hollow Hill, Spark, Hours After Rain, and The Long Turning verifiers pass;
  the Vigil builder validates all 12 tracks.
- Exact `620e5b6` source/render inventory: 39 MIDI sources changed, while every
  other album MIDI stayed byte-identical. At 11.025 kHz, 38 changed audibly;
  Snow remained audio-identical because its sole clamp is a percussion
  note-off that ferrosintesis ignores.

## Current pass: MM-BUG-KILN-00085

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00085-la-bass-crossfade`

- The record explicitly requires Arthur's ear and says not to restore the LA
  bass default until the crossfade shape is accepted.
- Arthur previously reversed the relevant sampled-bass A/B, so shortening the
  50–350 ms sum-to-one fade or making the onset additive is not an unattended
  implementation choice.
- Sixty-eight percent of the motivating notes exceed the top recorded zone.
  Resolving that needs either Arthur's repitch policy or new approved source
  recordings.
- Route Open to Blocked with Arthur as the human owner and both required
  decisions stated exactly.

## Current pass: MM-BUG-KILN-00090

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00090-modeled-only-test`

- Current trunk reproduced only three of the stale 31 failures: one deliberate
  sample-coverage panic and two modeled-only velocity-law failures.
- The default composite velocity table remains unchanged. The no-default build
  now carries measured GM24, GM56/57/59, and GM64–67 model calibrations; the
  separate runtime voice-aware defect remains MM-BUG-KILN-00105.
- The perceptual oracle still runs with samples. A positive source oracle now
  requires both fallback and workspace integration gates to pair it with the
  modeled-only suite, replacing a build configuration that failed by design.
- The exact locked no-default command passes 612 unit tests and 4 doc tests,
  with 22 diagnostic tests ignored.
- Fresh release binaries from exact base `d3ac026` produced 124 byte-identical
  default-feature catalog renders at 11.025 kHz.
- Integrated to `origin/main` as `dfc91dd`; ledger state Fixed.

## Current pass: MM-BUG-KILN-00092

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00092-audio-callback-all`

- The ledger already identifies a voice pool as a major whole-engine design
  choice and asks for timing before Arthur chooses it.
- A temporary release-profile probe swept 1,536 warmed 1024-frame callbacks
  across GM0–127 after sample and storage prewarm. It was removed afterward.
- Results: p50 0.438 ms, p95 0.704 ms, p99 0.973 ms, maximum 2.566 ms on GM52.
  The maximum is 11.05% of the 23.220 ms callback deadline.
- Recommendation: accept bounded voice construction, retain the ≤16-allocation
  NoteOn ratchet and exact-zero assertions everywhere else. No xrun is observed.
- Route Open to Blocked with Arthur as human owner. Exact choice: accept and
  document the bounded exception, or authorize the heterogeneous 128-slot
  voice-pool refactor.

## Current pass: MM-BUG-KILN-00105

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00105-voice-aware-veloci`

- The defect was broader than the caller's `samples` flag: both LA composites
  and sax loops can reject an extreme repitch and return the bare model.
- LA construction now reports whether it built a composite or returned its
  model. The melodic constructor carries the corresponding measured exponent
  to the one outer correction layer; the raw census remains uncorrected.
- GM24, GM56/57/59, and GM64–67 use model calibration only when the model was
  actually built. Eligible sampled paths keep their existing calibration.
- New routing and rendered-law oracles cover runtime sample opt-out, missing
  embedded assets, eligible samples, and pitch-range fallback.
- Full default and no-default ferrosintesis suites pass: 714 and 614 unit tests
  respectively, plus four doc tests in each configuration. Strict clippy passes.
- Exact `c0c6f82` render inventory at 11.025 kHz: 3 expected high-register
  fallback changes, 121 byte-identical, 0 contamination. The changed tracks are
  Big Weather 03, Atlas of Becoming 02, and Atlas of Becoming 06.

## Completed pass: MM-BUG-KILN-00112

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00112-duplicate-low-pian`

- The pinned VSCO revision has no second quiet C2/G2 take, so inventing an
  alternate would require unsupported audio judgment. The bug's explicit
  single-take route is the defensible fix.
- The generator now declares those two exceptions and produces 52 real piano
  files. The core package drops the two duplicate payloads, exposes the
  exceptions, and retains the old RR2 names as compatibility aliases.
- The runtime RR2 bank explicitly points quiet C2/G2 at their one take. A
  committed-bank hash oracle rejects every undeclared duplicate pair, and a
  runtime oracle proves all remaining RR banks differ.
- Generator, core-package, full default/model-only ferrosintesis suites, strict
  clippy, formatting, and diff checks pass.
- Exact `2b25655` render inventory at 11.025 kHz: all 124 catalog MIDIs are
  byte-identical and contamination is zero.

## Completed pass: MM-BUG-KILN-00116

Worktree:
`D:\worktrees\midi-music\20260725-TSK-HUM-fix-mm-bug-kiln-00116-derive-trims-parse`

- Reproduced the first-match defect: a plausible prefixed trim table before the
  real declaration could become `SHIPPED` without an error.
- The parser is now line-anchored to the exact constant and requires one match.
  Its self-test proves sibling/commented declarations are ignored and
  missing/duplicate/cfg-gated exact declarations fail loudly.
- A normal-gate Rust source oracle pins the parser shape and proves `SHIPPED`
  has exactly one assignment from `load_shipped()`.
- Full default and model-only ferrosintesis suites, strict clippy, formatting,
  and diff checks pass. No audio path changed, so no render inventory applies.
