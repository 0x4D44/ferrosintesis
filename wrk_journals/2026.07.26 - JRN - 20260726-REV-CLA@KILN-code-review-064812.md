# Review journal — gong sample crate

## Scope

Read-only coverage-ledger pass over
`crates/ferrosintesis-samples-gong/` at
`c596deb7338bba9f6b1c8727fe0c7ee6c93bb79b`.

The crate, its two committed WAVs, the generator path, consumer routing,
packaging, attribution/provenance oracles, and existing ledger overlap were
inspected statically. Seven distinct review lenses were used, including an
adversarial pass. No application, generator, build, tests, package command,
render, or exploratory harness ran.

## Result

- Filed `MM-BUG-KILN-00128`: documented `--local-only` gong regeneration also
  replaces the bottle output because `want("bottle")` remains true.
- Filed `MM-BUG-KILN-00129`: packaged provenance says the two takes crossfade,
  while the deliberate implementation hard-switches at velocity 84.
- Kept two test gaps in the CR report: no source-to-output gong parity oracle,
  and no soft-layer/83–84 selection coverage.
- Filed no Draft requirement. The output-binding gap is already recorded as a
  generated asset-crate family issue, and whole-bank transactional replacement
  has no current corruption or promised invariant.

Static asset checks found the current two-file inventory internally consistent:
names, aggregate bytes, source hashes, RIFF extents, PCM format, sample rate,
bit depth, channel count, and documented durations all agree.

## Handoff

Report:
`wrk_docs/2026.07.26 - CR - 20260726-REV-CLA@KILN-code-review-064812.md`.

The next dedicated area should be
`crates/ferrosintesis-samples-drumkit2/`, followed by
`crates/ferrosintesis-samples-b1-upright/`.
