# 2026.07.26 — Independent Fixed queue closure pass

Drained the three eligible `Fixed` bugs as a fresh verifier:
MM-BUG-KILN-00107, MM-BUG-KILN-00142, and MM-BUG-KILN-00143.

## MM-BUG-KILN-00107

Re-ran the full two-reference M-CAL panel on the current fix-bearing build:
128 programs × 6 keys × 2 velocities against ferrosintesis, its no-samples
twin, SC-55mkII, and S-YXG50. No trim change is proposed. SC-55 remains at
anchor +8.16 dB / MAD 0.51 dB; the current GM6 normalized drift
(+1.49/-1.16 dB) reproduces the accepted report (+1.46/-1.07 dB).

The committed 2026-07-22 residual baseline reports 42 differences. This is
not suppressed: it includes the deliberate voice work the later report
already measured, and the panel's decision remains `AUTO-SHIP: (none)`.
The current Reference Audition independently puts GM6 2.60 LUFS below the
GM0-7 median, matching the recorded 2.62 LUFS.

The current GM8/GM110 pins fail when transplanted onto `ff31237^` and pass on
trunk. The trim conversion/call-path tests and `derive_trims.py --selftest`
also pass.

## MM-BUG-KILN-00142

Transplanted the fixed `GrandRegenerationRecipeTest` onto `a80da2b^`. The
original README produces no fenced prepare command, so the regression fails.
All three class tests pass on trunk, including the wrong-fence/correct-prose
negative control.

## MM-BUG-KILN-00143

Transplanted `HonkytonkOutputInventoryTest` onto `bd72fe7^`. Both stale-output
cases reach decode/read work instead of rejecting, so the regressions fail.
Both pass on trunk and prove fetching, decoding, and writing remain untouched
when inventory validation rejects.

## Gate

The full `.deltic-integrate.toml` fallback is green on the fix-bearing tree:
formatting; workspace and modeled-only clippy with warnings denied;
modeled-only and workspace tests; and 78 sample-tool Python tests.
