# Review journal — `crates/ferrosintesis-samples-orchestral/`

## Outcome

One bounded, read-only multi-lens review completed on 2026-07-27.

- Confirmed three Medium-severity regeneration/provenance defects:
  unauthenticated direct-WAV warm caches, archive manifests that omit selected
  member paths, and persistent extraction directories that can preserve and attest
  stale members.
- Confirmed one Low-severity published-documentation defect: contradictory bagpipe
  counts/lifecycle plus a false `celens_*` GM mapping.
- Retained the crate-local WAV-format test weakness as report-only because all
  current files are structurally valid and downstream tests enforce the format.
- Created no Draft requirement. Existing `MM-REQ-KILN-00144` already covers the
  broader published-source-pin design debt.

## Evidence and decisions

All 158 WAVs were inspected read-only. The generated inventory, tracked filenames,
consumer keys, aggregate size, RIFF structure, PCM format, and package-family counts
agree. Every payload is referenced, and no filename collides with another sample
crate.

Independent correctness, security, performance, reliability, maintainability, and
test lenses reviewed the slice. A devil's advocate tried to refute every candidate.
Separate skeptics independently confirmed each generator-cache defect and checked
the bug/requirement ledgers for overlap.

The canonical bug mint and the patch writer both received `Access is denied` when
creating files under `bugs/`. No bug record or temporary file was created. The code
review report therefore preserves complete actionable records, but the four defects
remain outside the Open queue until the transaction owner restores ledger write
access.

No source, build script, manifest, lockfile, shipped asset, or existing tracked file
was modified. No application, test, build, package command, generator, render,
exploratory harness, fetch, staging, commit, branch, push, or integration ran.

The coverage artifact records
`crates/ferrosintesis-samples-orchestral/` for this pass. The next review should take
never-reviewed `crates/ferrosintesis-samples-drumkit2/`, followed by
`crates/ferrosintesis-samples-b1-upright/`.
