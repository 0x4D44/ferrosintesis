# Journal — static review of `crates/ferrosintesis/`

Reviewed the assigned
`D:\worktrees\ferrosintesis\20260731-REV-CLA@CRUCIBLE-code-review-014814\crates\ferrosintesis\`
slice with correctness, security, performance, reliability, maintainability,
test-coverage, and devil's-advocate lenses.

Four source-confirmed defects survived adversarial verification:

- malformed Program Change data reaches a 128-entry table as program 255;
- equal-tick tempo changes are reordered by numeric value;
- event reads can cross a declared `MTrk` boundary;
- realtime per-channel voice buckets can allocate after setup reservation.

Filed `MM-BUG-CRUCIBLE-00003` through `MM-BUG-CRUCIBLE-00006`. Captured the
duplicated sampled-drum/tom descriptor seam as Draft
`MM-REQ-CRUCIBLE-00007`. Held dense offline polyphony amplification as
UNVERIFIED because the source explicitly chooses unbounded offline polyphony
and this static pass could not prove a violated resource policy.

No source or shipped file changed. No build, test, application, render, or
exploratory harness ran. Git staging, commits, fetches, pushes, and integration
were left to Deltic.
