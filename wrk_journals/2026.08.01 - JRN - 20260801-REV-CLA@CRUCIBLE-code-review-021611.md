# Journal — static review of `crates/amp-lab/`

Reviewed `crates/amp-lab/` on 2026-08-01 using independent correctness, security,
performance, reliability, maintainability, test-coverage, and devil's-advocate lenses.
Read all Rust and Python source in the area plus its manifest, README, relevant root Cargo
and integration policy, the realtime synth command buffer, and related existing bug and
requirement records.

Confirmed seven defects and filed MM-BUG-CRUCIBLE-00012 through
MM-BUG-CRUCIBLE-00018. The main surprise was that two earlier fixes proved only capacity
or small-burst behavior: they did not prove consumer-visible transactionality or the
maximum legal command backlog.

No app, hardware, tests, or exploratory harness ran. No source or existing tracked file
changed. Deltic's clone-wide mint lock was inaccessible in this sandbox. Bug IDs were
therefore allocated from the documented CRUCIBLE host sequence in `bugs/README.md`.
`reqs-capture` forbids that fallback, so the standalone amp-lab gate debt is documented
in the CR report but has no requirement ID.
