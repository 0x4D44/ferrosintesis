# Review journal — `crates/ferrosintesis-cli/`

Static review of baseline `351fb1406f889eb8cb20b5737e73b677face417a` covered
the entire assigned crate with correctness, security, performance, reliability,
maintainability, test-coverage, and devil's-advocate lenses. The pass did not run
the binary, examples, renders, tests, or exploratory harness.

Five defects survived adversarial source verification: unchecked numeric CLI
domains, unsafe direct output in `raw_dump`, classic-RIFF length wrapping in that
writer, rounded/unrounded `calmeter` block drift, and non-Unicode argument panics.
Three lower-impact findings remain report-only: terminal-control titles,
calibration-argument grammar drift, and pre-roll-inclusive clipping.

Bug creation is blocked. The required `deltic bugs new` call could not create
`D:\language\ferrosintesis\.git\deltic-ledger-mint.lock` because the sandbox denied
access. No ID was guessed and no record was written. The CR report contains the
proposed title, severity, priority, fix, and regression oracle for each record so
the allocator can safely resume without repeating the review.

No Draft requirement was warranted. The next coverage target should be
`crates/ferrosintesis-samples-core/`, followed by
`crates/ferrosintesis-samples-clavinet/`.
