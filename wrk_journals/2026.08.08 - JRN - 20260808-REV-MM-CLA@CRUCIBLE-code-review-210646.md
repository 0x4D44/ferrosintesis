# Journal — core sample-crate code review

Reviewed `crates/ferrosintesis-samples-core/` at
`a5516bc62c4e53dd892ccc74b6dce2b5ba9e60a7` with six independent lenses and a
devil's advocate. The pass remained static and read-only: no app, render, build,
test, package, generator, or exploratory harness ran.

Confirmed three Low-severity defects: unreachable VSCO piano tails waste embedded
and decoded memory; core rebakes lack family-scoped stale-output validation while
the global oracle can accept unrelated conditional validation; and the packaged
recipe omits the safe custom inventory refresh. Also retained one Draft-worthy
per-file identity/root verification gap and one rustdoc wording nit. Current 69-file
inventory and WAV structure are clean.

`deltic bugs new` failed once because the sandbox cannot open the clone-wide mint
lock under `D:/language/ferrosintesis/.git`. Per the ledger skills, it was not
retried and no ID was hand-derived. Complete ready-to-file records are in the CR
report.
