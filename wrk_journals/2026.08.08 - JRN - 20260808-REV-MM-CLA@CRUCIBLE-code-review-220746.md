# Journal — dark-Salamander sample-crate code review

Reviewed `crates/ferrosintesis-samples-dark-salamander/` at
`40cae49881a0ca9f8388f6db09368f9719fd2629` with six independent lenses and a
devil's advocate. The pass remained static and read-only: no app, render, build,
test, package, generator, network fetch, or exploratory harness ran.

The current 54-file inventory, PCM structure, hashes, raw-grand projection,
package metadata, and CC0=5 runtime routing are internally consistent. One Low
documentation defect remains: `crates/ferrosintesis/src/sampler.rs:1685` still
calls dark-Salamander bank 4 after the route moved to bank 5.

No new ledger record was minted. Draft `MM-REQ-KILN-00033` already covers the
projection oracle. Closed `MM-BUG-KILN-00122` is the deduplication match for the
selector-documentation regression; reopening it would modify a tracked file,
which this transactional review explicitly forbids.
