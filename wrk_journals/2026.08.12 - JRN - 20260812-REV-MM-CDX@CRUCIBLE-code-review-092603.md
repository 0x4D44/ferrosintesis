# Journal — gong sample-crate code review

Reviewed `crates/ferrosintesis-samples-gong/` at
`cc3f3b4fdbe9c916566b734c6864fe6c7bc999a1` with six independent lenses and a
separate devil's advocate. The pass remained static: no app, render, build,
test, package, generator, or exploratory harness ran.

The two-file package is internally consistent at the container level:
2,971,398 file bytes, canonical mono PCM16/44.1 kHz headers, valid RIFF extents,
documented durations, unique outputs, and source hashes matching provenance.

One Medium defect survived adversarial verification and was filed as
`MM-BUG-CRUCIBLE-00024`. The soft WAV begins at PCM `-2769` rather than silence.
That 8.45%-full-scale jump is 12.7 times its ordinary first-2-ms step. The
zero-lead branch in `trim_lead_and_ring` skips the documented de-click fade, and
`GongOneShot` emits the value directly.

No requirement was minted. The absent upstream Freesound licence manifest is
already tracked by `MM-REQ-KILN-00029`; package-local source-pin discipline is
already tracked by `MM-REQ-KILN-00144`. Output digests, stricter WAV-format
oracles, and broader soft-layer render checks remain report-only preventive
work because current files are valid and the onset regression can cover the
actual shipped defect directly.
