# Journal — MuseScore MS Basic sample-crate review

Reviewed `crates/ferrosintesis-samples-musescore/` as one bounded, read-only
pass across correctness, security, performance, reliability, maintainability,
test coverage, and devil's-advocate lenses. Inspected every crate text file, all
36 WAV containers and hashes, the consumer banks, generator paths, history, and
existing ledgers. No app, test, build, render, generator, package command,
network lookup, or exploratory harness ran.

Filed Open `MM-BUG-KILN-00190`: the packaged regeneration command's global
`bottle` selector also rewrites the separate active whole-voice bottle crate.
Captured Draft `MM-REQ-KILN-00189`: current tests cannot detect a valid payload
swap or bind the six families' 36 assets to their independently maintained
runtime roots. Deduplicated exact-source-pin scope against Draft
`MM-REQ-KILN-00144` and kept MuseScore_General-grand scope separate from Draft
`MM-REQ-KILN-00188`.

Current static inventory is internally consistent: disk, embedded, and consumer
names agree; all WAVs declare PCM16 mono 44.1 kHz with matching extents; and all
payload hashes are distinct. No current runtime audio defect was found.
