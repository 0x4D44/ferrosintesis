# 2026.08.14 — JRN — Code-review pass: crates/ferrosintesis-samples-b1-upright/

Overnight review pass, area assigned by the driver (never-reviewed; last moved
2026-07-29). Read-only: docs and ledger output only; deltic owns commit and
integration.

## What happened

- Lead cross-checked the crate against the workspace: provenance SHA-256s (hashed both
  committed Opus archives — exact match), sampler wiring (velocity split, tail entry
  frame, zone counts, B7 ET fallback), licensing/inventory oracle coverage, gitignore
  and packaging. All clean.
- Spawned one fresh-context devil's advocate briefed to defeat the crate's test
  oracles (the KILN-00071/72/73 method). It returned six findings; I re-verified each
  against source before accepting. Two confirmed as defects, filed:
  - **MM-BUG-KILN-00195** (Medium): the crate-side `b1_tail` validator checks only the
    tail chunk; the runtime's `fmt `/`data`/pad/`entry < pcm_frames` checks are absent
    at publish time, so a structurally bad asset publishes green and panics every
    consumer in `b1_bank!`. Regen tool shares the same predicate — correlated-blind.
  - **MM-BUG-KILN-00196** (Low, latent): `include = ["samples/**"]` packages more than
    the inventory oracle enumerates (top-level lowercase `.wav` only); junk/nested
    content would ship unvetted. Current tree verified clean.
- Refuted/accepted-as-guarded findings (PCM checksums, aggregate-pin redistribution,
  regen `ARRAY_RE` nit, overflow paths) recorded in the CR report with reasons, not
  filed.
- No reqs raised — both actionable items are defects, not design debt.

## Surprises

- The crate's own oracles survived every inventory-drift attack (the `FILE_COUNT`-typed
  array plus fs-derived comparison is genuinely redundant with the regen tool); the
  holes are all in the *asset structure* dimension the tail-focused validator never
  looks at. Same lesson as KILN-00073: the predicate, not the derivation, is the
  assumption.
- Session permission mode blocked ad-hoc Python, so the README's 3.7–8.0 s decay-range
  claim stays UNVERIFIED (noted in the report), not silently passed.

## Output

- Report: `wrk_docs/2026.08.14 - CR - 20260814-REV-MM-CLA@KILN-code-review-080009.md`
- Bugs: `bugs/MM-BUG-KILN-00195.md`, `bugs/MM-BUG-KILN-00196.md`
- Coverage: `wrk_docs/review-coverage/20260814-REV-MM-CLA@KILN-code-review-080009.md`
- Next pass: sibling `ferrosintesis-samples-*` crates (never-reviewed, shared oracle
  shape per KILN-00196), then staleness-check the synth and render-catalog areas.
