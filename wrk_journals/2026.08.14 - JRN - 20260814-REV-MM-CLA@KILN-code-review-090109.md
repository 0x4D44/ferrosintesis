# 2026.08.14 — JRN — code-review pass over crates/ferrosintesis-samples-sax/

Autonomous overnight review pass (read-only; deltic owns git/integration).

**Area:** `crates/ferrosintesis-samples-sax/` (assigned). Read the whole crate plus
its consumers/guards in `crates/ferrosintesis/src/` (sax zone tables, `sax_bank`,
prewarm, licensing/inventory/provenance oracles) and the generator pair
`tools/ferrosintesis-samples/gen_crate_lib.py` / `test_prepare.py`.

**Outcome:** crate fundamentally healthy — strong derived oracles, exemplary
dual-licence (CC BY 4.0 AND 3.0) enforcement, well-guarded baritone-G#3 exclusion.
Two confirmed low-severity docs defects filed:

- **MM-BUG-KILN-00197** — the e121847 legal-header sync appended the generated
  `legal_doc_line` continuation line onto complete hand-written headers, leaving a
  dangling "`ferrosintesis`." fragment in the published rustdoc of 12 of the 25
  sample crates (sax included). Enumerated all 25 rather than fixing just the sax
  symptom; the commit diffstat (12 pure appends vs 13 replacements) confirms the
  split exactly.
- **MM-BUG-KILN-00198** — the shipped sax PROVENANCE.md claims every packaged root
  is pinned in the sampler zone tables, but the two G#3 takes removed by
  MM-BUG-KILN-00178 are packaged-for-provenance-only; the doc never says so.

**Verified directly:** 74 WAVs tracked, aggregate bytes == `EXPECTED_BYTES`
(4,101,968, measured), fmt headers of 5 spanning files all PCM mono 16-bit 44.1 kHz,
gitignore whitelist, prewarm reaches all 8 sax banks, both CC BY URIs required
independently by `licensing.rs`.

**Surprises / dead ends:** none material. A same-day CR report from the 080009 pass
exists; this pass wrote its own per-run report file per the transactional contract
(deltic merges). No reqs captured — the one durable idea (split `legal_doc_line` so
the marker doesn't force the fragment) is recorded inside MM-BUG-KILN-00197's fix
options.

**Next:** remaining never-reviewed sample-asset crates and `crates/render-catalog/`
per `wrk_docs/review-coverage.md`.
