# 2026.08.14 — JRN — code-review pass: crates/ferrosintesis-samples-vcsl-steinway/

Run 20260814-REV-MM-CLA@KILN-code-review-110309, Claude Fable 5, autonomous.

- Assigned area `crates/ferrosintesis-samples-vcsl-steinway/` (MOVED tier: ledger
  2026-07-18, area commit 1b3a172 2026-07-29). Small generated asset crate —
  lead reviewed inline across all lenses, then spawned ONE fresh-context
  devil's-advocate subagent briefed to refute the lead's findings, per the repo's
  own defeat-the-oracle doctrine. That split earned its cost both ways: the
  advocate refuted half of the lead's headline finding (the Python gate tests
  `test_prepare.py:2904` cover the hand-edit path the lead thought open) and
  surfaced the two best findings of the pass (the alias-namespace hole in the
  `payload.rs` shadowing oracle; the unguarded hand-transcribed zone-root pins).
- Lead independently re-verified every advocate claim taken forward: the
  `.deltic-integrate.toml:59,71` gate steps (first grep missed them — the args
  are separately quoted, "unittest","discover"), the `embedded_wav` chain order,
  the payload.rs enumeration predicate, the mandolin-only zone-root oracle, and
  that the `la_*_pitch_integrity` family skips all piano programs.
- Filed MM-BUG-KILN-00199 (Low: vacuous generated alias assertion),
  MM-BUG-KILN-00200 (Medium: ALIASES names escape the cross-crate basename
  oracle), MM-BUG-KILN-00201 (Medium: piano-family zone-root pins unguarded;
  all-equal payload sizes make swaps invisible). IDs minted via `deltic bugs
  new`. Cross-referenced rather than re-filed: MM-BUG-KILN-00196 (samples/**
  include vs .wav-only oracle — steinway confirmed class member), 00197 (header
  clean here), 00195 (fmt-chunk gap precedent).
- WAV verification: three headers via `od` (PCM mono 16-bit 44.1 kHz); the
  remaining 24 inferred from uniform 133 048-byte sizes + the workspace
  `parse_wav` enforcement path. `xxd`/python probes were permission-denied in
  this mode; `od` worked. No scratch files left anywhere.
- Outcome: 3 bugs, 0 reqs. Report at
  `wrk_docs/2026.08.14 - CR - 20260814-REV-MM-CLA@KILN-code-review-110309.md`;
  coverage artifact at
  `wrk_docs/review-coverage/20260814-REV-MM-CLA@KILN-code-review-110309.md`.
