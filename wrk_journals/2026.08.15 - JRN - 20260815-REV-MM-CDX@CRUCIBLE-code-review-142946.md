# Journal — core drum-kit code review

Reviewed `crates/ferrosintesis-samples-drumkit/` at baseline
`9c333d8162661b6b3f3e83bd1424bceb402456fe` through seven static lenses. No app,
build, test, package, generator, render, or exploratory harness ran.

The current 128-file inventory, WAV structure, bank mapping, direct indices, and
today's PCM miss-path fix are internally consistent. The pass filed
`MM-BUG-CRUCIBLE-00035` for an inherited environment marker that bypasses the
new regression's pristine-process isolation. It also confirmed that the crate's
recursive Cargo sample allowlist is broader than its top-level lowercase-WAV
oracles; open `MM-BUG-KILN-00196` already owns that sibling-crate sweep.

The private malformed-WAV parser candidate was rejected as a current defect
because only validated compile-time assets reach it. Descriptor-generation debt
remains covered by Draft `MM-REQ-KILN-00176`; no new requirement was needed.
