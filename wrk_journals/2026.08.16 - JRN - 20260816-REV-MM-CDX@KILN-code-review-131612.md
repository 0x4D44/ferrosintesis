# Review journal — `crates/ferrosintesis-samples-ccby/`

Static review of the 20-file CC-BY onset package and its direct generator,
licensing, provenance, sampler and realtime contracts. Seven read-only lenses
covered correctness, security, performance, reliability, maintainability, test
coverage and devil's advocacy. No app, generator, test, build, render, package
command or exploratory harness ran.

Confirmed two Medium defects and filed `MM-BUG-KILN-00215` (two dulcimer assets
start away from silence) and `MM-BUG-KILN-00216` (`LaVoice` allocates its scratch
buffer on first realtime render). Captured two Draft requirements:
`MM-REQ-KILN-00217` for independent per-work attribution and
`MM-REQ-KILN-00218` for parsing Cargo include lists when generating legal
pointers.

The August 15 packaging change is internally consistent: both packaged licence
manifests are byte-identical to the retained originals and match their documented
SHA-256 values; all 20 committed-source hashes match provenance. The old stale
Rhodes/dulcimer output defect is fixed by the derived family preflight.

Deduplicated generic partial-bank publication against Open
`MM-BUG-KILN-00209`, the bare regeneration recipe against Open
`MM-BUG-KILN-00212`, retained-evidence integrity against implemented
`MM-REQ-KILN-00029`, and owner-scoped source hashes against Draft
`MM-REQ-KILN-00144`.
