# B1 upright sample-crate review

Reviewed `D:\worktrees\ferrosintesis\20260817-REV-MM-CDX@KILN-code-review-061026\crates\ferrosintesis-samples-b1-upright\`
as one bounded, read-only static pass. Six lens reviewers, a devil's advocate,
and a separate skeptic completed without rate limits.

The bank's current inventory, source hashes, RIFF structure, tail metadata, and
runtime name mapping are internally coherent. Two findings survived adversarial
verification: the documented rebake shares host-global decode/slice intermediates
(`MM-BUG-KILN-00266`), and the crate's public Rustdoc lost the object after
“through” (`MM-BUG-KILN-00267`).

The incomplete negative-control matrix for `parse_b1_strict` remains report-only
test debt. The proposed stale-root concern is already covered by Draft
`MM-REQ-KILN-00170`; no new requirement was minted. No code, app, test, build,
package, generator, render, or exploratory harness ran.
