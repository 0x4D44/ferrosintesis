# Sax sample-crate review

Reviewed `D:\worktrees\ferrosintesis\20260817-REV-MM-CDX@KILN-code-review-071126\crates\ferrosintesis-samples-sax\`
as one bounded, read-only static pass. Six lens reviewers, a devil's advocate,
and a separate skeptic completed without rate limits.

The current 74-file FLAC inventory, STREAMINFO shape, embedded table, and 72-zone
runtime mapping are internally coherent. Two FLAC-migration regressions survived
adversarial verification: the documented sax rebake still writes an obsolete WAV
bank (`MM-BUG-KILN-00269`), and the published API/provenance still promise WAV keys,
payloads, and the retired byte total (`MM-BUG-KILN-00268`).

The crate-local magic-only check and the unused GM 64–66 RMS calculation remain
report-only local debt. The shared generator parse failure is already Open as
`MM-BUG-KILN-00241`. The baritone grain-correlation callback cost remains unverified
without timing evidence. No requirement was minted. No code, app, test, build,
package, generator, decoder, render, or exploratory harness ran.
