# Journal — static review of `crates/ferrosintesis/`

Reviewed baseline `488c2c5cca8e1aa60a6b848f8fb75cc61de75aed` through seven lenses: correctness, security, performance/scale, reliability, maintainability, test coverage, and devil's advocate. The pass stayed static: no source edits, builds, tests, renders, app execution, or exploratory harness.

Confirmed and filed five defects: decoded MIDI event amplification (`MM-BUG-KILN-00274`), zero Set-Tempo timing collapse (`MM-BUG-KILN-00275`), first-render `ScaledVoice` allocation (`MM-BUG-KILN-00276`), repeated Pluck articulation allocation (`MM-BUG-KILN-00277`), and stale payload rustdoc plus a wrapped-line oracle hole (`MM-BUG-KILN-00278`). Captured duplicated loudness-normalization policy as Draft requirement `MM-REQ-KILN-00279`.

Held explicit API tradeoffs out of the bug queue: large buffered renders are documented and have a streaming alternative; live MIDI queue overflow deliberately drops newest and exposes a counter; zero PPQN is already deliberately rejected for timed files through the finite-duration guard. Existing FLAC decoder findings were deduplicated against `MM-BUG-KILN-00232`–`00236`.
