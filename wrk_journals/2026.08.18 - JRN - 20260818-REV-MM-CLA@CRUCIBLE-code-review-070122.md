# 2026.08.18 — JRN — code-review pass: crates/ferrosintesis-samples-ccby/

Read-only overnight review of the assigned area, `crates/ferrosintesis-samples-ccby/`
(20 embedded CC-BY Freesound onsets: GM 4 Rhodes, GM 15 hammered dulcimer). The crate
was last touched by the 2026-08-17 release (FLAC conversion + container-claim sweep),
so the pass focused on whether that churn left anything behind.

Method: verified the whole evidence chain independently rather than trusting the
documents — manifest SHA-256s recomputed from git blobs, licence tallies counted, all
20 per-sound IDs matched into the manifests, `EXPECTED_BYTES` re-summed, `src/lib.rs`
compared against the current `gen_crate_lib.py` emission, and every oracle that could
plausibly guard the packaged evidence read (`provenance.rs`, `inventory.rs`,
`licensing.rs`).

Filed two bugs:

- **MM-BUG-CRUCIBLE-00042** (Low/Could) — five packaged NOTICEs (ccby, honkytonk,
  vcsl-kawai, vcsl-steinway, ydp-grand) still say "embedded WAV(s)" over all-FLAC
  banks. The `904cbe94` pre-release sweep fixed rustdoc/README/PROVENANCE but touched
  no NOTICE; headroom's copy was already open as MM-BUG-KILN-00247.
- **MM-BUG-CRUCIBLE-00043** (Low/Should) — the crate-local `licence-evidence/` copies
  the KILN-00193 fix introduced are read by no oracle: link existence and `include`
  coverage are checked, byte identity with the attested SHA-256 is not, so the
  published legal evidence can silently diverge.

Everything else checked out; details and the not-reported list are in
`wrk_docs/2026.08.18 - CR - 20260818-REV-MM-CLA@CRUCIBLE-code-review-070122.md`.
Coverage row for the area written to
`wrk_docs/review-coverage/20260818-REV-MM-CLA@CRUCIBLE-code-review-070122.md`.
No reqs raised (the one durable idea — a derived container-claim guard — is already
carried as KILN-00247's concrete fix). Surprise worth remembering: a "sweep" commit is
itself a list — `904cbe94` enumerated three of the four packaged document kinds, and
the missed kind (NOTICE) is the legal one.
