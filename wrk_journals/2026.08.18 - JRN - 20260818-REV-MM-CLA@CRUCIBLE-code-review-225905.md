# 2026.08.18 — JRN — code-review pass over crates/ferrosintesis-samples-core/ (CRUCIBLE)

Assigned area: `crates/ferrosintesis-samples-core/` (last reviewed 2026-07-18; the WAV→FLAC
payload migration landed since, released 2026-08-17 as 0.21.58).

Method: lead read the whole crate plus its consumer seams (`sampler.rs::embedded_wav` and the
piano zone tables, `inventory.rs` doc oracles, `prepare.py`), then one fresh-context
devil's-advocate agent briefed to refute the candidate finding and hunt for misses. The area is
small enough that the full deep-review fan-out would have been disproportionate.

Decisions and surprises:

- The interesting defect surface was documentation, not code: the crate's crates.io README
  example (`get("flute_A4.wav").unwrap()` + RIFF assert) panics as written post-FLAC. Open bug
  MM-BUG-CRUCIBLE-00044 (raised earlier today from the clavinet review) covers the stale
  rustdoc across 12 crates but its README-prose enumeration omits samples-core — and the
  devil's advocate found samples-orchestral's README has the identical broken example plus an
  independent chanter-count drift. Filed as **MM-BUG-CRUCIBLE-00046**, scoped as an extension
  of 00044's list (per the repo's "enumerate all of L" doctrine, the miss itself shows 00044's
  list was hand-maintained).
- Root mechanism worth remembering: neither crate wires README into module docs, so README
  ```rust blocks are never doctested — they were the only unverifiable claims in the crate,
  and both broke silently on migration day. The bug's fix asks for
  `#![doc = include_str!("../README.md")]` or a README-fence extension to 00044's planned
  container-claim oracle.
- Everything else came up clean: inventory/counts/provenance are all covered by derived
  oracles (the pattern CLAUDE.md prescribes), the payload is 3.2 MiB of pinned CC0 material,
  and the two legacy `.wav` aliases are dead weight but harmless (report note CR-2, not
  filed).
- Dedup honoured: stale rustdoc (00044) and the regeneration-path gap (KILN-00224) re-verified
  but not refiled.

Output: report `wrk_docs/2026.08.18 - CR - 20260818-REV-MM-CLA@CRUCIBLE-code-review-225905.md`,
bug `bugs/MM-BUG-CRUCIBLE-00046.md`, coverage artifact
`wrk_docs/review-coverage/20260818-REV-MM-CLA@CRUCIBLE-code-review-225905.md`. No reqs. Deltic
owns commit/integration for this pass.

Next pass: the whole ledger is dated 2026-07-18 (all stale). Suggest `crates/ferrosintesis/`
(largest, most-moved) or `crates/ferrosintesis-samples-orchestral/` (known doc drift found
here).
