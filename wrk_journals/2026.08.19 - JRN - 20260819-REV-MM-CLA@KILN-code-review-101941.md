# 2026.08.19 — JRN — review pass: crates/ferrosintesis-samples-drumkit2/

Overnight static review of the accent-cymbal asset crate (assigned area). Lead
read the whole crate plus its `Bank` contract and consumer seams; four worker
lenses (correctness, test-oracle adversary, docs/provenance, devil's advocate)
fanned out; the lead re-verified every candidate against source before filing.

**Outcome:** 8 new bugs filed (MM-BUG-KIL-00303..00310), report at
`wrk_docs/2026.08.19 - CR - 20260819-REV-MM-CLA@KILN-code-review-101941.md`.
Headline: the 2026-08-16 WAV→FLAC migration left the regeneration tooling behind —
`regen_samples_table.py` still filters `.wav` only, so the documented drum-kit
table-refresh recipe cannot run (00303) and both PROVENANCE files describe the
pre-FLAC payload (00304). The rest are oracle-integrity gaps: RIFF fallback
truncation clamp (00305), under-pinned velocity splits (00306), a re-exec probe a
zero-match filter neuters (00307), no name↔bytes binding so a take permutation
stays green (00308), a tautological realtime prewarm guard (00309), and the
hand-copied `routed_banks()` list on the crate seam (00310).

**Dedup mattered:** this crate had been reviewed three times since 2026-07-29; the
three most visible defects (stale `.wav` rustdoc, SPLASH audio-oracle gap, re-exec
child-mode residual) were already Open (CRUCIBLE-00044, KILN-00203, KILN-00202)
and were suppressed rather than re-filed.

**Surprises / friction:**
- Bash and Workflow were permission-denied to the lead this session; ID allocation
  ran `deltic bugs new` through a worker agent (allocation only). No hand-derived
  numbers.
- deltic minted the new IDs with host token `KIL` where all prior records say
  `KILN` (sequence continued from KILN's max, so no collision). Flagged in the
  report for a human look at the host-token derivation.
- The coverage ledger had no row for this crate at all — worth remembering that
  "reviewed recently in ad-hoc passes" and "recorded in the ledger" diverge.

Deltic owns commit/integration of this pass's outputs; nothing was staged or
committed by the session.
